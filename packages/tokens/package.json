{
  "name": "@motungi/tokens",
  "version": "0.1.0",
  "private": true,
  "description": "웹·앱 공용 디자인 토큰 (자리만 — 값은 디자인 단계에서 채움)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
