# Design

> 모퉁이 Corner의 시각 시스템. 단일 소스는 `@motungi/tokens`(트와일라잇 로즈 팔레트, 원티드 디자인 시스템 기반). 웹(Next.js)은 이 토큰을 `@theme` CSS 변수/Tailwind 유틸리티로, 앱(Expo/RN)은 `ui/theme.ts`의 style 객체로 참조한다. 값은 두 플랫폼 100% 동일.

## Theme

라이트 모드 전용(`color-scheme: light`). 배경은 **아주 옅게 누른 중립**(`#fafaf9`), 표면(카드)은 순백(`#ffffff`).

배경 톤은 두 번 바뀌었다. 원래는 웜 아이보리(`#f4f0eb`)였으나 2026-08-06 폐기했다 — 대비가 3.1:1도 안 되면서 웜 언더톤이 브랜드 로즈와 충돌했기 때문이다. 그런데 순백으로 내리자 `bg === surface`가 되어 카드가 배경에서 **전혀** 분리되지 않았고(대비 1.00:1), 위계가 8% 알파 보더와 그림자에만 걸리게 됐다. 그래서 같은 날 중립 회색(chroma 0)으로 반 단계만 되돌렸다. 크림/샌드 AI 기본값으로 돌아간 게 아니다 — 웜 언더톤이 없으므로 로즈와 충돌하지 않는다.

⚠️ `bg`↔`surface` 차이는 1.04:1로 그 자체로는 미미하다. **배경만으로 면을 나누려 하지 마라.** 카드·박스는 여전히 `border-line-alt` + `shadow-card`를 함께 써야 하고, 배경은 그 둘을 거들 뿐이다(아래 박스 배경 규칙).

색 전략은 **Restrained**: 중립 위에 로즈 프라이머리를 CTA·현재 선택·강조에만 쓴다. 리포트/기회 상세 같은 단일 화면은 민트(지원금/정책)·퍼플(보조)로 **Committed**까지 올릴 수 있다.

물리적 장면: 퇴근길 지하철, 한 손, 실내 조명 아래 짧게 확인 → 밝고 따뜻하되 눈부시지 않은 라이트 UI.

## Color

OKLCH가 아니라 커밋된 hex 소스를 유지한다(브랜드 정체성 보존 우선). 값은 `packages/tokens/src/index.ts`가 정본.

### Brand

| Token | Hex | 용도 |
| --- | --- | --- |
| `primary` | `#9e2b41` | 메인 로즈 · 주요 CTA·강조·현재 선택 |
| `primary-deep` | `#7e2836` | hover/press · 진지한 강조 |
| `tint` | `#fbe8ec` | 연한 로즈 틴트 · 배경·칩·secondary 버튼 |
| `sun` | `#f2a06a` | 선셋 오렌지 · 히어로 그라데이션 |
| `purple` | `#6e4e9c` | 보조 강조 |
| `mint` | `#1e6e64` | 지원금/정책 강조 |
| `mint-tint` | `#e0f0ec` | 민트 배경 |

### Neutral (순중립 회색, chroma 0)

| Token | Hex | 흰 대비 | 용도 |
| --- | --- | --- | --- |
| `bg` | `#fafaf9` | — | 앱 배경. surface 대비 1.04:1 |
| `surface` | `#ffffff` | — | 카드/표면 |
| `surface-alt` | `#f7f7f8` | 1.07:1 | 호버·비활성 등 **잠깐 나타나는 면**(정지 위계용 아님) |
| `gray-100` | `#f1f1f3` | 1.13:1 | 스켈레톤·트랙 등 "빈 자리" |
| `gray-200` | `#e6e6e9` | 1.25:1 | 회색 면 위 스켈레톤·진행바 트랙 |
| `ink` | `#1c1a17` | 17.4:1 | 최상위 텍스트 |
| `label` | `#4a453e` | 9.5:1 | 본문 텍스트 |
| `muted` | `#6b645a` | 5.8:1 | 보조 텍스트(AA 통과) |
| `faint` | `#807970` | 4.3:1 | 장식·아이콘·큰 텍스트 전용 — **본문 금지** |
| `line` / `line-alt` | `#1c1a1726` / `#1c1a1714` | — | 구분선(중립 반투명). `line-alt`가 카드 테두리 기본값 |

### Semantic

`negative #ff4242` · `google #4285f4` · `kakao #fee500`. 상태색은 아이콘·라벨과 병기하고 색만으로 의미를 전달하지 않는다.

### 대비 규칙

본문은 `label`/`ink` 사용. `muted`·`faint`를 본문으로 쓸 때 배경 대비 ≥4.5:1을 확인한다(배경 `#fafaf9` 기준 muted 5.59:1 통과 · faint 4.11:1은 큰 텍스트 전용). 컬러 배경 위 텍스트는 회색 대신 그 색의 진한 톤 또는 흰색.

**색약(CVD) 재계산 — primary 명도 인하(2026-08-06):**

대비는 정상 시각만으로 판정하면 안 된다. Brettel/Viénot 시뮬레이션으로 3종 색각이상을 통과시킨 뒤 흰 글씨 대비를 다시 재면 다음과 같다:

| 값 | 정상 | protan | deutan | tritan |
| --- | --- | --- | --- | --- |
| 구 `#d42f4a` | 4.88 | 7.11 | **4.19** ✗ | **2.89** ✗ |
| 현 `#9e2b41` | 7.30 | 9.62 | 6.49 | 4.81 |
| 현 `#7e2836` | 9.36 | 11.41 | 8.53 | 6.72 |

구 값은 정상 시각에서 4.88:1로 AA에 겨우 걸쳐 마진이 없었고, 색각이상이 조금만 들어가면 무너졌다. **원인은 "빨강이라서"가 아니라 명도 마진이 없어서다** — CVD는 색상 정보를 명도로 붕괴시키는데, 붕괴 후 남는 명도가 부족했다.

따라서 **hue는 옮기지 않았다.** 같은 로즈에서 명도만 내리면 해결되고, `sun`→`primary`→`purple` 노을 그라데이션 정체성도 그대로 유지된다. 테라코타·플럼으로 옮기는 안은 검토했으나 브랜드를 바꿀 이유가 없어 기각.

⚠️ `primary`를 밝게 되돌리려면 **CVD 3종을 반드시 재측정할 것.** 정상 시각 4.5:1 통과는 근거가 되지 않는다.

**랜딩 예외:** 랜딩(마케팅)만 구 값(`#d42f4a`/`#b0344e`)을 유지한다. 큰 히어로 그라데이션과 대형 타이포가 주인공이라 "작은 버튼 위 흰 글씨" 제약이 그대로 적용되지 않고, 노을 그라데이션의 채도가 마케팅 톤의 핵심이기 때문이다. 구현은 `globals.css`의 `--color-primary-landing`을 `web-landing.tsx` 루트에서 지역 override하는 방식(호출부를 가르지 않음). **제품 UI에는 절대 쓰지 마라.**

**M-030 이력(2026-08-05):** 그 이전 값 `#e25067`은 흰 텍스트 대비 ≈3.77:1로 정상 시각에서도 AA 미달이라 `#d42f4a`로 교체했었다. 16px bold 버튼은 18.66px bold 미만이라 "large text" 3:1 예외가 적용되지 않는다.

**색만으로 의미를 전달하지 않는다.** 색약에서 로즈와 중립은 명도로 붕괴해 서로 가까워지므로, 선택 상태는 색과 함께 **형태 신호**를 병기한다. `Chip`은 테두리 두께(미선택 옅은 `line` / 선택 `primary`, 양쪽 다 2px로 두어 레이아웃 시프트 방지)로 이를 만족한다.

## Typography

단일 패밀리 **Pretendard**(한글 우선, 시스템 산세리프 폴백). 디스플레이/본문 페어링 없이 한 패밀리를 두께로 운용한다.

고정 px 스케일(유동 clamp 아님 — product UI, 일정 DPI). 두께: regular 400 / medium 500 / semibold 600 / bold 700 / extrabold 800.

| 프리셋 | size/line (px) | weight | tracking |
| --- | --- | --- | --- |
| display | 30/39 | 800 | -0.02em |
| heading1 | 22/30 | 700 | -0.01em |
| heading2 | 19/27 | 700 | -0.01em |
| headline1 | 18/26 | 600 | -0.01em |
| headline2 | 17/24 | 600 | 0 |
| body1 | 15/23 | 400 | 0 |
| body2 | 14/22 | 400 | 0 |
| label | 13/18 | 500 | 0 |
| caption | 11/16 | 500 | 0.02em |

모바일(RN)은 약간 큰 대응값(display 32/40 등, `ui/theme.ts`의 `T`). 프로즈 줄길이 65–75ch.

## Spacing & Radius

8pt 그리드 간격: `xxs 2 · xs 4 · sm 8 · md 12 · lg 16 · xl 20 · 2xl 24 · 3xl 32 · 4xl 40 · 5xl 56 · 6xl 80`.

반경: `xs 4 · sm 6 · md 8 · lg 12 · xl 16 · 2xl 24 · pill 999`. 카드·주요 버튼은 `xl`(16), 칩은 `pill`, 앱 화면 컨테이너는 28px.

## Shadows

- `card`: `0 1px 2px rgba(28,26,23,.06), 0 8px 24px rgba(28,26,23,.10)` — 표면 부양
- `hero`: `0 12px 32px rgba(176,52,78,.18)` — 로즈 히어로/CTA
- `bottomBar`: `0 -1px 0 rgba(28,26,23,.10)` — 하단 내비 경계

⚠️ 베이지 폐기 이후 **그림자는 장식이 아니라 위계 그 자체다.** 배경(`#fafaf9`)과 카드(`#ffffff`)의 차이가 1.04:1뿐이라, 카드를 띄우는 일은 사실상 `border-line-alt` + `shadow-card`가 한다. 그래서 `card`는 예전보다 진하고(0.04/0.06 → 0.06/0.10), 웜 브라운 톤(46,42,36)도 순중립(28,26,23)으로 바꿨다 — 웜 배경이 사라진 화면에서 갈색 그림자는 누렇게 보인다.

## Layout

**모바일 퍼스트, 앱 폭 고정.** `MobileScreen`이 폰에선 `100dvh` 꽉 채우고, 넓은 화면에선 `max-w-[420px]` 가운데 정렬 + 28px 라운드 + `card` 그림자로 앱과 동일한 레이아웃을 유지한다. 데스크톱은 별도 레이아웃이 아니라 앱 프레임의 확장.

안전영역(`SafeTop`/`SafeBottom`)은 여백만 확보하고 상태바·홈 인디케이터는 OS가 그린다. 하단 내비(`bottom-nav`)로 주요 이동. 히트타깃 ≥44px(`tap-safe`, `MIN_HIT_TARGET`).

1D는 flex, 2D는 grid. 반응형은 구조적(폭 고정·하단 내비)이지 유동 타이포가 아니다.

## Components

공용 프리미티브: web `apps/web/src/components/ui.tsx`, mobile `apps/mobile/ui/components.tsx`. 두 곳의 시각 어휘를 일치시킨다.

- **Button** — variant: `primary`(로즈 채움) / `secondary`(틴트+딥로즈) / `ghost`(투명). size: `lg`(52px, radius xl) / `md`(44px, radius lg). `active:scale-[0.99]`, disabled 40% opacity, hover는 primary-deep. 상태: default·hover·active·disabled 정의됨 — 신규 인터랙션 요소도 이 세트를 갖춘다.
- **Chip** — 34px pill, 선택형. active: `primary` 보더 + `primary` 채움 + 흰 텍스트 / idle: `line` 보더 + surface + label 텍스트. **양쪽 다 2px 보더**로 색약 대응(색+두께 이중 신호)과 레이아웃 시프트 방지를 동시에 만족한다 — 위 대비 규칙 참조.
- **Tag** — 22px, 카테고리 라벨. tone: brand(로즈) / mint / muted.
- **Card** — surface 배경 + radius xl + `border-line-alt` + `card` 그림자. **보더는 선택이 아니다** — 배경과 카드가 1.04:1이라 그림자만으로는 경계가 서지 않는다(`InfoBox`도 같은 조합). 카드는 남발하지 않고 꼭 필요한 곳에만(큐레이션 앱은 덜어내는 게 원칙). 중첩 카드 금지.
- **InfoBox / 상태·안내 박스** — 선택 확인·안내·재진단 프롬프트처럼 "상태를 알려주는" 한 줄/블록 박스. `apps/web/src/components/ui.tsx`의 `InfoBox` 사용 권장. 상태 박스는 새로 만들 때도 이 프리미티브로.
- **Logo** — 로즈 스퀘어(모) + "모퉁이 Corner" 워드마크. `onDark`로 히어로 반전.

로딩은 `corner-spinner`(0.9s linear). product UI 원칙상 콘텐츠 자리엔 스피너보다 스켈레톤을 우선한다.

### 박스 배경 규칙 (불변)

**상태·안내·피드백 박스의 배경은 반드시 흰색(`surface`)이다.** 선택 확인, 재진단 프롬프트, 인라인 안내처럼 "상태를 알려주는" 박스는 `bg-surface` + `border-line-alt` + 그림자(모바일 `shadow-card` / 데스크톱 `shadow-web`)로 만든다. 포인트 컬러(로즈)는 **아이콘·텍스트·안쪽 CTA 버튼에만** 절제해서 쓴다.

- 상태 박스의 **박스 배경**에 `bg-tint`/`bg-primary` 금지, **박스 border**에 로즈 금지 — 틴트 배경 + 로즈 보더는 경고/에러처럼 읽혀 위압감을 준다.
- 예외(허용): 비용·참가비 **강조 블록**, 리포트 원픽 헤더 밴드처럼 **의도된 Committed 컬러 순간**. 이건 "상태 알림"이 아니라 콘텐츠 강조이므로 로즈 틴트 유지 가능.
- 판단 기준: "이 박스가 상황을 *알려주는가*(→흰색), 콘텐츠를 *강조하는가*(→틴트 가능)".

## Motion

트랜지션 150–250ms, 상태 전달 위주(하단 내비 전환, 버튼 press, 칩 선택). 페이지 로드 오케스트레이션 없음 — 앱은 작업으로 바로 진입한다. `corner-spin` 외 커스텀 모션은 exponential ease-out, no bounce.

**reduced-motion 필수**: 모든 모션은 `@media (prefers-reduced-motion: reduce)` 대안(크로스페이드/즉시 전환)을 가진다. A1 온보딩 히어로 등 컬러 화면의 진입 모션도 예외 없이.

## Accessibility

WCAG AA + 모바일 최적화. 본문 대비 ≥4.5:1(플레이스홀더 포함), 큰 텍스트 ≥3:1. 히트타깃 ≥44px. 색만으로 의미 전달 금지(아이콘·라벨 병기). reduced-motion 대안 상시.
