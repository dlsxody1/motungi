{
  "$schema": "https://deno.land/x/deno/cli/schemas/config-file.v1.json",
  "//": "Edge Function(Deno) 전용 설정. 이 폴더는 Node가 아니라 Deno 런타임이라, IDE의 기본 TS 서버가 보면 Deno 전역·jsr: 지정자·.ts 확장자 import를 전부 에러로 표시한다. 이 파일이 있으면 Deno LSP가 이 디렉토리를 담당해 그 오탐이 사라진다. 워크스페이스 typecheck(tsc)는 supabase/를 포함하지 않으므로 서로 간섭하지 않는다.",
  "imports": {
    "@supabase/supabase-js": "jsr:@supabase/supabase-js@2"
  },
  "compilerOptions": {
    "lib": ["deno.window", "ES2022"],
    "strict": true
  }
}
