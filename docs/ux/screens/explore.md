# 탐색 — `/explore`  · [탭]

## JTBD & 목적
원픽 외에 직접 둘러보고 싶은 사용자가 카테고리·검색으로 전체 활동을 브라우징한다.

## 레이아웃 & 위계
헤더 → (모바일)지역 선택 pill → 검색 input → 가로 필터 칩(전체/문화/운동/먹거리/클래스/부업) → 리스트. 카테고리+텍스트로 `catalog` 필터.

## 상태
- loaded / empty("조건에 맞는 활동이 아직 없어요") ✅.
- **loading/error 부재**(동기 스토어 읽기): 결함 M9.

## 결함 (심각도)
- **🟠 M5** 지역 선택 pill이 `onPress` 없는 죽은 컨트롤(모바일 `explore.tsx:48-51`). *피드백 부재.* → `/location` 연결 또는 제거.
- **🟠 M9** loading·error 표면 추가(서버 로드 시).
- **🟡 m13** "부업" 필터 칩 — 카테고리 라벨 정책(C4)과 톤 일치 검토.

## UX 카피
- empty 상태에 CTA 보강 여지: 필터 초기화 버튼.
