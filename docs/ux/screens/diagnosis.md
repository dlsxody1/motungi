# 진단 — `/diagnosis`

## JTBD & 목적
60초 안에 3문항으로 취향·상황을 파악해 원픽 스코어링 입력(관심사·시간대·에너지)을 만든다.

## 레이아웃 & 위계
진행바 + 스텝 카운터 → 문항 1개씩. Q2(시간대)는 `auto:true`로 탭 후 260ms 자동 진행. 완료 시 `saveAnswers` → `/loading`.

## 상태
- loaded / partial(문항 진행). 진행률 표시로 Goal Gradient·Zeigarnik 충족 ✅.
- 비-auto 문항은 선택 전 "결과 보기" 비활성 ✅ (피드백 명확).

## 결함 (심각도)
- **🟡 m13** Q1 옵션 "가벼운 부업(side_job)"은 일자리 잔재의 여가 재프레이밍. 여가 큐레이션 톤과 충돌 여지 — 유지하되 카피 톤 점검. C4(비용 라벨)와 연동.
- 회귀 주의(HANDOFF §6): 관심사 옵션 = `OpportunityCategory`와 1:1 → 카테고리 라벨 수정 시 web·mobile·core 3곳 동기.

## UX 카피
- 진단 질문은 전문용어 없이 구어체 유지 ✅. "얼마를 벌고 싶으세요?"류 옛 카피는 제거됨(검증).
