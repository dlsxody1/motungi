# Nightly Report — 2026-07-16

## TL;DR

- 집은 이슈: **M-009** — `useAppStore`를 `createAppStore` 팩토리로 `packages/core`에 승격해 저장 동기화(toggleSaved/isSaved) 로직을 단일 소유하게 함.
- 선정 배경: 백로그 최상위 todo는 **M-008**이었으나, 미머지 draft PR #11(`nightly/2026-07-15` → main, open)이 이미 M-008 서브스코프(`catalog.ts` 등)를 구현 중이라 충돌 회피를 위해 스킵하고 다음 순번 M-009를 집었다.
- 산출물: `packages/core/src/store.ts`(신규 팩토리) + `packages/core/src/store.test.ts`(14 tests) + `apps/web`·`apps/mobile`의 `useAppStore.ts`를 팩토리 위임 얇은 래퍼로 축소. 커밋 `f74127d`(backend-impl), `8ac7cf8`(frontend-impl).
- 검증(리뷰어가 직접 재실행, qa 결과와 일치): `pnpm --filter @motungi/core test` **118/118 통과**, `pnpm --filter web test` **99/99 통과**, `pnpm --filter mobile test` **22/22 통과**, `pnpm turbo run typecheck` **4/4 성공**, `pnpm --filter web build` **19/19 페이지 성공**. 시크릿·파괴적 마이그레이션 없음. **종합 판정: 통과(pass) — qa 승인 그대로 재확인됨.**
- **기준선 주의**: 오늘 밤도 `origin/dev`가 실재하지 않아 `origin/main`을 기준선으로 사용했고 PR도 main 대상이다(아래 리스크&후속 참고, 사람 조치 필요).

## 집은 이슈

- **M-009** — `useAppStore`를 `createAppStore` 팩토리로 core 승격(저장 동기화 단일 소유) (priority: medium, scope: `packages/core, apps/web, apps/mobile`, status: todo → **doing**)
- planner 스펙 요약: architecture finding "useAppStore 복제, 이미 드리프트 시작"(medium)에서 출발. `apps/web`·`apps/mobile`이 zustand+persist 스토어 로직(state 형태·setter·`toggleSaved` 서버동기화 upsert/delete)을 통째로 복붙해 왔고, 웹에는 `isSaved`가 누락되어 두 플랫폼 API가 이미 갈라지기 시작한 상태였음. 저장 동기화는 로그인 사용자 데이터 정합성과 직결돼 우선순위가 있었음. planner가 `origin/main` 실코드를 직접 대조해 `done_when` 4개 항목이 아직 미충족(stale 백로그 아님)임을 확인 후 실행 확정.
- M-008(우선순위 상위)은 미머지 draft PR #11이 이미 동일 서브스코프를 다루고 있어 이번 밤은 스킵. `docs/backlog/backlog.yml`의 M-008 항목은 이번 밤 건드리지 않고 `status: todo` 유지.

## 아키텍처 검토

- architect 결정: zustand는 `packages/core`에 `zustand/vanilla`의 `createStore`만 추가 — `zustand`의 React `create`나 `@supabase/supabase-js`는 core 의존성에 추가하지 않는다(React 훅 바인딩·구체 DB 클라이언트는 앱 책임).
- `toggleSaved`의 서버 동기화가 필요로 하는 최소 기능만 뽑아 `SavedOpportunitiesClient`라는 좁은 구조적 타입(structural interface)으로 정의해 실제 `SupabaseClient`와의 결합도를 낮춤(core가 supabase-js를 몰라도 실제 클라이언트가 구조적으로 이를 만족).
- `createAppStore<TOpportunity, TCatalogStatus>` 제네릭 설계로 core가 앱별 도메인 타입(`MockOpportunity`/`CatalogStatus`)을 몰라도 되게 함.
- M-008(PR #11, 진행 중)과 파일 충돌 여부를 architect가 사전 검토 — `catalog.ts`/`opportunities.ts` 등 M-008 대상 파일과 겹치지 않음을 확인. 리뷰어가 diff로 재검증한 결과도 동일(아래 검증 섹션).
- 작업 순서: backend-impl(core 팩토리) 먼저, frontend-impl(web/mobile 래퍼) 다음으로 지정. FSD 경계 위반 없음으로 결론.

## 변경 내용

리뷰어가 `git log --oneline origin/main..HEAD`, `git diff --stat origin/main...HEAD`로 직접 재확인한 최종 diff:

```
$ git log --oneline origin/main..HEAD
8ac7cf8 M-009: web/mobile useAppStore를 core 팩토리 위임으로 전환
f74127d M-009: core createAppStore 팩토리 추가

$ git diff --stat origin/main...HEAD
 apps/mobile/store/useAppStore.test.ts  | 189 ++++------------------
 apps/mobile/store/useAppStore.ts       | 110 +++----------
 apps/web/src/store/useAppStore.test.ts | 172 ++++-----------------
 apps/web/src/store/useAppStore.ts      | 123 +++++----------
 packages/core/package.json             |   3 +
 packages/core/src/index.ts             |   1 +
 packages/core/src/store.test.ts        | 275 +++++++++++++++++++++++++++++++++
 packages/core/src/store.ts             | 150 ++++++++++++++++++
 pnpm-lock.yaml                         |   4 +
 9 files changed, 556 insertions(+), 471 deletions(-)
```

- **커밋 `f74127d`(backend-impl, "M-009: core createAppStore 팩토리 추가")**: `packages/core/src/store.ts`(신규), `store.test.ts`(신규 14 tests), `index.ts`(export 추가), `package.json`(`zustand ^5.0.14` 의존성 추가, 이것 하나뿐임을 리뷰어가 직접 `package.json` 확인). `AppState` 전체(anchors/answers/results/catalog/catalogStatus/savedIds/user) + setter + `toggleSaved`(서버 upsert/delete) + `isSaved`를 단일 소유. 모바일 쪽 `nextSaved` → `wasSaved` 순수 리네임(기능 변경 없음, 웹과 의미 통일). persist `partialize`(anchors/answers/savedIds)는 유지.
- **커밋 `8ac7cf8`(frontend-impl, "M-009: web/mobile useAppStore를 core 팩토리 위임으로 전환")**: `apps/web/src/store/useAppStore.ts`·`.test.ts`, `apps/mobile/store/useAppStore.ts`·`.test.ts` 축소. `createAppStore<MockOpportunity, CatalogStatus>` 호출 + zustand `useStore`로 React 바인딩 + `Object.assign(hook, store)`로 `getState`/`setState`/`subscribe` 정적 메서드 패리티 유지(양 플랫폼 `lib/auth.ts`가 이 정적 메서드를 컴포넌트 밖에서 직접 호출하므로 필수 — 리뷰어가 코드로 직접 확인). 웹 SSR 빌드 중 `localStorage` 전역을 모듈 스코프에서 즉시 참조하면 Next.js Node 빌드 환경에서 `ReferenceError`가 나는 문제를 발견해, 메서드 본문 안에서만 참조하는 지연 바인딩 `webStorage` 어댑터로 방어적으로 수정.
- M-008(PR #11) 영역 파일(`apps/*/data/opportunities.ts`, `packages/core/src/catalog.ts`)에 대한 diff는 0줄 — 리뷰어가 `git diff origin/main...HEAD -- apps/web/src/data/opportunities.ts apps/mobile/data/opportunities.ts packages/core/src/catalog.ts`로 직접 재확인.
- 시크릿·API 키·파괴적 SQL 패턴을 diff 전체에서 grep — 검출 없음.

## 검증

리뷰어가 브랜치를 직접 체크아웃해 전부 재실행한 결과(qa 판정과 일치):

```
$ pnpm --filter @motungi/core test
 RUN  v2.1.9 /home/user/motungi/packages/core
 ✓ src/view.test.ts (25 tests) 19ms
 ✓ src/scoring.test.ts (30 tests) 8ms
 ✓ src/store.test.ts (14 tests) 12ms
 ✓ src/adapters/seoul-jobs.test.ts (13 tests) 5ms
 ✓ src/adapters/sports-facility.test.ts (9 tests) 6ms
 ✓ src/adapters/seoul-culture.test.ts (6 tests) 5ms
 ✓ src/adapters/util.test.ts (12 tests) 5ms
 ✓ src/adapters/trail.test.ts (5 tests) 6ms
 ✓ src/adapters/culture-info.test.ts (4 tests) 4ms
 Test Files  9 passed (9)
      Tests  118 passed (118)
```

```
$ pnpm --filter web test
 ...
 Test Files  11 passed (11)
      Tests  99 passed (99)
```

```
$ pnpm --filter mobile test
 ✓ data/opportunities.test.ts (11 tests) 155ms
 ✓ store/useAppStore.test.ts (5 tests) 13ms
 ✓ hooks/useEnsureCatalog.test.ts (5 tests) 132ms
 ✓ lib/geo.test.ts (1 test) 2ms
 Test Files  4 passed (4)
      Tests  22 passed (22)
```

```
$ pnpm turbo run typecheck
 @motungi/core:typecheck: ... (성공)
 @motungi/tokens:typecheck: ... (성공)
 @motungi/web:typecheck: ... (성공)
 @motungi/mobile:typecheck: ... (성공)
 Tasks:    4 successful, 4 total
```

```
$ pnpm --filter web build
 ✓ Compiled successfully
 ✓ Generating static pages (19/19)
```

- qa가 원래 확인한 수용 기준 10개 항목(팩토리 단일 소유, `isSaved` 양쪽 노출, 변수명 통일, 앱 래퍼 축소, `partialize` 유지, 테스트 통합, core 118 통과, web 99+mobile 22 통과, turbo typecheck 4/4, diff가 확정 파일로 한정 및 M-008 영역 diff 0)를 리뷰어가 재실행으로 재확인 — 전부 일치.
- SSR 수정이 실제로 필요했는지는 웹 build 19/19 재현으로 확인(빌드가 성공하므로 회귀 없음을 확인했으나, 수정 전 상태에서 실제로 실패했는지 리뷰어가 별도로 재현하지는 않았다 — 이 점은 qa 산출물과 코드 주석에 근거).
- `packages/core/package.json`을 직접 읽어 의존성이 `zustand`뿐임을 확인(React/`@supabase/supabase-js` 없음).
- **종합 판정: 통과(pass)**. qa의 "전체 판정: 승인"을 리뷰어가 독립 재실행으로 재확인했다.

## 리스크 & 후속

- **경미 지적 (qa 원 지적 유지)**: qa 리포트의 "SSR 버그 발견" 프레이밍이 다소 과장이었을 수 있음(실패 재현 없이 방어적 리팩터링에 가까움). 실질 위험은 없음 — 웹 build 19/19가 이를 뒷받침.
- **M-008(PR #11) 미머지 방치 위험**: PR #11이 계속 open 상태로 남아 있으면, 다음 밤들도 오늘 밤처럼 매번 "M-008과 겹치는지" 재확인을 반복해야 한다. 사람이 PR #11을 리뷰·머지(또는 close)해 M-008 스코프를 확정지어야 이 반복 비용이 사라진다.
- **`origin/dev` 미실재 — 브랜치 모델 불일치 (반복 지적)**: `docs/nightly/PIPELINE.md`는 "매일 밤 브랜치는 `origin/dev`에서 갈라지고 PR도 dev 대상"이라 서술하지만, `git ls-remote --heads origin` 확인 결과 `origin/dev`는 존재하지 않는다(존재하는 브랜치: `chore/nightly-pipeline`, `main`, `nightly/2026-07-15`). 어젯밤(2026-07-15) 리뷰어가 이미 PR #11에서 이 불일치를 기록했으나("과거 모든 밤 작업도 실제로는 main 기준으로 진행돼 왔다. 사람이 PIPELINE.md를 정정하거나 실제 dev 브랜치를 생성해야 한다") 아직 처리되지 않은 상태다. 오늘 밤도 동일하게 `origin/main`을 기준선으로 사용했고 PR도 main 대상으로 열었다. **사람 조치 필요**: (a) `origin/dev` 브랜치를 실제로 생성해 PIPELINE.md 서술과 맞추거나, (b) PIPELINE.md를 현재 실제 운영 방식(main 기준선)에 맞게 정정한다. 이 조치가 없으면 다음 밤들도 계속 이 재확인·불일치 서술을 반복하게 된다.
- **다음 밤 후보**: M-008(PR #11 정리 후 재평가), M-010(Edge ingest 파서 단일화, medium), M-011(Supabase Database 제네릭 주입, medium).

## PR

https://github.com/dlsxody1/motungi/pull/12 (draft, **main 대상**). GitHub MCP 도구로 리뷰어가 직접 생성했다. **머지·배포하지 않았다.**
