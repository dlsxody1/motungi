# Nightly Report — 2026-08-17

## TL;DR
M-054(모바일 탐색 화면 로딩 스켈레톤) 단독 클러스터로 진행해 **shipped**. qa 게이트(`bash scripts/gate.sh`) PASS(typecheck·test·lint·secrets 전부 ✅), dev에 push 완료. M-052(모바일 지도/산책로 표시)는 5일 연속(08-13~08-17) 동일 사유로 재제외 — 사람의 아키텍처 결정 대기 중. 연속 report-only 없음(직전 여러 밤 모두 shipped) — 헛도는 밤 경고 해당 없음.

## 집은 클러스터
- **씨앗**: `status: todo` 최상위 priority는 M-052(medium)였으나, planner·architect 양쪽이 독립적으로 재검증한 결과 5일 연속(08-13·08-14·08-15·08-16·08-17) 동일 결론 — react-native-maps 신규 네이티브 의존성 / NAVER 정적지도 백엔드 프록시 신설 / 키리스 서드파티 정적지도 3개 경로 모두 사람의 정책 결정 없이는 무인 야간 파이프라인이 진행할 수 없는 차단 상태. architect가 이번 밤도 공식적으로 M-052를 클러스터에서 제외한다고 판정(§아키텍처 검토 참조).
- M-052가 빠지면 남은 `todo`는 **M-054(low)뿐**. `apps/mobile/app/opportunity.tsx`(M-052 scope) vs `apps/mobile/app/(tabs)/explore.tsx`(M-054 scope)는 파일이 다르므로 scope 파일 겹침이 없고, 앱 단위 폴백(같은 앱 + 같은 priority)도 M-052=medium ≠ M-054=low라 성립하지 않는다.
- **최종 클러스터: M-054 단독, 1이슈.** 클러스터 규칙상 "1이슈 밤도 정상 결과"에 해당하며, 상한 3개로 미룬 이슈는 없음(todo가 애초에 2건뿐).

## 이슈별 결과
### M-054 — 모바일 탐색 화면에 로딩 스켈레톤 없음 (priority: low) → **shipped**
- `catalogStatus === "idle"`(카탈로그 로딩 중)일 때 기존에는 "아직 등록된 활동이 없어요" 빈 문구가 그대로 떠서 로딩 중과 빈 결과가 구분되지 않았다.
- `apps/mobile/ui/explore-skeleton.tsx` 신규 — `ExploreRowSkeleton`(RN `View`/`StyleSheet` 정적 회색 블록, `ActivityItem`과 동일 골격, 신규 의존성 없음, 애니메이션 없음).
- `apps/mobile/app/(tabs)/explore.tsx` `ListEmptyComponent`에 `catalogStatus === "idle"` 분기 추가 — 6개 스켈레톤 렌더(웹 `explore-skeleton.tsx`와 동일 카운트).
- 회귀 테스트 2건 추가: idle→스켈레톤 렌더 / ok+빈결과→기존 "없음" 문구 렌더(스켈레톤 0개) — 두 상태가 명확히 다른 화면으로 구분됨을 검증.
- 기존 error/unconfigured 테스트는 무변경, 회귀 없이 전부 통과.
- 커밋 `aa1cc59` — dev 직접 커밋(트렁크 모델, 브랜치·PR 없음).

### M-052 — 모바일 상세 화면에 위치 지도·산책로 폴리라인 없음 (priority: medium) → **재제외(구현하지 않음), status: todo 유지**
- 5일 연속(08-13~08-17) 동일 결론. planner와 architect가 각자 독립적으로 `grep -i map apps/mobile/package.json`, `.env.example`, `security-policy.md`, `grep -rn "react-native-maps|MapView" apps/mobile`을 재확인 — 새로운 unblock 신호(신규 의존성, 신규 env 키, 정책 변경) 전무.
- architect 공식 판정: react-native-maps 신규 네이티브 의존성 승인 / NAVER 정적지도 백엔드 프록시 신설 / 키리스 서드파티 정적지도 신뢰 — 3개 경로 모두 사람의 정책 결정 없이는 진행 불가.
- backlog.yml에 5일차 재제외 사실을 소급 기록(§M-052 notes).

## 아키텍처 검토
- **담당**: frontend-impl 단독(100% `apps/mobile` UI 렌더 분기 + 신규 presentational 컴포넌트, backend-impl 불필요).
- **FSD/구조 경계**: 위반 없음 — `apps/mobile`은 constitution.md의 flat 레이아웃(`app/ assets/ data/ hooks/ lib/ store/ ui/`) 그대로이며, 신규 파일은 기존 `ui/` 디렉토리에 추가(새 계층 디렉토리 생성 없음).
- **데이터 흐름**: `catalogStatus`는 이미 `packages/core`(`store.ts`/`catalog.ts`)가 제공하는 `"idle" | "ok" | "empty" | "error" | "unconfigured"` 유니온의 기존 값 — core 변경 없이 소비만 함. DB 마이그레이션 불필요.
- **테스트 전략**: 컴포넌트 레벨(RTL+Vitest, 기존 `explore.test.tsx` 하네스 확장 + 신규 컴포넌트 자체 스모크 테스트). e2e 불필요(네트워크·네비게이션 계약 변경 없음).
- **M-052 아키텍처 판정**: §이슈별 결과 참조. 아키텍트 권한 밖 관찰로, 5일 연속 동일 사유 재확인 자체가 이 오케스트레이터 사전제외 워크어라운드(08-15에 즉흥 도입, planner.md/architect.md에 성문화 안 됨)를 `blocked-human` 같은 정식 backlog 상태로 승격할 필요가 있다는 신호라고 리포트에 명시적으로 남김(사람 검토용, §리스크&후속 참조).

## 변경 내용
| 파일 | 상태 | 요약 |
|---|---|---|
| `apps/mobile/ui/explore-skeleton.tsx` | 신규 | `ExploreRowSkeleton` — 정적 회색 블록 스켈레톤 행, `ActivityItem`과 동일 골격 |
| `apps/mobile/ui/explore-skeleton.test.tsx` | 신규 | 컴포넌트 렌더 스모크 테스트(1개/6개 카운트) |
| `apps/mobile/app/(tabs)/explore.tsx` | 수정 | `ListEmptyComponent`에 `idle` 분기 추가(6개 스켈레톤), import 1줄 |
| `apps/mobile/app/(tabs)/explore.test.tsx` | 수정 | idle→스켈레톤 / ok+빈결과→기존 문구 회귀 테스트 2건 추가 |

diff 통계: 4 files changed, 130 insertions(+), 7 deletions(-). 신규 npm/네이티브 의존성 없음(`package.json` diff 없음).

## 검증
qa가 독립적으로 실제 실행한 `bash scripts/gate.sh` 출력(요약, 원문은 qa 단계 로그 참조):
```
── typecheck
 Tasks:    4 successful, 4 total
   ✅ typecheck

── test
@motungi/core:test:  Test Files  11 passed (11)   Tests  286 passed (286)
@motungi/mobile:test:  ✓ app/(tabs)/explore.test.tsx (11 tests)
@motungi/mobile:test:  ✓ ui/explore-skeleton.test.tsx (2 tests)
@motungi/mobile:test:  Test Files  18 passed (18)   Tests  95 passed (95)
@motungi/web:test:  Test Files  36 passed (36)   Tests  279 passed (279)
 Tasks:    3 successful, 3 total
   ✅ test

── lint
@motungi/web:lint: ✔ No ESLint warnings or errors
 Tasks:    1 successful, 1 total
   ✅ lint

── secrets
   (시크릿 패턴 없음)
   ✅ secrets

GATE: PASS — push 허용
```
수용 기준 5개 전부 ✅ (qa가 근거와 함께 항목별 판정 — idle 분기 존재/6개 카운트/회귀 테스트 2건/게이트 전체 통과/신규 의존성 없음). diff 오염·시크릿 노출·파괴적 마이그레이션 없음 확인.
`git log --oneline origin/dev..origin/main` — 비어 있음, main과의 divergence 없음(되받을 것 없이 바로 push 가능).

## 리스크 & 후속
- **⚠️ M-052 5일 연속 블록(08-13~08-17)**: 지도/산책로 표시 기능이 사람의 아키텍처 결정 없이는 진행 불가능한 상태가 더 굳어졌다. 세 옵션(`react-native-maps` 신규 네이티브 의존성 승인 / NAVER 정적지도 백엔드 프록시 신설 승인·배선 / 키리스 서드파티 정적지도 서비스 지정) 중 하나를 사람이 승인하거나, `M-052`의 priority를 medium에서 낮춰 씨앗 경합에서 자연히 빠지게 할 것을 강하게 권장한다. **오케스트레이터 단계 사전 제외 워크어라운드가 5일째 반복되는데도 여전히 `.claude/agents/planner.md`/`architect.md`에 성문화돼 있지 않다** — 다음 중 하나를 사람이 결정해주면 좋겠다: (a) backlog.yml에 `status: blocked-human` 같은 정식 상태를 도입해 씨앗 경합에서 명시적으로 제외되게 하거나, (b) 위 세 옵션 중 하나를 승인해 실제로 언블록하거나, (c) M-052를 priority 강등.
- **백로그 소진 임박**: 이번 밤으로 `todo`는 M-052(사실상 blocked) 1건만 남는다. 다음 밤에 M-052를 제외하면 실질 todo가 0건이 되어 **BACKLOG GATE에 따라 audit 모드로 전환될 가능성이 높다** — 8축(a11y·perf·보안·타입안전·구조경계·테스트커버리지·UX패리티·데드코드) 정적 감사를 준비해야 한다. 사람이 M-052를 언블록해주면 audit 모드 전환을 피할 수 있다.
- **모바일 lint 스크립트 부재**: 08-16 리포트에 이미 기록된 known gap — `pnpm lint`는 `apps/web`만 검사한다(`core`·`mobile`엔 lint 스크립트 없음). 이번 밤도 동일 상태 재확인, 새로운 이슈는 아님.
- **트리거 프롬프트 PRECEDENCE 절 점검 결과 — 오늘 밤도 모순 없음**: 이 트리거 프롬프트가 지정한 BACKLOG GATE·CLUSTER RULE 원칙과 현재 repo 문서(`docs/nightly/PIPELINE.md`, `.claude/rules/workflow/nightly-pipeline.md`)를 대조했다. 두 문서 모두 이미 현재의 클러스터 규칙(파일 겹침 기반, 상한 3개, 패딩 금지)과 0단계 백로그 게이트를 정확히 반영하고 있어 override가 필요한 지점이 없었다. 다만 위에서 언급한 "M-052류 반복 차단 이슈의 정식 상태 부재"는 트리거·repo 문서 어느 쪽에도 아직 규정이 없는 **공백**이므로, 이는 모순이 아니라 신규 룰 정비 필요 항목으로 별도 명시한다.
