# Nightly Report — 2026-09-15

## TL;DR

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `status: todo` 15건 → `todo ≥ 2`이므로 구현
  모드. 연속 report-only 경보 해당 없음(직전 산출물 밤 09-11이 M-108 shipped).
- **밤을 시작하기 전 `main`이 `dev`보다 앞서 있었다(nightly-pipeline.md 규칙 6).**
  09-14에 사람이 PR #24("레이어 규율 도입 + 컴포넌트 전면 리팩토링 + mobile 검색 버그
  픽스")를 머지해 main에 스쿼시 커밋 1개(`03eb972`)가 생겼는데 dev는 이를 모르는 상태였다.
  `git diff origin/dev origin/main --stat`로 확인한 결과 **트리(파일 내용)는 완전히
  동일**했다 — main의 스쿼시 커밋이 dev의 09-11 마지막 상태를 그대로 반영하고 있었다.
  `git merge origin/main`으로 되받았고 충돌 0건(머지 커밋 `2f3033f`).
- **클러스터 = M-101 + M-112 (2건).** 씨앗은 todo 중 유일한 최상위 priority(medium,
  나머지는 이보다 낮은 medium/low 혼재) 중 최소 id인 **M-101**. 단, priority 최상위
  후보였던 **M-097(좌표 백필)은 notes에 `blocked-human`으로 명시돼 있어(09-07·09-09
  야간이 이미 재검증: `.env` 부재·data.go.kr 인증키 없음·outbound network 차단) 씨앗
  후보에서 제외**했다 — 이 판단은 CLUSTER RULE 문구에 명시돼 있진 않지만, 실행 불가능이
  확정된 이슈를 씨앗으로 잡으면 클러스터 전체가 시작부터 막히므로 planner 재량으로
  판단했다(선례: 09-09·09-10 밤도 M-052를 동일한 이유로 씨앗에서 제외).
  M-101의 scope(`apps/web/src/lib/auth.ts`, `apps/web/.eslintrc.json`)와 파일 단위로
  겹치는 todo를 찾은 결과 **M-112**(scope에 동일 파일 `apps/web/.eslintrc.json` 포함)가
  겹쳤다. **M-103도 `.eslintrc.json`을 공유해 처음엔 후보였으나, M-103의 done_when이
  "잔여 2건(diagnosis·location)을 처리한 뒤 error로 승격한다"로 M-102(아직 todo)의
  완료를 전제하고 있어 이번 클러스터만으로는 done_when을 충족할 수 없다고 판단해
  제외**했다(상한 3개 미달이지만 억지로 채우지 않음 — CLUSTER RULE "수를 채우려 낮은
  priority를 끌어오지 마라"의 취지와 같은 이유).
- **M-101 shipped (커밋 `a04e88e`).** lib/auth.ts의 store 역참조(레이어 규칙 유일한
  예외)를 해소 — promoteLocalToAccount를 순수 함수로 바꾸고 세션 부트스트랩을 신설
  `hooks/useAuthBoot.ts`로 이관.
- **M-112 shipped (커밋 `708337f`), 단 부분 완료.** 미사용 export 3건(filterLabelOf·
  assertSupabase 정리, absoluteUrl은 실사용 확인돼 무변경) 정리는 done_when대로 끝냈다.
  재발 차단 수단은 **eslint-plugin-import의 `no-unused-modules`를 먼저 시도했다가
  비결정적 오탐(재실행마다 다른 결과, 실사용 중인 export까지 오탐)을 발견해 폐기**하고
  **knip**으로 교체했다 — 결정적이고 정확했다(2회 재실행 동일 결과, fix한 3건 포함
  실사용 export는 오탐 없음). 다만 **knip을 gate.sh에는 아직 연결하지 않았다**: 이
  이슈와 무관한 기존 미사용 export 6건이 베이스라인에 남아 있어 지금 게이트에 걸면
  오늘 밤과 무관한 사유로 막히기 때문이다. 상세 사유는 아래 "M-112 상세" 참조. 이 점은
  done_when의 "재발 차단 수단을 넣는다"를 문자 그대로는 충족하되 "실제로 막는다"는
  취지는 아직 절반만 충족한 상태라 status를 `done`이 아니라 `doing`으로 남긴다.
- **되돌린 이슈 없음.** 두 이슈 모두 qa 통과, 최종 dev 트리 green.
- ⚠️ **09-09~09-11 리포트가 이미 지적한 CLUSTER RULE 폴백 문구 불일치, 오늘 밤도
  재도출 없이 carry forward만.** `.claude/rules/workflow/nightly-pipeline.md:61`과
  `docs/nightly/PIPELINE.md:56-62`의 폴백 발동 조건("파일 겹침 후보가 0개면")이 트리거
  프롬프트의 더 좁은 조건("씨앗 scope에 파일도 좁은 디렉토리도 전혀 없을 때만")보다
  넓다. 오늘 밤은 폴백 자체를 쓰지 않아(M-101 scope에 이미 파일이 있고, 실제 파일
  겹침으로 클러스터가 채워짐) 실질적 영향이 없었다. 네 밤 연속 지적됐는데 안 고쳐진
  상태 — 사람이 트리거 프롬프트 또는 두 문서 중 한쪽을 정정해야 다섯 번째 지적을
  막는다.
- ℹ️ **09-12~09-14 사흘간 야간 산출물이 없다.** `docs/nightly/`에 해당 날짜 리포트가
  없고 dev 로그에도 그 기간 커밋이 없다 — 이 세션은 그 사흘간 무엇이 실행됐는지(트리거가
  안 돌았는지, 돌았지만 실패했는지) 알 수 있는 repo 내부 정보가 없다. 사실만 기록한다;
  원인 조사는 이 세션의 가시 범위 밖이다.

## 집은 클러스터

### 씨앗 선정
- `status: todo` 15건 중 priority 분포: medium 9건, low 6건, high 0건.
- priority 최상위는 medium. medium 9건 중 id 최소는 M-097이지만 notes에 `blocked-human`
  으로 명시돼 있어 제외(위 TL;DR 참조). 다음 최소 id는 **M-101** → 씨앗.

### scope 겹침 검토 (M-101 scope: `apps/web/src/lib/auth.ts`, `apps/web/.eslintrc.json`)
| id | scope | 겹침 판정 |
|---|---|---|
| M-102 | `apps/web/src/app/diagnosis/page.tsx`, `.../location/page.tsx` | 겹침 없음 |
| M-103 | `apps/web/.eslintrc.json` | **파일 겹침** — 그러나 done_when이 M-102 완료를 전제해 클러스터 부적격(아래 참조) |
| M-107 | `apps/web/src/hooks/useNeighborhoodSearch.ts`, `.../lib/query.tsx` | 겹침 없음 |
| M-109 | `packages/core/src/store.ts` | 겹침 없음 |
| M-110 | `packages/core/src/view.ts` | 겹침 없음 |
| M-111 | `apps/web/src/app/explore/[gu]/page.tsx`, `.../lib/seo.ts` | 겹침 없음 |
| **M-112** | `apps/web/.eslintrc.json`, `.../lib/explore-filters.ts`, `.../lib/seo.ts`, `.../lib/supabase.ts` | **파일 겹침(`.eslintrc.json`)** → 채택 |
| M-113 | `apps/web/src/lib/geo.ts`, `apps/mobile/lib/geo.ts`, `packages/core/src` | 겹침 없음(`packages/core/src`는 패키지 전체라 좁은 디렉토리 아님) |
| M-114 | `packages/core/src/index.ts`, `.../gu-summary.ts` | 겹침 없음 |
| M-115 | `apps/mobile/ui/theme.ts`, `packages/tokens/src/index.ts` | 겹침 없음 |
| M-088·M-092·M-093·M-102(low) | 각자 다른 파일 | 겹침 없음 |

파일 겹침 후보 2건(M-103·M-112) 중 **M-112만 채택**했다. M-103은 scope는 겹치지만
done_when 1항이 "max-lines는 잔여 2건(diagnosis·location)을 처리한 뒤 error로
승격한다"로 되어 있어, M-102(diagnosis·location 파일 분할, 여전히 todo)가 먼저
끝나야 성립하는 이슈다. 오늘 밤 클러스터에 M-102가 없으므로 M-103을 넣어도 그
done_when을 충족할 방법이 없다 — planner 재량으로 제외했다.

**폴백 미검토**: M-101의 scope에 이미 구체 파일 2개가 있고 실제 파일 겹침으로
클러스터가 채워졌으므로, 트리거 프롬프트의 폴백 발동 조건("씨앗 scope에 파일도 좁은
디렉토리도 전혀 없을 때만") 자체가 성립하지 않는다.

**결과: 클러스터 = M-101(씨앗) + M-112(파일 겹침). 2건.** 상한 3개 미달이지만 세
번째 후보(M-103)는 위 이유로 억지로 채우지 않았다 — 이는 정상 결과다.

## M-101 상세 — lib/auth.ts의 store 역참조 해소

**변경**
- `apps/web/src/lib/auth.ts`: `useAppStore` import 제거. `promoteLocalToAccount(userId,
  anchors, savedIds)`로 시그니처 변경(순수 함수화). `initAuthListener`는 파일에서
  완전히 제거.
- `apps/web/src/hooks/useAuthBoot.ts` (신설): `initAuthListener`를 이관. store
  구독(`getState`/`onAuthStateChange`)과 lib의 순수 함수 호출을 조합.
- `apps/web/src/components/auth-boot.tsx`: import 경로만 `@/lib/auth` →
  `@/hooks/useAuthBoot`로 변경.
- `apps/web/.eslintrc.json`: `src/lib/auth.ts`에 대한 `no-restricted-imports: off`
  override 제거.
- `apps/web/src/lib/auth.test.ts`: `useAppStore` 모킹 제거, `promoteLocalToAccount`
  호출부에 anchors/savedIds를 직접 인자로 전달하도록 4개 테스트 갱신.
- `apps/web/src/store/useAppStore.ts`, `useAppStore.test.ts`: "auth.ts가 getState를
  직접 호출한다"는 stale 주석을 `hooks/useAuthBoot.ts` 기준으로 정정.

**검증**
- `pnpm --filter @motungi/web typecheck` 클린.
- `pnpm --filter @motungi/web test` 394/394 통과(auth.test.ts 4/4, useAppStore.test.ts
  5/5 포함, 신규 실패 없음).
- `pnpm --filter @motungi/web lint` — 신규 경고/에러 없음(기존 max-lines/complexity
  경고만 잔존, M-101 변경과 무관).
- done_when 4개 전부 충족: ① auth.ts가 store를 직접 import하지 않음 ② eslintrc
  override 제거 ③ 레이어 예외 주석 제거 ④ lib/auth.test.ts 통과(회귀 없음).

## M-112 상세 — 미사용 export 재발 방지

**정리한 3건**
1. `filterLabelOf`(`apps/web/src/lib/explore-filters.ts`) — 같은 파일의
   `exploreHref`만 호출. `export` 제거해 모듈 내부로 좁힘.
2. `absoluteUrl`(`apps/web/src/lib/seo.ts`) — **변경 없음.** 이슈 notes는 "테스트만
   사용"이라 적었지만 실제로는 `seo.ts` 내부 77·140행에서도 호출되고 있어 이미
   실사용 중이었다(notes의 서술이 부정확했음을 기록).
3. `assertSupabase`(`apps/web/src/lib/supabase.ts`) — 프로덕션 코드 실사용 0건 확인
   (grep 결과 `useAppStore.test.ts`의 mock 정의 하나뿐, 그 mock을 실제로 소비하는
   import도 없었음) → 함수 제거, 대응하는 mock 항목도 `useAppStore.test.ts`에서
   함께 제거.
- `__resetRateLimitForTests`는 done_when대로 유지(route 테스트 2곳이 실사용).

**재발 차단 수단 — 시행착오 기록**
1. 1차 시도: `eslint-plugin-import`의 `import/no-unused-modules`를 `src/lib/**`
   override에 추가. 결과: `signInWithKakao`처럼 실제로 다른 파일이 import하는
   export까지 대거 오탐 — override 스코프 안에서는 플러그인이 다른 디렉토리의 import
   구문을 못 본다는 것을 확인.
2. 2차 시도: 같은 규칙을 최상위 `rules`로 옮겨 전체 파일이 처리되게 함(override 아님).
   오탐이 크게 줄어 `RateLimitResult`(진짜 미사용 타입) 1건만 남았다 — 이건 고쳤다.
   **그런데 같은 설정으로 재실행하자 이번엔 `checkRateLimit`·`clientKey`처럼
   route.ts 2곳이 실제로 import하는 export까지 오탐으로 나타났다.** 코드 변경 없이
   재실행만으로 결과가 달라지는 비결정적 동작 — 게이트에 걸면 무작위로 실패하는
   규칙이 되므로 채택 불가 판단, 폐기.
3. 최종: **knip**(`apps/web` devDependency로 추가, 6.35.1) + `apps/web/knip.json`
   (Next.js App Router 엔트리 패턴 지정) + `pnpm --filter web check:unused` 스크립트.
   2회 연속 재실행 결과 동일(결정적), 수정한 3건 모두 정상적으로 오탐 없음.

**knip을 gate.sh에 연결하지 않은 이유**
knip이 이 이슈와 무관한 기존 미사용 export 6건을 추가로 찾아냈다: `text`
(`components/ui.tsx`의 타이포 프리셋 — 어디서도 안 씀), `rowToMock`
(`data/opportunities.ts`의 재노출 배럴 — web에서 미사용), `OpportunityLoadStatus`
(`hooks/useOpportunity.ts`), `SavedLoadStatus`(`hooks/useSavedOpportunities.ts`),
`AnchorSlot`·`AuthUser`(`store/useAppStore.ts`의 재노출). 오늘 밤 이 6건까지
정리하면 M-112의 done_when(현재 걸린 3건 + 도구 도입)을 넘어서는 범위 확장이고,
특히 `text`는 M-115(모바일 타이포가 tokens 대신 손으로 재작성됨 — 웹·모바일
타이포 소유권 자체가 불명확한 상태)와 겹치는 문제라 이 이슈에서 독립적으로
정리하면 M-115의 판단(타이포를 tokens로 통합할지 결정)을 앞지를 위험이 있다고
봤다. 그래서 도구는 도입하되 게이트 연결은 보류하고, 베이스라인 6건 정리 +
gate.sh 편입을 후속 과제로 backlog notes에 남긴다.

**검증**
- `bash scripts/gate.sh` 최종 PASS(typecheck·test·lint·secrets·pure-tests 전부 ✅).
- test: web 394/394(신규 실패 없음), core·mobile 기존 카운트 그대로(파일 미변경).

## qa 결과 (이슈별 push 게이트)

| 이슈 | typecheck | test | lint | 판정 |
|---|---|---|---|---|
| M-101 | ✅ | ✅ 394/394 | ✅ 신규 경고 없음 | PASS → dev push |
| M-112 | ✅ | ✅ 394/394 | ✅ 신규 경고 없음(knip은 별도 스크립트, lint 태스크 미포함) | PASS → dev push |

두 이슈 커밋 후 `bash scripts/gate.sh`를 전체 트리 기준으로 다시 돌려 최종 dev
상태가 green임을 확인했다(typecheck·test·lint·secrets·pure-tests 전부 ✅ — 위
"M-112 상세 > 검증" 절 참조). revert된 이슈 없음.

## 커밋

- `2f3033f` — `Merge remote-tracking branch 'origin/main' into dev` (nightly-pipeline.md
  규칙 6 — main이 dev보다 앞서 있어 되받음. 충돌 0건, 트리 diff 0)
- `a04e88e` — `fix(web): lib/auth.ts의 store 역참조 해소 (M-101)`
- `708337f` — `fix(web): 미사용 export 3건 정리 + knip으로 재발 감지 도입 (M-112)`

## backlog.yml 갱신
- M-101: `todo` → `doing` (사람 최종 리뷰 후 `done` 승격)
- M-112: `todo` → `doing` (위와 동일 + 후속 과제 notes에 기록: 베이스라인 6건 정리 후
  knip을 gate.sh에 편입할 것)
- M-097: 씨앗 후보에서 제외한 사유를 이 리포트에 기록(별도 notes 갱신 없음 — 09-07·
  09-09가 이미 blocked-human으로 정리해 둔 상태 그대로 유지).
