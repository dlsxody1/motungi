# Nightly Report — 2026-07-20

## TL;DR

- 집은 이슈는 **M-014**(medium, "a11y: 구조·모션·확대 결함 수정(중첩 인터랙티브·포커스·live region·viewport)", scope `apps/web`). planner가 `dev@232ac4f` 기준으로 재검증한 결과 done_when 5개 중 4개(saved 카드 중첩 버튼, explore 체크박스 포커스링, loading live region, corner-spinner reduced-motion)는 미충족, 1개(`layout.tsx` viewport `maximumScale` 제거)는 **이미 충족**돼 있었다(WCAG 1.4.4 주석과 함께 사전에 제거됨).
- `apps/web`의 saved/explore/loading 3개 화면 + `globals.css`를 수정하고 신규·확장 테스트 3개 파일을 추가했다. `layout.tsx`는 검증만 하고 편집하지 않았다(diff 없음 확인).
- 검증: **typecheck 클린**, **web 92/92 테스트 통과(12개 파일)**, **build 19/19 라우트 성공**, `git diff --stat HEAD~1..HEAD`가 planner·architect가 합의한 7개 파일과 정확히 일치, axe 위반 0(saved·loading). qa 판정: **PASS**.
- 이번 커밋(`ae1aed9`)은 `origin/dev` 위에 직접 쌓인 트렁크 커밋이다. 이 리포트 자체도 같은 방식으로 `dev`에 직접 커밋되며, PR은 열지 않는다 — 아래 "dev 직접 커밋(PR 없음)" 절 참조.

## 집은 이슈

- **M-014** — a11y: 구조·모션·확대 결함 수정(중첩 인터랙티브·포커스·live region·viewport) (priority: medium, scope: `apps/web`, status: todo → **doing**)
- planner가 `dev@232ac4f` 기준 실코드 대조로 확인한 결과:
  - saved 카드: `<button>` 안에 `role="button"` 중첩 구조 확인 → 미충족.
  - explore 데스크탑 sr-only 체크박스: 포커스 시각화(ring) 부재 확인 → 미충족.
  - loading 화면: 텍스트 컨테이너에 `role="status"` `aria-live="polite"` 부재, corner-spinner에 `prefers-reduced-motion` 대응 부재 확인 → 미충족.
  - `layout.tsx` viewport: `maximumScale`이 **이미 unset 상태**이고 WCAG 1.4.4 관련 주석까지 이미 달려 있음(사전에 완료된 상태) → **이미 충족**.
- 즉 done_when 5개 항목 중 4개(구조·포커스·live region·모션)만 이번 밤의 실제 작업 대상이고, viewport 항목은 백로그 문구가 stale했던 것으로 확인됨(아래 "리스크 & 후속" 3번 참조).

## 아키텍처 검토

- architect가 M-014를 **frontend-impl 단독** 작업으로 배정했다(scope 전체가 `apps/web`, `backend-impl`/`core`/DB 작업 없음).
- 대상 파일 7개를 확정: `saved/page.tsx`, `explore/page.tsx`, `loading/page.tsx`, `globals.css`, `saved/page.test.tsx`(신규), `loading/page.test.tsx`(신규), `explore/page.test.tsx`(확장).
- `layout.tsx`는 명시적으로 스코프 밖으로 확정 — **검증만 하고 편집하지 않음**(planner가 이미 충족을 확인했으므로).
- saved 카드 마크업 설계를 architect가 결정: `button`/`role=button` 중첩 대신, `opportunity/page.tsx`에 이미 있는 선례 패턴을 따라 **형제 `<button>` 2개**(상세 이동 버튼 + 저장 토글 버튼)로 분리하는 방식을 채택.
- FSD 레이어 경계 위반 없음, 크로스 레이어 import 변경 없음을 확인.

## 변경 내용

- `apps/web/src/app/saved/page.tsx`: `<button><span role="button"></span></button>` 중첩 구조를 제거하고, 모바일·데스크탑 카드 각각에 형제 `<button>` 2개(상세 보기 버튼 `aria-label="{title} 상세 보기"` / 토글 버튼 `aria-label="저장 취소"` `aria-pressed={true}`)로 분리. `data-testid="saved-mobile"`/`"saved-desktop"` 추가.
- `apps/web/src/app/explore/page.tsx`: sr-only 체크박스에 `peer` 클래스를 추가하고, 옆의 장식용 span에 `peer-focus-visible:ring-2 peer-focus-visible:ring-primary peer-focus-visible:ring-offset-2`를 추가해 키보드 포커스(`:focus-visible`)일 때만 시각적 링이 보이도록 함.
- `apps/web/src/app/loading/page.tsx`: 공유 Body div(모바일·데스크탑 트리 양쪽에서 렌더됨)에 `role="status"` `aria-live="polite"` 추가 — 두 트리 모두 정상적으로 live region을 갖는 것을 확인.
- `apps/web/src/app/globals.css`: `@media (prefers-reduced-motion: reduce) { .corner-spinner { animation: none; } }` 추가.
- `apps/web/src/app/saved/page.test.tsx`(신규, 143줄), `apps/web/src/app/loading/page.test.tsx`(신규, 71줄), `apps/web/src/app/explore/page.test.tsx`(+16줄): `diagnosis/page.test.tsx`의 vitest-axe/RTL 패턴을 그대로 미러링.
- `apps/web/src/app/layout.tsx`: **편집하지 않음** — diff 없음을 확인, viewport `maximumScale`/`userScalable`은 원래부터 unset 상태 그대로.
- 총 7개 파일, 419줄 추가·131줄 삭제(`git diff HEAD~1..HEAD` 기준, 커밋 `ae1aed9`). DB 마이그레이션·시크릿·`.env`·supabase 변경 없음.

## 검증

qa가 실제로 돌린 명령과 그 출력(리뷰어가 재확인):

```
$ pnpm --filter @motungi/web typecheck
에러 0
```

```
$ pnpm --filter @motungi/web test
Test Files  12 passed (12)
     Tests  92 passed (92)
  Duration  5.37s
```

```
$ pnpm --filter @motungi/web build
성공 — 전체 19개 라우트 컴파일 완료(/saved /explore /loading 포함)
```

```
$ git diff --stat HEAD~1..HEAD
```
→ planner·architect가 합의한 7개 파일과 정확히 일치, 스코프 이탈 없음.

- 시크릿·`.env`·supabase 관련 변경 매치 0건.
- axe: saved(모바일+데스크탑)·loading 테스트 스위트에서 위반 0건.
- explore 페이지는 axe 테스트가 없음(기존부터 있던 공백, M-014 전후 동일) — 이번 체크박스 포커스링 변경은 DOM/클래스 직접 단언으로 검증했으며, CSS 전용 변경이라 이 정도 검증은 적절하다고 판단하나 axe 전체 스캔은 아님. 아래 "리스크 & 후속" 참조.
- 종합 판정: **PASS**.

## 리스크 & 후속

1. **테스트 워크어라운드**: `saved/page.test.tsx`의 axe 호출 2건에서 `landmark-no-duplicate-main` 룰을 비활성화했다. 원인은 `MobileScreen`(`<main>`)과 `DesktopShell`(`<main id="main">`)이 항상 동시에 마운트되고 `md:` 반응형 클래스로만 화면이 전환되는 구조인데, 미디어쿼리를 평가하지 않는 jsdom에서는 axe가 이를 중복 `<main>` 랜드마크로 검출하기 때문이다(실제 브라우저에서는 비활성 트리가 CSS `display:none`으로 접근성 트리에서 빠지므로 실사용자에게는 영향 없음). qa가 독립 검증한 결과 이 구조는 `opportunity`/`my`/`report`/`location`/`explore` 등 `DesktopShell`을 쓰는 6개 이상의 다른 페이지에도 동일하게 존재하는 **사전 조건(pre-existing)**이며, M-014가 새로 만든 문제는 아니다. 다만 그 페이지들은 지금까지 axe 전체 스캔을 받은 적이 없어 이 문제가 드러나지 않았을 뿐이고, saved 페이지가 axe를 처음 통과시키며 처음 드러난 것이다. 후속 과제: `MobileScreen`/`DesktopShell` 구조를 개선(비활성 트리를 실제로 언마운트하거나 `main` 랜드마크를 하나만 두는 방식)해 룰 비활성화 없이도 axe가 통과하도록 근본 해결 — 백로그 등재 검토 필요.
2. **explore 페이지 axe 커버리지 공백**: explore 페이지는 M-014 이전에도 이후에도 vitest-axe로 스캔된 적이 없다(M-013 시절부터 `getByLabelText`/`aria-pressed` 단언만 존재). 이번 체크박스 포커스링 변경은 DOM 클래스 직접 단언으로 검증했으나, 페이지 전체 axe 스캔은 여전히 공백이다 — 후속 과제로 명시.
3. **backlog 문구 stale**: `backlog.yml`의 M-014 `done_when` 4번째 항목("`layout.tsx` viewport에서 `maximumScale:1`을 제거")은 문구 자체가 stale했다 — 실제로는 오늘 밤 이전부터 이미 충족돼 있었다(M-001과 동일한 패턴의 backlog staleness). 이번 밤에는 이 항목에 대해 코드 수정 없이 검증만 수행했다.
4. **모바일(Expo) 미대응**: 동일 화면들의 `apps/mobile` 대응 버전은 이번 스코프(`apps/web`) 밖이다 — 필요 시 별도 백로그 이슈로 등재 검토.

## dev 직접 커밋 (PR 없음)

`docs/nightly/PIPELINE.md`(2026-07-20 갱신)에 따라 현재 모델은 트렁크 기반이다: 밤은 브랜치를 파지 않고 `origin/dev` 위에 직접 커밋·push하며, PR도 draft PR도 열지 않는다. 이번 밤의 코드 커밋(`ae1aed9`)과 이 리포트·backlog 커밋 모두 `dev`에 직접 쌓였다. qa가 PASS 판정했으므로 오케스트레이터가 이후 `git push origin dev`로 push할 예정이다(사람이 push 전 최종 확인). `dev → main` 승격 PR은 사람이 주기적으로 직접 열고 판단한다 — 이는 야간 파이프라인의 역할이 아니다.
