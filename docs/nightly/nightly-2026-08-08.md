# Nightly Report — 2026-08-08

## TL;DR
클러스터 = **M-028**(ingest 외부 API 응답 런타임 미검증) + **M-029**(packages/core 어댑터 5종 프로덕션 미도달·제거) + **M-036**(supabase/functions/ingest 테스트 공백). 셋 다 `bash scripts/gate.sh`(typecheck·test·lint·시크릿 스캔)를 통과해 **셋 다 dev로 나갔다**. revert 없음. 연속 report-only 0회 — 경고 불필요.

참고: `docs/nightly/`에 2026-08-07 노트가 없다 — 그날 밤은 파이프라인이 아예 실행되지 않은 것으로 보인다(`git log`상 08-06 이후 다음 커밋이 오늘 이 실행). 원인은 이 실행 범위 밖이라 조사하지 않았고, 사람이 트리거 상태를 확인할 필요가 있다(리스크 절 참조).

## 0단계 — 백로그 게이트
`docs/backlog/backlog.yml`의 `status:todo`가 **14건(≥2)** → **구현 모드**로 정상 진행. todo 14건 중 priority가 `high`인 것은 0건(전부 이전 밤들이 이미 `doing`으로 올려 사람 승인 대기 중)이므로 씨앗은 medium 티어에서 id 최솟값인 M-028.

## 집은 클러스터
- **씨앗**: M-028 (medium, todo 중 id 최솟값).
- **겹침 근거**: 씨앗 scope `[supabase/functions/ingest, packages/core/src/adapters/ingest-fetch.ts]`와 실제 파일/좁은 디렉토리를 공유하는 todo가 3건 나옴 — M-029(`packages/core/src/adapters` + `supabase/functions/ingest` 이중 겹침), M-036(`supabase/functions/ingest` 겹침), M-043(`supabase/functions/ingest, scripts/` 겹침). `supabase/functions/`에는 함수가 `ingest` 하나뿐이라(파일 3개) 앱 경계(`supabase/` 전체)보다 훨씬 좁은 실질 디렉토리로 판단.
- **상한 3개 초과 → 우선순위 정렬**: M-028(medium,28) · M-029(medium,29)가 상위, M-036(low,36)과 M-043(low,43)이 동순위 low에서 id 오름차순 타이브레이크 → **M-036 선정, M-043 유예**.
- **유예**: M-043("culture_info 154행 산문 백필")은 seed와 scope가 겹치지만 상한 3개 초과로 다음 밤 후보로 남김. M-043의 전제조건(M-040 적재 정상화)은 dev에 이미 반영돼 있어 착수 가능 상태임을 다음 밤을 위해 남긴다.
- 다른 todo(M-031/032/033/037/038/042/044/047/048/049)는 앱/패키지 전체 단위 scope(`apps/mobile`·`packages/core` 등)라 파일 겹침 판정이 안 돼 이번 클러스터 후보에서 제외(폴백 규칙은 "겹침 후보 0개일 때"만 적용 — 이번엔 파일 겹침 후보가 이미 3건 있어 폴백 불필요).

## 이슈별 결과

### M-028 — ingest 외부 API 응답 런타임 미검증 → **shipped** (커밋 `e0adac4`)
- backlog의 라인 참조(`index.ts:44-51` 등)는 M-026 인증 로직 삽입으로 60-84행으로 밀려 있었음 — planner가 실코드 대조로 갱신.
- `packages/core/src/adapters/ingest-fetch.ts`에 `isPlainRecord()` 가드 + `safeMapItems<T,R>()` 헬퍼 신설(mapper가 항목 하나에서 던져도 그 항목만 skip+카운트, 예외가 밖으로 전파되지 않음). `parseJsonItems`가 배열 원소 중 plain object가 아닌 것을 필터링.
- `supabase/functions/ingest/index.ts`의 네 매퍼 호출부(seoul_culture/culture_info/trail/seoul_jobs)를 전부 `safeMapItems`로 교체, 스킵 시 `console.warn`. `fetchSeoul()`의 `body.row`도 반환 전 `isPlainRecord`로 필터링.
- **매퍼 본문(`adapters.ts`)은 건드리지 않음** — 가드는 호출부/공유 헬퍼에만 위치(architect 지시).
- 테스트: core 5케이스(불량 항목 1건 스킵 후 나머지 정상 처리 포함) + Deno `adapters.test.ts` 2케이스(가드 없이는 매퍼가 여전히 던짐을 명시해 호출부 가드의 필요성을 고정).

### M-029 — packages/core 어댑터 5종 프로덕션 미도달 → **shipped** (커밋 `eb45bed`)
- 방향 결정: **(a) 제거**. (b) Deno 배선은 필드매핑 SoT 자체를 바꾸는 변경이라 한 밤 클러스터 리스크로는 과도(architect 판단, §아키텍처 검토 참조).
- `seoul-culture.ts`·`culture-info.ts`·`sports-facility.ts`·`trail.ts` + 각 `.test.ts` 삭제. `seoul-jobs.ts`는 `normalizeSeoulJob(s)`·`classifyEmployment`·`parseWageKrw`와 이들만 참조하던 `RawSeoulJob`·`buildSummary`·`PART_TIME_KEYWORDS`까지 연쇄 제거(삭제 후 grep으로 고아 코드임을 재확인). `isAfterWorkShift`/`parseShiftHours`/`parseWorkRegion`은 `adapters.ts`가 실사용하므로 보존.
- `packages/core/src/adapters/index.ts` 배럴에서 4개 재노출 제거, 헤더에 SoT 설명 추가. `packages/core/CLAUDE.md`에도 결정 근거 명시.
- 삭제 전 `.test.ts`에서 미커버 시나리오 3건(문화행사 설명 합치기·무료 비용 처리·문화정보 happy path)만 골라 `supabase/functions/ingest/adapters.test.ts`로 이관 — 기계적 전량 이관이 아니라, trail·sports-facility 시나리오는 M-036이 신규 작성하므로 중복 방지.
- core 테스트 299→252(47개 감소, 삭제된 파일 크기와 정확히 일치 — 회귀 아님).
- qa가 별도 재확인: `audience.ts`(scoring.ts의 `isKidsOnly`)·`util.ts`(`apps/web/src/app/api/trail-route/route.ts`의 `parseGpxPoints`)는 손대지 않았고 여전히 정상 동작.
- **잔여물(코드 미수정, qa가 발견)**: `supabase/functions/ingest/adapters.ts:226` 부근 주석이 이제 존재하지 않는 core `PART_TIME_KEYWORDS`를 가리킨다. `adapters.ts`는 이 이슈 scope 밖이라 그대로 두고 후속 과제로 남긴다.

### M-036 — supabase/functions/ingest 테스트 공백 → **shipped** (커밋 `d916a36`)
- backlog의 "테스트 0건" 전제가 **stale임을 확인**: `adapters.test.ts`가 2026-08-06(M-017류 부산물)에 이미 생겨 `mapSeoulCulture`/`mapCultureInfo` 8케이스를 커버 중이었다. done_when을 실제 공백(`mapTrail`·`mapSportsFacility`·`mapSeoulJob` 매퍼 + `index.ts` 로직)으로 좁혀 진행.
- `adapters.test.ts`에 세 매퍼(mapTrail/mapSportsFacility/mapSeoulJob) 신규 Deno 케이스 추가.
- 신설 `index.test.ts`: `culture_info` 페이지네이션+`dedupByKey`(core의 실제 export `dedupByKey`로 구동) 및 deadline-purge 특성 스펙.
- **유예(정직하게 명시)**: `runSource`의 소스별 격리는 `index.ts`가 `Deno.serve`를 모듈 최상단에서 실행하고 내부 함수를 export하지 않는 구조라 목업으로 직접 테스트할 수 없었다. 대신 코드를 손으로 대조해(각 소스가 `runSource` try/catch로 개별 감싸진 뒤 `Promise.all`에 들어가므로 한 소스의 예외가 형제 소스로 전파되지 않음, `index.ts:160-171`·`186-249`) 그 성질이 유지됨을 리포트에 근거로 남겼다. 가짜 목업을 강행하는 대신 정직한 유예를 택했고, qa가 코드를 재확인한 뒤 합리적 판단으로 승인.
- 이 커밋은 **테스트 전용, 프로덕션 코드 무변경**(qa가 `git show --stat`으로 확인).

## 아키텍처 검토
- 담당: **backend-impl 단독**(architect가 `grep -rln "ingest" apps/` 확인 결과 `apps/web`·`apps/mobile` 어디도 ingest 파이프라인을 참조하지 않음 — frontend-impl 불필요).
- 경계: `packages/core`는 순수 유지(M-028의 신규 헬퍼도 부작용 없음, M-029는 순수 함수 삭제만). `supabase/`는 Postgres+Edge Functions 경계 안에서만 수정. DB 마이그레이션 없음(3건 모두).
- M-028/M-029는 `packages/core/src/adapters/` 안에서 서로 다른 파일(`ingest-fetch.ts` vs 5개 어댑터 파일+배럴)을 건드려 **커밋 순서 의존성 없음** — 독립적으로 revert 가능함을 확인.
- Deno 런타임이 이 환경에 없어(`which deno` 실패) 신규/기존 Deno 테스트는 **실행이 아니라 수동 코드 대조로만 검증**됨 — `gate.sh`가 애초에 vitest만 검사하도록 설계돼 있어(주석에 이미 인지된 gap) 이번 게이트 판정에는 영향 없음.

## 변경 내용
| 커밋 | 파일 | 요약 |
|---|---|---|
| `e0adac4` (M-028) | `ingest-fetch.ts(+test)`, `supabase/.../index.ts`, `adapters.test.ts` | 4파일, +146/-13줄 |
| `eb45bed` (M-029) | 5개 core 어댑터+test 삭제, `seoul-jobs.ts(+test)` 축소, `adapters/index.ts`, `CLAUDE.md`, `adapters.test.ts`(+62줄 이관) | 13파일, +83/-815줄 |
| `d916a36` (M-036) | `adapters.test.ts`, 신설 `index.test.ts` | 2파일, +250/-1줄(테스트 전용) |

## 검증 (qa가 실제로 돌린 명령 + 실제 출력)
```
$ bash scripts/gate.sh
── typecheck  → Tasks: 4 successful, 4 total   ✅
── test       → core 252/252, mobile 54/54, web 247/247 (Test Files 10/13/31 전부 passed)  ✅
── lint       → @motungi/web:lint "✔ No ESLint warnings or errors"  ✅
── secrets    → (시크릿 패턴 없음)  ✅
GATE: PASS — push 허용
```
추가로 qa가 직접 확인:
- `grep -rn "seoul-culture|culture-info|sports-facility|trail\.ts|normalize(SeoulCulture|CultureInfo|Trail|SportsFacility|SeoulJob)"` — 삭제된 파일에 대한 살아있는 참조 0건(주석/결정노트 안의 언급만).
- 3개 커밋 `git show --stat` 전체 확인 — `supabase/migrations/*` 파일 없음, 시크릿·`.env` 없음.
- Deno 테스트(`adapters.test.ts`, `index.test.ts`)는 **실행 불가, 수동 검토만**(정직하게 기록 — 지어낸 출력 없음).

3건 모두 issue 단위 판정: **PASS** — revert 대상 없음.

## 리스크 & 후속
- **2026-08-07 밤 실행 누락**: `docs/nightly/`에 08-07 노트가 없고 `git log`상 08-06 이후 다음 커밋이 이 실행이다. 이 실행 범위에서 원인을 조사하지 않았다 — 사람이 클라우드 트리거(`trig_013Kmkrr…`) 상태를 확인 권장.
- `supabase/functions/ingest/adapters.ts:226` 부근 주석이 이제 존재하지 않는 core `PART_TIME_KEYWORDS`를 참조 — M-029 scope 밖이라 손대지 않음. 다음 밤 후속 후보.
- M-043("culture_info 154행 산문 백필")은 seed와 scope가 겹치지만 상한 3개로 유예됐다 — 전제조건(M-040)은 이미 충족돼 있어 다음 클러스터링의 유력 후보.
- Deno 테스트 실행 인프라(런타임 자체가 부재)가 이 환경에 없다는 사실이 이번에 다시 확인됐다 — `gate.sh` 주석이 이미 인지한 백로그감이지만, ingest 쪽 테스트가 이번 밤부터 3건→약 12건대로 늘며 "쓰기만 하고 못 돌리는" 테스트 부채가 커지고 있다. Deno 러너를 CI/이 환경에 들이는 게 다음 audit 후보로 적절해 보인다.

## 트리거 문서 정합성 (PRECEDENCE 절에 따른 file:line 기록)
이번 밤 실행에서 repo 문서와 이 실행 프롬프트가 직접 충돌하는 사안은 없었다(백로그 게이트·클러스터 규칙 모두 문서와 프롬프트가 일치). 다만 별건으로, `.claude/rules/core/constitution.md:24`가 "**FSD(entities/features/widgets/shared) 디렉토리는 실재하지 않는다**(옛 룰의 잔재였다)"라고 명시하는데, `.claude/agents/architect.md:3,9,18,28`은 여전히 "FSD 아키텍처"·"FSD 경계: entities/features/widgets/shared 계층 위반 없나"를 architect의 정식 체크리스트 항목으로 두고 있다. 이번 클러스터는 FSD 경계와 무관해 실무 영향은 없었으나(architect 단계에서 이 모순을 인지하고 실제로는 constitution.md의 평면 구조 규칙으로 대체 검증함), architect.md 자체가 폐기된 옛 아키텍처 언어를 그대로 갖고 있어 다음에 FSD 관련 이슈가 오면 다시 혼란을 줄 수 있다. 사람이 `.claude/agents/architect.md`의 FSD 문구를 constitution.md 기준으로 정리할 필요가 있다.
