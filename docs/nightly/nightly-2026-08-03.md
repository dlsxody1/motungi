# Nightly Report — 2026-08-03

## TL;DR

- 백로그 게이트: `docs/backlog/backlog.yml`의 `status:todo`가 16건(≥2) → **구현 모드**로 정상 진행.
- high 우선순위 todo가 M-026/M-027/M-030/M-034/M-035 5개 동률 → tie-break 원칙(동순위 시 id 오름차순)대로 **M-026**을 집었다. 어젯밤(2026-08-02) 리포트가 이미 "M-026이 유력 후보"로 예측한 그대로다.
- planner가 `dev` 기준 실코드로 재검증: `supabase/functions/ingest/index.ts`의 `Deno.serve` 핸들러에 인증 검사가 전혀 없고(SERVICE_ROLE 권한으로 upsert/delete), 요청 body의 `seoulKey`/`dataGoKrKey`가 env 값 없을 때 그대로 사용되는 confused-deputy 경로도 그대로였다 — M-026 notes의 주장과 100% 일치.
- `packages/core/src/adapters/ingest-fetch.ts`에 순수 함수 `isCronAuthorized`를 추가(시크릿 미설정 시 항상 거부)하고, `index.ts` 진입부에서 외부 fetch·DB 접근 전에 이를 검사해 401을 반환하도록 수정. body 파싱 블록을 제거해 API 키는 서버 env만 신뢰하게 했다. `supabase/migrations/0016_ingest_cron_auth.sql`(신규, 추가만)로 pg_cron이 보내는 실제 요청에 새 시크릿 헤더를 실어 보내도록 했다.
- 검증: **typecheck 클린(4패키지)**, **core 267/mobile 39/web 164 테스트 전부 통과**(core +4 신규: `isCronAuthorized`), lint 클린. 독립 qa 에이전트가 diff·file:line을 직접 재정독하고 명령을 재실행해 done_when 4개 항목 전부 ✅로 판정. **PUSH GATE: PASS.**
- 이번 구현은 `dev` 위에 직접 쌓인 트렁크 커밋이다. 이 리포트 자체도 같은 방식으로 `dev`에 직접 커밋되며, PR은 열지 않는다.

## 집은 이슈

- **M-026** — ingest Edge Function 무인증: 서비스롤 권한 엔드포인트를 누구나 트리거 가능 (priority: high, scope: `supabase/functions/ingest`, status: todo → **doing**)
- planner가 `supabase/functions/ingest/index.ts`를 직접 읽어 done_when 4개 항목이 전부 실제 미충족임을 재확인:
  - `Deno.serve` 핸들러(당시 147행)에 진입 시 어떤 인증 검사도 없이 바로 body 파싱·소스 fetch·Supabase upsert로 이어짐.
  - 당시 154-155행: `Deno.env.get("SEOUL_OPENAPI_KEY") ?? body.seoulKey ?? ""` — env가 비어 있으면 요청 body가 그대로 키로 쓰임(confused-deputy).
  - Deno 네이티브 테스트 인프라가 이 repo에 없음(`deno.json`엔 import map만 있고 test task 없음) — 기존 `packages/core/src/adapters/ingest-fetch.ts`의 선례(judgeIngest 등, "Edge Function 본문은 Deno·네트워크·DB에 묶여 테스트가 어렵다"는 이유로 순수 판정 로직만 분리해 vitest로 테스트)를 그대로 따를 근거가 이미 코드에 있음.
  - pg_cron이 실제로 보내는 요청은 `supabase/migrations/0007_cron_ingest_daily.sql`이 정의 — 새 시크릿을 여기 반영하려면 (기존 파일 수정 금지 원칙상) 새 마이그레이션으로 같은 job명을 갱신해야 함.

## 아키텍처 검토

- architect 판단: **backend-impl 단독**으로 충분(`apps/web`·`apps/mobile` 무관). scope는 planner 원안(`supabase/functions/ingest`)에서 아래 이유로 확장 승인:
  - `packages/core/src/adapters/ingest-fetch.ts` — Edge Function이 이미 이 파일을 상대경로로 직접 import하는 선례가 있고(leaf 모듈, import 없음 유지 조건 충족), done_when #3(테스트)을 만족하려면 순수 로직을 여기 두는 것이 기존 아키텍처와 정합적임.
  - `supabase/migrations/`(신규 파일만, 기존 파일 무수정) — done_when #4가 명시적으로 요구.
  - `.env.example`·`turbo.json`(globalEnv 목록) — 신규 시크릿 문서화. 값 없이 키 이름만 추가하는 저위험 변경.
- FSD/경계 위반 없음 — `supabase/functions/ingest → packages/core`(순수 로직만) import 방향은 `dedupByKey`/`judgeIngest` 등 기존 import와 동일 패턴.
- 파일 계획 확정(5개 수정 + 1개 신규):
  1. `packages/core/src/adapters/ingest-fetch.ts`(수정) — `isCronAuthorized(serverSecret, headerValue)` 추가.
  2. `packages/core/src/adapters/ingest-fetch.test.ts`(수정) — 위 함수 테스트 4건.
  3. `supabase/functions/ingest/index.ts`(수정) — 인증 검사·body 파싱 제거·상단 주석 갱신.
  4. `supabase/migrations/0016_ingest_cron_auth.sql`(신규) — cron job 헤더 갱신.
  5. `.env.example`(수정) — `INGEST_CRON_SECRET` 항목 추가.
  6. `turbo.json`(수정) — `globalEnv`에 `INGEST_CRON_SECRET` 추가.
- 테스트 전략: unit(vitest, `packages/core`)만으로 충분 — Deno 런타임 자체의 통합 테스트는 인프라 부재로 이번 밤 범위 밖(후속 과제로 기록).
- 범위 밖 명시: `apps/web`·`apps/mobile`(무관), 기존 마이그레이션 파일 전부(무수정), `supabase/functions/ingest/adapters.ts`(매퍼 로직 무관).

## 변경 내용

6개 파일 변경(5 수정 + 1 신규), 커밋 전 워킹트리 기준:

- `packages/core/src/adapters/ingest-fetch.ts` — `isCronAuthorized(serverSecret, headerValue): boolean` 추가. `serverSecret`이 falsy(미설정)면 무조건 `false` — "시크릿 없음=통과"를 원천 차단.
- `packages/core/src/adapters/ingest-fetch.test.ts` — `isCronAuthorized` describe 블록 4건(일치→true / 불일치→false / 헤더 null·undefined→false / 서버 시크릿 미설정→헤더 무엇이든 false).
- `supabase/functions/ingest/index.ts` — 상단 문서 주석을 인증 정책으로 갱신. `Deno.serve` 핸들러 최상단에서 `isCronAuthorized(Deno.env.get("INGEST_CRON_SECRET"), req.headers.get("x-cron-secret"))`를 검사해 실패 시 어떤 소스 fetch·DB 접근도 하지 않고 즉시 401 JSON 응답. 기존 `let body = ...; await req.json()` 파싱 블록 전체 삭제, `seoulKey`/`dataGoKrKey`는 `Deno.env.get(...) ?? ""`만 신뢰(body 폴백 제거).
- `supabase/migrations/0016_ingest_cron_auth.sql`(신규) — `cron.schedule('ingest-daily', ...)`을 재선언(0007과 동일 job명이라 pg_cron이 갱신)해 `headers`에 `x-cron-secret`(vault의 `INGEST_CRON_SECRET` 복호화 값) 추가, 더는 쓰이지 않는 `body`(seoulKey/dataGoKrKey) 블록 제거. 배포 선행 조건(사람이 수동 실행)을 0007과 같은 스타일의 주석으로 명시.
- `.env.example` — `INGEST_CRON_SECRET=`(빈 값) 섹션 추가, 등록 방법(`supabase secrets set`)과 서버 전용 경고 명시.
- `turbo.json` — `globalEnv` 목록에 `INGEST_CRON_SECRET` 추가(기존 `SEOUL_OPENAPI_KEY`/`DATA_GO_KR_SERVICE_KEY` 관례를 따름).

범위 밖 파일(`apps/web`, `apps/mobile`, 기존 마이그레이션 0001~0015)은 무변경임을 `git diff --stat`으로 확인.

## 검증

qa(독립 에이전트)가 실제로 돌린 명령과 그 출력:

```
$ git status --short
 M .env.example
 M packages/core/src/adapters/ingest-fetch.test.ts
 M packages/core/src/adapters/ingest-fetch.ts
 M supabase/functions/ingest/index.ts
 M turbo.json
?? supabase/migrations/0016_ingest_cron_auth.sql
```

```
$ pnpm typecheck   (4패키지, 강제 재실행)
@motungi/tokens:typecheck  ✓
@motungi/core:typecheck    ✓
@motungi/mobile:typecheck  ✓
@motungi/web:typecheck     ✓
Tasks: 4 successful, 4 total
```

```
$ pnpm test   (강제 재실행)
@motungi/core:   267/267 passed (13 files, ingest-fetch.test.ts 23건 포함)
@motungi/mobile:  39/39 passed (10 files)
@motungi/web:    164/164 passed (19 files)
Tasks: 3 successful, 3 total (tokens는 test 스크립트 없음)
```

```
$ pnpm lint
클린, 경고/에러 0건
```

- qa가 `index.ts` 전문을 처음부터 끝까지 읽어, 인증 검사가 `fetchSeoul`/`fetchDataGoKrJson`/`fetchDataGoKrXml`/Supabase 호출보다 **먼저** 실행되고 우회 경로가 없음을 확인(핸들러 최상단 첫 statement).
- `isCronAuthorized`가 "시크릿 미설정 시 항상 거부"를 명시적으로 구현·테스트했는지 직접 읽어 확인(`??`로 빈 값을 인증 성공으로 오인하는 버그 부재).
- `git diff --stat`으로 `0007_cron_ingest_daily.sql`을 포함한 기존 마이그레이션 15개가 전부 무수정이고 `0016`만 신규(untracked)임을 확인 — 마이그레이션 append-only 원칙 준수.
- 시크릿 grep: 하드코딩된 실키 매치 0건(`.env.example`의 `INGEST_CRON_SECRET=`은 빈 값, 마이그레이션의 publishable 키는 0007에서 이미 있던 공개 키 그대로).
- 파괴적 SQL 없음(`0016`은 `select cron.schedule(...)` 단일 구문, `DROP`/`DELETE`/`TRUNCATE` 없음).
- qa 판정: **"PASS — safe to push to dev."** (부가 의견: `isCronAuthorized`가 `===` 비교라 타이밍 세이프하지 않음 — 이 위협 모델에선 허용 가능한 수준이나, 더 엄격한 기준이 필요하면 후속 과제로 남김. blocking 아님.)
- M-026 done_when 4개 항목 전부, 구현자 주장이 아니라 qa의 실제 diff 정독 + 명령 재실행 출력 대조로 ✅ 확인됨.

## 리스크 & 후속

1. **배포 동기화 필요**: 이 커밋만으로는 아무 것도 깨지지 않지만(코드는 통과), 실제 프로덕션에서 인증이 걸리려면 사람이 (a) `select vault.create_secret('<랜덤값>', 'INGEST_CRON_SECRET')`를 1회 실행하고 (b) Edge Function secret으로 같은 값을 `supabase secrets set INGEST_CRON_SECRET=...`로 등록하고 (c) `0016` 마이그레이션을 실제 DB에 적용해야 한다. 세 가지를 같은 타이밍에 하지 않으면 — 특히 마이그레이션만 먼저 적용되고 Edge Function secret이 비어 있으면 — cron이 매일 401을 받아 적재가 멈춘다(M-041의 "실패가 조용히 넘어가지 않는다" 설계 덕분에 이번엔 500/401로 드러나긴 함). 아침에 사람이 dev→main 승격 전 반드시 확인할 항목.
2. **타이밍 세이프 비교 아님**: `isCronAuthorized`의 `===` 비교는 이론상 타이밍 공격에 취약하다. 서버 대 서버(cron) 호출만 있는 현재 위협 모델에선 낮은 우선순위지만, 더 엄격한 기준을 원하면 상수시간 비교로 교체할 것(qa 부가 의견).
3. **Deno 네이티브 통합 테스트 공백**: `Deno.serve` 핸들러 자체(HTTP 계층)는 이번에도 직접 테스트되지 않았다 — 순수 로직(`isCronAuthorized`)만 vitest로 검증됐다. 이 repo엔 Deno test 인프라가 전무해 M-026 done_when #3이 명시한 "Deno test 또는 vitest 이식" 중 후자를 택했다. 실제 401 응답 자체의 통합 검증은 여전히 사람의 수동 curl 확인 또는 후속 Deno test 인프라 도입에 의존한다.
4. status는 `todo → doing`으로만 갱신했다 — `done` 승격은 사람의 최종 리뷰 몫이며, 위 배포 선행 조건 완료를 사람이 확인한 뒤 승격하는 것을 권장한다.
5. **문서 모순 기록 없음**: 트리거 프롬프트와 repo 문서(`CLAUDE.md`, `.claude/agents/*.md`, `docs/nightly/PIPELINE.md`, `.claude/rules/workflow/nightly-pipeline.md`) 사이에 이번 밤 발견된 모순은 없었다 — 모두 dev 트렁크 모델·qa 푸시 게이트·audit 게이트 조항이 트리거 PRECEDENCE 절과 일치했다(M-024에서 정정된 상태 유지 확인).
6. 다음 밤 후보: 남은 high todo는 M-027(catalog.ts unchecked cast)·M-030·M-034·M-035 — tie-break상 M-027이 유력.

## dev 직접 커밋 (PR 없음)

이번 밤도 트렁크 기반 모델을 따랐다: 브랜치를 파지 않고 `origin/dev` 위에 직접 커밋·push하며 PR은 열지 않는다. 구현 변경과 이 리포트·backlog 커밋 모두 `dev`에 직접 쌓이고, qa PASS 판정에 따라 `git push origin dev`로 push한다. `dev → main` 승격은 사람이 주기적으로 직접 판단하며, 이는 야간 파이프라인의 역할이 아니다 — main은 이번 밤에도 건드리지 않았다.
