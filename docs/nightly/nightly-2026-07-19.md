# Nightly Report — 2026-07-19

## TL;DR

- 집은 이슈는 **M-012**(medium, "타입 안전성 하드닝: 진단 답변 유니온 검증 + `pickTop` 제네릭", `packages/core`+`apps/web`+`apps/mobile`). M-008~M-011(더 낮은 id의 todo 이슈)은 전부 이미 미병합 draft PR(#11~#14)이 열려 있어 재작업 시 중복/충돌 위험이 커서, planner가 실제로 아직 아무도 손대지 않은 다음 todo 이슈로 넘어갔다.
- `packages/core`에 `draftToAnswers()`(진단 draft→`DiagnosisAnswers` 순수 검증 함수, 기존 사장 코드였던 `isDiagnosisComplete` 재사용)를 추가해 `interests: [undefined]`가 만들어질 수 있던 무검증 캐스팅 경로를 없앴고, `pickTop`을 `scoreAll`과 동일한 `<T extends Opportunity>` 제네릭으로 일반화해 loading 화면의 `as MockOpportunity` 다운캐스트를 제거했다.
- 검증: core **9 files / 115 tests 통과**, web **11 files / 103 tests 통과**, mobile **4 files / 32 tests 통과**, 4개 패키지 typecheck 전부 클린, web build **19/19 라우트** 성공 — 리뷰어가 브랜치를 직접 열어 전부 재실행해 qa 판정과 정확히 일치함을 확인했다.
- diff는 8개 파일(`packages/core` 4개 중 1개 신규, `apps/web`+`apps/mobile` 4개)로 planner 스펙·architect 결정 범위와 정확히 일치, 시크릿·파괴적 SQL 없음.
- **후속 조치 필요(강조)**: `main` 대상 미병합 draft PR이 이미 4개(#11~#14, M-008~M-011, 2026-07-15~18일 밤 산출물) 쌓여 있고, 각 PR 본문에서 4일 연속 `dev` 브랜치 부재 문제를 사람에게 제기했으나 아직 아무 조치가 없다. 오늘 밤 PR이 5번째로 쌓인다. 아래 "리스크 & 후속" 참조.

## 집은 이슈

- **M-012** — 타입 안전성 하드닝: 진단 답변 유니온 검증 + `pickTop` 제네릭 (priority: medium, scope: `packages/core`, `apps/web`, `apps/mobile`, status: todo → **doing**)
  - planner 스펙 요약: (1) 진단 옵션 값을 유니온으로 좁혀 저장하거나 `saveAnswers` 직전 `isDiagnosisComplete`로 검증해 `final[0]` undefined 가드로 `interests: [undefined]` 생성을 막을 것, (2) `scoreOpportunity`/`pickTop`을 `<T extends Opportunity>(candidates: T[]) => ScoredOpportunity<T>[]` 제네릭으로 만들어 loading 페이지의 `as MockOpportunity` 다운캐스트를 제거할 것, (3) 웹/모바일 typecheck·scoring 테스트 통과.
  - type-safety finding 2건(감사 파이프라인 산출) 통합: "진단 답변 `Record<number,string>` → 도메인 유니온 무검증 캐스팅"(medium)과 "`pickTop`이 구체 타입을 잃어 다운캐스트를 강제"(low).
- M-008(#11)·M-009(#12)·M-010(#13)·M-011(#14)은 전부 status 상으로는 여전히 `todo`이지만, 실질적으로 이미 미병합 draft PR이 각각 열려 있어 이번 밤 재작업 대상에서 제외됐다(중복 작업/브랜치 충돌 방지). backlog.yml에서 이들의 status는 이번 밤 건드리지 않았다.

## 아키텍처 검토

- architect가 planner 스펙을 실제 코드와 대조해 참조된 파일·라인 번호(당시 `diagnosis/page.tsx`·`loading/page.tsx`의 캐스팅 위치, `scoring.ts`의 `pickTop` 시그니처)가 모두 정확함을 확인했다.
- 담당 배정: **backend-impl 먼저(`packages/core`, 블로킹)** → **frontend-impl(`apps/web`+`apps/mobile`, 병렬, 서로 non-blocking)**.
- architect가 스펙의 "선택 사항"이던 부분을 **필수로 격상**: 진단 draft→`DiagnosisAnswers` 검증 가드를 web·mobile 화면에 각각 중복 구현하는 대신, `packages/core`에 순수 함수(`draftToAnswers`)로 중앙화하도록 결정. 이유는 `apps/mobile`에 화면 단위 컴포넌트 테스트 인프라가 전혀 없어서, 모바일 로컬 가드를 만들어도 검증할 방법이 없기 때문 — 로직을 core에 두면 core의 유닛 테스트로 플랫폼 무관하게 커버된다.
- 이 결정은 실제 구현에서 그대로 적용됐다: `draftToAnswers`는 core에만 존재하고, web·mobile은 호출부만 갖는다(아래 "변경 내용" 참조).

## 변경 내용

- `packages/core/src/scoring.ts`: `pickTop`을 `scoreAll`과 동일한 제네릭 패턴(`<T extends Opportunity>`)으로 일반화. 반환 타입도 `(ScoredOpportunity & { opportunity: T })[]`로 입력 타입을 보존하도록 변경. 함수 본문(로직) 자체는 변경 없음.
- `packages/core/src/diagnosis.ts`: 신규 export `draftToAnswers(draft: Record<number, string | undefined>): DiagnosisAnswers | null`. 이전까지 미사용(사장 코드)이던 `isDiagnosisComplete`를 재사용. `draft[0]`=interest, `draft[1]`=timeSlot, `draft[2]`=energy를 각각 알려진 값 집합(`VALID_INTERESTS`/`VALID_TIME_SLOTS`/`VALID_ENERGIES`)에 대해 검증하고, 하나라도 유효하지 않거나 미완료면 `null`을 반환한다 — 이전처럼 `interests: [undefined]`가 만들어질 수 없다.
- `packages/core/src/diagnosis.test.ts` (신규, 10 tests), `packages/core/src/scoring.test.ts` (+1 test): 위 두 변경에 대한 단위 테스트.
- `apps/web/src/app/diagnosis/page.tsx`, `apps/mobile/app/diagnosis.tsx`: `final[0] as Interest` 등 무검증 캐스팅 3종을 제거하고 `draftToAnswers(final)` 호출로 대체. 반환값이 `null`이면 조기 `return`(방어적 분기 — 자동 진행 UI 상 실제로는 도달 불가, 아래 검증 참조).
- `apps/web/src/app/loading/page.tsx`, `apps/mobile/app/loading.tsx`: `pickTop` 결과에 대한 `as MockOpportunity` 다운캐스트를 제거(제네릭 추론으로 대체돼 더 이상 필요 없음).
- 총 8개 파일(`packages/core` 4개 중 1개 신규 + `apps/web`/`apps/mobile` 4개), DB 마이그레이션·시크릿·CI 설정 변경 없음. 리뷰어가 `git diff origin/main..HEAD --name-only`로 직접 재확인해 위 8개 파일과 정확히 일치함을 확인했다.

## 검증

qa가 실제로 돌린 명령 + 리뷰어가 브랜치를 직접 열어 동일 명령을 독립적으로 재실행한 결과(전부 일치):

```
$ pnpm --filter @motungi/core test
 ✓ src/scoring.test.ts (31 tests)
 ✓ src/adapters/seoul-jobs.test.ts (13 tests)
 ✓ src/view.test.ts (25 tests)
 ✓ src/adapters/sports-facility.test.ts (9 tests)
 ✓ src/adapters/seoul-culture.test.ts (6 tests)
 ✓ src/diagnosis.test.ts (10 tests)
 ✓ src/adapters/util.test.ts (12 tests)
 ✓ src/adapters/culture-info.test.ts (4 tests)
 ✓ src/adapters/trail.test.ts (5 tests)

 Test Files  9 passed (9)
      Tests  115 passed (115)
```

```
$ pnpm --filter @motungi/core typecheck   # 에러 0
$ pnpm --filter @motungi/web typecheck    # 에러 0
$ pnpm --filter @motungi/mobile typecheck # 에러 0
```

```
$ pnpm --filter @motungi/web test
 Test Files  11 passed (11)
      Tests  103 passed (103)
```
(진단 전체 플로우 통합 테스트·explore 테스트 포함 — 이번 diff가 건드리지 않은 파일들이라 회귀 없음을 재확인.)

```
$ pnpm --filter @motungi/mobile test
 ✓ store/useAppStore.test.ts (15 tests)
 ✓ data/opportunities.test.ts (11 tests)
 ✓ hooks/useEnsureCatalog.test.ts (5 tests)
 ✓ lib/geo.test.ts (1 test)

 Test Files  4 passed (4)
      Tests  32 passed (32)
```
(모바일은 화면 단위 테스트가 애초에 없음 — 기존 공백이며 이번 변경이 만든 것이 아니다. architect가 사전에 명시적으로 플래그했다.)

```
$ pnpm --filter @motungi/web build   # 성공, 19/19 라우트
```

- qa가 8개 수용 기준 전부 ✅ 판정, diff 스코프가 planner가 허용한 8개 파일과 정확히 일치함을 확인, 시크릿·파괴적 SQL 매치 0건 확인.
- qa는 테스트 통과만으로 끝내지 않고 코드를 직접 읽어 `draftToAnswers`의 `null` 분기가 기존 auto-advance UI 가드(미완료 단계에서 버튼 비활성화)로 인해 **실제로는 도달 불가능함을 증명**했다 — 즉 조기 `return`(no-op)이 안전함을 확인했다.
- 리뷰어도 위 판정에 동의한다: `git diff origin/main..HEAD | grep -iE "password|secret|api_key|DROP TABLE|DELETE FROM|TRUNCATE"` 재실행 결과 매치 없음.
- 종합 판정: **통과 (pass)**.

## 리스크 & 후속

- **[운영 에스컬레이션 — 사람 확인 필요, 최우선]** `main` 대상 미병합 draft PR이 현재 **4개** 쌓여 있다: `#11`(2026-07-15, M-008), `#12`(2026-07-16, M-009), `#13`(2026-07-17, M-010), `#14`(2026-07-18, M-011). 각 PR 본문에서 `docs/nightly/PIPELINE.md`가 규정한 `dev` 브랜치 기준선 모델이 실제와 맞지 않는다(`origin/dev`가 존재하지 않음)는 문제를 사람에게 명시적으로 제기했으나, 4일 이상 아무 조치가 없었다. 오늘 밤 PR이 **5번째**로 쌓이게 된다. 사람이 해야 할 일 두 가지: (a) `#11`~`#14` 리뷰 후 머지 또는 정리, (b) `dev` 브랜치 문제를 확정 — `origin/dev`를 실제로 만들거나, `PIPELINE.md`를 고쳐 `main`이 실제 기준선임을 명시할 것. 리뷰가 미뤄질수록 PR 적체와 브랜치 간 드리프트 위험이 커진다.
- 이번 밤도 위 미해결 상태 때문에 `origin/dev` 대신 `origin/main`을 기준선으로 사용했다(2026-07-15일 밤 이후 동일하게 반복된 관행). 이는 리뷰어의 임의 판단이 아니라 `origin/dev`가 실제로 존재하지 않는 데 따른 불가피한 선택이었다.
- **알려진 수용된 공백(이번 이슈 범위 밖)**: `apps/mobile`에 화면 단위 컴포넌트 테스트 인프라가 전혀 없음 — architect·qa 양쪽이 사전에 플래그했고, 이번 밤 해결하지 않았다. 다음 밤 후보로 남겨둘 만하다.
- `draftToAnswers`의 `null` 분기(조기 return)는 현재 UI 가드 하에서는 도달 불가능하지만, 향후 UI 가드가 느슨해지는 방향으로 리팩터링될 경우 이 분기가 실제로 발동할 수 있다 — 그 시점엔 조용한 no-op 대신 사용자에게 보이는 에러 처리가 필요할 수 있음을 유의.
- 다음 밤 후보: M-013 이후 이슈들(단, `#11`~`#14` 적체가 먼저 정리돼야 M-008~M-011도 안전하게 재평가 가능).

## PR

(PR 링크는 오케스트레이터가 브랜치 push 후 채웁니다) — draft, `main` 대상 예정. 리뷰어는 `gh pr create` 실행 권한이 없어(현재 실행 환경에 `gh` CLI·GitHub MCP 도구 모두 없음) 브랜치 커밋까지만 수행했다.
