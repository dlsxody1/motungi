# Nightly Report — 2026-09-21

## TL;DR

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `status: todo` 11건(직접 파싱 스크립트로 확인 —
  `docs/backlog/backlog.yml`의 `scope: [...]` 안에 `[gu]` 같은 대괄호가 섞여 있어 PyYAML 파싱이
  `line 2857`에서 실패한다. 단순 `grep -c "status: todo"`도 notes 본문 속 문구까지 걸릴 수 있어
  `re.split(r'\n(?=  - id: )')`로 이슈 블록을 나눠 `status`/`priority`/`scope`만 정규식으로
  추출했다) → `todo ≥ 2`이므로 구현 모드.
- **연속 report-only 경보 해당 없음** — 직전 세 밤(09-15: M-101/M-112, 09-17: M-107, 09-19: M-109)
  모두 코드가 나갔다.
- **세션이 빈 컨테이너로 시작** — `mcp__Claude_Code_Remote__list_repos`로 `dlsxody1/motungi`를
  찾아 clone 후 `git checkout dev`(→ `origin/dev` 추적)로 시작했다.
- **`main`이 `dev`보다 앞서 있지 않음을 확인**(`git log origin/dev..origin/main` 비어 있음) —
  되받기 절차 불필요.
- **클러스터 = M-111 단독(1건).** 아래 "집은 클러스터" 참조.
- **M-111 shipped (커밋 `9581fc1`).** `/explore/[gu]` 페이지의 로컬 `itemListJsonLd`를 제거하고
  `lib/seo.ts`의 공용 export를 쓰도록 통합.
- **되돌린 이슈 없음.** `bash scripts/gate.sh` 전체 PASS, 최종 dev 트리 green.
- **문서 불일치 재확인(7번째 지적)** — 아래 "레포 문서와의 불일치" 참조. 이번 밤은 영향 없었지만
  (M-111도 scope에 이미 구체 파일이 있어 폴백 조건 자체가 성립하지 않음) 사람이 정정 필요.
- **버그 발견(신규, 코드 미수정)**: `docs/backlog/backlog.yml:2857` 부근 `scope: [apps/web/src/app/explore/[gu]/page.tsx, ...]`가
  대괄호 미이스케이프로 YAML 파서 오류를 낸다. 이번 밤 작업엔 영향 없었으나(정규식으로 우회) 사람이
  나중에 이 파일을 YAML 파서(PyYAML 등)로 다루면 즉시 걸린다. 백로그 신규 등재는 audit 모드
  전용이라 이 밤은 등재하지 않고 여기 기록만 남긴다.

## 집은 클러스터

### 씨앗 선정
- `status: todo` 11건 중 priority 분포: medium 5건(M-097·M-111·M-113·M-114·M-115),
  low 6건(M-088·M-092·M-093·M-102·M-103·M-110), high 0건.
- priority 최상위는 medium. medium 중 id 최소는 **M-097**이지만 notes에 `blocked-human`으로
  명시돼 있어(.env 없음·data.go.kr/서울 열린데이터광장 인증키 없음·outbound network 프록시
  차단 — 09-07부터 반복 재검증) 씨앗에서 제외했다. 오늘 밤도 같은 사실을 재확인했다:
  `.env`는 `.env.example`만 존재하고 `DATA_GO_KR_SERVICE_KEY`·`SEOUL_OPENAPI_KEY` 모두 공란,
  `curl --max-time 5 https://apis.data.go.kr/` → `CONNECT tunnel failed, response 403`
  (agent-proxy: `connect_rejected`, organization policy). 변화 없음 → 계속 제외.
- 다음 최소 id는 **M-111** → 씨앗. scope: `apps/web/src/app/explore/[gu]/page.tsx`,
  `apps/web/src/lib/seo.ts`(구체 파일 2개).

### scope 겹침 검토 (M-111 scope: `[gu]/page.tsx`, `lib/seo.ts`)

| id | scope | 겹침 판정 |
|---|---|---|
| M-088 | `apps/mobile/app/(tabs)/report.tsx` | 겹침 없음 |
| M-092 | `packages/tokens/src/index.ts` | 겹침 없음 |
| M-093 | `packages/core/src/adapters/seoul-jobs.ts`, `.../util.ts` | 겹침 없음 |
| M-097 | `supabase/migrations`, `supabase/functions/ingest/adapters.ts`, `packages/core/src/catalog.ts` | 겹침 없음(blocked-human으로 씨앗 후보에서도 제외됨) |
| M-102 | `apps/web/src/app/diagnosis/page.tsx`, `.../location/page.tsx` | 겹침 없음(다른 파일) |
| M-103 | `apps/web/.eslintrc.json` | 겹침 없음 |
| M-110 | `packages/core/src/view.ts` | 겹침 없음 |
| M-113 | `apps/web/src/lib/geo.ts`, `apps/mobile/lib/geo.ts`, `packages/core/src` | 겹침 없음(`geo.ts` ≠ `seo.ts`, `packages/core/src`는 패키지 전체라 좁은 디렉토리 아님) |
| M-114 | `packages/core/src/index.ts`, `.../gu-summary.ts` | 겹침 없음 |
| M-115 | `apps/mobile/ui/theme.ts`, `packages/tokens/src/index.ts` | 겹침 없음 |

파일/좁은 디렉토리 겹침 후보 0건.

**폴백 미적용**: 트리거 프롬프트의 폴백 발동 조건은 "씨앗 scope에 파일도 좁은 디렉토리도
전혀 없을 때만"이다. M-111의 scope는 이미 구체 파일 2개(`[gu]/page.tsx`, `lib/seo.ts`)를
담고 있으므로 이 조건 자체가 성립하지 않는다 — 폴백을 쓰지 않았다.

### 레포 문서와의 불일치 (7번째 지적)
`.claude/rules/workflow/nightly-pipeline.md:61`(및 `docs/nightly/PIPELINE.md`의 대응 절)의
폴백 조건은 "**파일 겹침 후보가 0개면**(씨앗 scope에 파일이 섞여 있든 없든 무관 — 걸린 게
없다는 사실이 기준)"으로, 트리거 프롬프트보다 더 넓다. 이 repo 문서 그대로라면 오늘 밤도
폴백 후보(같은 medium priority인 M-113/M-114/M-115 중 id 오름차순 2개, 즉 M-113·M-114)를
클러스터에 더했을 것이다. 09-09부터 이번까지 반복 지적된 것과 동일한 지점이며, 이번 밤도
PRECEDENCE 절에 따라 트리거 프롬프트의 좁은 조건을 우선했다. M-111의 scope가 이미 구체
파일을 담고 있어 두 조건 모두 "폴백 미적용"이라는 결론은 같았지만, 씨앗의 scope가 파일
없이 앱/패키지 단위로만 적힌 밤이 오면 두 문서가 서로 다른 클러스터 크기를 낼 것이다.
**일곱 번째 지적: 사람이 트리거 프롬프트 또는 두 repo 문서 중 한쪽을 정정해야 한다.**

**결과: 클러스터 = M-111 단독. 1건.** 상한 3개 미달이지만 겹치는 이슈가 없고 폴백도
발동하지 않아 억지로 채우지 않았다 — 정상 결과다.

## 이슈별 결과

| 이슈 | 결과 |
|---|---|
| M-111 | shipped (커밋 `9581fc1`) |

되돌린 이슈 없음. 미룬 이슈 없음(상한 3개 미달).

## 아키텍처 검토

- **경계 배치**: `apps/web/src/app/explore/[gu]/page.tsx`(서버 컴포넌트, App Router 라우트) +
  `apps/web/src/lib/seo.ts`(순수 lib) 두 파일 모두 `apps/web` 단일 앱 내부 변경 →
  **frontend-impl** 담당. 레이어 방향(`app/ → lib/`)은 그대로 유지 — `page.tsx`가 `lib/seo.ts`를
  import하는 기존 방향에서 로컬 함수 하나가 그 방향을 따라가지 않던 것(같은 로직을 `app/` 안에
  중복 정의)을 바로잡은 것뿐, 새로운 역방향 의존은 생기지 않았다.
- **파일구조 규율**: `constitution.md`의 "FSD 디렉토리는 실재하지 않는다 · 평면 배치 · 파일명
  접두사" 규율과 무관한 변경 — 새 파일·새 디렉토리 없음, 기존 두 파일만 수정.
- **데이터 흐름**: `itemListJsonLd`의 시그니처가 `(gu, items)` → `(items, name)`(lib/seo.ts의
  기존 시그니처)로 바뀌었다. 호출부 1곳(`[gu]/page.tsx`)만 존재해 시그니처 변경의 파급 범위가
  이 커밋 안에서 닫힌다. `name` 문자열(`${gu} 퇴근 후·주말 활동`)은 done_when 요구대로 그대로
  유지했다.
- **null 계약**: `lib/seo.ts`의 `itemListJsonLd`는 빈 배열이면 `null`을 반환한다(M-096 선례,
  `faqJsonLd`와 동일 계약). 로컬 구현은 이 계약이 없어 빈 목록이어도 빈 `<script>`를 냈을
  가능성이 있었다(`getGuData`가 `GU_MIN_ACTIVITIES` 임계 미달이면 애초에 `null`을 반환해 이
  분기 자체가 실제로는 안 타지만, 계약을 명시적으로 맞추는 편이 안전하다) — 렌더를
  `{jsonLd && <script .../>}` 가드로 바꿔 계약을 명시했다.
- **테스트 전략**: 순수 함수(`itemListJsonLd`)는 이미 `apps/web/src/lib/seo.test.ts`가 31개
  테스트로 이스케이프·빈 배열·필드 조립을 커버한다. `[gu]/page.tsx` 자체는 기존에도 전용 테스트
  파일이 없었고(서버 컴포넌트 + Supabase 의존이라 통합 성격), done_when도 "기존 seo.test.ts와
  구 페이지 테스트가 통과한다"만 요구한다 — 신규 단위 테스트를 추가하지 않고 기존 스위트
  회귀 여부로 충분하다고 판단했다.

## 변경 내용

- `apps/web/src/app/explore/[gu]/page.tsx` — 로컬 `itemListJsonLd(gu, items)` 함수(26줄, 수기
  `.replace(/</g, "\\u003c")` 이스케이프)를 제거하고 `@/lib/seo`의 `itemListJsonLd(items, name)`를
  import해 사용. `SITE_URL` import는 로컬 함수 제거로 더는 필요 없어 함께 제거(`opportunityPath`는
  링크 렌더에 계속 쓰여 유지). 빈 목록 시 `null`을 반환하는 계약에 맞춰 `{jsonLd && <script .../>}`
  가드 추가. 순 변경: +3/-25줄.

## 검증 (실제 출력)

```
$ pnpm --filter web test -- src/lib/seo.test.ts
 ✓ src/lib/seo.test.ts (31 tests) 13ms
 Test Files  1 passed (1)
      Tests  31 passed (31)

$ pnpm --filter @motungi/core test
 Test Files  16 passed (16)
      Tests  429 passed (429)

$ pnpm --filter mobile test
 Test Files  23 passed (23)
      Tests  184 passed (184)

$ bash scripts/gate.sh
── typecheck   ✅
── test        ✅ (core 429 / mobile 184 / web 395, 전부 48+16+23 파일)
── lint        ✅ (신규 경고 없음 — apps/web의 기존 max-lines/max-lines-per-function/
                  complexity 경고 10건만 잔존, 전부 이번 diff와 무관한 파일:
                  diagnosis·explore(list)·location·saved·neighborhood-menu·
                  opportunity-detail-desktop/mobile·report-desktop·web-landing)
── secrets     ✅ (시크릿 패턴 없음)
── pure-tests  ✅ (누락 없음, 면제 6건 그대로)
GATE: PASS — push 허용
```

done_when 4개 판정:
1. ✅ "`[gu]/page.tsx:126`의 로컬 `itemListJsonLd`를 제거하고 `lib/seo.ts`의 export 버전을
   쓴다" — 로컬 함수 삭제, `@/lib/seo`에서 import.
2. ✅ "구 페이지의 name 문자열(`${gu} 퇴근 후·주말 활동`)이 그대로 유지된다" — 호출부에서
   동일 문자열을 `name` 인자로 전달.
3. ✅ "빈 목록일 때 seo.ts 버전이 null을 반환하므로 구 페이지가 빈 스크립트를 내지 않는지
   확인한다" — `{jsonLd && <script .../>}` 가드로 명시.
4. ✅ "기존 seo.test.ts와 구 페이지 테스트가 통과한다" — `seo.test.ts` 31개 전부 통과,
   `[gu]/page.tsx` 전용 테스트는 원래 없음(서버 컴포넌트, 통합 성격) — `pnpm --filter web test`
   전체 395개 회귀 없음으로 확인.

diff 오염 없음(`git diff --stat` = `[gu]/page.tsx` 1개 파일만). 시크릿 노출 없음. 파괴적
마이그레이션 없음(DB 변경 자체가 없음).

## 리스크 & 후속

- **낮은 리스크.** JSON-LD 조립 로직 자체(필드 구성)는 두 구현이 완전히 동일했고(이슈
  notes에서 확인) 바뀐 건 이스케이프 방식과 호출 계약뿐이다. `seo.test.ts`가 이미 이스케이프·
  빈 배열 케이스를 커버해 회귀 위험이 낮다.
- **M-097(동 단위 좌표 백필)이 여전히 blocked-human이다.** 09-07부터 여섯 밤 연속 재검증
  결과가 같다 — `.env` 값·data.go.kr 인증키·지오코딩 데이터셋 중 하나를 사람이 제공해야
  다음 밤이 이걸 씨앗으로 잡을 수 있다.
- **문서 정정 필요 (누적, 사람 개입 요청)**:
  1. `.claude/rules/workflow/nightly-pipeline.md:61` / `docs/nightly/PIPELINE.md`의
     클러스터 폴백 조건("파일 겹침 후보가 0개면")을 트리거 프롬프트의 조건("씨앗 scope에
     파일도 좁은 디렉토리도 전혀 없을 때만")과 일치시킬 것 — 09-09부터 이번까지 7회 지적.
  2. `docs/backlog/backlog.yml:2857`(M-111 항목)의 `scope: [apps/web/src/app/explore/[gu]/page.tsx, ...]`가
     대괄호 안에 또 대괄호(`[gu]`)를 이스케이프 없이 넣어 표준 YAML 파서(PyYAML 등)가
     `ParserError`로 죽는다. 오늘 밤은 정규식 기반 파서로 우회했지만, 앞으로 이 파일을
     `yaml.safe_load`로 다루는 도구/스크립트를 추가하면 즉시 걸린다. `scope`를 flow
     시퀀스 대신 block 시퀀스(`- apps/web/...`)로 쓰거나 따옴표로 감싸는 편을 권한다
     (audit 모드가 아니라 이 항목을 새 M-NNN으로 등재하지는 않았다 — 코드 버그가 아니라
     백로그 자체의 문법 문제이고, 사람이 직접 고치는 편이 다음 밤의 파싱 스크립트를
     복잡하게 만들지 않는 길이라 판단).
- **backlog.yml**: M-111 → `doing`. 사람 최종 리뷰 후 `done` 승격.

## 커밋

- `9581fc1` — `fix(web): [gu]/page의 로컬 itemListJsonLd를 lib/seo.ts 공용 구현으로 통합 (M-111)`
