# Nightly Report — 2026-08-13

## TL;DR
클러스터 = **M-050 + M-051 + M-053**(모바일, 웹-패리티 계열). `bash scripts/gate.sh`(typecheck·test·lint·시크릿 스캔, 4패키지 전체)를 reviewer가 dev HEAD(`4802e8e`)에서 fresh하게 재실행해 **PASS**(typecheck 4/4 ✅, test core 263+mobile 85+web 279=627/627 ✅, lint ✅, secrets ✅) 확인 — **3건 모두 shipped, dev로 나갔다.** revert 없음. 클러스터 구성 과정에서 씨앗 M-052(지도·경로 렌더)는 architect 단계에서 사람 승인 없이는 채택 가능한 구현 경로가 없다고 판단해 **제외**됐고, 상한 3개 때문에 한 번 밀려났던 M-053이 그 빈자리를 메우며 다시 편입됐다(§집은 클러스터 참조 — 다소 이례적인 2단계 결정이라 전체 경위를 상세히 남긴다). 연속 report-only 0회(§0단계 참조) — 경고 불필요.

## 0단계 — 백로그 게이트
밤 시작 시점 `docs/backlog/backlog.yml`의 `status:todo` 개수는 8건(M-038·M-043·M-050~M-055) → **≥2, 구현 모드**로 정상 진행. audit 모드 미발동.

연속 report-only 여부: `docs/nightly/` 최근 노트를 확인했다 — 2026-08-11(`nightly-2026-08-11.md`)은 M-044+M-049 shipped, 2026-08-12(`nightly-2026-08-12.md`)는 M-048 shipped. 둘 다 코드/문서 산출물이 실제로 dev에 나간 밤이라 **오늘 밤 직전의 연속 report-only 횟수 = 0**. TL;DR 상단 경고(`⚠️`) 불필요 — reviewer.md 0단계 규칙에 따라 실제 파일을 열어 확인했다(가정하지 않음).

## 집은 클러스터

### 1단계 — 씨앗 선정과 파일겹침 폴백
- **씨앗**: `status:todo` 8건 중 priority 최상위는 medium 4건(M-050~M-053, M-054·M-055는 low) — 동순위라 id 오름차순으로 **M-050**(scope `apps/mobile/app/index.tsx`)이 씨앗.
- **파일 겹침 판정**: 나머지 medium 3건(M-051 `apps/mobile/app/(tabs)/explore.tsx`+`opportunity.tsx`, M-052 `apps/mobile/app/opportunity.tsx`, M-053 `apps/mobile/app/opportunity.tsx`) 중 어느 것도 `index.tsx`를 건드리지 않는다 — **파일 겹침 후보 0개**.
- **폴백 발동**: 같은 앱(`apps/mobile`) + 같은 priority(medium) + id 오름차순 최대 2개 추가 → **M-051, M-052 편입**. **M-053도 자격은 있었으나(같은 앱·같은 priority) 상한 3개(씨앗+2)에 걸려 이 시점엔 미편입** — 이 사실을 이 리포트 자신이 명시적으로 기록해둔다(2단계에서 다시 등장하기 때문).
- **탈락**: M-054·M-055는 priority가 low라(씨앗은 medium) 폴백 대상이 아니다 — 애초에 자격 없음.
- **1단계 결과 클러스터**: M-050(씨앗) + M-051 + M-052.

### 2단계 — architect의 M-052 재검토와 M-053 승격
planner 단계에서 이미 M-052를 이번 밤의 리스크 항목으로 표시했다(§아키텍처 검토의 M-052 판정 참조). architect가 독립적으로 세 접근을 전부 검증한 뒤 **셋 다 무인 야간 실행에서 채택 불가**하다고 판단해 M-052를 클러스터에서 빼고, **1단계에서 상한 때문에 밀려났던 M-053을 그 자리에 승격**시켰다. 즉:

```
1단계: [M-050(씨앗), M-051, M-052]  ← 파일겹침 0 → 앱+priority 폴백, 상한 3개
                          ↓ architect가 M-052를 인간 승인 필요로 판정, 제외
2단계: [M-050(씨앗), M-051, M-053]  ← M-053은 1단계에서 이미 자격 확인됐던 대기 후보가 승격
```

이건 클러스터 규칙의 통상 실행 순서(1회성 폴백)와 다른 2단계 결정이라 이례적이지만, "상한에 걸려 대기했던 자격 있는 후보를 architect의 사후 교체 사유로 끌어올린 것"이므로 클러스터 규칙(같은 앱·같은 priority) 자체를 위반한 게 아니다 — M-053은 애초에 폴백 자격 판정을 이미 통과해 있었다. **최종 클러스터: M-050 + M-051 + M-053(3건).**

### 상한 3개로 미룬 이슈
- **M-052**(medium, `apps/mobile/app/opportunity.tsx`) — 자격은 있었으나(1단계 폴백으로 일시 편입) architect가 무인 야간 실행에 채택 가능한 구현 경로가 없다고 판단해 **제외**했다. `todo`로 남겨 다음 밤(사람 개입 후)에 재시도 가능하게 한다 — §리스크 & 후속 참조.
- M-054·M-055(low)는 씨앗과 priority가 달라 애초에 폴백 자격이 없었다 — 상한 때문에 잘린 게 아니다.

## 이슈별 결과

### M-050 — 랜딩 2차 CTA 죽은 /report 링크 (shipped, doing)
- **done_when 4개**: (1)(2)(3) ✅ 2차 CTA가 웹과 동등한 목적지 `/explore`로 이동, 안내 카피는 비인터랙티브 캡션으로 CTA 아래 유지, 1차(`/location`)·2차(`/explore`) 경로가 웹과 개수·의미 대응. (4) ✅ `index.test.tsx`에 두 CTA의 실제 push 목적지 검증 테스트 확장.
- 커밋: `a528c43` `fix(mobile): 온보딩 2차 CTA를 죽은 /report 대신 /explore로 연결 (M-050)` — `apps/mobile/app/index.tsx`, `apps/mobile/app/index.test.tsx` (2 files, +34/-4).

### M-051 — 탐색·상세 화면 활동 썸네일 이미지 부재 (shipped, doing)
- **done_when 4개**: (1) ✅ 탐색 `ActivityItem`에 64x64 고정 썸네일 추가(웹 `thumbnail.tsx`와 동등 폴백 규칙). (2) ✅ 상세 화면 상단에 16:9 배너 썸네일 추가. (3) ✅ 고정 크기/비율로 CLS 방지(레이아웃 밀림 없음). (4) ✅ 이미지 로드 실패 시 폴백 렌더 테스트(`thumbnail.test.tsx` 3케이스, jsdom `window.Image` 더블로 재현).
- 신규 `apps/mobile/ui/thumbnail.tsx`는 `hero-carousel.tsx`의 기존 RN `Image` 패턴을 따르며 **새 의존성 없음**.
- `apps/mobile/app/(tabs)/saved.tsx`는 backlog notes가 명시한 scope 제외 대상이라 **의도적으로 손대지 않음** — grep으로 재확인.
- 커밋: `91e3ee5` `feat(mobile): 탐색·상세 화면에 활동 썸네일 이미지 렌더 (M-051)` — `apps/mobile/app/(tabs)/explore.tsx`+`.test.tsx`, `apps/mobile/app/opportunity.tsx`+`.test.tsx`, 신규 `apps/mobile/ui/thumbnail.tsx`+`.test.tsx` (6 files, +170).

### M-052 — 상세 화면 지도·산책로 폴리라인 부재 (제외 — 이번 밤 미착수, todo 유지)
클러스터 1단계에는 편입됐으나 architect 검증 후 클러스터에서 빠졌다. 상세 사유는 §아키텍처 검토. **shipped도 revert도 아니다 — 애초에 구현되지 않았다.**

### M-053 — 상세 화면 마감(D-day)·출처 미표시 (shipped, doing)
- **done_when 4개**: (1) ✅ `packages/core`의 `deadlineLabel()`로 D-day 계산, 웹 `DdayPill`과 동일한 3톤(지남/임박≤3일/여유) 구분. (2) ✅ `sourceLabel` 있으면 출처 표시. (3) ✅ 기존 `o.meta` 3칸 칩 유지 + 신규 블록은 보강(대체 아님). (4) ✅ 마감 지남/임박/여유 3톤 렌더 테스트, `getComputedStyle`로 실제 `backgroundColor`까지 검증.
- architect 지시대로 M-051 적용 이후의 `opportunity.tsx`를 기준으로 diff를 새로 생성해 커밋했다(§아키텍처 검토의 커밋 순서 지시 참조).
- 커밋: `4802e8e` `feat(mobile): 기회 상세에 마감(D-day)·출처 표시 추가 (M-053)` — `apps/mobile/app/opportunity.tsx`+`.test.tsx` (2 files, +131/-1).

## 아키텍처 검토

### M-052 기각 사유 — 3개 접근 전부 무인 야간 실행 부적합
planner가 먼저 M-052를 리스크 항목으로 표시했고, architect가 `.claude/rules/core/security-policy.md`·`.env.example`·코드베이스를 직접 읽어 독립적으로 재검증했다. 검토한 3개 접근과 각각의 기각 사유:

1. **`react-native-maps` 신규 네이티브 의존성** — backlog M-052의 done_when 자체가 "신규 네이티브 의존성이라 Expo prebuild(및 EAS 빌드 설정)에 영향을 줄 수 있으므로 사람 승인이 필요할 수 있다"고 명시한다. 오늘 밤은 무인 실행이라 그 승인을 받을 방법이 없다. **기각**.
2. **NAVER 정적 지도 이미지 URL** — `security-policy.md:8` "NAVER 시크릿은 `/api/geo` 프록시 뒤에 숨긴다(reverse-geocoding). 클라이언트에서 직접 네이버를 부르지 마라"가 서버-프록시 매핑 정책을 명시하는데, 이 접근은 새 백엔드 라우트(스코프 확장) + `.env.example`에 미등록된 NAVER static-map 키의 NCP(Naver Cloud Platform) 프로비저닝 확인이 필요하다. `.env.example`을 직접 확인한 결과 static-map 전용 키 항목이 없다. **기각**(스코프 초과 + 프로비저닝 미확인).
3. **키 불필요 공개 정적지도 서비스(3rd-party)** — planner가 평가했으나 검증되지 않은 외부 서비스이고, 코드베이스 전체에 기존 서드파티 지도 타일 사용 사례가 0건이다(grep 확인). `security-policy.md`가 명시하는 서버-프록시 매핑 정책(NAVER-only)과 어긋나는 새로운 외부 서비스 의존을 무인 야간에 도입하는 것은 리스크가 크다고 판단. **기각**.

세 접근 모두 "사람 승인 필요" 또는 "스코프·정책 위반"으로 귀결돼, architect는 M-052를 이번 클러스터에서 빼고 M-053으로 교체하기로 결정했다. **M-052는 오늘 밤 착수되지 않았으므로 `todo`를 유지한다** — 다음 밤이 다시 씨앗으로 집을 수 있으나, 사람이 위 세 옵션 중 하나를 승인/제공하지 않는 한 같은 이유로 재차 제외될 가능성이 높다(§리스크 & 후속 참조).

### 커밋 순서와 M-051/M-053 파일 겹침
architect는 M-051과 M-053이 둘 다 `apps/mobile/app/opportunity.tsx`를 건드리는 것을 사전에 파악하고, 실제 diff를 열어 **겹치는 영역이 서로소(disjoint)**임을 확인했다 — M-051은 파일 상단 근처(배너 썸네일), M-053은 하단 근처(D-day/출처 블록). 이를 근거로 커밋 순서를 **M-050 → M-051 → M-053**으로 지정하고, M-053의 diff는 M-051 반영 후의 파일을 기준으로 새로 생성하도록 지시했다. 구현 결과도 이 순서를 그대로 따랐다(§변경 내용의 커밋 SHA 순서 참조).

### 문서 부정합 — FSD 아키텍처 서술 (repo-doc 오류, PRECEDENCE 절 적용)
architect가 `.claude/agents/architect.md:9`("모노레포: ... TypeScript · pnpm · turbo. **FSD 아키텍처**.")를 자신의 역할 정의로 읽던 중, 이 저장소의 실제(그리고 constitution.md가 권위 있다고 명시하는) 구조와 모순됨을 발견했다. `.claude/rules/core/constitution.md:24`가 명시적으로 이렇게 규정한다:

> "**파일 구조는 기술 종류별 평면 배치다** — `app/` · `components/`(평면, kebab-case) · `hooks/` · `lib/` · `data/` · `store/`. web·mobile 모두 이 구조이며, **FSD(entities/features/widgets/shared) 디렉토리는 실재하지 않는다**(옛 룰의 잔재였다)."

grep으로 직접 확인한 결과, "FSD" 문구가 남아 있는 파일은 다음과 같다(전부 constitution.md와 모순):
- `.claude/agents/architect.md:9` — "FSD 아키텍처"
- `.claude/agents/architect.md:18` — "**FSD 경계**: entities/features/widgets/shared 계층 위반 없나."
- `.claude/agents/architect.md:28` — "FSD 계층 위반이 없음을 확인했다" (완료 정의 체크리스트 항목)
- `.claude/agents/frontend-impl.md:25` — "기존 코드 스타일·FSD 계층을 그대로 따른다"
- `.claude/agents/pixel-frontend.md:22` — "FSD 경계·import 방향을 거스르지 마라"

**중요한 정정**: 2026-08-11·2026-08-12 야간 리포트는 이 부정합을 "CLAUDE.md 12~19행 vs constitution.md 24행"으로 지목했으나, 이번 밤 reviewer가 루트 `CLAUDE.md`(42행 전체)를 직접 grep한 결과 **"FSD" 문자열이 단 한 곳도 없다** — CLAUDE.md 12~19행(모노레포 지도 표)는 계층 구조를 암시하는 표현이 없는 순수 스택/하위규칙 매핑 표일 뿐이다. 즉 과거 두 밤의 인용은 부정확했거나(혹은 그사이 CLAUDE.md가 이미 수정됐을 가능성도 있으나, `git log --oneline -- CLAUDE.md`로 확인한 최근 두 커밋 모두 이 밤보다 훨씬 이전 것이라 최근 수정 흔적은 없다) — 실제 모순의 근원은 **`.claude/agents/` 하위의 architect.md·frontend-impl.md·pixel-frontend.md 3개 파일**이다. 사람이 이 5곳(위 파일:line)을 constitution.md:24 기준으로 정정할 필요가 있다 — 상세는 §리스크 & 후속.

### 담당·경계·테스트 전략
- 3건 전부 `apps/mobile` 순수 프론트엔드 — `packages/core`·`supabase/`는 무변경(M-053이 core의 기존 `deadlineLabel()`을 그대로 소비할 뿐 core 자체는 건드리지 않음). backend-impl 불필요, frontend-impl 단독.
- FSD 경계: 위 문서 부정합 항목과 별개로, 실제 작업은 constitution.md의 평면 구조(`app/`·`ui/`) 그대로 따랐다 — M-051의 신규 `apps/mobile/ui/thumbnail.tsx`도 기존 `ui/` 디렉토리 평면 배치.
- 테스트 전략: 3건 모두 컴포넌트 렌더 테스트(vitest + RN 렌더 유틸)로 커버 — M-050 CTA 목적지 단언, M-051 이미지 렌더/폴백 3케이스, M-053 D-day 3톤(`getComputedStyle`로 실제 색상 단언). e2e는 범위 밖.

## 변경 내용
```
 apps/mobile/app/index.tsx               |  13 +++++++++++--
 apps/mobile/app/index.test.tsx          |  25 +++++++++++++++++++++--
 apps/mobile/app/(tabs)/explore.tsx      |   7 ++++
 apps/mobile/app/(tabs)/explore.test.tsx |  23 +++++++++++++
 apps/mobile/app/opportunity.tsx         |  62 +++++++++++++++++++++++++++++
 apps/mobile/app/opportunity.test.tsx    | 100 +++++++++++++++++++++++++++++++++++++++++++++
 apps/mobile/ui/thumbnail.tsx            |  52 +++++++++++++++++++++++++
 apps/mobile/ui/thumbnail.test.tsx       |  58 +++++++++++++++++++++++++++
 8 files changed, 340 insertions(+), 5 deletions(-) (근사치, 커밋별 stat 합산)
```
커밋 3개(이슈당 1개, 지정 순서대로, 각각 독립 revert 가능하도록 설계됨):
1. `a528c43` `fix(mobile): 온보딩 2차 CTA를 죽은 /report 대신 /explore로 연결 (M-050)`
2. `91e3ee5` `feat(mobile): 탐색·상세 화면에 활동 썸네일 이미지 렌더 (M-051)`
3. `4802e8e` `feat(mobile): 기회 상세에 마감(D-day)·출처 표시 추가 (M-053)`

`apps/mobile/app/(tabs)/saved.tsx` 무변경 확인(M-051 scope 제외 대상), `.env`/시크릿 파일 무변경 확인.

## 검증
reviewer가 fresh하게(qa 캐시에 의존하지 않고) `bash scripts/gate.sh`를 dev HEAD(`4802e8e`)에서 직접 재실행한 결과:

```
── typecheck
   Tasks:    4 successful, 4 total
   ✅ typecheck
── test
@motungi/core:test    Test Files  10 passed (10)   Tests  263 passed (263)
@motungi/mobile:test  Test Files  16 passed (16)   Tests   85 passed (85)
@motungi/web:test     Test Files  36 passed (36)   Tests  279 passed (279)
   Tasks:    3 successful, 3 total
   ✅ test
── lint
@motungi/web:lint  ✔ No ESLint warnings or errors
   Tasks:    1 successful, 1 total
   ✅ lint
── secrets
   (시크릿 패턴 없음)
   ✅ secrets

GATE: PASS — push 허용
```
core 263 + mobile 85 + web 279 = **627/627 테스트 통과**, qa가 보고한 수치와 정확히 일치. `pnpm --filter @motungi/mobile build`는 `apps/mobile/package.json`의 scripts에 `build` 항목 자체가 없어(Expo 앱은 `dev`/`android`/`ios`만 존재) 정상적으로 스킵됨을 reviewer가 재확인 — qa 보고를 그대로 믿지 않고 package.json을 직접 열어 검증했다.

각 커밋의 diff를 직접 열어 done_when 대조:
- M-050: `apps/mobile/app/index.tsx`가 2차 CTA를 `router.push("/explore")`로 연결(웹 `page.tsx`와 동등 목적지).
- M-051: `apps/mobile/ui/thumbnail.tsx` onError 폴백 로직 + `explore.tsx`·`opportunity.tsx` 적용 지점 확인, `saved.tsx` 무변경 grep 확인.
- M-053: `apps/mobile/app/opportunity.tsx`가 `deadlineLabel()`(packages/core) import해 D-day 블록 렌더, 기존 `o.meta` 3칸 칩 라인 그대로 유지됨을 diff로 확인.

**커밋 메커니즘 관찰(정보성, 이번 밤 영향 없음)**: qa가 M-051 단독 `git revert` 시뮬레이션을 시도한 결과, M-053이 같은 파일(`apps/mobile/app/opportunity.test.tsx`)의 인접한 텍스트 영역을 건드려 M-053 이후 시점에서 M-051만 깔끔하게 revert되지 않는다(텍스트 근접 충돌). 오늘 밤은 3건 모두 통과해 실제 revert가 필요하지 않았으므로 영향은 없었지만, **다음에 이 두 이슈 중 하나만 되돌려야 하는 상황이 생기면 수동 conflict 해결이 필요할 수 있다**는 점을 기록해둔다.

## 리스크 & 후속
- **M-052(지도·경로 렌더)는 여전히 `todo`로 백로그에 남아 있다 — 사람 개입 없이는 다음 밤도 같은 이유로 막힐 가능성이 높다.** 아래 셋 중 하나가 필요하다:
  1. `react-native-maps` 신규 네이티브 의존성 도입에 대한 **사람의 명시적 승인**(Expo prebuild/EAS 빌드 영향 인지 포함), 또는
  2. NAVER 정적 지도 백엔드 프록시 라우트를 사람이 설계·NCP에서 static-map 키를 프로비저닝하고 `.env.example`에 등록, 또는
  3. 특정 서드파티 무료/키리스 지도 서비스 사용에 대한 사람의 명시적 **보안 정책 예외 승인**(security-policy.md의 NAVER-only 서버프록시 원칙에 대한 예외로).
  이 결정 없이는 다음 밤의 architect도 동일한 3갈래 기각에 도달할 것이다.
- **FSD 문서 부정합 — 사람이 5곳을 정정 필요.** `.claude/agents/architect.md:9,18,28` · `.claude/agents/frontend-impl.md:25` · `.claude/agents/pixel-frontend.md:22`가 `.claude/rules/core/constitution.md:24`("FSD 디렉토리는 실재하지 않는다")와 모순된다. 과거 2개 밤(08-11, 08-12)이 이 부정합의 소재를 CLAUDE.md로 잘못 지목했었다는 점도 함께 정정해둔다(§아키텍처 검토 참조) — 사람이 다음 문서 정합성 스윕 때 위 5개 file:line을 실제 대상으로 잡을 것.
- **M-051/M-053 revert-mechanics 관찰**: 위 §검증 참조. 향후 밤이 이 두 이슈 중 하나만 되돌려야 할 경우를 위해 기록.
- backlog 상태: M-050·M-051·M-053은 `doing`으로 올렸다(사람 최종 리뷰 전). M-052는 `todo` 유지(오늘 밤 미착수). `todo`로 남은 3건(M-038 low·M-043 low·M-052 medium) + M-054·M-055(low)는 내일 밤 씨앗 후보 — M-052가 medium 중 유일하게 남아 씨앗이 될 가능성이 높으나, 위 3갈래 중 하나가 해소되지 않으면 다시 제외될 수 있다.
