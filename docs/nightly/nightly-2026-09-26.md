# Nightly Report — 2026-09-26

## TL;DR

- **BACKLOG GATE 판정: 구현 모드.** `docs/backlog/backlog.yml`을 이슈 단위로 다시 세어
  `status: todo` **6건**(`grep -c "status: todo"` 결과와 동일, notes 본문 오탐 없음) →
  `todo ≥ 2`이므로 구현 모드. audit 모드로 전환하지 않았다.
- **연속 report-only 경보 해당 없음** — 직전 밤(09-25)도 코드가 나갔다. 스트릭 이어짐.
- **씨앗 M-097(유일한 medium)은 이번에도 blocked-human이라 제외**하고, 남은 5건(전부
  `low`, id 동순위 최소) 중 **M-088**로 씨앗을 잡았다. 다른 4건과 scope 겹침이 전혀 없어
  **클러스터는 M-088 1건.**
- **결과: SHIPPED 1/1.** M-088 — 모바일 리포트 화면이 `catalogStatus === "idle"`(조회
  중)에도 "아직 추천할 활동이 없어요"를 띄워 로딩 중을 빈 결과처럼 거짓 안내하던 것을,
  탐색 화면(M-054)과 동일한 패턴으로 `ReportSkeleton`(RN)을 신설해 구분했다.
  `bash scripts/gate.sh` PASS(typecheck·test·lint·secrets·pure-tests 5/5, 커밋 후
  재확인). 커밋 `ba22cd8`을 `dev`에 push. reverted 이슈 없음.
- **PRECEDENCE 절 발동 — CLUSTER RULE 폴백 조건 불일치 재확인**: 이번 트리거 프롬프트의
  폴백 조건("씨앗 scope에 파일도 좁은 디렉토리도 전혀 없을 때만 발동")과
  `.claude/rules/workflow/nightly-pipeline.md:61`("파일 겹침 후보가 0개면 씨앗 scope에
  파일이 섞여 있든 없든 무관하게 발동")이 여전히 다르다(09-22 리포트부터 반복 기록,
  아직 미수정). 트리거 프롬프트를 따랐다. 오늘 밤은 결과에 영향 없음 — 어느 쪽 기준으로도
  M-088과 같은 앱(`apps/mobile`)·같은 priority(`low`)인 다른 todo가 없어 폴백을 적용해도
  추가되는 이슈가 0건이었다(클러스터는 두 기준 모두에서 M-088 1건으로 동일). 사람이 두
  문서 중 하나로 통일해 주기를 다시 요청한다.

## 집은 클러스터

**씨앗 선정**: `status: todo` 6건 — `M-097`(medium) 1건, `M-088`·`M-093`·`M-102`·`M-103`·
`M-110`(전부 low) 5건. 규칙대로면 priority 최상위 `medium`인 **M-097**이 씨앗이어야 한다.

**M-097을 씨앗에서 재차 제외한 이유**: M-097(`동 단위 좌표 백필`)은 2026-09-07·09-09 밤이
이미 `blocked-human`으로 판정해 "씨앗에서 사전 제외"를 notes에 남겨둔 이슈다. 오늘 밤
재검증:

```
$ ls -la .env*
-rw-r--r-- 1 root root 5734 Sep 26 12:10 .env.example   (.env 자체는 없음)

$ grep -i DATA_GO_KR .env.example
DATA_GO_KR_SERVICE_KEY=   (공란)

$ curl -sS -m 5 https://apis.data.go.kr/
curl: (56) CONNECT tunnel failed, response 403
```

`.env`가 없고 `DATA_GO_KR_SERVICE_KEY`가 공란이며, outbound network가 조직 정책으로
`apis.data.go.kr`를 여전히 차단한다. 좌표를 지어내는 건 근사치보다 나쁘다는 기존 판단을
유지한다. `status: todo` 유지(재시도 대상). 사람이 API 키를 발급하거나 network 예외를
허용하지 않으면 계속 이 자리에서 막힐 것이다 — 우선순위를 낮추거나 "사람 조치 대기"로
분류해 씨앗 후보에서 자동 제외되게 하는 걸 다시 요청한다(09-25 리포트에서도 같은 요청).

**다음 씨앗 후보**: M-097 제외 후 남는 5건이 전부 `low`로 동순위다. 규칙(동순위→id
오름차순)대로 최소 id인 **M-088**(`모바일 리포트 화면이 로딩 중(idle)과 실제 빈 상태를
구분하지 않음`)을 씨앗으로 잡았다. scope: `["apps/mobile/app/(tabs)/report.tsx"]`.

**done_when 재검증(planner 규율)**: `dev` 실코드(`apps/mobile/app/(tabs)/report.tsx:46-67`)를
직접 확인 — `!onePick` 분기가 `catalogStatus === "error" || "unconfigured"`만 `isError`로
가르고 `idle` 체크가 없어, notes가 기술한 버그가 그대로 남아 있었다. done_when 3개 모두
미해결 상태였다.

**scope 겹침으로 묶이는 다른 todo 확인(파일 단위)**:
- `M-093`(`packages/core/src/adapters/seoul-jobs.ts`, `.../util.ts`) — 다른 파일.
- `M-102`(`apps/web/src/app/diagnosis/page.tsx`, `.../location/page.tsx`) — 다른 파일.
- `M-103`(`apps/web/.eslintrc.json`) — 다른 파일. (M-102가 완료돼야 M-103의 done_when
  #1이 의미를 갖는 논리적 선후관계는 있으나, CLUSTER RULE의 겹침 기준은 "실제 파일/좁은
  디렉토리 공유"이지 논리적 의존관계가 아니므로 겹침으로 세지 않았다.)
- `M-110`(`packages/core/src/view.ts`) — 다른 파일.
- (M-097은 씨앗 후보에서 이미 제외됨 — 겹침 검토 대상 아님.)
→ **파일 겹침 후보 0건.** 트리거 프롬프트 기준: 씨앗(M-088) scope에 이미 실제 파일이
있으므로 폴백 자체가 발동 조건("파일도 좁은 디렉토리도 전혀 없을 때")을 충족하지 않아
**폴백 미적용**. repo 문서(`nightly-pipeline.md:61`) 기준이었어도 "같은 앱(`apps/mobile`)
+ 같은 priority(`low`)"인 다른 todo가 없어 결과는 동일했을 것이다(위 TL;DR 참조).
→ **클러스터는 M-088 1건.** 상한 3개 중 미룬 이슈: 없음(추가 후보 자체가 없었다).

## 이슈별 결과

### M-088 — SHIPPED

- **제목**: 모바일 리포트 화면이 로딩 중(idle)과 실제 빈 상태를 구분하지 않음
- **커밋**: `ba22cd8` (dev 직접 커밋, 트렁크 모델, PR 없음)
- **구현**: `apps/mobile/ui/report-skeleton.tsx` 신설. `apps/mobile/ui/explore-skeleton.tsx`
  (M-054)의 `Bar` 정적 회색 블록 패턴을 그대로 가져와 원픽 히어로(썸네일·태그·제목·요약·
  참가비 박스·CTA)와 "함께 보면 좋아요" 행 2개 골격으로 구성했다. pulse 애니메이션은
  기존 컨벤션대로 넣지 않았다. `report.tsx`의 `!onePick` 분기 최상단에
  `if (catalogStatus === "idle") return <ReportSkeleton dongName={dongName} />;`를
  추가해, `idle`→스켈레톤 / 그 외(`empty`·`error`·`unconfigured`)→기존 문구 분기로
  갈랐다.
- **수용 기준 판정**:
  - ✅ done_when #1 — `catalogStatus === 'idle'`일 때 `ReportSkeleton`을 렌더(빈 상태
    문구 대신).
  - ✅ done_when #2 — `report.test.tsx`에 idle 케이스를 신규 추가해 스켈레톤
    (`data-testid="report-skeleton"`) 렌더 + "아직 추천할 활동이 없어요"·"60초
    진단하기" 미노출을 assert. 기존 `empty` 케이스(그대로 유지)가 "실제 빈 결과"를
    계속 커버해, 두 상태가 서로 다른 화면임을 두 테스트의 대비로 검증한다.
  - ✅ done_when #3 — `pnpm --filter @motungi/mobile test` 통과(아래 검증 절 참조).
- **부수 테스트**: `apps/mobile/ui/report-skeleton.test.tsx` 신규(렌더 스모크 2건 —
  `explore-skeleton.test.tsx`와 동일하게 testID→`data-testid` 매핑으로 존재만 확인,
  dongName 기본값 케이스 포함).

## 아키텍처 검토

- **담당**: `apps/mobile`(UI 컴포넌트 1개 신설 + 화면 분기 4줄) — 단일 파일 스코프의
  저위험 UI 버그 수정이라, 오케스트레이터가 별도 architect/frontend-impl 서브에이전트
  분리 없이 직접 계획·구현·검증했다.
- **패턴 재사용**: 새 컴포넌트를 처음부터 설계하지 않고 이미 검증된 M-054
  (`ExploreRowSkeleton`) 패턴을 그대로 이식했다 — RN 스켈레톤 컨벤션(정적 블록, 로컬
  `Bar` 헬퍼, `testID` 노출)이 이번 결정으로 report 화면까지 일관되게 적용됐다.
- **웹 대응 확인**: `apps/web/src/components/report-skeleton.tsx`가 이미 동일 문제를
  `catalogStatus === "idle"` 분기로 고쳐둔 상태였다(`apps/web/src/app/report/page.tsx:104`).
  이번 작업은 그 패리티를 모바일 쪽에서 뒤늦게 맞춘 것 — 웹 쪽 변경은 없었다.
- **레이어 경계**: `apps/mobile/ui/`(순수 프레젠테이션 컴포넌트) 안에서만 신설했고
  `app/(tabs)/report.tsx`의 데이터 흐름(`catalogStatus`)은 건드리지 않았다. RN↔DOM
  경계 위반 없음(`View`/`ScrollView`/`StyleSheet`만 사용).
- **예상 밖 파급**: 없음. 신규 분기가 `idle` 한 값에만 걸리고 나머지 상태(`ok`·`empty`·
  `error`·`unconfigured`) 처리 로직은 변경하지 않았다.

## 변경 내용

| 파일 | 요약 |
|---|---|
| `apps/mobile/ui/report-skeleton.tsx` | 신설 — 원픽 히어로+관련 행 골격의 정적 스켈레톤(M-088) |
| `apps/mobile/ui/report-skeleton.test.tsx` | 신설 — 렌더 스모크 2건 |
| `apps/mobile/app/(tabs)/report.tsx` | `!onePick` 분기에 `catalogStatus === "idle"` 우선 분기 추가 |
| `apps/mobile/app/(tabs)/report.test.tsx` | idle 케이스 테스트 1건 추가(기존 7건 유지) |
| `docs/backlog/backlog.yml` | M-088 `status: todo → doing`, notes에 오늘 밤 판단·구현 근거 추가 |

## 검증

최신 `dev`(`6f45f9d` 기준, 커밋 후 `ba22cd8`) 위에서 실제 실행. 출력 원문 발췌:

```
$ pnpm install --frozen-lockfile
Packages: +1308
Done in 13.3s

$ bash scripts/gate.sh   (커밋 전, 워킹트리 변경 상태)
── typecheck   ✅
── test        ✅ (core 16/16 · mobile 24/24 · web 49/49 파일, 전부 통과)
── lint        ✅ (경고만 — 기존 max-lines-per-function/complexity, 이번 변경과 무관)
── secrets     ✅ (diff 없음 — 아직 커밋 전이라 base 대비 diff 없음)
── pure-tests  ✅ (순수 레이어 테스트 누락 없음, 면제 6건)
GATE: PASS — push 허용

$ git commit ...   (ba22cd8)

$ bash scripts/gate.sh   (커밋 후, 최종 트리)
── typecheck   ✅ (cache hit — 동일 내용 재확인)
── test        ✅
── lint        ✅
── secrets     ✅ (시크릿 패턴 없음 — 실제 diff 스캔)
── pure-tests  ✅
GATE: PASS — push 허용

$ pnpm test 최종 합계(mobile)
@motungi/mobile:test:  Test Files 24 passed (24) / Tests 187 passed (187)
  (report.test.tsx 10개 · report-skeleton.test.tsx 2개 포함)
```

push 전 머지 체크: 클론 시점에 `origin/dev`(`6f45f9d`)와 `origin/main`(`03eb972`)을 각각
확인한 결과 `git log --oneline origin/dev..origin/main`이 비어 있어(main이 dev를 앞서지
않음) 되받을 것 없이 바로 진행했다.

## 리스크 & 후속

- **M-097(좌표 백필)은 blocked-human 유지.** `.env`에 `DATA_GO_KR_SERVICE_KEY`를 채우거나
  outbound network 예외를 허용해야 다음 밤이 진행할 수 있다. 09-25 리포트와 동일한 요청을
  반복한다 — 계속 막혀 있을 거면 우선순위를 낮추거나 "사람 조치 대기"로 분류해 씨앗
  후보에서 자동 제외되게 하는 걸 고려해 달라.
- **CLUSTER RULE 폴백 조건이 트리거 프롬프트와 `nightly-pipeline.md:61` 사이에서 여전히
  다르다**(09-22 리포트부터 반복 기록, 오늘 밤도 미수정 확인). 오늘은 두 기준 모두 같은
  결론(M-088 단독)으로 귀결돼 영향이 없었지만, 앞으로 씨앗 scope에 파일이 섞여 있으면서
  겹침 후보가 0인 상황이 오면 두 문서가 다른 클러스터를 만들어낼 것이다. 트리거 프롬프트
  또는 repo 문서 중 하나로 통일해 주기를 요청한다.
- **M-102/M-103 의존관계**: M-103의 done_when #1은 "M-102(diagnosis·location 300줄 분할)
  처리 후 max-lines를 error로 승격"이라고 명시해 논리적으로 M-102 완료가 먼저다. 둘 다
  `apps/web`이고 `low` priority지만 scope 파일이 겹치지 않아 오늘 밤의 CLUSTER RULE(파일
  겹침 기준)로는 함께 묶이지 않았다. 다음에 이 둘 중 하나가 씨앗이 되면 순서(M-102 먼저)를
  참고할 것 — 지금은 기록만 남긴다.
