# Nightly Report — 2026-08-20

## TL;DR

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `docs/backlog/backlog.yml`의 `status: todo` 개수는 **16건**(≥2) — 정상 구현 모드로 진행했다.
- **클러스터: M-057 + M-064 + M-065** (셋 다 shipped, 되돌린 것 없음). 셋 다 `apps/web/src/hooks/` 아래 파일을 scope로 갖는 유일한 todo 3건.
- qa 게이트(`bash scripts/gate.sh`) **PASS** — typecheck 4/4, test(core 292/292 · web 290/290 · mobile 125/125) 전체 통과, lint 통과, 시크릿 없음. reviewer가 diff·게이트 핵심 수치를 직접 재확인.
- **연속 report-only 경보 해당 없음**: `docs/nightly/`의 최근 파일들을 직접 열어 확인했다. 직전 밤(08-19)은 M-056/M-058 shipped 구현 밤이었고, 이번 밤(08-20)도 shipped 구현 밤이다 — report-only가 연속되는 흐름 자체가 없다(08-18은 report-only였지만 audit 모드로 17개 이슈를 새로 등재한 생산적 결과이며, 그 직후 08-19·08-20 이틀 연속 정상 shipped이므로 "헛도는 밤" 경보 대상이 아니다).
- **가장 챙겨야 할 항목**: M-064의 backlog `done_when` 셋째 항목("저장 30건 시나리오에서 요청 수 1로 줄었음을 테스트로 확인")이 문자 그대로는 충족되지 않았다(테스트는 2건 규모로만 검증). 기능·구조적으로는 해결됐다고 판단하지만, 아래 §이슈별 결과·§리스크에 명시한다.

## 집은 클러스터

**씨앗**: priority 최상위 todo 중 유일한 `high` — **M-057**(`apps/web/src/hooks/useReportFallback.ts`).

**scope 겹침 탐색**: 전체 todo 16건 중 M-057과 같은 좁은 디렉토리(`apps/web/src/hooks/`)를 scope로 갖는 이슈를 찾은 결과 정확히 2건 — **M-064**(`apps/web/src/hooks/useSavedOpportunities.ts`), **M-065**(`apps/web/src/hooks/useNeighborhoodSearch.ts`). 그 외 어떤 todo도 이 디렉토리를 건드리지 않는다.

클러스터 = **M-057 + M-064 + M-065**, 상한 3개를 정확히 채웠다(진짜 파일/디렉토리 scope 겹침으로 도달, app-level 폴백 불필요, 상한을 넘겨 미룬 후보도 없음 — 겹치는 3건이 전부였다).

**제외된 이슈**: 없음(3건이 정확히 상한과 일치). M-052(medium, 모바일 지도/산책로, 사람 결정 대기)는 애초에 이번 클러스터의 seed/scope-overlap 후보가 아니었다(§리스크에서 별도로 재확인).

## 이슈별 결과

### M-057 — 웹 리포트 반경 사다리 폴백(useReportFallback.ts) 테스트 0건 → **shipped**

- 커밋: `ea13359` `test(web): useReportFallback 반경 사다리 폴백 테스트 추가 (M-057)`
- 변경: `apps/web/src/hooks/useReportFallback.test.ts` 신규 1개 파일, 159줄. **`useReportFallback.ts` 자체는 0-line diff** — `git show --stat ea13359`로 재확인, 프로덕션 코드 변경 없음.
- done_when 대조:
  - ✅ 5→10→20km 반경 사다리(각 단계 `near.radiusKm` 인자) + 조기종료(6건 충족 시 단일 호출) 테스트로 커버
  - ✅ `needsFallback=false`→ok, `isError`→error, `isPending`→idle 세 status 매핑 각각 검증
  - ✅ `pnpm --filter web test` 통과

### M-065 — 웹 동네 검색 IME 조합 처리(useNeighborhoodSearch.ts) 테스트 0건 → **shipped**

- 커밋: `8efa93c` `test(web): useNeighborhoodSearch IME 조합 처리 테스트 추가 (M-065)`
- 변경: `apps/web/src/hooks/useNeighborhoodSearch.test.ts` 신규 1개 파일, 114줄. **`useNeighborhoodSearch.ts` 자체는 0-line diff** — 재확인 완료, 프로덕션 코드 변경 없음.
- done_when 대조:
  - ✅ `composingRef` 기반 조합 중 debounce 스킵 + `onCompositionEnd` 재검색 트리거 검증
  - ✅ "망원동" 조합 중간값("망원")에서 끊기지 않고 최종 완성 값으로 검색되는 시뮬레이션 테스트 존재
  - ✅ `pnpm --filter web test` 통과

### M-064 — 웹 보관함 N+1 쿼리(useSavedOpportunities.ts) → **shipped, done_when 1개 항목 문자 그대로는 미충족(기능은 해결)**

- 커밋: `041c83b` `feat(web): 보관함 저장 활동 조회를 단일 벌크 쿼리로 전환, 상세 캐시 선시딩 (M-064)`
- 핵심 변경: `packages/core/src/catalog.ts`에 `fetchOpportunitiesByIds(client, ids)` 신규 — `.in("id", ids)` 단일 쿼리, `DETAIL_COLUMNS` 사용(description 포함, `CATALOG_COLUMNS` 아님 — 상세 캐시로 그대로 시딩되므로 필수), near/카테고리/마감 필터 없음(과거 "300건 창 밖 실종" 버그 수정을 보존). `useSavedOpportunities.ts`는 id별 `useQueries` N번 호출 → 단일 `useQuery`로 전환, `queryFn` 내부에서 응답 즉시 `queryKeys.opportunity(id)`에 시딩(별도 `useEffect` 없음 — v5는 `useQuery`에 `onSuccess`가 없어 이 방식이 맞는 패턴), `enabled: savedIds.length > 0` 명시(빈 배열 호출 방지, 기존 "저장 0건→미조회" 테스트 유지).
- 계획 외 추가 파일 2개(`apps/web/src/app/saved/page.test.tsx`, `render-isolation.test.tsx`): 인터페이스 전환으로 옛 `fetchOpportunityById` mock이 깨져 필연적으로 함께 고쳐야 했던 mock 대상 교체뿐(assertion·동작 변경 없음) — line-by-line 재확인, 범위 이탈(scope creep) 아님으로 판단.
- done_when 대조:
  - ✅ id별 `useQueries` → 단일 `.in("id", ids)` 쿼리로 승격(32행 주석의 제안대로)
  - ✅ 저장 순서 유지·마감 지난 활동도 계속 표시·실패/재시도 동작 — 기존 `SavedView` 계약 유지 확인
  - ⚠️ **"저장 30건 시나리오에서 요청 수 1로 줄었음을 테스트로 확인" — 문자 그대로는 미충족.** `useSavedOpportunities.test.ts:152`가 `mockedByIds`가 `toHaveBeenCalledTimes(1)`임을 검증하지만, 이 테스트는 saved id 2건 규모다. 30건 규모의 전용 테스트는 없다. 다만 구조적으로는(id별 반복 호출 없이 단일 `.in()` 호출 하나로 통일됐으므로) 저장 건수와 요청 수가 원천적으로 무관해졌다 — QA·reviewer 공통 판단: **coverage-literalism gap이며 기능적 리그레션은 아니다.** M-064를 되돌리지 않고 dev에 유지하되, 이 gap을 후속 이슈로 명시한다(§리스크 & 후속).

## 아키텍처 검토

- 담당: **frontend-impl 단독**. `packages/core/src/catalog.ts` 변경은 원래 backend-impl 도메인(`.claude/agents/backend-impl.md`)이지만, architect가 "스키마·마이그레이션 없는 기계적 함수 추가이며 기존 `fetchOpportunityById` 패턴을 그대로 미러링"이라고 판단해 예외적으로 frontend-impl 단일 커밋에 포함시켰다 — 2커밋으로 쪼개면 "이슈당 1커밋, 독립 되돌리기 가능" 원칙에 실익 없이 위배되기 때문.
- architect가 planner 스펙 대비 확정한 정정 4건 — 전부 실제 diff에서 반영 확인:
  1. 캐시 시딩은 `useEffect`가 아니라 `queryFn` 내부에서(React Query v5에 `onSuccess` 없음, `apps/web/package.json`에서 v5 확인) → `useSavedOpportunities.ts` 확인.
  2. `enabled: savedIds.length > 0` 명시 필요(기존 "저장 0건→미조회" 테스트 보호) → 확인.
  3. `packages/core/src/catalog.test.ts`에 신규 함수 자체의 단위 테스트 추가(웹 훅 테스트는 mock으로 가려 core 로직 자체를 커버하지 못함) → 7건 계약 테스트 확인(빈 ids/컬럼/필터 없음/ok·error·empty 매핑).
  4. `DETAIL_COLUMNS` 강제(상세 캐시 선시딩 대상이므로 `CATALOG_COLUMNS` 쓰면 상세 페이지에서 description 누락 리그레션) → `fetchOpportunitiesByIds` 구현에서 확인.
- FSD/테스트 전략: M-057·M-065는 훅을 직접 호출하는 test-only 스펙(소비처 mocking이 아님), 프로덕션 코드 0-diff. M-064는 core 단위 테스트 + 웹 훅 통합 테스트 이중 커버리지.
- planner 스펙 반려 없음 — architect가 실제 코드 대비 4건 정정 후 승인, 이후 변경 없음.

## 변경 내용

```
$ git diff 771c162 HEAD --stat
 apps/web/src/app/saved/page.test.tsx             |  20 +--
 apps/web/src/app/saved/render-isolation.test.tsx |   9 +-
 apps/web/src/data/opportunities.ts               |   9 ++
 apps/web/src/hooks/useNeighborhoodSearch.test.ts | 114 ++++++++++++++++
 apps/web/src/hooks/useReportFallback.test.ts     | 159 +++++++++++++++++++++++
 apps/web/src/hooks/useSavedOpportunities.test.ts |  49 ++++---
 apps/web/src/hooks/useSavedOpportunities.ts      |  58 +++++----
 apps/web/src/lib/query.tsx                       |   7 +
 packages/core/src/catalog.test.ts                | 100 +++++++++++++-
 packages/core/src/catalog.ts                     |  30 +++++
 10 files changed, 497 insertions(+), 58 deletions(-)
```

`apps/web`·`packages/core` 밖으로 새 나간 변경 없음(mobile·tokens 무변경). 시크릿·`.env`·마이그레이션·파괴적 SQL 없음 — reviewer가 `git diff 771c162 HEAD`를 직접 grep으로 재스캔해 확인.

## 검증

reviewer가 직접 재확인한 핵심 사실:

- `git show --stat ea13359`, `git show --stat 8efa93c` — M-057·M-065 모두 프로덕션 소스 파일(`useReportFallback.ts`, `useNeighborhoodSearch.ts`) 자체는 diff에 등장하지 않음(test-only).
- `packages/core/src/catalog.ts:285-298`(`fetchOpportunitiesByIds`) — `DETAIL_COLUMNS` 사용, near/카테고리/마감 필터 없음을 소스에서 직접 확인.
- `apps/web/src/hooks/useSavedOpportunities.ts` — `queryFn` 내부 캐시 시딩, `enabled: savedIds.length > 0` 둘 다 소스에서 직접 확인.
- `apps/web/src/hooks/useSavedOpportunities.test.ts:152` — `mockedByIds` 단일 호출 검증이 2건 규모 시나리오에서만 이뤄짐(30건 시나리오 테스트 없음)을 직접 확인.

qa가 실제로 돌린 게이트(`bash scripts/gate.sh`, dev 트리 `041c83b` 기준, reviewer가 동일 커밋에서 개별 필터 재실행으로 교차검증):

```
── typecheck
 Tasks:    4 successful, 4 total   (core, mobile, tokens, web)
   ✅ typecheck
── test
@motungi/core:test:  Test Files  11 passed (11) / Tests 292 passed
@motungi/mobile:test:  Test Files  19 passed (19) / Tests 125 passed
@motungi/web:test:  Test Files  38 passed (38) / Tests 290 passed
 Tasks:    3 successful, 3 total
   ✅ test
── lint
@motungi/web:lint: ✔ No ESLint warnings or errors
 Tasks:    1 successful, 1 total
   ✅ lint
── secrets
   (시크릿 패턴 없음)
   ✅ secrets

GATE: PASS — push 허용
```

세 커밋 모두 revert 대상 없음 — 전부 push.

## 리스크 & 후속

- **M-064 done_when 커버리지 gap(30건 시나리오)**: `useSavedOpportunities.test.ts`의 요청 수 검증이 2건 규모로만 이뤄져, backlog 원문이 요구한 "30건 시나리오" 테스트가 문자 그대로는 없다. 구조적으로(단일 `.in()` 호출로 통일돼 건수와 요청 수가 원천 무관) 기능 리그레션은 아니라고 판단해 되돌리지 않았지만, 정직하게 gap으로 남긴다. **후속 제안**: 새 backlog 이슈로 등재하거나(예: "M-064 후속 — 30건 규모 요청수 회귀 테스트 추가"), 다음 밤 짧은 addendum 커밋으로 처리 — 둘 다 저비용이라 사람이 우선순위만 정하면 된다.
- **M-052 여전히 블록 상태(8일째, 08-13~08-20)**: `docs/backlog/backlog.yml`에서 직접 재확인 — `status: todo` 그대로, 모바일 상세 화면 지도·산책로 표시가 사람의 구현 방식 승인(react-native-maps 신규 네이티브 의존성 vs 정적 지도 이미지 vs 서드파티 프록시) 없이는 여전히 무인 파이프라인이 뚫을 수 없는 차단이다. **사람 개입 요청 반복**: (a) 세 경로 중 하나 승인, (b) priority를 medium에서 강등, (c) `blocked-human` 같은 정식 상태 도입 — 08-14부터 매일 동일 요청이 쌓이고 있다. 이번 밤은 클러스터 seed/scope-overlap 후보 자체가 아니었으므로(scope가 `apps/mobile`, 클러스터는 전부 `apps/web/src/hooks`) 별도 재확인 절차를 거치지 않았지만, backlog 상태 조회로 여전히 미해결임을 확인했다.
- **다음 밤 씨앗 후보**: 이번 밤으로 `apps/web/src/hooks/` 겹침 클러스터는 소진됐다. 남은 todo 13건(16 - 3) 중 다음 최상위 priority/scope 조합을 다음 밤 planner가 처음부터 다시 스캔해야 한다 — 이번 밤엔 확인하지 않았다.
- 그 외 신규 리스크 없음 — architect가 지정한 4개 정정사항(캐시 시딩 위치·`enabled` 게이트·core 단위 테스트·`DETAIL_COLUMNS`) 전부 구현에 반영됨을 reviewer가 직접 소스에서 재확인했다.

## PR

없음. `docs/nightly/PIPELINE.md`의 dev 직접 커밋(트렁크) 모델에 따라 브랜치·PR 없이 `dev`에 직접 커밋·push한다.
