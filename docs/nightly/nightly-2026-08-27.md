# Nightly Report — 2026-08-27

## TL;DR
- 연속 report-only 경보 해당 없음(§0 참조) — 이번 밤도 정상 shipped 구현 밤이다.
- 백로그 게이트: 밤 시작 시 `status:todo` 3건(M-052, M-067, M-070) → todo≥2 → 구현 모드.
- 씨앗 후보 M-052(mobile 지도/산책로, medium)는 **오늘로 15일째(2026-08-13~08-27) 동일 사유로 재제외** — 사람의 아키텍처 결정 대기(§리스크 참조).
- 클러스터 = **M-067 단독**(low, M-070과 scope 겹침 0, 앱 단위 폴백도 부적용). **shipped** — 커밋 `50e6754`.
- planner는 M-067의 done_when이 사람에게 요구한 두 선택지(a: hitSlop 10곳 실배선 / b: 문서 정리) 중 **b(문서 정리)**를 선택 — a는 시각 판단이 필요해 무인 파이프라인 범위 밖.
- qa 게이트(`bash scripts/gate.sh`): typecheck 4/4, test core 303/303 + web 40 files/311 + mobile 전부 통과, lint clean, 시크릿 스캔 clean — **PASS**. 되돌린 이슈 없음.
- reviewer 최종 확인: `docs/backlog/backlog.yml` M-067 status `doing` 정상 반영, 시크릿 스캔 재확인 clean, main 미접촉.

## 0. 헛도는 밤 감지
`docs/nightly/`의 최근 8개 파일(08-19~08-26)을 직접 열어 TL;DR을 확인했다:
- 08-19: M-056+M-058 shipped
- 08-20: M-057+M-064+M-065 shipped
- 08-21: M-059 shipped
- 08-22: M-061+M-062+M-063 shipped
- 08-23: M-068+M-069 shipped
- 08-24: M-072 shipped
- 08-25: M-060+M-071 shipped
- 08-26: M-066 shipped

8일 연속 전부 shipped 구현 밤이었고 report-only가 하나도 없다. **연속 report-only 카운트 = 0.** 경보 해당 없음.

## 1. 집은 클러스터
- 씨앗 우선순위상 M-052(medium, 유일한 medium todo)가 정상적으로는 씨앗이어야 하지만, **15일 연속(2026-08-13~08-27)** 동일 사유로 사전 제외됐다(아래 §리스크 참조). 오늘 밤도 재검증: `grep -i map apps/mobile/package.json` 여전히 공백, `.env.example`에 지도 관련 신규 키 없음, `security-policy.md`에 지도 언급 없음 — 지난 14일과 상태 변화 없음.
- M-052를 제외한 나머지 todo는 M-067(low, scope: `packages/tokens/src/index.ts`, `apps/mobile`)과 M-070(low, scope: `packages/core/src/store.ts`) 둘뿐. **파일/디렉토리 겹침 0** — `packages/tokens/src/index.ts` ≠ `packages/core/src/store.ts`, `apps/mobile` ≠ `packages/core`.
- 앱 단위 폴백은 **부적용**: 폴백은 "씨앗 scope에 파일이 하나도 없을 때"만 발동하는데, M-067의 scope에는 이미 실 파일(`packages/tokens/src/index.ts`)이 있다. 따라서 폴백 조건 자체가 성립하지 않는다.
- 결과: **클러스터 = M-067 단독**(id 오름차순 + 동순위 low 중 유일 후보). M-070은 상한 3개에 밀린 게 아니라 애초에 클러스터에 합류할 자격이 없어 `todo`로 남았다 — 다음 밤이 다시 씨앗 후보로 집을 것이다.

## 2. 이슈별 결과

### M-067 — shipped (커밋 `50e6754`)
- **배경**: `docs/backlog/backlog.yml`의 M-067 done_when이 사람에게 두 선택지 중 하나를 명시적으로 요구했다 — (a) `apps/mobile`의 hitSlop 하드코딩 값 10곳을 `MIN_HIT_TARGET` 기준으로 실제 계산해 배선하거나, (b) 강제할 의도가 없다면 export·문서 서술을 정리한다.
- **선택**: 사람이 없는 밤이라 planner가 **(b) 문서 정리**를 택했다. 사유: 10개 콜사이트(아이콘 버튼, 텍스트 버튼, Chip, 아이콘+라벨 조합 등)가 서로 다른 요소를 감싸고 있어 올바른 계산에는 시각적 판단이 필요하고, 계산을 잘못하면 히트 영역이 겹치거나 과대해지는 시각적 회귀가 발생하는데 이는 typecheck·test로 잡히지 않는다 — 무인 자동화로는 안전하지 않다고 판단. (a) 순수 텍스트 수정인 (b)는 무인으로 안전하게 완결 가능.
- **결과**: `docs/ux/ux-design.md:87`에 "apps/mobile의 hitSlop 값들은 아직 MIN_HIT_TARGET을 참조하지 않는 하드코딩 리터럴이며 미배선 상태"라는 사실 정정 문구를 44px 기준 서술은 그대로 둔 채 추가. `backlog.yml`의 M-067 status를 `todo → doing`으로 갱신(architect가 `done`이 아닌 `doing`으로 정정 — 아래 §아키텍처 검토), 기존 notes는 보존한 채 `[2026-08-27 야간 파이프라인]` 노트를 추가.
- **범위 확인**: `packages/tokens/src/index.ts`, `apps/mobile` 이하는 diff 0(reviewer가 재확인). 변경 파일은 정확히 2개(`docs/backlog/backlog.yml`, `docs/ux/ux-design.md`)뿐.
- 되돌린 이슈 없음.

## 3. 아키텍처 검토
- **status 정정**: planner의 초안 스펙은 backlog status를 `done`으로 설정하라고 지시했으나, architect가 이를 잡아냈다 — 야간 파이프라인은 오직 `todo→doing`까지만 갱신하며 `done` 승격은 **dev→main 승격 후 사람만** 할 수 있는 행위다(선례: M-018/M-019/M-061). frontend-impl에 넘기기 전에 `doing`으로 정정했다.
- **담당 확정**: 순수 문서/YAML 편집이라 architect가 frontend-impl 단독 담당으로 확정했다. 근거: `docs/nightly/nightly-2026-08-12.md`의 M-048(조사·분류성 이슈, docs+YAML만)도 동일하게 frontend-impl 단독 배정된 선례.
- 테스트 전략: 코드 변경이 없으므로 신규 테스트 불필요. 기존 typecheck/test 스위트가 그대로 회귀 방지선.

## 4. 변경 내용
- `docs/ux/ux-design.md:87` — 히트 타겟 기준선 문장에 "MIN_HIT_TARGET 미배선" 사실 정정 절을 추가(원래 44px 기준 문장은 무변경).
- `docs/backlog/backlog.yml` — M-067 항목: `status: todo → doing`, notes에 `[2026-08-27 야간 파이프라인]` 절 추가(기존 notes 보존).
- `apps/mobile/**`, `packages/tokens/src/index.ts` — diff 없음(reviewer 재확인 완료).
- 커밋: `50e6754efafbaca461c906bcb99f166f62c37f4c` — `docs(ux): MIN_HIT_TARGET 미배선 사실 정정 + M-067 종결 (M-067)`

## 5. 검증
qa가 실제로 실행한 `bash scripts/gate.sh` 결과(reviewer가 재확인):
- **typecheck**: 4/4 워크스페이스 통과
- **test**: core 303/303, web 40 files / 311 tests 통과, mobile 전부 통과. `tokens`는 test 스크립트 없음(정상, 기존과 동일).
- **lint**: `next lint` → `✔ No ESLint warnings or errors`
- **시크릿 스캔**: clean, 검출 없음(diff가 순수 문서 텍스트라 패턴 매치 자체가 없음) — reviewer가 `bash scripts/scan-secrets.sh`로 재확인, `(시크릿 패턴 없음)`.
- **GATE: PASS**

qa는 web 테스트 stderr에 나온 jsdom `HTMLCanvasElement.getContext not implemented` 경고(axe-core color-contrast 체크, `canvas` npm 패키지 미설치)가 **기존부터 있던 무관한 환경 이슈**이며 이번 diff와 무관하다고 명시했다 — reviewer도 동의, 실패 아님.

reviewer 자체 확인 사항:
- `git diff` 상 변경 파일 정확히 2개(`docs/backlog/backlog.yml | 6 +++++-`, `docs/ux/ux-design.md | 2 +-`) — 확인.
- 커밋 메시지가 Conventional Commits(`docs(ux):`) + 제목에 `M-067` 포함 — 추적성 규율 충족.
- `git log origin/dev..origin/main` 확인 결과 로컬이 shallow clone이라 `git merge-base`가 실패(unrelated-history로 보이는 아티팩트)하지만, `origin/main`의 HEAD(`744411d`, PR #21, 2026-08-06)는 dev가 이미 3주 전에 통과한 지점의 스쿼시 커밋이고 이후 main에 새 커밋이 없다 — dev가 정상적으로 main보다 훨씬 앞서 있는(사람의 주기적 승격을 기다리는) 상태이며, dev가 놓친 main 전용 변경은 없다고 판단. 되받을 것 없음.

## 6. 리스크 & 후속
- **M-052 — 15일 연속 재제외(2026-08-13~08-27), 사람 개입 필요.** 모바일 상세 화면 지도/산책로 표시는 3개 구현 경로(react-native-maps 네이티브 의존성 추가 / NAVER 정적지도 백엔드 프록시 신설 / 키리스 서드파티 정적지도) 중 하나를 사람이 승인해야 진행 가능 — 이미 08-22~08-25 리포트에서 반복 에스컬레이션됨. 오늘 재검증도 상태 변화 없음(`apps/mobile/package.json`에 지도 의존성 없음, `.env.example`에 지도 키 없음, `security-policy.md`에 지도 언급 없음). 사람이 다음 셋 중 하나를 택해야 한다: (1) 세 경로 중 하나 승인, (2) M-052 priority를 medium에서 낮춰 씨앗 후보에서 자연스럽게 제외, (3) 공식 `blocked-human` status 도입.
- **todo 풀 고갈 임박**: M-052를 빼면 todo는 M-070 하나뿐이다. 다음 밤 M-070이 나가거나(단독 클러스터가 될 가능성이 높음), 새 audit 라운드가 이슈를 채우지 못하면 이후 밤은 `todo<2`가 되어 audit 모드로 전환될 수 있다 — 이는 파이프라인 설계상 정상적인 결과이며 경보 대상이 아니다.
- M-067은 `doing`으로만 남아 있다 — `done` 승격 및 (원한다면) 실제 hitSlop 배선(a안) 착수 여부는 사람의 판단.
- shallow clone 환경이라 `git merge-base origin/dev origin/main`이 실패하는 것을 확인했다(§검증). 실제 운영 clone에서는 문제가 없을 가능성이 높지만, 다음 밤이 동일 증상을 보면 fetch depth 설정을 점검할 필요가 있다는 점을 남겨둔다.
