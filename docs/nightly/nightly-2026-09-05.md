# Nightly Report — 2026-09-05

## TL;DR

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `docs/backlog/backlog.yml`의 `status: todo`는 **11건**이었다 — `todo ≥ 2`이므로 구현 모드로 진행했다.
- **연속 report-only 경보 해당 없음.** 직전 밤(09-04)은 M-075/M-087이 shipped된 정상 구현 밤이었다(리포트 확인). 스트릭 0.
- **브랜치 동기화 점검(nightly-pipeline.md 6번) 수행함**: `git log --oneline origin/dev..origin/main`이 비어 있어(main에 dev가 모르는 커밋 없음) 되받을 것 없이 그대로 진행.
- **클러스터 = M-081(씨앗) + M-083 + M-084, 앱 단위 폴백으로 3건 전부 shipped.** 되돌린 이슈 없음.
- `bash scripts/gate.sh` **PASS** — typecheck(core·mobile·tokens·web 4/4) · test(mobile 21파일 159/159, web 47파일 378/378 — M-081/083/084로 추가된 23건 포함) · lint clean(`✔ No ESLint warnings or errors`) · 시크릿 스캔 clean(`(diff 없음)`).
- **환경 이슈 기록**: 이 세션은 repo가 사전 클론되지 않은 상태로 시작됐다(작업 디렉터리가 빈 채로 fresh) — `mcp__Claude_Code_Remote__list_repos`로 `dlsxody1/motungi`를 찾아 직접 clone한 뒤 `origin/dev`를 체크아웃해 진행했다. 파이프라인 문서 위반은 아니지만, 트리거 환경 설정에 repo 소스가 배선되지 않은 것으로 보여 사람이 확인할 필요가 있다.
- **doc 모순 재확인(2026-09-04 리포트에서 이미 기록)**: `.claude/agents/architect.md`가 FSD 계층을 언급하는 것과 `constitution.md`("FSD 디렉토리는 실재하지 않는다")의 모순은 아직 미해결 상태다. 이번 밤도 constitution.md(평면 구조)를 따랐다 — 새 디렉토리를 만들지 않고 기존 `app/`·`hooks/`에만 작업.

## ⚠️ 사람 개입 필요 — M-052, 24일 연속 동일 사전제외 (신규 강조)

`M-052`(모바일 상세 화면 지도·산책로 폴리라인)는 **2026-08-13부터 오늘(09-05)까지 24일 연속** 씨앗 단계에서 재검증 후 사전 제외되고 있다. 사유는 매번 동일: `apps/mobile`에 지도 관련 의존성이 전혀 없고(`grep -i map apps/mobile/package.json` 공백), 08-13이 제시한 3개 구현 경로(react-native-maps 신규 네이티브 의존성 / NAVER 정적지도 백엔드 프록시 신설 / 키리스 서드파티 정적지도) 모두 사람의 정책 승인 없이는 무인 야간 파이프라인이 뚫을 수 없다.

- 오늘 밤 재검증: `grep -i map apps/mobile/package.json` 여전히 공백, `apps/mobile` 코드 전체에 `MapView`/`react-native-maps` 참조 0건, `.env.example`·`security-policy.md`에 지도 관련 항목 없음 — 변화 없음.
- 08-22/08-23/08-25 리포트가 이미 "3택1(경로 승인 / priority 하향 / blocked-human 상태 도입)"을 사람에게 요청했고 처리되지 않은 채 한 달 가까이 지났다.
- **이번 밤 오케스트레이터가 PushNotification으로 사람에게 직접 알림을 보낸다** — backlog 안 반복 기록만으로는 24일째 닿지 않았다는 판단.

## 집은 클러스터

- **씨앗**: M-081 (priority `medium`, M-052 사전 제외 후 최소 id). "웹 리포트 원픽 카드가 core의 deadlineLabel 대신 로컬 formatDeadline을 재구현".
- **파일 겹침 후보 0개** — M-081의 scope(`apps/web/src/app/report/page.tsx`)와 실제 파일이 겹치는 다른 todo가 없었다.
- **앱 단위 폴백으로 M-083 + M-084 추가** — 이유: `apps/web` + `medium` priority가 M-081과 일치하는 todo 중 id 오름차순 최소 2개가 M-083·M-084였다. scope는 각각 `apps/web/src/app/login/page.tsx`·`apps/web/src/hooks/useTrailRoute.ts`로 M-081과 실제 파일 겹침은 없다 — 순수 폴백 클러스터링.
- **상한 3개 전부 채움.**
- **미룬 이슈**: 같은 폴백 요건(apps/web + medium)을 만족하는 M-091·M-096이 상한(3개) 초과로 다음 밤으로 미뤄졌다. M-097은 medium이지만 scope가 `supabase/`·`packages/core`이고 `apps/web`이 없어 폴백 대상이 아니었다. low priority(M-079·M-088·M-092·M-093)는 seed priority(medium)와 달라 애초에 폴백 대상이 아니다. M-052는 위 "사람 개입 필요" 섹션대로 사전 제외.

## 이슈별 결과

### M-081 — shipped (`569eaec`)
웹 리포트(`report/page.tsx`) 원픽 히어로 카드의 마감 배지가 로컬 `formatDeadline`(단일 중립 톤, "~11/28까지")을 쓰고 있어 마감이 지난 원픽도 여유 있는 원픽과 똑같이 보이던 문제.
- **기준선 재확인**: `origin/dev` 실코드로 `report/page.tsx`에 로컬 `formatDeadline`이 여전히 존재하고 `deadlineLabel`(core) import가 없음을 직접 읽어 확인 — `done_when` 미충족.
- **구현**: 로컬 `formatDeadline` 제거, `@motungi/core`의 `deadlineLabel` import. `today`/`deadline` 계산을 `opportunity-detail.tsx`와 동일 패턴(`new Date().toISOString().slice(0,10)`)으로 추가. `DdayPill`(3톤: 지남=회색/임박≤3일=강조/여유=은은)을 이 파일에 로컬로 추가 — `opportunity-detail.tsx`의 동명 컴포넌트와 로직은 같지만, scope가 이 파일로 한정돼 공유 컴포넌트로 승격하지 않았다(2026-09-04 밤 M-087이 모바일에서 세운 것과 동일 선례). 모바일·데스크톱 두 렌더 지점(`md:hidden` CSS로 둘 다 마운트) 모두 교체.
- **테스트**: `report/page.test.tsx`에 4케이스 추가(지남/D-3 이내/여유/마감없음) — `opportunity-detail.test.tsx`의 "DdayPill 톤·텍스트" 관례(시스템 시간 고정, `getAllByText().filter(span)`)를 그대로 따름.
- **done_when 4개 전부 충족**: 로컬 formatDeadline 제거+deadlineLabel/DdayPill 교체, 3톤 동일 렌더, 마감 케이스 테스트 추가, `pnpm --filter web typecheck·test` 통과.
- 되돌림 없음.

### M-083 — shipped (`db2f776`)
로그인 화면(`login/page.tsx`)의 오픈 리다이렉트 방어 로직(`next` 파라미터, CWE-601)에 테스트가 0건이었던 문제.
- **기준선 재확인**: `origin/dev` 실코드로 `apps/web/src/app/login/` 디렉터리에 `page.tsx`만 있고 `page.test.tsx`가 없음을 `ls`로 직접 확인 — `done_when` 미충족.
- **구현**: 프로덕션 코드 변경 없음(이미 방어 로직은 존재) — `login/page.test.tsx` 신설. `next/navigation`을 `explore/(list)/page.test.tsx`와 동일 패턴(제어 가능한 `searchParamsRef` + `replaceSpy`)으로 재모킹.
- **테스트 6건**: `next` 없음→`/my`, `next=//evil.com`→`/my`, `next=https://evil.com`→`/my`, `next=/saved`(유효)→보존, 로그인 상태에서 `router.replace(next)` 호출, 로그인 상태+위험한 next에서도 `/my`로 replace(리다이렉트 경로에도 방어가 적용됨을 확인 — done_when에 명시되지 않았지만 같은 `next` 변수를 재사용하는 코드 경로라 자연히 검증됨).
- **done_when 5개 전부 충족**: //evil.com·https://evil.com 폴백, /saved 보존, 기본값 /my, 로그인 상태 replace, `pnpm --filter web test` 통과.
- 되돌림 없음.

### M-084 — shipped (`846e00a`)
`useTrailRoute` 훅을 참조하는 모든 테스트가 훅 자체를 `vi.mock`으로 대체해와서, 훅의 3개 분기(`!res.ok`/malformed points/fetch 에러)가 한 번도 실행된 적이 없던 문제.
- **기준선 재확인**: `apps/mobile`·`apps/web` 전체에서 `find`로 `useTrailRoute.test.ts` 부재를 확인, `opportunity-detail.render-isolation.test.tsx`·`venue-map.test.tsx`가 여전히 `vi.mock(() => ({ useTrailRoute: () => null }))`로 대체하고 있음을 확인 — `done_when` 미충족.
- **구현**: 프로덕션 코드 변경 없음 — `useTrailRoute.test.ts` 신설. 같은 프로젝트의 동형 훅 테스트(`useWhyReasons.test.ts` — `useQuery` + `reportError` catch 패턴)를 템플릿으로 전역 `fetch` stub(`vi.stubGlobal`) + `reportError` mock 관례를 그대로 따랐다.
- **테스트 8건**: enabled=false/id=null 가드 2건, !res.ok, points 없음/빈배열/비배열 3건, 정상 응답, fetch reject 시 `reportError("useTrailRoute", err)` 호출+재throw 없음.
- **done_when 4개 전부 충족**: !res.ok→null, malformed points 3종→null, fetch 실패 시 reportError+null(재throw 아님), enabled/id 가드로 호출 안 함, `pnpm --filter web test` 통과.
- 되돌림 없음.

## 아키텍처 검토

- 세 이슈 모두 architect 단계에서 **frontend-impl 단독 담당**으로 확정 — 전부 순수 웹 프론트엔드(페이지·훅·테스트) 변경이고 DB/Edge Function 접점이 없다. 세 이슈 다 `packages/core`를 건드리지 않았다 — M-081이 필요로 한 `deadlineLabel`은 이미 core에 존재해 소비만 했다.
- M-081: 새 추상화 없음. `DdayPill`을 `opportunity-detail.tsx`에서 import하지 않고 이 파일에 다시 정의한 것이 유일한 설계 판단 — 두 파일 다 scope가 좁고(각 페이지 전용), 억지로 공유 컴포넌트로 뽑으면 `opportunity-detail.tsx`까지 손대게 돼 이번 클러스터 scope를 넘어선다. M-087(2026-09-04)이 모바일에서 이미 같은 판단을 내린 선례를 그대로 따랐다.
- M-083·M-084: 둘 다 **테스트만 추가, 프로덕션 코드 변경 0**. 기존 방어 로직/훅 구현이 done_when이 요구하는 계약을 이미 만족하고 있음을 테스트로 고정했을 뿐이다. 렌더 격리·FSD 경계 이슈 해당 없음(새 컴포넌트/상태 없음).

## 변경 내용

**M-081** (커밋 `569eaec`):
- `apps/web/src/app/report/page.tsx` — 로컬 formatDeadline 제거, deadlineLabel(core)+로컬 DdayPill로 교체
- `apps/web/src/app/report/page.test.tsx` — 마감 지남/임박/여유/없음 4케이스 추가

**M-083** (커밋 `db2f776`):
- `apps/web/src/app/login/page.test.tsx` — 신설, 오픈 리다이렉트 방어 6케이스

**M-084** (커밋 `846e00a`):
- `apps/web/src/hooks/useTrailRoute.test.ts` — 신설, 훅 3분기 검증 8케이스

## 검증

```
bash scripts/gate.sh
── typecheck   ✅ (core·mobile·tokens·web 4/4)
── test        ✅ (mobile 21파일 159/159, web 47파일 378/378)
── lint        ✅ (next lint — ✔ No ESLint warnings or errors)
── secrets     ✅ (diff 없음)
GATE: PASS — push 허용
```

세 커밋 모두 개별적으로 revert 가능하며, 최종 트리(3커밋 전부 반영)로 gate.sh를 다시 돌려 green을 확인한 뒤 push한다.

## 리스크 & 후속

- **M-052(모바일 지도) 24일 연속 블로킹** — 위 "⚠️ 사람 개입 필요" 섹션 참조. 사람이 3택1(경로 승인/priority 하향/blocked-human 도입) 중 하나를 처리하지 않으면 매일 밤 동일한 사전제외 기록이 반복된다.
- **트리거 실행 환경에 repo가 사전 배선되지 않음** — 이번 세션은 `/home/user`가 빈 디렉터리로 시작해 직접 clone해야 했다. 매번 발생하는지, 이번 세션만의 일회성 문제인지는 이 리포트만으로 판단할 수 없어 사람 확인이 필요하다.
- `architect.md`(FSD 언급) ↔ `constitution.md`(FSD 디렉토리 부재 명시) 모순은 여전히 미해결 — 2026-09-04 리포트에 이미 기록됨, 재수정 권고만 반복.
