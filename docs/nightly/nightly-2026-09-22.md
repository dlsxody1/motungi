# Nightly Report — 2026-09-22

## TL;DR

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `docs/backlog/backlog.yml`의 `status: todo`
  10건(`grep -c "status: todo"`) → `todo ≥ 2`이므로 구현 모드. audit 모드로 전환하지 않았다.
- **연속 report-only 경보 해당 없음** — 직전 세 밤(09-17: M-107 shipped, 09-19: M-109 shipped,
  09-21: M-111 shipped) 모두 코드가 나갔다. 오늘 밤도 코드가 나가 스트릭이 이어진다.
- **씨앗 M-097(최우선 medium)은 blocked-human이라 제외**하고 다음 씨앗 **M-113**으로 밤을 짰다.
  **클러스터는 M-113 1건**(scope 파일 겹침 0, 그리고 아래 CLUSTER RULE 폴백 불성립으로 확장 없음).
- **결과: SHIPPED.** M-113 — `geo.ts`(web/mobile) 타입·응답 파싱을 `packages/core`로 승격.
  `bash scripts/gate.sh` PASS(typecheck·test·lint·secrets·pure-tests 5/5) + web build 27/27
  라우트 성공. 커밋 `4f73adb`를 `dev`에 push. reverted 이슈 없음.
- ⚠️ **PRECEDENCE 위반 기록(트리거 프롬프트 요구사항)**: 오늘 밤 CLUSTER RULE 적용 과정에서
  트리거 프롬프트의 폴백 조건과 repo 3개 문서(`nightly-pipeline.md`·`PIPELINE.md`·
  `planner.md`)의 폴백 조건이 실제로 다른 결과를 냈다. 트리거를 따라 **1이슈 클러스터**로
  진행했다(아래 "집은 클러스터" 절 참조) — repo 문서대로였다면 3이슈 클러스터(M-113+M-114+M-115)가
  됐을 것이다.

## 집은 클러스터

**씨앗 선정**: `status: todo` 10건 중 priority 최상위는 `medium`(high 없음). medium은
M-097·M-113·M-114·M-115(id 오름차순) 4건. 규칙대로면 최소 id인 **M-097**이 씨앗이어야 한다.

**M-097을 씨앗에서 제외한 이유**: M-097(`동 단위 좌표 백필`)은 2026-09-07·09-09 밤이 이미
`blocked-human`으로 판정해 "씨앗에서 사전 제외"를 notes에 남겨둔 이슈다(`docs/backlog/backlog.yml:2042-2043`).
오늘 밤 재검증도 동일: `.env` 없음(`.env.example`만 존재, 전 항목 공란), `DATA_GO_KR_SERVICE_KEY`
공란, outbound network가 조직 정책으로 차단(`curl https://apis.data.go.kr/` → `CONNECT tunnel
failed, response 403`). 그럴듯한 좌표를 지어내는 건 정직한 근사치보다 나쁘다고 판단해 계속
제외한다. `status: todo` 유지(재시도 대상).

**다음 씨앗: M-113**(`web↔mobile 동명 파일 17쌍` 중 `geo.ts`, priority medium, id 최소).
scope: `[apps/web/src/lib/geo.ts, apps/mobile/lib/geo.ts, packages/core/src]`.

**scope 겹침으로 묶이는 다른 todo가 있는지 확인**(파일 단위):
- M-114 scope `[packages/core/src/index.ts, packages/core/src/gu-summary.ts]` — M-113이 건드리는
  파일(`geo.ts` 2개)과 겹치지 않는다. `packages/core/src`(M-113 scope의 세 번째 원소)는 core
  패키지 전체를 가리키는 성긴 표현이라 "좁은 디렉토리"로 보지 않았다(`apps/mobile` ↔ `apps/mobile`이
  겹침이 아니라는 규칙과 같은 근거).
- M-115 scope `[apps/mobile/ui/theme.ts, packages/tokens/src/index.ts]` — 겹치지 않는다.
- M-088·M-092·M-093·M-102·M-103·M-110(low priority) — 전부 다른 파일. 겹치지 않는다.
→ **파일 겹침 후보 0건.**

**폴백 여부 — 트리거 프롬프트와 repo 문서가 갈리는 지점**:
- 트리거 프롬프트의 CLUSTER RULE 3번은 "seed의 scope에 파일이 하나도 없고 좁은 디렉토리도 없을 때만"
  폴백(같은 앱+같은 priority 최대 2건 추가)을 허용한다. M-113의 scope에는 `geo.ts` 파일이 **2개**
  포함돼 있으므로 이 조건은 성립하지 않는다 → **폴백 없음** → 클러스터는 M-113 1건.
- 반면 `.claude/rules/workflow/nightly-pipeline.md:61`, `docs/nightly/PIPELINE.md:58`,
  `.claude/agents/planner.md:25`는 셋 다 "씨앗 scope에 파일이 섞여 있든 없든 무관 — 기준은
  *파일 겹침 후보가 0개라는 사실*"이라고 명시한다. 이 기준이면 M-113은 파일 겹침 후보가
  0개이므로 폴백이 성립해 **같은 priority(medium) + 같은 앱 경계**인 M-114(`packages/core`
  공유)·M-115(`apps/mobile` 공유)를 id 오름차순으로 최대 2건까지 더해 **3이슈 클러스터**
  (M-113+M-114+M-115, 상한 3 정확히 충족)가 됐을 것이다.
- 오케스트레이터 트리거 프롬프트의 PRECEDENCE 절("이 프롬프트가 repo 문서와 상충하면 이 프롬프트를
  따르고 상충을 file:line으로 기록하라")에 따라 **트리거의 좁은 폴백 조건을 따랐다** — 결과는
  M-113 단독 클러스터. **사람에게 요청**: 어느 쪽이 의도인지 정하고 트리거 프롬프트와 위 3개
  repo 문서 중 뒤처진 쪽을 갱신해 달라. (2026-08-05에 repo 문서가 먼저 바뀌고 트리거가 나중에
  갱신되며 조건 문구가 미묘하게 달라진 것으로 추정된다 — M-024의 재발 패턴과 같은 종류의 위험:
  트리거와 repo 문서가 다시 벌어지고 있다.)
- 상한 3개로 미룬 이슈: 없음(트리거 기준 적용 결과 클러스터가 애초에 1건).

## 이슈별 결과

### M-113 — SHIPPED
- **제목**: web↔mobile 동명 파일 17쌍 — `geo.ts` 조각(나머지 훅 5쌍은 M-108 종속으로 범위 밖)
- **커밋**: `4f73adb` (dev 직접 커밋, 트렁크 모델, PR 없음)
- **수용 기준 판정**:
  - ✅ "geo.ts의 타입과 응답 파싱을 core로 올린다" — `packages/core/src/geo.ts` 신설:
    `ReverseGeoResult`·`NeighborhoodSearchResult` 타입 + `parseReverseGeoResponse`·
    `parseNeighborhoodSearchResponse` 순수 함수. web(`apps/web/src/lib/geo.ts`)·
    mobile(`apps/mobile/lib/geo.ts`)은 오리진별 `fetch` 호출과 에러 리포팅(web만
    `reportError` 보유 — mobile은 애초에 리포팅 인프라가 없어 그대로 둠)만 남기고 파싱을
    core 함수 호출로 교체.
  - ✅ "core에 테스트를 붙인다(check-pure-tests.sh가 강제)" — `packages/core/src/geo.test.ts`
    8케이스(정상/부분 필드/빈 객체/null/undefined/원시값/items 비배열). `gate.sh`의
    `pure-tests` 단계 통과 확인.
  - ✅ "훅 5쌍은 M-108에 종속, 여기서 선행 처리하지 않는다" — `useOpportunity` 등 훅 파일은
    건드리지 않았다(diff에 없음).

## 아키텍처 검토

- **담당**: frontend-impl 범위(apps/web·apps/mobile) + packages/core(공유 로직) — 오케스트레이터가
  이슈 규모(1개, 소규모 리팩터)를 감안해 별도 architect/frontend-impl 서브에이전트 분리 없이 직접
  계획·구현·검증을 수행했다.
- **레이어 경계**: core는 여전히 순수 함수만 갖는다(fetch·환경변수 접근 없음 — 그건 각 앱의
  `lib/geo.ts`에 남겼다). `apps/web/.eslintrc.json`의 `import/no-restricted-paths`는
  `lib/ → @motungi/core` 방향을 막지 않는다(이미 `catalog.ts` 등 기존 core 함수들도 lib에서
  호출 중) — 위반 없음. 헌법의 "FSD 디렉토리는 실재하지 않는다"(constitution.md) 원칙대로
  파일 위치는 평면 구조(`lib/`) 그대로 유지, 새 디렉토리를 만들지 않았다.
- **데이터 흐름**: 타입이 core → web/mobile로 전파되는 변경이라 두 앱 모두 typecheck를
  재확인했다(둘 다 clean). DB 마이그레이션 불필요(순수 타입/파싱 이동).
  참고: `import.*api-error` 순환 없음 — `reportError`는 web 로컬에 남아 core는 이를 모른다.
- **테스트 전략**: unit(core 순수 함수 8케이스 신규) + 기존 계약 테스트 재사용(web 10케이스·
  mobile 1케이스, 리팩터 후에도 무변경으로 통과 — 응답 파싱 동작이 바뀌지 않았음을 회귀로 증명).

## 변경 내용

| 파일 | 요약 |
|---|---|
| `packages/core/src/geo.ts` (신규) | `ReverseGeoResult`·`NeighborhoodSearchResult` 타입, `parseReverseGeoResponse`·`parseNeighborhoodSearchResponse` 순수 파서 |
| `packages/core/src/geo.test.ts` (신규) | 위 두 파서의 단위 테스트 8케이스 |
| `packages/core/src/index.ts` | `export * from "./geo"` 추가 |
| `apps/web/src/lib/geo.ts` | 타입 재export + 파싱을 core 함수 호출로 교체(fetch·`reportError`는 그대로) |
| `apps/mobile/lib/geo.ts` | 타입 재export + 파싱을 core 함수 호출로 교체(fetch·`WEB_ORIGIN` 판단은 그대로) |

## 검증

모두 최신 `dev`(`e4681e2` 기준, 커밋 후 `4f73adb`) 위에서 실제 실행. 출력 원문 발췌:

```
$ pnpm install --frozen-lockfile
...
Done in 15s

$ bash scripts/gate.sh
── typecheck   ✅ (core·mobile·tokens·web 4/4, FULL TURBO 캐시 히트)
── test        ✅
  @motungi/core:test:  ✓ src/geo.test.ts (8 tests) 17ms   ← 신규
  @motungi/core:test:  Test Files 17 passed (17) / Tests 437 passed (437)
  @motungi/web:test:   ✓ src/lib/geo.test.ts (10 tests) 9ms   ← 기존, 리팩터 후에도 무변경 통과
  @motungi/web:test:   Test Files 48 passed (48) / Tests 395 passed (395)
  (mobile: 이전 turbo 캐시 히트로 gate.sh 로그엔 축약 — 아래 별도 재실행으로 확인)
── lint        ✅ (경고만 존재 — diagnosis/explore/location/saved 등 기존 max-lines·complexity
                 경고. geo.ts 관련 경고 0건, 전부 이번 변경과 무관한 기존 파일)
── secrets     ✅ (diff 없음)
── pure-tests  ✅ (순수 레이어 테스트 누락 없음, 면제 6건 — geo.ts는 면제 목록 밖이라 강제 대상이었고 통과)

GATE: PASS — push 허용

$ pnpm --filter @motungi/mobile test -- geo
 ✓ lib/geo.test.ts (1 test) 2ms
 Test Files 1 passed (1) / Tests 1 passed (1)

$ pnpm --filter @motungi/web build
✓ Generating static pages (27/27)
(라우트 27/27 성공, geo 변경으로 인한 빌드 실패 없음)
```

push 전 머지 체크: `git log --oneline origin/dev..origin/main` → 비어 있음(main이 dev를
앞서지 않음, 되받을 것 없음). `git merge-base --is-ancestor origin/dev dev` → OK(fast-forward,
push 시점까지 dev에 동시 작업 없음 확인).

## 리스크 & 후속

- **M-097(좌표 백필)은 여전히 blocked-human.** 사람이 `data.go.kr`/서울 열린데이터광장 API 키를
  발급해 `.env`에 채우거나(또는 outbound network 예외를 허용) 대체 데이터 출처를 제공해야
  다음 밤이 진행할 수 있다. 세 번째 밤(09-07→09-09→오늘)째 같은 사유로 막혀 있다 — 반복
  재검증만 하고 있으니, 계속 막혀 있을 거면 `priority: low`로 낮추거나 이슈 자체를
  "사람 조치 대기" 라벨로 백로그 상단에서 내리는 걸 고려해 달라(매 밤 씨앗 후보로 재검토하는
  비용이 든다).
- **CLUSTER RULE 폴백 조건 불일치**(위 TL;DR·"집은 클러스터" 절 참조) — 트리거 프롬프트 vs
  `.claude/rules/workflow/nightly-pipeline.md:61` / `docs/nightly/PIPELINE.md:58` /
  `.claude/agents/planner.md:25`. 사람이 의도를 확정하고 뒤처진 쪽을 갱신해야 다음 밤부터
  일관된 클러스터 크기가 나온다. 현재 상태로는 어떤 밤이 오케스트레이터 프롬프트를 그대로
  따르느냐, 서브에이전트(`planner.md`)가 만든 스펙을 그대로 따르느냐에 따라 클러스터 크기가
  1개 vs 3개로 갈릴 수 있다.
- **M-114(공개 API 경계 명시)·M-115(mobile 타이포 tokens 불일치)**는 오늘 밤 클러스터에
  포함되지 않았다(위 폴백 판단 때문). 둘 다 `packages/core/src`·`apps/mobile` 파일을 만지므로
  다음 밤 씨앗이 되면 서로 스코프가 겹칠 여지가 있다 — 특히 M-115는 "구현 전에 결정이 먼저"라고
  스스로 명시하고 있어(타이포 격차가 의도인지) 사람 판단이 필요할 수 있다.
- **`lint`가 이미 걸고 있는 사전 경고**(이번 변경과 무관, 기존 상태): `diagnosis/page.tsx`·
  `explore/(list)/page.tsx`·`location/page.tsx` 등 다수 파일이 `max-lines-per-function`/
  `complexity` 경고를 내고 있다(M-102가 이미 이 중 2파일을 백로그에 등재해둠). 오늘 밤 범위
  밖이라 손대지 않았다.
