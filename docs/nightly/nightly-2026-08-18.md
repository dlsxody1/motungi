# Nightly Report — 2026-08-18

## TL;DR

- **BACKLOG GATE 판정: audit 모드.** 밤 시작 시 `docs/backlog/backlog.yml`의 `status: todo` 개수를 셌더니 **1건**(M-052, 5일 연속 블록 상태)뿐이었다 — 트리거 프롬프트 규칙상 `todo < 2`이므로 구현 모드(planner/architect/impl/qa) 대신 **audit 모드**를 실행했다. 코드는 전혀 건드리지 않았다.
- `origin/dev` 실코드를 8축(a11y·perf·보안·타입안전·FSD 경계·테스트 커버리지·UX 일관성·데드코드)으로 병렬 감사(축당 1개, 총 8개 에이전트)했다. 원 findings 21건 → 각 finding을 직접 파일:라인을 다시 읽어 적대검증(반증 시도) → 21건 전부 생존(허위 finding 없음) → 같은 파일·같은 결함 패턴인 것끼리 dedup/클러스터링해 **17개 이슈(M-056~M-072)**로 `backlog.yml`에 등재(`status: todo`).
- `bash scripts/gate.sh`(typecheck·test·lint·시크릿 스캔) 실행 — 코드 무변경이므로 예상대로 **전부 PASS**(typecheck 4/4, test core/web/mobile 전체 통과, lint 통과, 시크릿 없음). audit 모드는 이 게이트를 "코드가 안 깨졌다"는 확인용으로만 돌렸다(원래도 통과가 자명함).
- **연속 report-only 경보 해당 없음**: 직전 밤(08-17)은 M-054를 shipped한 정상 구현 밤이었다. 이번이 그 뒤 첫 report-only/audit 밤이라 "2회 이상 연속" 경보 조건에 해당하지 않는다.
- **트리거 PRECEDENCE 절 점검**: 이 트리거 프롬프트의 BACKLOG GATE·CLUSTER RULE 원칙을 `docs/nightly/PIPELINE.md`·`.claude/rules/workflow/nightly-pipeline.md`와 대조했다. 두 문서 모두 이미 동일한 0단계 백로그 게이트와 클러스터 규칙을 담고 있어 override가 필요한 지점은 없었다. 새로 발견한 모순은 없음.

## 감사 범위와 방법

8개 에이전트를 축별로 병렬 fan-out했다(각 read-only, 제품 코드 미수정). 각 에이전트에게 이미 등재된 관련 M-NNN 이슈 목록(총 55개, 대부분 `doing`/`done`)을 주고 "이미 고쳐진 것과 겹치지 않는, 진짜 새로운 결함만" 찾도록 지시했다.

| 축 | 담당 에이전트 findings | 검증 후 채택 |
|---|---|---|
| a11y | 4건 | 4건 (1건은 M-058 스윕에, 1건은 M-071로 단독) |
| perf | 2건 | 2건 (M-063, M-064) |
| 보안 | 1건 | 1건 (M-059) |
| 타입안전 | 3건 | 3건 (M-061, M-066, M-070) |
| FSD/의존성 경계 | 1건 | 1건 (M-072) |
| 테스트 커버리지 | 5건 | 5건 (M-056, M-057, M-065, M-068, M-069) |
| UX 일관성(web↔mobile) | 2건 | 1건으로 병합 (M-062) |
| 데드코드 | 3건 | 2건으로 병합 (M-060, M-067) |
| **합계** | **21건** | **17개 이슈로 등재** |

각 에이전트가 제출한 finding은 등재 전 내가 실제 파일:라인을 직접 다시 읽어 재검증했다(적대검증 — 반증을 시도했다). 21건 전부 코드에서 실제로 확인돼 살아남았고, 허위양성(false positive)으로 탈락한 finding은 없었다. 단, "같은 파일·같은 결함 패턴"인 4쌍을 하나의 이슈로 병합해 등재 건수를 21→17로 줄였다(아래 §병합 사유 참조).

## 등재된 이슈 (M-056 ~ M-072)

| id | priority | scope | 요약 |
|---|---|---|---|
| M-056 | high | `apps/mobile/lib/auth.ts` | 모바일 OAuth/세션 흐름 테스트 0건 |
| M-057 | high | `apps/web/src/hooks/useReportFallback.ts` | 반경 사다리 폴백(과거 실제 버그 수정 로직) 테스트 0건 |
| M-058 | high | 모바일 5개 화면/컴포넌트 | 핵심 인터랙션 요소에 accessibilityRole 누락 스윕 |
| M-059 | medium | `supabase/functions/ingest/index.ts` | 산책로 GPX ingest fetch에 SSRF 방어(호스트 검증) 누락 |
| M-060 | low | web 아이콘 파일 2개 | 미사용 아이콘 export 11개 정리 |
| M-061 | medium | `apps/mobile/tsconfig.json` | `noUncheckedIndexedAccess` 미설정 |
| M-062 | medium | `apps/mobile/app/opportunity.tsx` | summary·주말나들이 배지 누락(웹엔 있음) |
| M-063 | medium | 모바일 카탈로그 훅 2개 | 모바일 카탈로그가 캐시 없이 순차 3왕복(M-041 웹에만 적용됨) |
| M-064 | medium | `apps/web/src/hooks/useSavedOpportunities.ts` | 보관함 N+1 쿼리(코드 자체 주석에 이미 명시된 부채) |
| M-065 | medium | `apps/web/src/hooks/useNeighborhoodSearch.ts` | IME 조합 처리(과거 실제 버그 수정 로직) 테스트 0건 |
| M-066 | low | `apps/web/src/app/api/why-reasons/route.ts` | category 캐스트에 멤버십 검증 없음 |
| M-067 | low | `packages/tokens` + `apps/mobile` | `MIN_HIT_TARGET` 미사용이면서 문서는 적용됐다고 서술 |
| M-068 | medium | `apps/web/src/lib/kakao.ts` | 공유 3단 폴백 테스트 0건 |
| M-069 | medium | `apps/web/src/lib/geo.ts` | 항상 mock만 되고 직접 테스트 0건(모바일과 비대칭) |
| M-070 | low | `packages/core/src/store.ts` | persist 재수화 시 answers 런타임 검증 없음 |
| M-071 | low | `apps/web/src/components/neighborhood-menu.tsx` | 동네선택 다이얼로그 선택 상태 미노출 |
| M-072 | medium | core + web + mobile 카탈로그 관련 3파일 | 반경 사다리 정책이 core가 아니라 앱 2곳에 중복 구현 |

우선순위 분포: high 3 · medium 9 · low 5.

## 병합 사유 (dedup/클러스터링)

- **M-058**(a11y): 원래 3건의 개별 finding(공용 `Button` 프리미티브, 진단/상세 CTA, 홈/탐색 카드)이 전부 "RN Pressable에 `accessibilityRole` 누락"이라는 동일 결함 패턴이었다. `M-013`/`M-014` 선례(같은 패턴을 여러 파일에 걸쳐 한 스윕 이슈로 등재)를 따라 5개 파일을 하나의 이슈로 묶었다.
- **M-062**(UX 패리티): `apps/mobile/app/opportunity.tsx` 한 파일에서 나온 두 개의 독립 결함(summary 누락, 주말나들이 배지 누락)을 `M-053` 선례(같은 파일의 두 항목을 한 이슈로 처리)에 맞춰 병합했다.
- **M-060**(데드코드): `icons.tsx`·`landing-icons.tsx` 두 파일의 "미사용 아이콘 export" — 같은 결함 패턴이라 병합.
- 나머지 13건은 서로 독립적인 결함이라 각각 별도 이슈로 유지했다.

## 검증 (적대검증 상세)

각 finding에 대해 에이전트가 제시한 파일:라인을 직접 `Read`로 다시 읽어 다음을 확인했다:
- **실재성**: 코드가 실제로 그 상태인가(예: M-058 — Button/diagnosis.tsx/opportunity.tsx/report.tsx/explore.tsx 6곳 모두 직접 읽어 `accessibilityRole` 부재를 확인했고, 대조군으로 같은 파일의 이미 고쳐진 컨트롤(Chip, 북마크 토글)에는 실제로 있음을 확인).
- **중복 여부**: 이미 `doing`/`done`인 M-NNN이 같은 결함을 이미 고쳤는지 실코드로 재확인(예: M-030 웹 버튼 대비, M-041 웹 카탈로그 캐시 등은 전부 실제로 고쳐져 있어 재보고 안 함).
- **grep 재검증**: 데드코드 계열(M-060, M-067)은 `grep -rn -w`로 전체 리포(`apps packages supabase scripts docs .claude`)를 다시 훑어 정의처 외 참조가 0건임을 직접 확인.
- **보안(M-059)**: SSRF 대조군으로 이미 방어된 읽기 경로(`apps/web/src/app/api/trail-route/route.ts:72-74`, `protocol/hostname` 검증 존재)와 무방비인 적재 경로(`supabase/functions/ingest/index.ts:112-129`, 검증 없음)를 나란히 읽어 갭이 실재함을 확인.

21건 전원 생존, 반증되어 탈락한 finding은 없었다.

## 검증 (qa 게이트)

```
$ bash scripts/gate.sh
── typecheck
   ✅ typecheck   (4/4 워크스페이스)
── test
   ✅ test        (core/web/mobile 전체 통과, 279+ 웹 테스트 포함)
── lint
   ✅ lint        (web ESLint 경고/에러 없음)
── secrets
   ✅ secrets     (diff에 시크릿 패턴 없음)
GATE: PASS — push 허용
```

`git log --oneline origin/dev..origin/main` — 처음 얕은 클론(depth=1) 상태에서는 비어있지 않은 것처럼 보였으나, `--deepen=50`으로 실제 이력을 받아 `git merge-base origin/dev origin/main`을 계산하니 merge-base가 `origin/main` HEAD(`744411d`)와 정확히 일치 — 즉 main은 이미 dev의 조상(ancestor)이라 실제 divergence는 없었다(얕은 클론의 아티팩트였음). 되받을 것 없이 바로 진행.

`git status --short` / `git diff --stat` — 변경 파일은 `docs/backlog/backlog.yml` 1개뿐(303 insertions, 0 deletions), 제품 코드 미수정 확인.

## 리스크 & 후속

- **M-052가 여전히 유일한 실질 todo**: 지도/산책로 표시 기능이 6일째(08-13~08-18) 사람의 아키텍처 결정 없이는 진행 불가능한 상태다. 이번 밤 audit로 backlog가 다시 채워졌으니(todo 1→18) 당장 다음 밤부터는 구현 모드로 정상 복귀하겠지만, M-052 자체의 블로킹은 여전히 미해결 — 사람이 (a) 구현 방식 승인, (b) priority 강등, (c) `blocked-human` 상태 도입 중 하나를 결정해줄 것을 다시 요청한다(08-17 리포트와 동일 요청 반복).
- **다음 밤 예상 클러스터**: `todo`가 18건으로 늘었으니 다음 밤은 정상적으로 구현 모드(BACKLOG GATE `todo ≥ 2`)로 돌아간다. priority 최상위는 M-052(medium, blocked)를 제외하면 M-056/M-057/M-058(모두 high) 중 id 오름차순 M-056이 씨앗이 될 가능성이 높다 — 다만 M-056(`apps/mobile/lib/auth.ts`)은 다른 high 이슈와 파일 겹침이 없어(scope가 서로 다른 파일) 1이슈 클러스터가 될 수 있음을 미리 밝혀둔다. planner가 밤 시작 시 다시 판단할 것.
- **모바일 lint 스크립트 부재**: 기존에 이미 기록된 known gap(`pnpm lint`가 web만 검사, core·mobile엔 lint 스크립트 없음) — 이번 밤 새로 발견한 건 아니고 재확인만 했다. 백로그 후보로 남겨두되 이번 audit에서 별도 이슈로 등재하지는 않았다(이미 기록됐다는 사실 자체를 리스크로만 남김 — 원하면 다음 audit에서 정식 이슈화 가능).
- **M-067·M-072는 방향 결정이 필요한 이슈**: M-067(MIN_HIT_TARGET을 배선할지 문서를 정리할지)과 M-072(반경 사다리 정책을 core로 승격하는 리팩터링 범위)는 done_when 자체에 "판단"이 섞여 있다 — 다음 밤 planner가 진행 전에 다시 한번 서브스코프를 좁혀야 할 수 있다.

## PR

없음. `docs/nightly/PIPELINE.md`의 dev 직접 커밋(트렁크) 모델에 따라 브랜치·PR 없이 `dev`에 직접 커밋·push한다.
