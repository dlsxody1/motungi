# Nightly Report — 2026-09-02

## TL;DR

- **BACKLOG GATE 판정: 구현 모드.** `todo ≥ 2`(밤 시작 시 다수 잔여) → 클러스터 1개(1~3이슈) 진행.
- **연속 report-only 경보 해당 없음.** `ls docs/nightly/` 최근 파일(08-27~09-01) 확인 결과 직전 밤 2026-09-01은 M-082/M-074/M-077이 shipped된 정상 구현 밤이었다. 연속 report-only 스트릭은 0이라 경고 배너 불필요.
- **클러스터 = M-085(씨앗, high) + M-080(medium) + M-086(medium), 3건 전부 shipped.** 되돌린 이슈 없음.
- `bash scripts/gate.sh` 3커밋 전체 트리 기준 **PASS** — typecheck 4/4 · test(core 321/321 · web 343/343 · mobile 150/150) · lint clean(`next lint` — `✔ No ESLint warnings or errors`) · 시크릿 스캔 clean(`(시크릿 패턴 없음)`). qa가 별도로 `pnpm --filter web build`도 돌려 27페이지 빌드 성공을 확인했다.
- **오늘 밤도 doc 모순 1건 재확인**(아키텍트가 이번 클러스터 작업 중 발견) + **origin/main↔origin/dev 분기 조사 완료(머지 안 함, 오탐 판정)** — 둘 다 아래 §리스크에 상세.

## 집은 클러스터

- **씨앗**: M-085 (priority `high`, high 등급 todo 중 최소 id 후보). 모바일 탐색 화면 "동네 변경"이 매번 전체 재진단(`/location`→`/diagnosis`)으로 강제 이동돼 탐색 맥락을 잃는 문제.
- **M-080 추가** — 이유: **파일 완전 겹침**. M-085의 scope(`apps/mobile/app/(tabs)/explore.tsx`, `apps/mobile/app/location.tsx`)와 M-080의 scope(`apps/web/src/lib/explore-filters.ts`, `apps/mobile/app/(tabs)/explore.tsx`)가 `apps/mobile/app/(tabs)/explore.tsx`에서 겹친다.
- **M-086 추가** — 이유: **파일 완전 겹침**. M-086의 scope(`apps/mobile/app/location.tsx`)가 M-085의 scope와 `apps/mobile/app/location.tsx`에서 겹친다.
- **상한 3개에 정확히 도달** — 겹침 후보가 M-080·M-086 2건이었고 둘 다 채택했다. 폴백(같은 앱+같은 priority)은 필요하지 않았다(실제 파일 겹침으로 자연 충족).
- **미룬 이슈 없음** — 씨앗과 파일 겹침이 있던 후보는 M-080·M-086뿐이었다.

## 이슈별 결과

- **M-085 (high) — shipped.** 커밋 `1b381aa feat(mobile): M-085 동네 변경을 탐색 화면 내 인라인 앵커 갱신으로 분리`. 새 `apps/mobile/ui/neighborhood-menu.tsx`(웹의 `neighborhood-menu.tsx` 패턴을 RN Modal로 이식)로 탐색 화면 pill을 누르면 그 자리에서 인기 동네를 골라 홈 앵커만 갱신한다. "동네 다시 설정하기"는 별도 행으로 분리해 `/location`→`/diagnosis` 재진단 플로우로만 이동한다.
- **M-080 (medium) — shipped.** 커밋 `23327e3 refactor(core): M-080 promote explore FILTERS taxonomy to single source`. `packages/core/src/view.ts`에 `EXPLORE_CATEGORY_FILTERS`(7항목, "전체" 포함)를 신설해 web(`apps/web/src/lib/explore-filters.ts`)·mobile(`explore.tsx`)이 각자 들고 있던 byte-동일 리터럴을 제거하고 import로 대체. 기존 `CATEGORY_LABEL`(카드 태그용 6개 Record)과는 의도적으로 별개로 유지(라벨 문구가 다름, 병합 안 함).
- **M-086 (medium) — shipped.** 커밋 `166990a fix(mobile): M-086 위치 권한 영구 거부 시 설정 안내 복구 문구 추가`. `location.tsx`가 `requestForegroundPermissionsAsync()` 호출 전에 `getForegroundPermissionsAsync()`로 먼저 상태를 조회해, 영구 거부(`status:"denied" && canAskAgain:false`)일 때만 "설정 앱 > 개인정보 보호 > 위치 서비스…" 구체 안내로 갈라낸다. 최초 거부 시의 기존 일반 문구와는 다른 문구임을 테스트로 명시 검증.

## 아키텍처 검토

- M-085가 담당 경계를 새로 만들지 않고 **기존 웹 패턴(`neighborhood-menu.tsx`)을 그대로 모바일에 이식**하는 방향으로 결정됐다 — RN에 `<dialog>`가 없어 Modal(transparent + backdrop Pressable)로 동등 역할을 구현. 앵커 상태는 여전히 `useAppStore`(zustand)가 소유하며 컴포넌트는 갱신만 트리거 — "상태는 쓰는 컴포넌트가 소유" 원칙 위반 없음.
- M-080의 core 승격은 `CATEGORY_LABEL`(카드 태그, 6개)과 `EXPLORE_CATEGORY_FILTERS`(탐색 필터, 7개 + "전체")를 **의도적으로 병합하지 않는** 경계 판단 — 라벨 문구가 다르고(`culture` → "문화·공연" vs "동네 문화·공연") 용도가 다르므로 하나의 taxonomy로 억지로 합치지 않았다. `packages/core`가 순수 도메인 로직이라는 경계에도 부합(부작용 없는 리터럴 배열 export).
- M-086은 새 의존성이나 계층 변경 없이 기존 `expo-location` API(`getForegroundPermissionsAsync`)를 프리체크로 추가하는 최소 변경 — 테스트 전략은 `location.test.tsx`에 mock을 통해 3분기(최초 거부/영구 거부/성공)를 모두 렌더 검증.
- 세 이슈 모두 파일 겹침 기준으로 정확히 클러스터링됐고, 서로 다른 관심사(동네 전환 UX / 카테고리 taxonomy 중복 / 권한 복구 안내)가 우연히 같은 두 파일(`explore.tsx`, `location.tsx`)을 건드려 묶인 것 — 로직 간 실질적 결합은 없다.

## 변경 내용

- `apps/mobile/ui/neighborhood-menu.tsx` (신규, 159줄) — 탐색 화면 인라인 동네 전환 메뉴(Modal). 인기 동네 목록 pick + "동네 다시 설정하기" 별도 행.
- `apps/mobile/app/(tabs)/explore.tsx` — 기존 `Pressable` 동네 pill + `router.push("/location")`를 `<NeighborhoodMenu dongLabel={dongName} />`로 교체. `FILTERS` 로컬 리터럴을 core의 `EXPLORE_CATEGORY_FILTERS`로 교체.
- `apps/mobile/app/(tabs)/explore.test.tsx` — 인라인 메뉴 관련 렌더/네비게이션 테스트 갱신.
- `apps/mobile/app/location.tsx` — `getForegroundPermissionsAsync()` 프리체크 추가, 영구 거부 시 별도 안내 문구 분기.
- `apps/mobile/app/location.test.tsx` — 영구 거부 케이스 신규 테스트 + 기존 케이스에 `getForegroundPermMock` mock 추가.
- `apps/web/src/lib/explore-filters.ts` — `FILTERS` 로컬 리터럴 제거, `EXPLORE_CATEGORY_FILTERS` re-export로 교체. web 전용 헬퍼(`filterLabelOf`/`exploreHref`)는 그대로 유지.
- `packages/core/src/view.ts` — `EXPLORE_CATEGORY_FILTERS` 신설(7항목, "전체" 포함).
- `packages/core/src/view.test.ts` — 신규 상수 검증 테스트 추가.

## 검증

qa가 3커밋 전체 트리에서 `bash scripts/gate.sh`를 실행, 실제 출력(요약):
```
✅ typecheck   — 4/4 패키지(core/mobile/tokens/web), FULL TURBO 캐시 hit
✅ test        — core: Test Files 11 passed (11), Tests 321 passed (321)
                 mobile: Test Files 21 passed (21), Tests 150 passed (150)
                 web: Test Files 43 passed (43), Tests 343 passed (343)
✅ lint        — next lint: "✔ No ESLint warnings or errors"
✅ secrets     — "(시크릿 패턴 없음)"
GATE: PASS — push 허용
```
(jsdom의 `window.scrollTo`/`HTMLCanvasElement.getContext` not-implemented 경고는 web 테스트의 기존 알려진 잡음이며 테스트는 모두 pass — 신규 실패 아님.)

qa가 추가로 `pnpm --filter web build` 실행 — 성공, 27개 페이지 빌드 완료.

리뷰어(본인)가 별도로 재확인한 것: `git log --oneline 5de3968..166990a`(3커밋), `git diff --stat`(8파일, 321줄 추가/56줄 삭제), 각 커밋 diff를 직접 읽어 정합성·범위·스타일 확인(코드 스니펫은 §변경 내용 참조), `scripts/scan-secrets.sh`를 `origin/dev` 베이스로 재실행해 clean 재확인.

## 리스크 & 후속

1. **커밋 메시지 규율 확인: 통과.** 3개 커밋 모두 Conventional Commits 형식(`refactor(core):`/`feat(mobile):`/`fix(mobile):`)이고 제목에 `M-080`/`M-085`/`M-086`이 명시돼 있어 `git log`만으로 코드→백로그 역추적이 가능하다.

2. **doc 모순 발견 — `.claude/agents/architect.md`가 폐기된 FSD 구조를 계속 언급함.** 이번 밤 architect가 클러스터 작업 중 재확인. `architect.md`의 9번째·18번째 줄이 여전히 "FSD"(`entities/features/widgets/shared` 계층)를 이 repo의 아키텍처로 서술한다. 이는 오래된 서술이다 — 항상 로드되는 상위 권위 문서인 `CLAUDE.md`와 `.claude/rules/core/constitution.md`는 명시적으로 파일 구조가 **평면**(`app/`·`components/`(평면, kebab-case)·`hooks/`·`lib/`·`data/`·`store/`)이며 "FSD 디렉토리는 실재하지 않는다(옛 룰의 잔재였다)"라고 규정한다. 이번 밤 문서 간 상충 시 상위 권위(항상 로드 문서)를 따르는 규칙에 따라 architect는 평면 구조 기준으로 올바르게 작업했고, 이 모순을 사람이 고쳐야 할 항목으로 플래그만 남겼다. **사람 조치 필요**: `.claude/agents/architect.md`에서 FSD 언급을 제거.

3. **`origin/main` vs `origin/dev` 분기 조사 완료 — 머지하지 않음(오탐 판정).** 밤 시작 시 `git log origin/dev..origin/main`이 커밋 1건(`744411d`, PR #21, 2026-08-06, "베이지 폐기 · 카카오 로그인 화면 · 리포트 fallback 동네 버그")을 "앞서 있음"으로 보여줬다. `nightly-pipeline.md`의 브랜치 모델 6번 규칙(최근 추가/수정됨, 바로 이 PR #21을 선례로 인용 — "스쿼시 4건이 안 돌아와 충돌 5건")은 이런 경우 `origin/main`을 dev로 되받아 merge하라고 지시한다. 이번 밤은 맹목적으로 머지하지 않고 조사했다: `git diff origin/dev origin/main`은 **137개 파일, 약 10,630줄** 규모의 diff였고, 내용은 dev가 이후 이미 삭제한 옛 코드(오래된 테스트 파일, 옛 `store.ts`/`view.ts`, 백필 스크립트 등)를 **되돌려 재도입**하는 것이었다. dev는 8/6 이후 계속 전진했지만 main은 그 스쿼시 커밋 하나에 멈춰 있어 벌어진 괴리다. 실제 내용 검증을 위해 스쿼시 커밋 메시지가 주장하는 변경 2건을 직접 확인: (a) M-030 색상 수정(`packages/tokens/src/index.ts`의 `#d42f4a`) — dev에 이미 존재. (b) M-049(진단 Q1 다중선택) — dev의 백로그 노트에 후속 커밋 해시까지 상세히 기록돼 이미 구현·문서화됨. 결론: 이는 규칙 6번 자신의 선례 노트가 설명하는 **스쿼시 머지 SHA 불일치 오탐** 케이스 그 자체다 — main의 내용은 이미 dev에 (다른, 스쿼시 안 된) 커밋 SHA로 완전히 포함돼 있다. 머지를 강행했다면 dev가 의도적으로 제거한 ~1만 줄을 순손실로 재도입했을 것이다. **머지를 수행하지 않았다.** 사람이 확인할 것: (i) `origin/main` 자체를 dev 기준으로 fast-forward/갱신할지 여부(이는 사람의 dev→main 승격 판단 영역, 이번 범위 밖), (ii) 규칙 6번의 "자동 머지" 지시가 커밋 개수가 아니라 **내용**을 기준으로 게이트되도록 고쳐서, 다음 밤이 이런 파괴적 머지를 맹목적으로 실행하지 않도록 예방하는 것.

4. **다음 밤 후보**: 남은 high priority todo(M-030 등은 이미 doing/완료 처리됐을 수 있으므로 실제 `backlog.yml` 재확인 필요)와, 이번 밤에 다루지 않은 medium/low 이슈들. 특히 M-081(리포트 원픽 카드 formatDeadline 재구현), M-083(로그인 오픈 리다이렉트 테스트 0건), M-084(useTrailRoute 미검증), M-087/M-088(모바일 리포트 화면 갭)이 여전히 `todo`로 남아 있다.
