# Nightly Report — 2026-08-05

## TL;DR
클러스터 = **M-030**(웹 primary 버튼 WCAG AA 대비 미달) + **M-034**(apps/web 핵심 서버 로직 테스트 공백: auth.ts·api/geo route). 둘 다 qa 게이트(typecheck·test, 4개 패키지 전체)를 통과해 **둘 다 dev로 나갔다**. revert 없음.

## 집은 클러스터
- **씨앗**: `status: todo` 중 priority 최상위 1개, 동순위면 id 오름차순 → 후보 `M-030`·`M-034`·`M-035`·`M-045`·`M-046`(모두 high) 중 최소 id인 **M-030**.
- **파일 겹침 판정**: M-030의 `scope`는 `[packages/tokens, apps/web]`로, 둘 다 파일이 아닌 패키지/앱 전체 단위 — 다른 어떤 todo 이슈의 `scope`와도 실제 파일·좁은 디렉토리 겹침이 없다(0건).
- **폴백 적용**: 파일 겹침 후보 0건이므로 같은 앱 경계(`apps/web`) + 같은 priority(`high`)인 todo를 id 오름차순으로 최대 2개까지 추가 — 해당하는 것은 **M-034**(`scope: [apps/web]`) 하나뿐. `M-035`·`M-045`·`M-046`은 전부 `apps/mobile` 스코프라 앱 경계 불일치로 제외.
- 결과: **클러스터 = {M-030, M-034}**, 2건(상한 3건 이내). 이 조합은 `.claude/agents/planner.md`가 명시한 예시("씨앗 M-030 → 폴백으로 M-034")와 정확히 일치한다.
- 상한 초과로 미룬 이슈는 없다(폴백 후보가 애초에 M-034 하나뿐이었음).

## planner 재검증 (기준선: origin/dev 366bc15)
- 작업 시작 시 최신 `origin/dev`(366bc15, M-049 등재 커밋)로 갱신 후 백로그 재확인 — 클론 직후 읽은 백로그는 `main` 기준이었고, `dev`에는 M-049(2026-08-05 사람이 직접 등재, medium)가 추가로 있었다. M-049는 medium이라 high 씨앗 선정에는 영향 없음.
- M-030: `packages/tokens/src/index.ts:44`의 `primary`가 여전히 `#e25067`, `DESIGN.md`도 미수정 — 미해결 확인.
- M-034: `apps/web/src/lib/auth.test.ts`·`apps/web/src/app/api/geo/route.test.ts` 둘 다 미존재 — 미해결 확인.
- 둘 다 이미 충족된 상태가 아니어서 그대로 스펙 진행.

## 이슈별 결과

### M-030 — shipped (커밋 `55d7fff`)
- **문제**: `color.brand.primary(#e25067)`가 흰 텍스트 위 16px bold CTA 버튼 조합에서 대비 ≈3.77:1로 WCAG AA(4.5:1, large-text 3:1 예외 미적용) 미달.
- **범위 경계**: `packages/tokens/src/index.ts`(토큰 원본) + `apps/web/src/app/globals.css`(Tailwind `bg-primary`/`text-primary`가 실제로 참조하는 CSS 변수 — 파일 헤더 주석에 "@motungi/tokens와 1:1로 맞춘다"고 명시된 수동 동기화 대상이라 토큰만 고치면 렌더링에 반영 안 됨) + `apps/web/src/app/global-error.tsx`(동일한 bg+흰텍스트 패턴을 리터럴 hex로 중복 보유) + `DESIGN.md`(수치 갱신).
  건드리지 않은 것: `apps/web/src/app/page.tsx` 히어로 그라데이션(34px bold 대형 텍스트라 3:1 예외로 기존에도 통과), `apps/web/src/components/landing-photo.tsx`(장식용 그라데이션, 텍스트 없음), `apps/mobile/**`(클러스터 스코프 밖).
- **수정값**: `#e25067` → `#d42f4a`. WCAG relative-luminance 공식으로 흰색 대비 재계산 → 새 값 ≈4.88:1(AA 통과, 여유 있음). `primary-deep(#b0344e)`는 기존 ≈6.09:1로 이미 통과, 변경 없음.
- **되돌림 없음**.

### M-034 — shipped (커밋 `207f500`)
- **문제**: `apps/web/src/lib/auth.ts`의 `promoteLocalToAccount`(로그인 시 로컬 저장 데이터→계정 병합)와 `apps/web/src/app/api/geo/route.ts`(5개 에러 분기 서버 프록시)에 테스트 0건.
- **범위 경계**: 신규 파일 `apps/web/src/lib/auth.test.ts`, `apps/web/src/app/api/geo/route.test.ts`만 추가. 기존 소스 파일(`auth.ts`, `route.ts`)은 무변경.
- **커버리지**: auth.test.ts 4케이스(정상 병합·앵커 null 케이스·savedIds 빈 배열 스킵·`!supabase` 가드), route.test.ts 10케이스(400/503×2/502/404×2/500/200/admcode→legalcode 폴백).
- **되돌림 없음**.

## 아키텍처 검토
- 담당: 둘 다 **frontend-impl** 단독(`apps/web` + `packages/tokens`, backend-impl 불필요 — supabase/DB 미변경).
- FSD 경계 위반 없음: M-030은 토큰→CSS 변수 단방향 전파, M-034는 순수 테스트 추가로 계층 변경 없음.
- 데이터 흐름: M-030의 토큰 변경은 `packages/tokens`가 web·mobile 공용이라 이론상 mobile에도 색상이 전파되나, mobile은 각 화면에서 리터럴 hex를 별도로 쓰고 있어(`apps/mobile/app/index.tsx` 등) 실제로는 영향 없음 — 확인함.
- 테스트 전략: M-030은 unit(tokens) + 기존 axe a11y 회귀 테스트 재실행(구조적 검증; jsdom/axe-core는 실제 렌더 대비 계산을 못 해 색상 자체는 육안·공식 계산으로 별도 검증). M-034는 RTL 없이 순수 unit(vi.mock 기반 route/함수 테스트), `apps/web/src/app/api/neighborhoods/route.test.ts`·`trail-route/route.test.ts` 컨벤션을 따름.

## 변경 내용
| 커밋 | 이슈 | 파일 |
|---|---|---|
| `55d7fff` | M-030 | `packages/tokens/src/index.ts`, `apps/web/src/app/globals.css`, `apps/web/src/app/global-error.tsx`, `DESIGN.md` |
| `207f500` | M-034 | `apps/web/src/lib/auth.test.ts`(신규), `apps/web/src/app/api/geo/route.test.ts`(신규) |

## 검증 (오케스트레이터가 qa 단계로 직접 재실행 — 구현자 자체 보고를 신뢰하지 않고 독립 재확인)
- `pnpm typecheck --force`(캐시 우회, 4패키지 전체): `Tasks: 4 successful, 4 total` — `@motungi/core`·`@motungi/tokens`·`@motungi/web`·`@motungi/mobile` 전부 clean.
- `pnpm turbo run test --force`(캐시 우회, 전체): `@motungi/core:test Test Files 13 passed (13)` / `@motungi/mobile:test Test Files 11 passed (11)` / `@motungi/web:test Test Files 25 passed (25)`, `Tests 232 passed (232)`. `Tasks: 3 successful, 3 total`.
  - web 테스트 stderr에 axe-core의 `HTMLCanvasElement.getContext not implemented`(jsdom 환경 한계, `canvas` npm 패키지 미설치로 인한 기존 노이즈) 경고가 다수 출력되나 전부 사전부터 있던 환경 잡음이고 테스트 자체는 통과(실패 아님).
  - `src/lib/auth.test.ts`(4) / `src/app/api/geo/route.test.ts`(10) 신규 14케이스 전부 통과 확인.
- diff 오염 없음(각 커밋이 계획된 파일만 건드림), 시크릿·`.env`·파괴적 SQL 없음.
- push 게이트 판정: **두 이슈 모두 PASS** → revert 없이 그대로 dev에 push.

## 트리거-repo 문서 정합성 점검
이번 밤 시작 시 `CLAUDE.md`·`docs/nightly/PIPELINE.md`·`.claude/rules/workflow/nightly-pipeline.md`·`.claude/agents/planner.md`를 모두 읽고 이 트리거 프롬프트(클러스터 규칙 2026-08-05판)와 대조 — **모순 없음**. 네 문서 모두 이미 "하룻밤 1이슈" 옛 규칙을 폐기하고 클러스터(1~3개) 규칙으로 갱신돼 있었다. `planner.md`가 예시로 든 "M-030 씨앗 → 폴백으로 M-034"가 이번 밤 실제로 뽑은 클러스터와 정확히 일치.

## 리스크 & 후속
- M-030에서 의도적으로 스코프 밖에 둔 항목(다음 밤 후보): `apps/web/src/app/page.tsx` 히어로 그라데이션 위 15px 본문 텍스트(대형 텍스트 예외 미적용 구간이라 별도 점검 필요할 수 있음), `apps/mobile`의 동일 hex 리터럴들(토큰과 별도로 하드코딩돼 있어 이번 웹 전용 클러스터에서 손대지 않음 — M-048의 "웹→모바일 미반영 패턴"과 겹치는 사례이므로 참고).
- 남은 high todo: `M-035`(모바일 location.tsx 테스트 공백), `M-045`(모바일 보관함 저장 실종), `M-046`(모바일 보관함 에러 상태 부재) — 전부 apps/mobile. 다음 밤 씨앗 후보.
- backlog `status: doing`으로 갱신된 M-030·M-034는 사람이 dev→main 승격 후 `done`으로 전환 필요.
