# Nightly Report — 2026-08-04

## TL;DR

- 백로그 게이트: `docs/backlog/backlog.yml`의 `status:todo`가 15건(≥2) → **구현 모드**로 정상 진행.
- high 우선순위 todo가 M-027/M-030/M-034/M-035 4개 동률 → tie-break 원칙(동순위 시 id 오름차순)대로 **M-027**을 집었다. 어젯밤(2026-08-03) 리포트가 "다음 밤 후보 중 M-027이 유력"이라 예측한 그대로다.
- planner가 `dev`(cb48194) 기준 `packages/core/src/catalog.ts`를 직접 읽어 재검증: M-027 notes의 주장(단건 조회 `fetchOpportunityById`가 `data as OpportunityRow` 무검증 단언, 목록 조회 `fetchOpportunities`도 category/source 2개 필드만 가드하고 title/cost_krw/lat/lng 등 나머지 필드는 구조적 검증 없이 신뢰)이 dev 실코드에 100% 그대로 남아있음을 확인 — 아직 미해결.
- `packages/core/src/database.types.ts`에 `Database` 인터페이스 자체가 postgrest-js `GenericSchema` 제약(테이블별 `Relationships`, 스키마 `Views`/`Functions`)을 만족하도록 보강하고(이전엔 `apps/web`·`apps/mobile`의 `lib/supabase.ts`가 각자 로컬에서 이 패치를 중복 적용했음 — 이번에 core로 승격해 중복 제거), 21개 필드 전체를 구조적으로 검증하는 `isOpportunityRow(v): v is OpportunityRow` 런타임 가드를 신설했다. `catalog.ts`의 `fetchOpportunities`/`fetchOpportunityById` client 파라미터를 `SupabaseClient<Database>`로 바인딩하고, `data as OpportunityRow` 단언을 전부 제거해 `isOpportunityRow`로 대체했다.
- 검증: **typecheck 클린(4패키지)**, **core 283/mobile 39/web 194 테스트 전부 통과**(core +16 순증, 그중 신규 스키마 불일치 케이스 2건이 M-027 done_when #3 대상), lint 클린(web만 lint 스크립트 보유, 기존 상태 그대로). 독립 qa 에이전트가 diff·file:line을 직접 재정독하고 명령을 전부 재실행해 done_when 4개 항목 전부 ✅로 판정. **PUSH GATE: PASS.**
- 이번 구현은 `dev` 위에 직접 쌓인 트렁크 커밋이다. 이 리포트 자체도 같은 방식으로 `dev`에 직접 커밋되며, PR은 열지 않는다.
- 문서 모순 기록: 이번 밤 발견된 트리거-repo 문서 간 모순 없음 — `CLAUDE.md`, `.claude/agents/*.md`, `docs/nightly/PIPELINE.md`, `.claude/rules/workflow/nightly-pipeline.md` 모두 dev 트렁크 모델·qa 푸시 게이트·백로그 게이트(todo<2 시 audit)가 트리거 프롬프트와 일치했다(M-024에서 정정된 상태 유지 확인). 참고로 클론 직후 `origin/dev`가 얕은 단일 브랜치 클론(`--depth 1`, 기본 브랜치만)에는 안 잡혀 있어 `git fetch origin dev:refs/remotes/origin/dev --depth 1`로 별도 fetch가 필요했다 — repo 자체 모순은 아니고 클론 절차상 흔한 함정이라 기록만 남긴다.

## 집은 이슈

- **M-027** — `packages/core` catalog.ts가 Supabase 응답을 Database 제네릭 없이 unchecked cast로 신뢰 (priority: high, scope: `packages/core`, status: todo → **doing**)
- planner가 `catalog.ts`를 처음부터 끝까지 읽어 done_when 4개 항목이 전부 실제 미충족임을 재확인:
  - `fetchOpportunities`/`fetchOpportunityById` 둘 다 `client: SupabaseClient | null`(스키마 제네릭 없는 바닐라 타입)이었음.
  - 목록 조회의 row 좁히기는 `isOpportunityCategory(row.category) && isSourceKind(row.source)` 2개 필드만 확인 — title/summary/cost_krw/lat/lng 등 나머지 필드는 TS 구조적 타입(컴파일 타임)만 믿을 뿐 런타임 검증이 없었음.
  - 단건 조회는 아예 `rowToMock(data as OpportunityRow)` — category/source 가드를 통과한 뒤에도 명시적 캐스트로 나머지 필드를 전부 무검증 신뢰.
  - `apps/web/src/lib/supabase.ts`가 이미 "core의 손으로 작성된 `Database`가 postgrest-js GenericSchema 제약을 만족 못 해 `SupabaseClient<Database>`가 `never`로 무너진다"는 이유로 로컬 패치(Relationships/Views/Functions 보강)를 두고 있었고, `apps/mobile/lib/supabase.ts`가 **완전히 동일한 패치를 다시 복붙**해 두고 있었다 — 이걸 core로 승격하는 것이 M-027 done_when #1의 "동등한 것을 core에 둔다" 요구와 정확히 맞아떨어짐.

## 아키텍처 검토

- architect 판단: **backend/core-impl 단독**으로 충분(UI 컴포넌트 변경 없음). scope는 planner 원안(`packages/core`)에서 아래 이유로만 최소 확장 승인:
  - `apps/web/src/lib/supabase.ts`·`apps/mobile/lib/supabase.ts` — core의 `Database`가 이미 GenericSchema를 만족하도록 바뀌면 두 파일의 로컬 패치는 순수 중복이 되어 즉시 dead code화된다. 남겨두면 "core Database가 이미 패치됐는데 앱이 또 패치해 덮어씀" 상태로 오히려 혼란을 유발하므로 삭제가 필요 — 이 변경이 없으면 M-027 done_when #1("동등한 것을 core에 둔다")의 취지(중복 제거)가 무색해진다.
  - `packages/core/src/catalog.test.ts` — done_when #3이 명시적으로 요구.
- FSD/경계 위반 없음 — `apps/web`·`apps/mobile`은 여전히 core가 export하는 타입만 import한다(`Database`를 재가공하지 않고 그대로 씀). import 방향 변화 없음, 단지 중복 로직이 사라졌을 뿐.
- 위험 신호 사전 점검: `apps/web/src/store/useAppStore.ts`·`apps/mobile/store/useAppStore.ts`에 "`SupabaseClient<Database>` 제네릭을 그대로 넘기면 TS2589(재귀 타입 폭발)로 터진다"는 기존 주석이 있어, `catalog.ts`의 client 파라미터를 제네릭으로 바인딩하는 이번 변경이 같은 함정을 밟을 위험을 planner가 먼저 검토했다. 확인 결과 그 TS2589는 `createAppStore<T, S>`처럼 **제네릭 타입 파라미터를 한 겹 더 거쳐 전달**할 때만 발생하는 문제였고, `catalog.ts`처럼 함수 파라미터에 **직접·구체적으로** `SupabaseClient<Database>`를 바인딩하는 건 이미 `apps/web/src/lib/supabase.ts`의 `createClient<Database>(...)` 자체가 매일 밤 통과해온 동일 패턴이라 안전 — 실제로 typecheck가 이를 증명했다(아래 검증 참조).
- 파일 계획 확정(5개 수정, 신규 없음):
  1. `packages/core/src/database.types.ts`(수정) — `Database`에 `Relationships`/`Views`/`Functions` 보강, `isOpportunityRow` 신설.
  2. `packages/core/src/catalog.ts`(수정) — client 파라미터 타입 교체, `isOpportunityCategory`/`isSourceKind` 개별 호출 → `isOpportunityRow`로 교체, `as OpportunityRow` 단언 제거.
  3. `packages/core/src/catalog.test.ts`(수정) — 스키마 필드 타입 불일치 케이스 추가(목록/단건 각 1건 이상).
  4. `apps/web/src/lib/supabase.ts`(수정) — 로컬 `Database` 패치 삭제, core의 `Database` 직접 import.
  5. `apps/mobile/lib/supabase.ts`(수정) — 동일.
- 테스트 전략: unit(vitest, `packages/core`)만으로 충분 — 순수 함수·타입 레벨 변경이라 통합/E2E 불필요.
- 범위 밖 명시: `apps/web`·`apps/mobile`의 UI/화면 코드(무관), `supabase/migrations/`(스키마 변경 없음), `packages/core/src/store.ts`의 `SavedOpportunitiesClient`(별도의 최소 구조 인터페이스로 이번 이슈와 무관 — qa가 별도로 확인).

## 변경 내용

5개 파일 변경(전부 수정, 신규 파일 없음), 커밋 전 워킹트리 기준:

- `packages/core/src/database.types.ts` — `Database`의 4개 테이블(`opportunities`/`profiles`/`saved_opportunities`/`neighborhoods`) 각각에 `Relationships: []`를 추가하고 `public`에 `Views`/`Functions`(둘 다 `{ [_ in never]: never }`)를 추가해 postgrest-js `GenericSchema` 제약을 core 자체에서 만족시켰다. `isOpportunityRow(v: unknown): v is OpportunityRow`를 신설 — `id`/`title`/`summary`는 string, `category`/`source`는 기존 enum 가드, 나머지 15개 필드(`external_id`/`cost_krw`/`difficulty`/`dong_name`/`lat`/`lng`/`cta_url`/`image_url`/`deadline`/`source_label`/`time_start_hour`/`time_end_hour`/`course_start`/`course_end`/`course_notes`/`duration_min`/`is_loop`)는 각각의 nullable 타입(`string|null`/`number|null`/`boolean|null`)을 검증한다. `description`은 목록 조회(`CATALOG_COLUMNS`)엔 컬럼 자체가 없어 `undefined`로 오는 경우와, 단건 조회(`DETAIL_COLUMNS`)엔 `string|null`로 오는 경우를 둘 다 정상 처리하도록 `r.description === undefined || isNullableString(r.description)`로 분기했다(view.ts의 `OpportunityRow.description?: string | null` 옵셔널 정의와 일치).
- `packages/core/src/catalog.ts` — `fetchOpportunities`/`fetchOpportunityById`의 `client` 파라미터를 `SupabaseClient | null` → `SupabaseClient<Database> | null`로 교체. 목록 조회의 `data.filter((row): row is OpportunityRow => isOpportunityCategory(row.category) && isSourceKind(row.source))`를 `data.filter(isOpportunityRow)`로 교체(2필드 가드 → 21필드 가드). 단건 조회의 `if (!isOpportunityCategory(data.category) || !isSourceKind(data.source)) return {data:null,status:"empty"}` + `rowToMock(data as OpportunityRow)`를 `if (!isOpportunityRow(data)) return {data:null,status:"empty"}` + `rowToMock(data)`(캐스트 없음)로 교체.
- `packages/core/src/catalog.test.ts` — "category/source는 유효하지만 다른 필드 타입이 스키마와 어긋나는 row는 제외한다(M-027)" 테스트 추가(`fetchOpportunities`, `title`이 숫자로 온 row + `cost_krw`가 문자열로 온 row가 둘 다 걸러지고 정상 row만 남는지 확인). "category/source는 유효하지만 다른 필드가 스키마와 어긋나면 empty로(M-027, as 단언 제거 검증)" 테스트 추가(`fetchOpportunityById`, `lat`이 문자열로 온 row가 empty로 처리되는지 확인).
- `apps/web/src/lib/supabase.ts` — `CoreDatabase`를 `Omit`+매핑 타입으로 재가공하던 12줄짜리 로컬 패치를 삭제하고 `import type { Database } from "@motungi/core"`로 직접 사용.
- `apps/mobile/lib/supabase.ts` — 동일(웹과 완전히 같은 코드가 복붙돼 있었음, 동일하게 삭제).

범위 밖 파일(`apps/web`·`apps/mobile`의 UI/화면, `supabase/migrations/`, `packages/core/src/store.ts`)은 무변경임을 `git diff --stat`으로 확인.

## 검증

qa(독립 에이전트)가 실제로 돌린 명령과 그 출력(캐시 신뢰 없이 `npx vitest run`으로 각 패키지 직접 재실행까지 수행):

```
$ git status --short
 M apps/mobile/lib/supabase.ts
 M apps/web/src/lib/supabase.ts
 M packages/core/src/catalog.test.ts
 M packages/core/src/catalog.ts
 M packages/core/src/database.types.ts
```

```
$ pnpm typecheck   (4패키지)
@motungi/tokens:typecheck  ✓
@motungi/core:typecheck    ✓
@motungi/mobile:typecheck  ✓
@motungi/web:typecheck     ✓
Tasks: 4 successful, 4 total
```

```
$ pnpm test
@motungi/core:   283/283 passed (13 files, catalog.test.ts 45건 — 신규 스키마 불일치 케이스 2건 포함)
@motungi/mobile:  39/39 passed (10 files)
@motungi/web:    194/194 passed (22 files)
Tasks: 3 successful, 3 total
```

```
$ pnpm lint
apps/web: "No ESLint warnings or errors" (next lint)
packages/core / apps/mobile / packages/tokens: lint 스크립트 미정의(기존 상태 그대로, 이번 변경과 무관 — qa가 일반적 repo 공백으로만 기록)
```

- qa가 `catalog.ts`·`database.types.ts` 전문을 읽고 `as OpportunityRow` 문자열을 전체 repo grep해, 남은 매치 2건이 전부 "제거된 옛 동작을 설명하는 주석"일 뿐 실제 캐스트가 아님을 확인.
- qa가 `isOpportunityRow`를 `view.ts`의 `OpportunityRow` 인터페이스와 필드 단위로 대조해 전 필드 커버·nullable 일치·`description` optional 분기(목록 vs 단건)까지 정확함을 확인 — 목록 경로에서 `description`이 `undefined`로 오는데 가드가 이를 정상 처리하지 못했다면 모든 목록 row가 오탐으로 걸러지는 회귀가 났을 지점이라 특히 중점 검토됨.
- 시크릿 grep 없음(이번 변경은 `.env`류 무관), 마이그레이션 무변경, 파괴적 SQL 없음(SQL 자체가 diff에 없음).
- M-027 done_when 4개 항목 전부, 구현자 주장이 아니라 qa의 실제 diff 정독 + 명령 재실행 출력 대조로 ✅ 확인됨.
- qa 판정: **"PASS — safe to push to dev."**

## 리스크 & 후속 (qa 부가 의견 포함)

1. **`isOpportunityRow` 자체의 직접 단위 테스트 부재**: `packages/core/src/database.types.test.ts`는 여전히 `isOpportunityCategory`/`isSourceKind`만 테스트하고, `isOpportunityRow`는 `catalog.test.ts`를 통해 간접적으로만 검증된다. done_when #3("catalog.test.ts에 최소 1건")은 문언 그대로 충족했지만, 가드 자체를 독립적으로 흔드는 테스트(필드 누락·`is_loop` 타입 오류·예상 밖 추가 키 등)가 있으면 `fetchOpportunities`/`fetchOpportunityById`의 동작 변화와 무관하게 회귀를 더 촘촘히 잡을 수 있다. 후속 후보로 남긴다.
2. **`packages/core/src/store.ts`의 `SavedOpportunitiesClient`**: `toggleSaved`가 쓰는 별도의 최소 구조 인터페이스로, 이번 M-027 범위 밖이다(스키마 제네릭이 아니라 의도적으로 좁힌 쓰기 전용 인터페이스). 회귀는 아니지만, 향후 "core 전체 타입 안전 감사"를 한다면 시야에 넣을 후보로만 기록.
3. **repo 전반 lint 공백**: `packages/core`/`apps/mobile`/`packages/tokens`에 lint 스크립트가 아예 없어 `pnpm lint`가 사실상 `apps/web`만 검사한다. 이번 변경으로 생긴 문제는 아니고 기존부터 그랬다 — 백로그 감사 후보로만 기록(이미 M-030 등 다른 이슈가 apps/web 위주라 별도 이슈화는 사람 판단에 맡김).
4. status는 `todo → doing`으로만 갱신했다 — `done` 승격은 사람의 최종 리뷰 몫이다.
5. **문서 모순 기록**: 위 TL;DR 마지막 항목 참조 — 이번 밤은 트리거-repo 문서 간 실질적 모순 없음. 얕은 클론에서 `origin/dev`가 안 잡히는 절차상 함정만 별도로 기록했다(다음 밤도 같은 클론 방식을 쓴다면 재발할 수 있어 남긴다).

## dev 직접 커밋 (PR 없음)

이번 밤도 트렁크 기반 모델을 따랐다: 브랜치를 파지 않고 `origin/dev` 위에 직접 커밋·push하며 PR은 열지 않는다. 구현 변경과 이 리포트·backlog 커밋 모두 `dev`에 직접 쌓이고, qa PASS 판정에 따라 `git push origin dev`로 push한다. `dev → main` 승격은 사람이 주기적으로 직접 판단하며, 이는 야간 파이프라인의 역할이 아니다 — main은 이번 밤에도 건드리지 않았다.
