# Nightly Report — 2026-08-23

## TL;DR
**BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `docs/backlog/backlog.yml`의 `status: todo` 개수는 **9건**(≥2) — 정상 구현 모드로 진행했다.

클러스터 = **M-068 + M-069**(둘 다 `apps/web/src/lib/`) — **둘 다 shipped**. qa 게이트(`bash scripts/gate.sh`: typecheck·test·lint·시크릿 스캔 4종) 전부 ✅, dev로 push 완료. 되돌린 이슈 없음.

M-052(모바일 지도/산책로)는 **11일 연속(08-13~08-23)** 동일 사유로 씨앗 단계에서 사전 제외 — 사람의 아키텍처 결정 대기 중, 아래 "리스크 & 후속" 참조. 연속 report-only 없음(최근 밤 전부 shipped) — 헛도는 밤 경고 해당 없음.

부수 발견: **backlog.yml YAML 손상**을 이번 밤 씨앗 재검증 중 발견해 별도 커밋으로 수정했다(아래 "리스크 & 후속" 참조).

## 집은 클러스터

**씨앗 선정**: `status: todo` 9건 중 priority 최상위는 medium(high 없음), 동순위 중 id 최솟값은 **M-052**였으나 11일 연속 사전제외 사유(아래 참조)로 재검증 후 배제 — 다음 순위인 **M-068**(medium, id 68)을 씨앗으로 채택.

**묶은 근거**: M-068(`apps/web/src/lib/kakao.ts`)과 M-069(`apps/web/src/lib/geo.ts`)는 같은 좁은 디렉토리 `apps/web/src/lib/`를 공유한다 — `apps/web` 전체가 아니라 실제 서브디렉토리 겹침이라 CLUSTER RULE의 "narrow directory" 겹침 기준을 충족한다. 둘 다 2026-08-18 audit이 등재한 test-coverage 성격 findings로, 소스 변경 없이 테스트 파일만 추가하면 되는 동형 작업이라 한 밤에 묶기 적합했다.

**⚠️ 트리거 프롬프트 vs repo 문서 불일치 기록 (PRECEDENCE 절 요구사항)**: 트리거 프롬프트의 CLUSTER RULE 3단계(폴백)는 "씨앗의 scope에 파일도 좁은 디렉토리도 전혀 없을 때만" 앱 단위 폴백을 적용하라고 명시한다. 그러나 `.claude/rules/workflow/nightly-pipeline.md:56-63`(및 `docs/nightly/PIPELINE.md:56-63` 동일 문구)은 "씨앗 scope에 파일이 섞여 있든 없든 무관하다 — 기준은 걸린 게 없다는 사실이다"라고 이 조건을 더 넓게 서술해 트리거와 모순된다. M-052의 scope(`apps/mobile/app/opportunity.tsx`)는 이미 파일 단위라서, 트리거 문구대로면 폴백 미적용(파일 겹침 후보가 0개면 씨앗 단독), repo 문서 문구대로면 폴백 적용(같은 앱+같은 priority 최대 2개 추가) — 실제로는 이 문제 이전에 M-052 자체가 씨앗 단계에서 사전 배제됐으므로(11일 연속 사유) 이번 밤은 이 모순이 최종 결과에 영향을 주지 않았다(M-068의 scope도 파일 단위라 두 해석 모두 동일하게 "겹침 0 → 폴백 미적용, 파일 겹침(M-069)이 실제로 있어 폴백 자체가 불필요"로 수렴). PRECEDENCE 절에 따라 트리거 문구를 우선했음을 기록하며, 사람이 repo 문서 두 곳(`nightly-pipeline.md:56-63`, `PIPELINE.md:56-63`)을 트리거 문구에 맞게 수정할 것을 권고한다.

상한 3개 중 2개만 채워짐(추가로 겹치는 todo 이슈 없음 — M-066/M-070/M-071/M-072는 각각 다른 파일/디렉토리).

## 이슈별 결과

### M-068 — 웹 카카오 공유(kakao.ts shareContent) 3단 폴백 캐스케이드 테스트 0건
**shipped** (커밋 `e230e88`)
- `apps/web/src/lib/kakao.test.ts` 신설, 7 tests.
- `shareContent()`의 4개 반환 분기 전부 커버: kakao(SDK, imageUrl 명시 + `${origin}/opengraph-image.png` 폴백 두 경우) / web-share(성공 + 취소→clipboard 폴백) / clipboard(성공 + 실패→none 폴백) / none.
- `KAKAO_JS_KEY`가 모듈 로드 시점에 `process.env.NEXT_PUBLIC_KAKAO_JS_KEY`를 캡처하는 구조라 정적 import로는 kakao 분기가 도달 불가 — `vi.stubEnv` + `vi.resetModules` + 매 테스트 동적 `await import("./kakao")`로 우회.
- 소스(`kakao.ts`) 변경 없음.

### M-069 — 웹 geo.ts(reverseGeocode/searchNeighborhoods) 직접 테스트 0건
**shipped** (커밋 `731d9bf`)
- `apps/web/src/lib/geo.test.ts` 신설(모바일 `apps/mobile/lib/geo.test.ts`와 대칭), 10 tests.
- `reverseGeocode`·`searchNeighborhoods` 둘 다 실제 호출(fetch만 `vi.stubGlobal`로 대체, 함수 자체는 mock 안 함).
- done_when 3개 폴백 전부 커버: `!res.ok`(두 함수 모두), `data.items ?? []`, AbortError 억제(`reportError` 미호출)를 일반 에러(`reportError` 호출)와 대비 검증.
- 소스(`geo.ts`) 변경 없음.

두 이슈 모두 되돌림 없음.

## 아키텍처 검토
- **담당**: frontend-impl (두 이슈 모두 `apps/web` lib 레벨 순수 유닛 테스트, 컴포넌트/렌더 관여 없음).
- **경계**: FSD 계층 해당 없음 — `constitution.md`가 명시하듯 이 리포는 FSD 디렉토리가 실재하지 않고 `apps/web`은 평면 구조(`lib/`)다. `architect.md` 체크리스트의 "FSD 경계" 문구는 이 리포에는 적용 불가한 옛 잔재 표현이나, 판정 자체에는 영향 없음(위반할 계층이 없음).
- **테스트 전략**: unit 레벨(vitest). `apps/web/vitest.config.ts`가 커스텀 include 없이 기본 glob(`**/*.test.ts`)을 쓰므로 두 신규 파일이 자동으로 수집됨(기존 `api-error.test.ts` 등과 동일 패턴으로 검증).
- architect가 planner 스펙을 되돌려보낸 사례 없음 — 클러스터 그대로 승인.

## 변경 내용
```
docs/backlog/backlog.yml       |   변경 (M-053 YAML 분리 + M-068/M-069 status doing + notes)
apps/web/src/lib/kakao.test.ts | 150 +++++ (신규, 7 tests)
apps/web/src/lib/geo.test.ts   | 138 +++++ (신규, 10 tests)
```
`kakao.ts`·`geo.ts` 소스 파일은 무변경 — 두 이슈 모두 기존 구현이 이미 done_when의 동작 요건을 충족하고 있었고, 부족한 건 테스트 커버리지뿐이었다(architect가 직접 소스를 읽고 확인).

## 검증
qa가 `bash scripts/gate.sh` 직접 실행, 실제 출력:
```
── typecheck    ✅  (4/4 패키지)
── test         ✅  (core 298 · mobile 131 · web 40 files/307 tests, 전부 통과)
── lint         ✅  (No ESLint warnings or errors)
── secrets      ✅  (시크릿 패턴 없음)

GATE: PASS — push 허용
```
`src/lib/kakao.test.ts (7 tests)` · `src/lib/geo.test.ts (10 tests)` 개별 확인, done_when 항목별로 실제 어서션이 해당 분기(swallow catch, `??` 폴백, AbortError 억제 등)를 진짜로 실행하는지 qa가 직접 읽고 재확인(타우톨로지 없음). `git diff --stat origin/dev..HEAD`로 diff 오염 없음 확인(정확히 3개 파일: backlog.yml + 신규 테스트 2개).

## 리스크 & 후속

**M-052 — 11일 연속 사전제외, 사람 결정 필요.** "모바일 상세 화면에 위치 지도·산책로 폴리라인 없음" — 08-13부터 오늘(08-23)까지 매일 밤 씨앗 단계에서 재검증 후 배제되고 있다. 이번 밤도 `grep -i map apps/mobile/package.json` 공백, `apps/mobile` 코드 전체에 MapView/react-native-maps 참조 0건, `.env.example`·`security-policy.md`에 지도 관련 항목 없음을 재확인했다. 08-13이 제시한 3개 구현 경로(react-native-maps 신규 네이티브 의존성 / NAVER 정적지도 백엔드 프록시 신설 / 키리스 서드파티 정적지도)는 전부 사람의 정책 승인 없이는 무인 야간 파이프라인이 진행할 수 없다. 08-21/08-22가 이미 제안한 3택1이 아직 처리되지 않았다:
1. 세 경로 중 하나를 사람이 승인(의존성 추가 / 프록시 신설 / 서드파티 신뢰),
2. M-052의 priority를 medium에서 낮춰 씨앗 경쟁에서 자연히 밀려나게 하거나,
3. `blocked-human` 같은 정식 상태를 backlog 스키마에 도입해 씨앗 후보에서 구조적으로 제외.
이 중 하나를 처리하지 않으면 앞으로도 매일 밤 같은 사전제외가 반복된다.

**backlog.yml YAML 손상 발견·수정.** M-052 항목의 notes 블록 끝에 `- id: M-053` 구분선이 누락돼 있어, M-052(위치 지도, todo)의 YAML 매핑에 M-053(마감 D-day, doing)의 title/priority/status/scope/done_when/notes가 중복 키로 이어져 있었다. 표준 YAML 파서(last-key-wins 시맨틱, `python yaml.safe_load`로 재현 확인)로 읽으면 M-052 항목 자체가 M-053 필드로 완전히 덮여써 사라지는 구조였다 — 사람이 눈으로 읽을 땐 순서상 "M-052(첫 title/status) → notes 여러 밤치 → M-053(둘째 title/status)"로 자연스럽게 읽히지만, 프로그램적으로 파싱하면 정반대 결과가 난다. 커밋 `1101a22`로 `- id: M-053` 한 줄만 삽입해 분리(필드 값 변경 없음). 원인은 특정할 수 없으나 과거 어느 밤이 M-052 notes에 항목을 추가하며 실수로 이 줄을 지웠을 가능성이 높다 — **향후 밤은 notes 블록을 편집할 때 YAML 파싱 결과를 재확인하는 습관을 권장**한다(이번처럼 `python3 -c "import yaml; yaml.safe_load(...)"`로 dup id 체크 한 줄이면 충분).

**CLUSTER RULE 폴백 조건, 트리거 vs repo 문서 불일치.** 위 "집은 클러스터" 절에서 기록한 대로, 트리거 프롬프트와 `.claude/rules/workflow/nightly-pipeline.md:56-63`(+`docs/nightly/PIPELINE.md:56-63`)의 폴백 발동 조건이 서로 다르다. 오늘 밤은 결과에 영향 없었지만, 씨앗이 정말로 앱 단위 coarse scope인 경우(예: `[apps/mobile]`만 있는 이슈가 씨앗일 때)라면 두 해석이 실제로 다른 클러스터를 만들어낼 수 있다. 사람이 repo 문서 두 곳을 트리거 문구("파일/좁은 디렉토리가 전혀 없을 때만 폴백")에 맞춰 정정할 것을 권고한다.

**다음 밤 후보**: 남은 todo 7건 — M-052(블록 지속), M-060(low, 아이콘 데드코드), M-066(low), M-067(low), M-070(low), M-071(low), M-072(medium, 카탈로그 반경 사다리 중복). M-052를 배제하면 다음 밤 씨앗은 M-072(medium, id 72) 또는 low 그룹이 된다.
