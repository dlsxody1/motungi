# Nightly Report — 2026-09-01

## TL;DR

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `docs/backlog/backlog.yml`의 `status: todo`는 **17건**이었다 — `todo ≥ 2`이므로 구현 모드로 진행했다.
- **연속 report-only 경보 해당 없음.** 직전 5개 밤(08-27~08-31)을 `ls docs/nightly/` + 각 TL;DR로 직접 열어 확인 — 08-29(audit, todo<2로 정상 report-only) 1건을 빼면 전부 shipped 구현 밤이었다. 연속 report-only는 없다.
- **브랜치 동기화 점검(nightly-pipeline.md 6번) 수행함**: `git log --oneline origin/dev..origin/main`이 비어 있었다(`origin/main`의 tip `744411d`가 이미 `origin/dev`의 조상) — 되받을 게 없어 no-op으로 진행.
- **클러스터 = M-082(씨앗) + M-074 + M-077, 3건 전부 shipped.** 되돌린 이슈 없음.
- `bash scripts/gate.sh` **PASS** — typecheck 4/4(core·mobile·tokens·web) · test(core 318/318 · web 343/343 · mobile 147/147) · lint clean · 시크릿 스캔 clean. 최종 트리에서 재확인함.
- **doc 모순 2건, 여전히 미해결**(08-30·08-31 리포트에서 이미 지적) — 이번 밤도 재확인만 하고 직접 고치지 않았다. 아래 §리스크 참조.

## 집은 클러스터

- **씨앗**: M-082 (priority `high`, high 등급 todo 2건 M-082/M-085 중 최소 id).
- **M-074 추가** — 이유: **파일 완전 겹침**. M-082의 scope(`apps/web/src/components/opportunity-detail.tsx`, `course-guide.tsx`)와 M-074의 scope(`apps/web/src/hooks/useOpportunity.ts`, `apps/web/src/components/opportunity-detail.tsx`)가 `opportunity-detail.tsx`에서 겹친다.
- **M-077 추가** — 이유: **파일 완전 겹침**. M-077의 scope(`supabase/functions/ingest/adapters.ts`, `apps/web/src/components/opportunity-detail.tsx`, `apps/mobile/app/opportunity.tsx`)도 같은 `opportunity-detail.tsx`에서 겹친다.
- **상한 3개에 정확히 도달** — 겹침 후보가 정확히 M-074·M-077 2건뿐이었고(둘 다 medium), 폴백(같은 앱+같은 priority)도 필요 없었다.
- **미룬 이슈**: 남은 high priority todo **M-085**(모바일 탐색 '동네 변경' UX)는 이번 클러스터의 씨앗 파일(`apps/web/src/components/opportunity-detail.tsx`·`course-guide.tsx`)과 겹치지 않아(스코프가 `apps/mobile/app/(tabs)/explore.tsx`·`apps/mobile/app/location.tsx`) 포함하지 않았다 — 다음 밤 유력 씨앗 후보.

## 이슈별 결과

### M-082 — shipped (`1bd134b`)
활동 상세 핵심 표시 로직(DdayPill/hasLink/isWeekendOuting)·CourseGuide 테스트 0건.
- **기준선 재확인**: `origin/dev` 실코드로 `done_when` 미충족 확인 — 유일한 기존 테스트(`opportunity-detail.render-isolation.test.tsx`)는 리렌더 횟수만 검증하고 deadline 없는 fixture를 쓴다(직접 읽어 확인). `course-guide.test.tsx`는 `ls`로 부재 확인.
- **구현**: 프로덕션 코드 변경 없음(순수 테스트 커버리지 이슈). `opportunity-detail.test.tsx` 신설(DdayPill 3톤·hasLink 3분기·isWeekendOuting 2분기, 8+1건). `course-guide.test.tsx` 신설(courseStart 있음/courseNotes만/둘 다 없음, 4건).
- **done_when 4항목 전부 충족**: DdayPill 3톤·hasLink=false·isWeekendOuting·CourseGuide 3분기 테스트 모두 추가, `pnpm --filter web test` 통과.
- 되돌림 없음.

### M-074 — shipped (`44de936`)
활동 상세 페이지가 SSR로 이미 받은 데이터를 클라이언트가 즉시 재조회.
- **기준선 재확인**: `useOpportunity.ts:24-30`을 직접 읽어 `enabled: id != null`로 `initial` 유무와 무관하게 항상 `useQuery`가 실행됨을 재확인 — audit 시점(08-29)과 동일하게 미해결이었다.
- **구현**: `useOpportunity(id, initial)`로 시그니처 확장, `initial`이 있으면 `initialData`로 react-query 캐시를 선시딩(`useSavedOpportunities.ts`의 `setQueryData` 선시딩과 같은 목적). `OpportunityDetail`이 `useOpportunity(id, initial)`로 전달.
- **회귀 테스트의 함정**: 전역 테스트 wrapper(`vitest.setup.ts`)가 `staleTime:0`을 강제해, `initialData`를 넣어도 마운트 즉시 백그라운드 refetch가 붙어 최초 작성한 테스트가 실패했다(직접 재현: `mockedFetch`가 1회 호출됨). 프로덕션 `QueryProvider`(`lib/query.tsx`)와 같은 `staleTime`(5분)을 가진 전용 `QueryClient`를 그 테스트에만 `wrapper`로 넘겨 해결 — 프로덕션 동작을 실제로 검증하되 전역 테스트 인프라의 의도된 설정(다른 테스트가 조회 누락을 놓치지 않게 하는 장치)은 건드리지 않았다.
- **done_when 4항목 전부 충족**: 재조회 안 함(신규 회귀 테스트)·순수 클라이언트 내비게이션은 기존대로 조회(신규 테스트)·회귀 테스트 추가·`pnpm --filter web typecheck test` 통과.
- 되돌림 없음.

### M-077 — shipped (`17007fc`)
외부 피드의 `cta_url`을 프로토콜 검증 없이 링크/딥링크로 그대로 열어줌.
- **기준선 재확인**: `adapters.ts`의 `mapSeoulCulture`(`raw.ORG_LINK || null`)·`mapSportsFacility`(`raw.HMPG_URL?.trim() || null`), 웹 `opportunity-detail.tsx:121`·모바일 `opportunity.tsx:72`의 `hasLink` 판정을 직접 읽어 프로토콜 검사가 없음을 재확인.
- **구현**:
  - `packages/core/src/adapters/util.ts`에 `parseHttpUrl(raw)` 추가 — http(s) 스킴만 통과, 그 외(javascript:/data:/intent:/깨진 URL/빈값)는 `undefined`. 적재(`adapters.ts`)와 두 앱의 `hasLink` 판정이 **공유하는 단일 출처**로 설계(중복 로직 방지).
  - `adapters.ts`: `mapSeoulCulture`·`mapSportsFacility`의 `cta_url`을 `parseHttpUrl(...) ?? null`로 게이팅.
  - 웹 `opportunity-detail.tsx`·모바일 `opportunity.tsx`: `hasLink` 판정에 `!!parseHttpUrl(o.ctaUrl)`을 추가(방어 심층화 — M-059의 `gpx_url` 이중검증과 같은 판단. 적재 경로가 바뀌어도 화면이 무방비가 되지 않게).
- **테스트**: `packages/core`에 `parseHttpUrl` 단위 테스트(vitest, 4건, http(s) 통과·공백 trim·악성 스킴 3종 거부·깨진 URL 거부) — **실행 검증됨**. 웹·모바일 각각 `hasLink`가 `javascript:` 스킴을 거부하는 회귀 테스트 1건씩 추가 — 실행 검증됨. `supabase/functions/ingest/adapters.test.ts`(Deno 테스트)에도 `ORG_LINK`/`HMPG_URL` 스킴 거부 테스트를 추가했다.
- **정직한 기록**: `adapters.test.ts`는 Deno 테스트라 이 샌드박스에 `deno` 바이너리가 없어 **실행 검증을 못 했다**(2026-08-21 밤이 같은 문제를 이미 기록함 — `scripts/gate.sh`에도 애초에 편입돼 있지 않다, 파일 자체 주석에 "CI에 Deno 러너가 아직 없다"). 로직 자체는 `parseHttpUrl`을 `packages/core`에서 import해 쓰므로(같은 함수) vitest 커버리지로 실질적으로 실행 검증됐다고 본다 — 다만 Deno import 경로(`../../../packages/core/...`)가 실제로 해석되는지는 코드 읽기로만 확인했다.
- **done_when 4항목 중 3항목 명시적 충족**(어댑터 파싱 검증 후 대입·web/mobile 프로토콜 재확인·`core/supabase 관련 typecheck·test 통과` — core는 통과, supabase Deno 쪽은 위 사유로 미실행), **adapters.test.ts 테스트 추가는 작성은 했으나 미실행**으로 부분 충족.
- 되돌림 없음.

## 아키텍처 검토
- 담당: **frontend-impl**(M-082·M-074, `apps/web` 단독) + **frontend-impl·backend-impl 공동**(M-077, `packages/core`+`supabase/functions/ingest`+`apps/web`+`apps/mobile`).
- FSD 계층 위반 없음 — 애초에 이 리포에 FSD 디렉토리는 실재하지 않는다(`constitution.md`·`apps/web/CLAUDE.md`·`.claude/rules/languages/react-web.md` 전부 명시). 아래 §리스크의 `architect.md` 모순 참조.
- 데이터 흐름: M-077의 `parseHttpUrl`을 `packages/core`(단일 출처)에 두어 적재(`adapters.ts`, Deno 상대경로 import)와 두 앱의 화면 로직(`@motungi/core` 패키지 import)이 같은 검증 함수를 공유하게 했다 — 로직 두 벌로 갈라지는 걸 피했다.
- 테스트 전략: M-082는 순수 component/hook 레벨(RTL+Vitest), M-074는 hook 레벨(RTL renderHook, react-query 캐시 동작 직접 검증), M-077은 unit(core `parseHttpUrl`)+component(web/mobile hasLink 회귀)+Deno 통합(작성만, 미실행) 3중.

## 변경 내용
- `apps/web/src/components/opportunity-detail.test.tsx`(신설) — M-082, DdayPill/hasLink/isWeekendOuting 9건.
- `apps/web/src/components/course-guide.test.tsx`(신설) — M-082, CourseGuide 3분기 4건.
- `apps/web/src/hooks/useOpportunity.ts` — M-074, `initial` → `initialData` 선시딩.
- `apps/web/src/hooks/useOpportunity.test.ts` — M-074, 재조회 안 함/순수 내비게이션 조회 회귀 테스트 2건 추가.
- `apps/web/src/components/opportunity-detail.tsx` — M-074(`useOpportunity(id, initial)` 전달) + M-077(`hasLink` 프로토콜 재확인).
- `packages/core/src/adapters/util.ts`·`util.test.ts` — M-077, `parseHttpUrl` 신설 + 단위 테스트 4건.
- `supabase/functions/ingest/adapters.ts`·`adapters.test.ts` — M-077, `cta_url` 게이팅 + Deno 테스트 3건(미실행).
- `apps/mobile/app/opportunity.tsx`·`opportunity.test.tsx` — M-077, `hasLink` 프로토콜 재확인 + 회귀 테스트 1건.

## 검증
```
$ bash scripts/gate.sh
── typecheck   → ✅ (core·mobile·tokens·web 4/4)
── test        → ✅ (core 318/318 · mobile 147/147 · web 343/343 = 808/808)
── lint        → ✅ (next lint --dir src, No ESLint warnings or errors — web만 검사, core·mobile엔 lint 스크립트 없음: 기존에 알려진 갭)
── secrets     → ✅ (시크릿 패턴 없음)
GATE: PASS — push 허용
```
클러스터 3건 전부 위 게이트 결과에 포함된 최종 트리 기준. 개별 이슈 반영 후 매번 `pnpm --filter <pkg> test`/`typecheck`로 먼저 확인했고, 마지막에 전체 `gate.sh`를 다시 돌려 재확인했다.

## 리스크 & 후속
- **doc 모순 2건, 3일째 미해결**(08-30 최초 지적 → 08-31 재확인 → 오늘 3번째 재확인만 하고 직접 안 고침):
  1. `.claude/agents/architect.md:9`("FSD 아키텍처")·`:18`("FSD 경계: entities/features/widgets/shared 계층 위반 없나")가 `constitution.md`·`apps/web/CLAUDE.md`·`react-web.md`("FSD 디렉토리는 실재하지 않는다")와 정면 충돌. 오늘 밤도 이 프롬프트의 PRECEDENCE 절에 따라 constitution.md 쪽(평면 구조)을 따랐다. **사람이 architect.md에서 FSD 문구를 지워야** 다음 밤들이 매번 이 판단을 반복하지 않는다.
  2. `.claude/agents/reviewer.md:16`("qa 게이트(`pnpm typecheck`+`pnpm test`)를 통과한 것만 dev에 올라간다")가 실제 게이트(`bash scripts/gate.sh` = typecheck+test+**lint**+**시크릿 스캔** 4종, 2026-08-05부터)보다 구식이다.
- **Deno 테스트 실행 불가가 반복되는 구조적 갭**: `supabase/functions/ingest/adapters.test.ts`(Deno)는 이 샌드박스에 `deno` 바이너리가 없어 밤마다 작성만 되고 실행 검증이 안 된다(2026-08-21에 이미 같은 사유 기록, 오늘로 최소 2번째). `scripts/gate.sh`에도 애초에 편입돼 있지 않다(파일 자체 주석: "CI에 Deno 러너가 아직 없다. 백로그 감."). **다음 audit 밤이 "supabase/ 어댑터 테스트가 게이트 밖이라 무인 파이프라인이 실행 검증을 못 한다"를 테스트 커버리지 축 findings로 등재할 만하다** — 지금 손대지는 않았다(이 프롬프트 범위 밖: 인프라에 Deno 러너를 까는 건 야간 파이프라인의 권한이 아니라고 판단).
- **M-052는 오늘도 todo에 남아 있다**(16일 이상 째, 08-29에 이미 사람에게 PushNotification으로 알림 발송함 — 아직 응답 없음). 이번 밤은 씨앗 우선순위(M-082가 M-052보다 high>medium으로 앞섬)에 밀려 애초에 재검토 대상이 아니었다.
- **다음 밤 후보**: M-085(모바일 탐색 UX, high, `apps/mobile`)가 유력 씨앗. scope 겹침 후보를 그때 다시 계산해야 한다.
