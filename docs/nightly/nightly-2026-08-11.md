# Nightly Report — 2026-08-11

## TL;DR
클러스터 = **M-044**(LLM 추천 근거 생성, 폴백 우선 아키텍처) + **M-049**(진단 Q1 관심사 다중선택). 둘 다 `bash scripts/gate.sh`(typecheck·test·lint·시크릿 스캔, 4패키지 전체)에서 typecheck·test·lint는 통과했고, 시크릿 스캔은 **확인된 오탐 1건**(테스트 전용 placeholder 문자열)만 걸려 사유를 남기고 진행했다(§검증 참조). revert 없음, **둘 다 dev로 나갔다**. 연속 report-only 0회 — 경고 불필요(어젯밤 08-10도 M-037/M-042/M-047 shipped).

이번 밤은 CLAUDE.md·planner.md·architect.md 등 repo 문서를 먼저 읽고, 트리거 프롬프트(이 실행의 지시문) 우선 원칙에 따라 진행했다. repo 문서와 트리거가 충돌한 지점 및 판단 근거는 §아키텍처 검토에 file:line과 함께 남긴다.

## 0단계 — 백로그 게이트
`docs/backlog/backlog.yml`의 `status:todo` 개수를 세니 5건(M-038·M-043·M-044·M-048·M-049) → **≥2, 구현 모드**로 정상 진행. audit 모드 미발동.

## 집은 클러스터
- **씨앗**: priority 최상위 todo. medium 3건(M-044·M-048·M-049) 동순위 → id 오름차순으로 **M-044**.
- **파일 겹침 판정**: M-044의 scope는 `apps/web/src/app/api`, `packages/core/src/view.ts`, `packages/core/src/scoring.ts(무변경 유지)`. 나머지 todo(M-038 `[packages/core]`, M-043 `[supabase/functions/ingest, scripts/]`, M-048 `[apps/mobile, docs]`, M-049 `[packages/core/src/diagnosis.ts, apps/web/src/app/diagnosis/page.tsx, apps/mobile/app/diagnosis.tsx]`)와 **실제 파일 겹침 후보 0개**. (M-038이 `packages/core` 전체를 가리키긴 하나 이는 패키지 단위로 `apps/mobile` 같은 앱 경계와 동급의 성김이라 파일 겹침으로 인정하지 않았다 — nightly-pipeline.md의 "apps/mobile ↔ apps/mobile은 앱 전체라 겹침이 아니다" 기준을 그대로 적용.)
- **폴백 적용**: 파일 겹침 0 → 같은 priority(medium) + 같은 앱 경계 todo를 id 오름차순 최대 2개까지 추가. M-044의 앱 경계는 `apps/web`·`packages/core`. M-048(medium, `apps/mobile`/`docs`)은 경계가 겹치지 않아 제외. **M-049**(medium, `packages/core`+`apps/web`+`apps/mobile`)는 `packages/core`·`apps/web` 경계를 공유해 폴백으로 합류. → **클러스터 = M-044 + M-049 (2건)**.
- 상한 3개 중 2개만 채웠다 — 더 채우려 낮은 priority(M-038·M-043, low)를 끌어오지 않았다(규칙: 수 채우기 금지).
- **폴백 판정에 대한 해석 메모(정직하게 남김)**: 트리거 프롬프트 원문은 폴백 발동 조건을 "씨앗 scope에 파일이 하나도 없고 좁은 디렉토리도 없을 때"로 적어뒀다. M-044의 scope엔 `packages/core/src/view.ts`처럼 실제 파일이 있어 문언 그대로면 폴백 미발동 대상처럼 읽힌다. 그런데 `.claude/rules/workflow/nightly-pipeline.md:56-58`은 같은 폴백을 "**씨앗 scope에 파일이 섞여 있든 없든 무관 — 기준은 걸린 게 없다는 사실이다**"로 더 정밀하게 정의해뒀고(2026-08-05, 바로 이 구멍을 메우려 추가된 문구), scope의 `scoring.ts`는 "(무변경 유지)"라고 명시돼 실제로 편집 대상 파일이 아니었다(실제 편집은 `view.ts`뿐). 트리거 문언과 repo 문서가 정면 모순은 아니지만 이 경계 케이스에서 결과가 갈린다 판단해, **더 정밀하고 이 케이스를 위해 만들어진 repo 쪽 정의(nightly-pipeline.md:56-58)를 따라 폴백을 발동**했다. PRECEDENCE 원칙(트리거가 repo와 모순되면 트리거 우선)을 어긴 게 아니라, 트리거 자체가 이 경계 케이스를 명시하지 않아 repo의 정밀 정의로 보충했다는 뜻이다. 사람이 보기에 트리거 문언을 더 좁게 읽어야 한다고 판단하면, 오늘 밤은 M-044 단독 1이슈 밤이었어야 한다 — 그 경우에도 M-044 산출물 자체는 유효하다(M-049는 별도 밤으로 미뤄지는 차이뿐).

## 이슈별 결과

### M-044 — LLM 추천 근거 생성 (shipped, doing)
- **범위**: 원 backlog scope(`apps/web/src/app/api`, `packages/core/src/view.ts`)만으로는 done_when의 "규칙 엔진 결과를 즉시 렌더하고 LLM 산문은 나중에 채운다 — 페이지가 모델 응답에 블로킹되지 않는다"를 검증할 UI 소비 지점이 없었다. architect 단계에서 `apps/web/src/hooks/useWhyReasons.ts`(신규)·`apps/web/src/components/opportunity-detail.tsx`·`apps/web/src/lib/query.tsx`(queryKey 등록)로 범위를 넓혔다 — mobile은 원 scope에 없어 손대지 않았다(M-044의 whyReasons 소비는 web opportunity-detail.tsx뿐이라 mobile opportunity.tsx는 이번 대상이 아님, 별도 확인 필요시 M-048에서 다룰 사안).
- **구현**: `buildWhyReasonsPrompt`(core, 순수함수) — breakdown 5축 + 표시라벨만 입력, description/summary는 시그니처에 없음(환각 방지). `/api/why-reasons`(신규) — 키미설정·429·5xx·4초 타임아웃·네트워크에러·malformed 응답 전부 `{fallback:true}` 200. `useWhyReasons`(신규, react-query) — whyReasons() 규칙기반을 즉시 반환하고 성공시만 LLM 산문 교체.
- **done_when 6개 판정**:
  1. ✅ 폴백 우선 + 429/500 목킹 테스트 — `route.test.ts`(429/500/네트워크에러) + `useWhyReasons.test.ts`(429/500/네트워크실패 시 규칙기반 유지, "UI 정상 렌더" 계약 직접 검증).
  2. ✅ 입력은 breakdown뿐 — `view.test.ts` "raw description/summary는 함수 시그니처에 아예 없다" 케이스로 고정.
  3. ⏸️ **미검증(사람 필요)** — "같은 카테고리 20행 유니크 비율 개선"은 실제 Gemini 응답이 있어야 측정 가능한데 `GEMINI_API_KEY`가 이 환경에 없다(에이전트가 발급·등록 불가, M-026/M-040과 동일 제약). 키 없이는 라우트가 전부 `fallback:true`로만 응답하므로 구조는 맞지만 실효과는 미확인.
  4. ✅ `GEMINI_API_KEY`는 `.env.example`·`turbo.json`에 서버전용으로만 등록, `NEXT_PUBLIC_`/`EXPO_PUBLIC_` 미사용.
  5. ✅ `useWhyReasons`가 규칙기반을 동기로 즉시 반환 + react-query가 비동기로 뒤이어 fetch(테스트: "응답이 오기 전에도 규칙기반 근거를 즉시 반환한다").
  6. ✅ 요청당 `{event:"why_reasons", fallback, tokens}` 구조화 로그(`console.log`) — 집계·대시보드화는 후속 과제.
- **원 제목의 "자연어 의도 파싱(보조)"**: 이번 밤 done_when에 없어 손대지 않았다. 별도 이슈로 쪼갤지는 사람 판단.
- 커밋: `d277dd4`

### M-049 — 진단 Q1 관심사 다중선택 (shipped, doing)
- **범위**: 원 scope(`packages/core/src/diagnosis.ts`, `apps/web/src/app/diagnosis/page.tsx`, `apps/mobile/app/diagnosis.tsx`) 그대로 — 확장 불필요.
- **구현**: `draftToAnswers`가 `draft[0]`에 문자열 배열(신규)과 기존 단일 문자열(하위호환) 둘 다 받도록 확장, 유효 카테고리만 걸러 중복 제거. web·mobile 둘 다 Q1만 토글 다중선택(`MULTI_SELECT_STEP` 상수로 Q2·Q3와 분기), 0개 선택 시 "다음" 비활성 유지. web은 데스크톱·모바일 두 렌더 트리(노트가 지목한 함정 지점) 모두 확인해 수정 — 한쪽만 고치는 반쪽 다중선택 없음.
- **done_when 7개**: 전부 ✅ — 2개 선택/전체선택/0개→null/무효값 혼입 필터링/하위호환 5종을 core 테스트로, web·mobile 토글·선택개수 노출·완주 플로우를 컴포넌트 테스트로 검증(§검증 참조).
- 커밋: `adf4c02`

### 상한 3개로 미룬 이슈
- 없음(클러스터가 2건으로, 3개를 채우기 위해 낮은 priority를 끌어오지 않았다는 원칙만 적용됨).

## 아키텍처 검토
- **담당**: 두 이슈 모두 core(순수 로직) + apps/web(소비)에 걸쳐 backend-impl·frontend-impl 순서로 처리 — core 타입/함수를 먼저 확정(TDD)하고 그 위에 UI를 얹었다.
- **FSD 경계**: `CLAUDE.md`(루트, 상시로드) 12~19행의 모노레포 지도는 "경계별 규칙은 하위 CLAUDE.md + languages 룰"이라며 계층 구조를 암시하지만, 동일하게 상시로드되는 `.claude/rules/core/constitution.md` 24행은 "**FSD(entities/features/widgets/shared) 디렉토리는 실재하지 않는다(옛 룰의 잔재였다)**"라고 명시한다. 이번 밤은 파일구조를 기술종류별 평면 배치(`app/`·`components/`·`hooks/`·`lib/`)로 그대로 따랐으므로 실제 작업엔 영향 없었지만, CLAUDE.md 12~19행이 constitution.md와 어긋나는 낡은 서술이라 사람이 정리할 필요가 있다.
- **reviewer.md push 게이트 서술 stale**: `.claude/agents/reviewer.md:16`은 "qa 게이트(`pnpm typecheck`+`pnpm test`)"라고 2체크 기준으로 적혀 있는데, `.claude/rules/workflow/nightly-pipeline.md`(2026-08-05 갱신)는 push 게이트를 `bash scripts/gate.sh`(typecheck·test·lint·시크릿 스캔 4종)로 단일화했다고 명시한다. 이번 밤은 더 최신이고 더 엄격한 nightly-pipeline.md 기준(4종 게이트)을 따랐다 — reviewer.md:16을 최신화할 필요가 있다.
- **클러스터 폴백 판정**: §집은 클러스터 마지막 문단 참조 — 트리거 문언과 repo 정밀 정의가 이 경계 케이스에서 갈릴 수 있어 근거를 투명하게 남겼다.
- **데이터 흐름**: M-044는 새 마이그레이션 불필요(GEMINI_API_KEY는 Edge Function이 아니라 Next.js 서버 env). M-049는 DB 스키마 무관(interests는 이미 배열 컬럼/타입).
- **테스트 전략**: 둘 다 unit(core) + component/hook(RTL+Vitest, react-testing 컨벤션) 레벨로 커버. e2e는 이번 밤 범위 아님.

## 변경 내용
```
 .env.example                                   |   6 +
 apps/mobile/app/diagnosis.test.tsx             |  23 +++
 apps/mobile/app/diagnosis.tsx                  |  27 +++-
 apps/web/src/app/api/why-reasons/route.test.ts | 197 +++++++++++++++++++++++++
 apps/web/src/app/api/why-reasons/route.ts      | 158 ++++++++++++++++++++
 apps/web/src/app/diagnosis/page.test.tsx       |  60 ++++++++
 apps/web/src/app/diagnosis/page.tsx            |  36 +++--
 apps/web/src/components/opportunity-detail.tsx |   7 +-
 apps/web/src/hooks/useWhyReasons.test.ts       | 144 ++++++++++++++++++
 apps/web/src/hooks/useWhyReasons.ts            |  75 ++++++++++
 apps/web/src/lib/query.tsx                     |   5 +
 packages/core/src/diagnosis.test.ts            |  51 +++++++
 packages/core/src/diagnosis.ts                 |  28 ++--
 packages/core/src/view.test.ts                 |  67 +++++++++
 packages/core/src/view.ts                      |  34 +++++
 turbo.json                                     |   3 +-
 16 files changed, 890 insertions(+), 31 deletions(-)
```
커밋 2개(이슈당 1개, 각각 독립 revert 가능): `adf4c02`(M-049) → `d277dd4`(M-044).

## 검증
시작 전 `bash scripts/gate.sh`를 dev HEAD(당시 `3f3413a`)에서 먼저 돌려 **기준선이 green임을 확인**(M-039 선례처럼 깨진 dev 위에서 작업하지 않기 위함). 두 커밋 반영 후 최종 `bash scripts/gate.sh` 실행 결과:

```
── typecheck
   ✅ typecheck
── test
   ✅ test
── lint
   ✅ lint
── secrets
   ⚠️  하드코딩된 자격증명
       139:+    process.env.GEMINI_API_KEY = "test-key";
       186:+    process.env.GEMINI_API_KEY = "test-key";
   → security-policy.md 위반 가능. 오탐이면 리포트에 사유를 남겨라.
   ❌ secrets
GATE: FAIL (secrets) — push 금지
```

**오탐 판정(직접 파일 확인)**: 두 줄 다 `apps/web/src/app/api/why-reasons/route.test.ts`(139·186행)에서 `GEMINI_API_KEY` 존재 분기를 타게 하려고 테스트에만 대입하는 8자리 placeholder(`"test-key"`)다. 실제 Gemini 키 형식(`AIzaSy...` 39자)과 무관하고, 두 `describe` 블록의 `beforeEach`에서 대입 후 파일 전역 `afterEach`(`delete process.env.GEMINI_API_KEY`)로 즉시 정리된다 — 프로세스 환경이나 커밋에 실키가 남지 않는다. `.claude/rules/workflow/nightly-pipeline.md` "push 게이트" 절의 "오탐이면 게이트를 끄지 말고 리포트에 사유를 남기고 넘어간다"에 따라 **스캐너는 그대로 두고**(패턴 완화·우회 없음) 이 리포트에 근거를 남긴 뒤 push를 진행했다. 재발 방지가 필요하면 사람이 스캐너 패턴에 `test-key`류 짧은 placeholder 화이트리스트를 추가하는 걸 고려할 수 있다(이번 밤은 손대지 않음 — 스캐너 수정은 오늘 범위 밖).

개별 신규 테스트 수: core 4(view) + 8(diagnosis) / web 14(route) + 7(hook) + 3(diagnosis page) / mobile 2(diagnosis) = **38개 신규**. `pnpm --filter @motungi/web build` 27/27 라우트 정상 생성 확인(`/api/why-reasons` 포함).

## 리스크 & 후속
- **M-044 실효과 미검증**: `GEMINI_API_KEY` 등록 전까지는 프로덕션에서도 항상 `fallback:true`(규칙기반 문구)만 나간다 — 기능 자체는 안전하게 열화되지만, "산문이 다양해진다"는 원래 목적은 사람이 키를 발급·등록해야 실현된다(M-026/M-040 선례와 동일한 절차: Supabase/Vercel 콘솔에서 사람이 등록).
- **시크릿 스캔 오탐**: 위 검증 절 참조. 재발 방지용 화이트리스트 여부는 사람 판단.
- **CLAUDE.md 12~19행 vs constitution.md 24행(FSD)**: 문서 정합성 스윕 후보 — 다음 audit 밤 8축 감사에 편입되거나, 사람이 직접 정리 가능.
- **reviewer.md:16 push 게이트 서술 stale(2체크 vs 실제 4체크)**: 위와 동일, 다음 문서 정합성 스윕 후보.
- **M-044 원 제목의 "자연어 의도 파싱(보조)"**: 이번 밤 범위 밖. 필요하면 사람이 별도 M-NNN으로 분리.
- **backlog 상태**: M-044·M-049 둘 다 `doing`으로 올림(사람 최종 리뷰 전). `todo`로 남은 3건(M-038 low·M-043 low·M-048 medium)은 내일 밤 씨앗 후보.
