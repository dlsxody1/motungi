# Nightly Report — 2026-08-12

## TL;DR
클러스터 = **M-048 단독**(앱↔웹 간극 정리 — 웹에서 고친 것이 모바일에 반영 안 되는 패턴). `bash scripts/gate.sh`(typecheck·test·lint·시크릿 스캔, 4패키지 전체)를 reviewer가 직접 재실행해 **PASS**(typecheck ✅, test 613/613 ✅, lint ✅, secrets ✅) 확인 — **shipped, dev로 나갔다.** revert 없음. `docs/ux/web-mobile-parity.md`(웹↔모바일 7화면 대조표) 신설 + `docs/backlog/backlog.yml`에 M-050~M-054(필수 5건)·M-055(선택 1건) 신규 등재. reviewer 단계에서 **status 오류 1건을 발견해 이 커밋에서 직접 정정**했다(§아키텍처 검토 참조) — frontend-impl이 M-048의 `status`를 `done`으로 세팅했는데, backlog.yml 헤더 규약과 reviewer.md의 완료 정의 둘 다 밤이 낼 수 있는 값은 `doing`까지라고 명시한다. 연속 report-only 0회(§0 참조) — 경고 불필요.

## 0단계 — 백로그 게이트
밤 시작 시점 `docs/backlog/backlog.yml`의 `status:todo` 개수는 3건(M-038 low, M-043 low, M-048 medium) → **≥2, 구현 모드**로 정상 진행. audit 모드 미발동.

## 집은 클러스터
- **씨앗**: `status:todo` 3건 중 priority 최상위는 M-048(medium) 하나뿐(M-038·M-043은 둘 다 low) — 동순위 경쟁 없이 **M-048**이 씨앗.
- **파일 겹침 판정**: M-048의 scope는 `apps/mobile`, `docs`. 나머지 todo(M-038 `[packages/core]`, M-043 `[supabase/functions/ingest, scripts/]`)와 **실제 파일 겹침 후보 0개** — 앱/디렉토리 경계 자체가 완전히 다르다.
- **폴백 적용 시도**: 파일 겹침 0 → 같은 priority + 같은 앱 경계 todo를 id 오름차순 최대 2개까지 추가하는 폴백을 검토했으나, M-048과 같은 `priority: medium`인 todo가 애초에 M-048 자신뿐이라(M-038·M-043은 둘 다 low) 폴백 후보도 0건.
- **결과: M-048 단독 1이슈 클러스터.** `nightly-pipeline.md` 클러스터 규칙 마지막 줄("폴백까지 해도 겹치는 게 없으면 1이슈 밤이고, 그것도 정상 결과다")에 정확히 해당하는 정상 케이스다 — 실패도 예외 처리도 아니다.
- 상한 3개 중 1개만 채웠지만, 겹칠 후보가 애초에 없었으므로 "미룬 이슈"는 없다(M-038·M-043은 클러스터에 넣을 자격 자체가 없었지 상한 때문에 잘린 게 아니다).

## 이슈별 결과

### M-048 — 앱↔웹 간극 정리 (shipped, doing)
- **원 done_when 3개**: (1) 웹↔모바일 기능 대조표가 docs에 있고 각 항목이 '의도된 차이'/'누락'으로 표시된다, (2) 누락 중 사용자가 막히는 항목이 개별 M-NNN으로 등재된다, (3) 한쪽만 고치고 넘어가는 것을 막을 방법이 정해진다.
- **planner 단계에서 먼저 확인한 것**: dev 실코드 기준으로 done_when이 이미 충족돼 있는지부터 봤다(`nightly-pipeline.md` "planner는 최신 origin/dev 기준... 중복 생성 금지" 원칙). `docs/ux/`에 대조표가 없었고 M-050 이상 이슈도 없어 **미충족 확인** — 정상 진행.
- **notes 재검증 결과(stale 발견)**: M-048의 원래 notes(2026-08-05 작성)는 예시 간극 3건을 지목했다 — 보관함 실종(M-045), IME 폭주(M-047), 랜딩 2차 CTA 오작동. planner가 코드로 재확인하니 **2건은 이미 다른 밤이 해소**했다(보관함 실종 → M-045, 커밋 `291e0e8`, 2026-08-06 / IME 폭주 → M-047, 커밋 `ebaeb27`, 2026-08-10). notes가 작성된 시점 이후 다른 클러스터가 먼저 처리해 stale해진 것 — 백로그 notes를 맹신하지 않고 실코드를 근거로 삼은 절차가 정상 작동한 사례다. **랜딩 2차 CTA만 아직 실재**했다: `apps/mobile/app/index.tsx:75-77`의 고스트 버튼("로그인 없이 바로 시작")이 여전히 `router.push("/report")`인 반면, 웹 `apps/web/src/app/page.tsx:56` 주석에 "예전엔 /report로 가는 링크였는데, 진단을 건너뛴 폴백 리포트로 떨어졌다"는 자기 고백과 함께 이미 `/explore`로 고쳐져 있었다 — reviewer가 두 파일을 직접 열어 재확인함(§변경 내용 하단 grep 결과 참조).
- **planner가 발견한 신규 간극(원 notes엔 없던 것)**: M-044(LLM 근거 생성, 2026-08-11 shipped)가 만든 `apps/web/src/hooks/useWhyReasons.ts`가 웹 전용이고 모바일 쪽 대응 훅이 없다 — M-044가 나간 지 하루 만에 이미 패리티 공백이 생긴 것을 잡아냈다.
- **architect 단계**: planner의 "모바일에 이미지 컴포넌트가 0개"라는 초기 서술을 정정 — `apps/mobile/ui/hero-carousel.tsx`가 이미 온보딩 히어로에 RN `Image`를 쓰고 있어 완전히 0은 아니다. 실제 간극은 더 좁게 **탐색 목록 + 상세 화면**에 이미지 렌더가 없는 것으로 한정했다. 문서 경로는 기존 `docs/ux/`·`docs/{topic}/` 컨벤션에 맞춰 `docs/ux/web-mobile-parity.md`로 정함. 담당은 frontend-impl 단독(문서+YAML만, 코드 변경 없음이므로 backend-impl 불필요)으로 배정하고 M-050~M-055 백로그 항목 초안을 구체적으로 작성.
- **구현(frontend-impl, 커밋 `d5f8b94`)**: `docs/ux/web-mobile-parity.md`(227행) 신설 — 홈/랜딩·진단·리포트·탐색·상세·보관함·위치 7화면을 실코드(dev HEAD `694a563`) 대조. 모든 행에 🟢해소됨/🔵의도된 차이/🔴누락(user-blocking)/🟡누락(연출·품질) 4태그 부여, §10에 "공유 로직을 packages/core로 옮기는 것만으론 재발을 못 막는다"를 `deadlineLabel`/`timeRangeLabel`/`isWeekendOuting`이 이미 core에 있는데 `apps/mobile` 참조가 0건이라는 실증(그리고 `useWhyReasons`도 같은 패턴)으로 뒷받침한 재발방지 제안 4개를 실었다. `docs/backlog/backlog.yml`에 M-050(필수, user-blocking)·M-051~M-054(필수, 연출·품질)·M-055(선택, GEMINI_API_KEY 미등록 제약) 6건 등재.
- **범위 확인**: `git diff 694a563..d5f8b94 --name-only` = `docs/backlog/backlog.yml`, `docs/ux/web-mobile-parity.md` 딱 2개. `apps/mobile`·`apps/web`·`packages/core`는 전혀 건드리지 않음 — 조사·분류 이슈라는 성격에 맞다. reviewer가 직접 재확인.
- **done_when 3개 판정**: (1) ✅ 대조표 존재 + 4태그 분류(reviewer가 문서 §1~§7 직접 열람해 태그·근거 file:line 확인). (2) ✅ user-blocking 1건(M-050)이 개별 이슈로 등재됨. (3) ✅ 재발방지 제안이 §10에 구체적으로(frontend-impl.md DoD 문구 초안·nightly-pipeline.md reviewer 단계 문구 초안까지) 제시됨 — 다만 이건 "방법이 정해진다"이지 "적용된다"가 아니므로, 실제 두 문서(frontend-impl.md·nightly-pipeline.md) 편집은 사람 승인 후 후속 밤 과제로 남는다(§리스크 & 후속 참조).
- 커밋: `d5f8b94`

### 상한 3개로 미룬 이슈
- 없음. §집은 클러스터에서 밝혔듯 M-038·M-043은 파일 겹침도 폴백 자격(같은 priority)도 없어 애초에 클러스터 후보가 아니었다 — 상한 때문에 잘린 게 아니다.

## 아키텍처 검토

### status 오류 정정 (process-gap finding)
frontend-impl의 커밋 `d5f8b94`가 `docs/backlog/backlog.yml`에서 M-048의 `status`를 **`todo` → `done`**으로 직접 바꿔 놓았다. 이건 두 근거 문서 모두와 정면으로 어긋난다.
- `docs/backlog/backlog.yml:14` (파일 헤더 필드 설명): `status     todo | doing | done  (reviewer가 PR 열 때 todo→doing, 사람이 머지 후 done)` — 밤(reviewer)이 낼 수 있는 값은 `doing`까지이고, `done`은 사람이 `dev→main` 머지 후에만 매기는 값이라고 명시돼 있다.
- `.claude/agents/reviewer.md:45`의 완료 정의(Definition of Done): `backlog에서 나간 이슈만 status를 갱신했다 (되돌린 이슈는 todo 유지 — doing으로 바꾸면 큐에서 사라진다)` — 여기서도 밤이 다루는 상태 전이는 `todo↔doing`뿐이고 `done`은 등장하지 않는다.
- 과거 선례(`docs/nightly/nightly-2026-08-11.md`, `nightly-2026-08-10.md` 등)도 shipped 이슈를 전부 `doing`으로만 올렸다 — `done`을 밤이 세팅한 전례가 이번이 처음이다.

architect의 계획과 frontend-impl의 구현 둘 다 이 규약을 놓쳤고, qa의 수용 기준 체크리스트에도 이 항목이 없어 **3단계가 연속으로 못 잡았다.** reviewer가 이번 커밋에서 M-048의 `status`를 `doing`으로 되돌렸다(§변경 내용 참조) — frontend-impl의 기존 커밋 `d5f8b94`는 **amend하지 않고**, 이 정정을 reviewer 자신의 새 커밋에 반영해 이력을 보존했다. backlog.yml의 M-048 notes에도 정정 사유를 남겨 다음에 이 항목을 보는 사람(또는 밤)이 왜 바뀌었는지 추적할 수 있게 했다.

### reviewer.md의 push 게이트 서술 stale (재기록)
`.claude/agents/reviewer.md:16`은 여전히 `qa 게이트(pnpm typecheck+pnpm test)`라고 2체크 기준으로 적혀 있다. 그러나 실제 push 게이트는 `.claude/rules/workflow/nightly-pipeline.md`(2026-08-05 갱신)가 명시한 `bash scripts/gate.sh`(typecheck·test·lint·시크릿 스캔 4종)다. 이 불일치는 **어젯밤(2026-08-11) 리포트의 §아키텍처 검토에서 이미 지적됐고 아직 고쳐지지 않았다.** 이번 밤도 실제로는 4종 게이트(nightly-pipeline.md 기준, 더 최신·더 엄격한 쪽)를 따랐으므로 작업 결과에 영향은 없었지만, repo 문서 간 모순을 기록해두는 이 세션의 PRECEDENCE 원칙에 따라 다시 한번 명시적으로 남긴다 — **2개 밤 연속으로 미수정 상태**이며, 사람이 `reviewer.md:16`을 `bash scripts/gate.sh`(4종) 기준으로 갱신할 필요가 있다.

### 담당·경계·테스트 전략
- M-048은 조사·분류형 이슈라 담당은 frontend-impl 단독(문서+YAML 편집만), backend-impl 불필요 — architect의 판단이었고 실제 diff도 이를 그대로 반영했다(코드 파일 0건).
- FSD 경계·데이터 흐름: 해당 없음(제품 코드 무변경).
- 테스트 전략: 이 이슈 자체는 대조·문서화 작업이라 신규 유닛/컴포넌트 테스트가 없다. 대신 문서의 사실관계(태그·file:line 인용)를 qa가 6건, reviewer가 그중 핵심 2건(M-050의 랜딩 CTA 경로, M-052/M-053의 grep 결과)을 각각 실코드 대조로 독립 재검증했다(§검증 참조) — "테스트"의 역할을 사실관계 검증이 대신한 케이스.

## 변경 내용
```
 docs/backlog/backlog.yml     | 147 +++++++++++++++++++++++++++-
 docs/ux/web-mobile-parity.md | 227 +++++++++++++++++++++++++++++++++++++++++++
 2 files changed, 372 insertions(+), 2 deletions(-)
```
커밋 1개(이슈 1건 = 커밋 1개, 독립 revert 가능): `d5f8b94`(M-048).

reviewer가 직접 재확인한 범위 준수 증거:
```
$ git diff 694a563..d5f8b94 --name-only
docs/backlog/backlog.yml
docs/ux/web-mobile-parity.md
```
`apps/mobile`·`apps/web`·`packages/core` 무변경 — 커밋 메시지의 "이 이슈는 조사·분류용이라 apps/mobile·apps/web·packages/core는 건드리지 않았다"는 주장이 사실과 일치한다.

reviewer가 직접 재확인한 핵심 사실관계 2건:
```
$ grep -rn "deadlineLabel\|timeRangeLabel\|isWeekendOuting" apps/mobile   → (결과 없음, exit 1)
$ grep -rn "react-native-maps\|MapView" apps/mobile                      → (결과 없음, exit 1)
```
문서 §10·§5의 "모바일에서 참조 0건" 주장을 뒷받침. `apps/mobile/app/index.tsx:75-77`(고스트 버튼 → `/report`)과 `apps/web/src/app/page.tsx:56`(주석 + `/explore` 링크)도 직접 열람해 M-050 근거가 정확함을 확인.

## 검증
reviewer가 fresh하게(qa 캐시에 의존하지 않고) `bash scripts/gate.sh`를 dev HEAD(`d5f8b94`)에서 재실행한 결과:

```
── typecheck
   ✅ typecheck
── test
@motungi/core:test   Test Files  10 passed (10)   Tests  263 passed (263)
@motungi/mobile:test Test Files  15 passed (15)   Tests   71 passed (71)
@motungi/web:test    Test Files  36 passed (36)   Tests  279 passed (279)
   ✅ test
── lint
@motungi/web:lint  ✔ No ESLint warnings or errors
   ✅ lint
── secrets
   (시크릿 패턴 없음)
   ✅ secrets

GATE: PASS — push 허용
```
core 263 + mobile 71 + web 279 = **613/613 테스트 통과**, qa가 보고한 수치와 정확히 일치. 코드 변경이 없는 이슈이므로 신규 테스트는 0건(문서·YAML 편집만) — 위 613개는 전부 기존 테스트가 회귀 없이 그대로 통과한 것이다.

`docs/backlog/backlog.yml`은 `python3 -c "import yaml; yaml.safe_load(...)"`로 파싱 성공(신규 55개 엔트리, id 중복 없음) 확인.

## 리스크 & 후속
- **M-050~M-055는 다음 밤들의 실질적인 작업 큐다.** 오늘 밤 종료 시점 `status:todo`는 M-038(low)·M-043(low)·M-050(medium, user-blocking)·M-051~M-054(medium/low)·M-055(low, 선택) = **9건**으로 늘었다 — 내일 밤 씨앗은 M-050(유일한 user-blocking 성격, medium)이 유력하다.
  - M-050(랜딩 2차 CTA 오작동, `apps/mobile/app/index.tsx`) — 유일한 🔴user-blocking, 다음 밤 최우선 후보.
  - M-051(탐색·상세 이미지 렌더 부재) — `hero-carousel.tsx`에 이미 RN Image 선례가 있어 구현 비용 낮음.
  - M-052(지도·폴리라인 부재) — done_when 자체에 "react-native-maps 신규 의존성 vs 정적 이미지" 트레이드오프를 architect 단계에서 결정하고 문서화하라는 조건이 걸려 있다 — 신규 네이티브 의존성 경로를 택하면 Expo prebuild에 영향, 사람 승인이 필요할 수 있음을 다음 밤 architect가 유의할 것.
  - M-053(마감 D-day·출처 미표시) — `packages/core`에 이미 순수함수(`deadlineLabel` 등)가 있어 3건 중 구현 비용이 가장 낮다.
  - M-054(탐색 로딩 스켈레톤 부재) — priority low, 과거 웹 M-042(loading.tsx 부재)의 재발 패턴.
  - M-055(선택, LLM 근거 모바일 미소비) — `GEMINI_API_KEY` 미등록이라 규칙기반 폴백 경로까지만 밤이 검증 가능하고, 실제 LLM 응답 경로는 사람이 키 등록 후 종단 검증해야 한다(M-026/M-040/M-044와 동일 제약).
- **문서 §10의 재발방지 제안은 아직 "제안" 상태다.** `frontend-impl.md`의 DoD에 core→mobile 소비 확인 문구를 추가하는 것과 `nightly-pipeline.md` reviewer 단계에 grep 체크 문구를 추가하는 것 둘 다, 이번 밤은 **backlog.yml과 리포트에만** 반영했고 실제 두 md 파일은 편집하지 않았다 — 사람이 검토 후 채택 여부를 결정해야 반영된다.
- **`reviewer.md:16` push 게이트 서술 stale — 2개 밤 연속 미수정.** §아키텍처 검토 참조. 사람이 `qa 게이트(pnpm typecheck+pnpm test)`를 `bash scripts/gate.sh`(4종) 기준으로 갱신 필요.
- **status:done→doing 정정 배경 재확인 필요.** 이번 정정이 맞다는 근거(backlog.yml:14, reviewer.md:45, 과거 선례)를 리포트와 backlog notes에 남겼지만, 혹시 "done을 밤이 직접 매기도록 규약을 바꾸자"는 의도가 있었다면(예: 조사·분류형 이슈는 코드 배포가 없으니 즉시 done 처리해도 안전하다는 논리) 그건 사람이 명시적으로 규약을 고쳐야 할 사안이지 개별 밤이 임의로 판단할 사안이 아니다 — 다음 사람 리뷰 때 확인 권장.
- **CLAUDE.md 12~19행 vs constitution.md 24행(FSD) 불일치** — 2026-08-11 리포트에서 지적된 항목, 이번 밤은 범위 밖이라 재확인만 하고 넘어감. 다음 audit 밤 8축 감사 후보로 유효.
