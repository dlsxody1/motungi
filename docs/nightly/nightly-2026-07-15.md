# Nightly Report — 2026-07-15

## TL;DR

- 백로그 최우선 이슈 **M-008**("opportunities/catalog/useEnsureCatalog를 packages/core로 승격")을 집었으나, `useEnsureCatalog` 공유 훅 통합(core에 React 훅 신규 도입)은 범위가 커서 이번 밤은 **opportunities 데이터/타입 레이어 승격만** 서브스코프로 축소 진행. `useEnsureCatalog` 통합은 다음 밤 후속 과제로 명시.
- `packages/core/src/catalog.ts`(신규)로 `MockOpportunity`/`CatalogStatus`/`CatalogResult`/`NEIGHBORHOOD_POINTS`/`rowToMock`/`fetchOpportunities`를 이관하고, web·mobile의 `data/opportunities.ts`를 core re-export + supabase 클라이언트 주입 래퍼(99줄→16줄)로 축소.
- 검증: core `pnpm --filter @motungi/core test` **129/129 통과**, web **87/87**, mobile **23/23**, `npx turbo run typecheck --force` **4/4 성공**. 리뷰어가 전부 독립 재실행해 qa 판정과 일치함을 확인.
- `useEnsureCatalog*.ts`/`.test.ts` 4개 파일(web+mobile)은 git diff 0 — architect가 못박은 "호출부 수정 금지" 요건 충족.
- 최종 diff: 10 files changed, 520 insertions(+), 646 deletions(-) (net -126줄, 순수 코드 이동+통합).
- backlog M-008 status는 부분 완료이므로 `done`이 아니라 **`doing`**으로 갱신(잔여 범위는 notes에 명시).
- PR: draft, main 대상 — 오케스트레이터가 GitHub MCP로 생성 예정 (아래 PR 섹션 참조).

## 집은 이슈

- **M-008** — opportunities/catalog/useEnsureCatalog를 packages/core로 승격(web·mobile 복제 제거) (priority: medium, scope: `packages/core, apps/web, apps/mobile`, status: todo → **doing**)
  - planner가 todo 중 최우선(전부 medium, id 오름차순)으로 선정.
  - 원래 done_when에는 `useEnsureCatalog`가 store 셀렉터·fetch 주입형 공유 훅으로 통합되는 것까지 포함돼 있었으나, planner가 core에 React 의존성을 신규 도입해야 하고 웹/모바일 훅 시그니처가 상이해 테스트 재설계가 필요하다는 이유로 **이번 밤 범위에서 제외**하고 데이터/타입 레이어(opportunities.ts → catalog.ts) 승격만 서브스코프로 확정.
  - 따라서 done_when 5개 항목 중 데이터 레이어 관련 4개(§141~142, §144~145)는 충족, `useEnsureCatalog` 통합(§143)은 미충족 — M-008은 부분 완료.

## 아키텍처 검토

- architect가 코드를 실제로 대조 검증(web/mobile `data/opportunities.ts` 바이트 동일 여부, `useEnsureCatalog` 4개 파일 목록, 13개 호출부 파일 목록 등)한 뒤 승인. 결함 없음.
- 담당·순서: backend-impl(core 먼저) → frontend-impl(web/mobile 래퍼화) 순서로 확정 — core가 먼저 안정화돼야 앱단 래퍼가 그 위에 얹힐 수 있으므로 타당.
- 13개 호출부 파일은 `"@/data/opportunities"`에서 **동일 이름으로 import하는 한 수정 불필요**(수정 금지, 검증만)라고 architect가 사전에 명확화 — 실제로 frontend-impl 산출물에서 web/mobile `data/opportunities.ts`는 `export`/`export type`으로 동일 이름(`MockOpportunity`, `CatalogStatus`, `CatalogResult`, `NEIGHBORHOOD_POINTS`, `rowToMock`, `fetchOpportunities`)을 그대로 재노출하므로 요건 충족.
- core는 앱에 의존하지 않는 순수 계층 원칙 유지 — `fetchOpportunities(client: SupabaseClient | null)`처럼 모듈 전역 supabase 참조 대신 클라이언트를 파라미터로 주입받는 구조로 만들어, core에 부작용(전역 상태)을 남기지 않았다. web/mobile 각각의 `lib/supabase.ts`가 생성한 클라이언트(둘 다 `@supabase/supabase-js`의 `createClient` 반환값)를 그대로 주입하므로 타입 호환성도 리뷰어가 직접 확인함.
- `@supabase/supabase-js` 버전(`^2.47.10`)이 core/web/mobile 세 곳 모두 동일 — 리뷰어가 `package.json` 3곳을 직접 대조해 확인. 버전 스큐 리스크 없음.
- FSD/의존성 방향 위반 없음.

## 변경 내용

리뷰어가 `git diff --stat origin/main...HEAD`로 직접 재확인:

```
 apps/mobile/data/opportunities.test.ts            | 260 ++--------------
 apps/mobile/data/opportunities.ts                 |  99 +-----
 apps/web/src/data/opportunities.rowToMock.test.ts | 177 -----------
 apps/web/src/data/opportunities.test.ts           |  70 +----
 apps/web/src/data/opportunities.ts                |  99 +-----
 packages/core/package.json                        |   3 +
 packages/core/src/catalog.test.ts                 | 350 ++++++++++++++++++++++
 packages/core/src/catalog.ts                      | 103 +++++++
 packages/core/src/index.ts                        |   1 +
 pnpm-lock.yaml                                     |   4 +
 10 files changed, 520 insertions(+), 646 deletions(-)
```

- **신규** `packages/core/src/catalog.ts`(103줄): `MockOpportunity`/`CatalogStatus`/`CatalogResult`/`NEIGHBORHOOD_POINTS`/`rowToMock`/`fetchOpportunities(client: SupabaseClient | null)`. 구현은 web/mobile 구 `opportunities.ts`와 로직 동일(리뷰어가 diff로 직접 대조 — select 컬럼 목록, limit(200), 상태 분기 4종 모두 일치).
- **신규** `packages/core/src/catalog.test.ts`(350줄, 25 tests): web 구 테스트 2개(`opportunities.test.ts`, `opportunities.rowToMock.test.ts`) + mobile 구 테스트 1개(`opportunities.test.ts`)의 시나리오를 손실 없이 통합.
- `packages/core/src/index.ts`: `export * from "./catalog"` 추가.
- `packages/core/package.json`: `@supabase/supabase-js: ^2.47.10` 의존성 신규 추가(+3줄), `pnpm-lock.yaml` 갱신(+4줄).
- `apps/web/src/data/opportunities.ts`, `apps/mobile/data/opportunities.ts`: core re-export + supabase 클라이언트 주입 래퍼로 축소(각 99줄 → 16줄). 타입/상수는 `export type`/`export`로 재노출, `fetchOpportunities()`는 무인자 시그니처를 유지한 채 내부에서 `coreFetchOpportunities(supabase)`로 위임 — 호출부(13개 파일) import 이름·시그니처 불변.
- 구 테스트 3개(web 2개, mobile 1개) 삭제, 대신 위임 스모크 테스트(클라이언트 주입 확인) 각 앱 2건씩 신규 — web `opportunities.test.ts` 70줄(구 로직 테스트 제거 후 위임 검증만 남음), mobile `opportunities.test.ts` 260→대폭 축소.
- `useEnsureCatalog*.ts`/`.test.ts` 4개 파일(web+mobile): **git diff 0** — 리뷰어가 `git diff --stat origin/main...HEAD -- '**/useEnsureCatalog*'`로 재확인, 출력 없음(변경 없음).

## 검증

리뷰어가 직접 재실행한 결과(qa 판정과 일치):

```
$ pnpm --filter @motungi/core test -- --run
 RUN  v2.1.9 /home/user/motungi/packages/core

 ✓ src/view.test.ts (25 tests) 33ms
 ✓ src/scoring.test.ts (30 tests) 12ms
 ✓ src/catalog.test.ts (25 tests) 49ms
 ✓ src/adapters/seoul-jobs.test.ts (13 tests) 8ms
 ✓ src/adapters/sports-facility.test.ts (9 tests) 9ms
 ✓ src/adapters/seoul-culture.test.ts (6 tests) 8ms
 ✓ src/adapters/util.test.ts (12 tests) 7ms
 ✓ src/adapters/trail.test.ts (5 tests) 6ms
 ✓ src/adapters/culture-info.test.ts (4 tests) 6ms

 Test Files  9 passed (9)
      Tests  129 passed (129)
```

```
$ pnpm --filter web test -- --run
 ✓ src/components/bottom-nav.test.tsx (10 tests) 370ms
 ✓ src/data/opportunities.test.ts (2 tests) 6ms
 ✓ src/components/web-landing.test.tsx (5 tests) 492ms
 ✓ src/app/report/page.test.tsx (2 tests) 103ms
 (외 6개 파일 생략)

 Test Files  10 passed (10)
      Tests  87 passed (87)
```

```
$ pnpm --filter mobile test -- --run
 ✓ store/useAppStore.test.ts (15 tests) 13ms
 ✓ data/opportunities.test.ts (2 tests) 6ms
 ✓ hooks/useEnsureCatalog.test.ts (5 tests) 132ms
 ✓ lib/geo.test.ts (1 test) 2ms

 Test Files  4 passed (4)
      Tests  23 passed (23)
```

```
$ npx turbo run typecheck --force
 • Packages in scope: @motungi/core, @motungi/mobile, @motungi/tokens, @motungi/web
 • Running typecheck in 4 packages
 Tasks:    4 successful, 4 total
```

- qa가 먼저 `pnpm install --frozen-lockfile` 정상 완료, core/web/mobile 전체 테스트, 4워크스페이스 typecheck를 독립적으로 재실행해 통과 확인. `git diff --stat origin/main...HEAD` 10개 파일만 변경(스코프 내)임을 재확인. `useEnsureCatalog` 4개 파일 diff 0을 재확인. 구 테스트 3개 시나리오가 `catalog.test.ts`(27 tests — qa 리포트 표기, 실측 25 tests. 아래 리스크 참조)로 손실 없이 이관됐음을 원본과 대조로 확인. 시크릿 노출·파괴적 SQL 없음. **12개 수용 기준 전부 통과.**
- 리뷰어가 위 명령을 전부 독립 재실행해 동일 결과(core 129/129, web 87/87, mobile 23/23, typecheck 4/4)를 확인했고, `@supabase/supabase-js` 버전 일치·`useEnsureCatalog` diff 0·핵심 로직(`catalog.ts` vs 구 `opportunities.ts`) 동등성을 직접 검토함.
- 종합 판정: **통과 (pass)**.

## 리스크 & 후속

- **M-008 잔여 범위**: `useEnsureCatalog`를 store 셀렉터·fetch 주입형 공유 훅으로 통합하는 작업(core에 React 훅 신규 도입 포함)은 이번 밤 범위 밖으로 남았다. 다음 밤 별도 후속 이슈로 진행 필요. backlog에는 `done`이 아닌 `doing`으로 정확히 반영했다.
- **경미 지적**: qa 리포트 원문에 `catalog.test.ts`가 "27 tests"로 기재돼 있으나, 리뷰어가 직접 실행한 결과와 `catalog.test.ts` 파일 헤더 상 실제로는 **25 tests**이다(전체 core 스위트는 129 tests로 qa 최종 합계와 일치하므로 개별 파일 표기상의 오기로 보이며, 최종 판정에는 영향 없음). 다음 밤 qa 리포트 작성 시 개별 파일 tests 수 표기에 주의 필요.
- **문서-현실 불일치(코드 수정 아님, 기록만)**: `docs/nightly/PIPELINE.md`가 "dev가 통합 기준선"이라 서술하지만 `origin/dev`가 실재하지 않는다(planner/architect/backend-impl/frontend-impl/qa 전원이 `git ls-remote`/`git branch -a`로 확인, 리뷰어도 `git branch -a`로 재확인 — `origin/dev` 없음, `origin/main`만 존재). 이번 밤을 포함한 모든 과거 밤 작업이 실제로는 main 기준으로 진행돼 병합돼 왔다. 문서가 stale하다 — 사람이 다음에 `PIPELINE.md`를 정정하거나, 실제로 `dev` 브랜치를 생성해 문서와 일치시켜야 한다.
- **다음 밤 후보**: M-008 잔여(`useEnsureCatalog` 통합) 또는 backlog의 다음 medium 이슈(사람이 M-008 잔여 처리 여부를 먼저 판단한 뒤 결정).

## PR

draft, main 대상 — 오케스트레이터가 GitHub MCP 도구로 생성 예정. 리뷰어는 `gh pr create` 실행 권한이 없어 브랜치 커밋 + push까지만 수행했다.
