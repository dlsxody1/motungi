# Nightly Report — 2026-08-15

## TL;DR
클러스터 = **M-038 단독**(`packages/core/src/scoring.ts`의 미사용 공개 export 4종 정리). `bash scripts/gate.sh`(typecheck·test·lint·시크릿 스캔, 4패키지 전체) **PASS** — **shipped, dev로 나갔다**(커밋 `1272e50`). revert 없음.

씨앗 후보는 원래 `M-052`(유일한 medium priority todo, 모바일 상세 지도·산책로 렌더)였다. 08-13·08-14 이틀 연속 architect가 이미 이 이슈를 "무인 야간 실행에 부적합"으로 제외했다(3개 구현 경로 전부 사람 승인·신규 프로비저닝 필요). 오늘 밤 그 판단이 여전히 유효한지 `.env.example`·`security-policy.md`·`apps/mobile/package.json`을 직접 재확인했고 08-14 이후 변경이 전혀 없었다 — **동일하게 제외**했다(§아키텍처 검토 참조). `M-052`는 `todo`로 유지된다.

08-14 밤에는 `M-052`가 제외돼도 파일 겹침 대체 이슈(`M-055`, 같은 파일)가 있어 클러스터가 채워졌지만, 그 `M-055`가 이미 08-14에 shipped·`doing`으로 넘어가면서 오늘 밤은 대체할 파일 겹침 후보가 없었다. THIS PROMPT의 오케스트레이터 재량(“큐가 있는데 밤을 idle로 흘려보내지 않는다”)에 따라, 알려진 대로 막혀 있는 `M-052`를 씨앗으로 architect 단계까지 밀어붙여 같은 결론을 반복하며 사이클을 낭비하는 대신, **오케스트레이터 단계에서 즉시 재검증 후 제외하고 다음 우선순위 todo로 재시딩**했다 — 상세 근거는 아래 §집은 클러스터·§아키텍처 검토.

연속 report-only 0회(08-06~08-14 전부 shipped) — 경고 불필요.

## 집은 클러스터
- 원 씨앗 `M-052`(priority medium, 유일한 medium todo) → 08-13·08-14와 동일 사유로 **오케스트레이터가 사전 제외**(재검증 결과 상세는 §아키텍처 검토).
- `M-052` 제외 후 남은 todo는 전부 `low`: `M-038`[packages/core] · `M-043`[supabase/functions/ingest, scripts/] · `M-054`[apps/mobile/app/(tabs)/explore.tsx]. 셋 다 파일/디렉토리 겹침이 없다(서로 다른 앱 경계).
- **재시딩**: 남은 todo 중 priority 최상위(전부 low 동순위) → id 오름차순으로 `M-038`을 새 씨앗으로 선택.
- `M-038` 기준 파일 겹침 후보 0개. 앱 단위 폴백(같은 앱 + 같은 priority) 시도 — `packages/core`를 scope로 가진 다른 low todo가 없어 폴백도 0개.
- 클러스터 = `M-038` 단독. **1이슈 밤은 정상 결과**(패딩 없음).
- 상한 3개로 밀린 이슈는 없다(애초에 겹치는 후보가 없었다). `M-043`·`M-054`는 다음 밤 씨앗 후보로 남는다(둘 다 low, id 오름차순으로 `M-043`이 다음 유력 씨앗).

## 이슈별 결과
| id | 결과 | 비고 |
|---|---|---|
| M-052 | **제외**(구현 안 함, `todo` 유지) | 08-13·08-14와 동일 사유 3연속 재확인 — 사람 승인 없이 채택 가능한 지도 구현 경로가 없음 |
| M-038 | **shipped** | 커밋 `1272e50`, dev push 완료, qa 게이트 PASS |

## 아키텍처 검토
**담당**: backend-impl(`packages/core`) 단독.

**M-052 재제외 상세**: `docs/backlog/backlog.yml`(M-052 notes)에 08-13·08-14 두 밤의 판정이 이미 기록돼 있다 — 3개 구현 경로(react-native-maps 신규 네이티브 의존성 / NAVER 정적지도 백엔드 프록시 신설 / 키리스 서드파티 정적지도) 전부 무인 야간 실행 부적합(사람 승인 필요·스코프 초과·미프로비저닝·`security-policy.md`의 NAVER-only 서버프록시 원칙 위반 소지). 오늘 밤도 그대로 수용하지 않고 직접 재검증했다: `.env.example`에 지도 관련 신규 키 없음, `apps/mobile/package.json`에 지도 의존성 없음(`grep -i map` 0건), `security-policy.md`에 지도 관련 변경 없음 — 08-14 이후 사람의 결정·키 제공이 전혀 없었다. 3일 연속 같은 결론이므로, 다음 밤이 또 씨앗으로 집기 전에 **사람이 위 세 옵션 중 하나를 승인하거나 새 옵션을 제시해야** 이 반복이 끝난다.

**M-038 설계**: `packages/core/src/scoring.ts`의 `ScoreWeights`(interface, 12행)·`DEFAULT_WEIGHTS`(const, 20행)·`ScoredOpportunity`(interface, 54행)·`distanceKm`(function, 61행) 4개는 `export` 키워드가 붙어 있고 `packages/core/src/index.ts`가 `export * from "./scoring"`으로 재노출하므로 `@motungi/core`의 암묵적 공개 API 표면이었다. 구현 전 repo 전체(`apps/web`·`apps/mobile`·`packages/core` 전부)를 grep으로 재확인 — 4개 식별자를 이름으로 import하는 곳이 **0건**(테스트 파일 포함). 반면 이 타입들을 파라미터/반환 타입으로 쓰는 `scoreOpportunity`·`scoreAll`·`pickTop`은 `apps/web`(`explore/page.tsx`·`loading/page.tsx`·`hooks/useWhyReasons.ts`) · `apps/mobile`(`(tabs)/explore.tsx`·`loading.tsx`·`hooks/useWhyReasons.ts`)에서 실사용 중이며 전부 기본 가중치(`weights` 인자 생략)로만 호출한다. `packages/core`는 소스를 직접 참조하는 `"types": "./src/index.ts"` 구조라(별도 `.d.ts` 빌드 없음) 비공개 타입을 공개 함수 시그니처에서 구조적으로 사용해도 타입 에러가 나지 않는다 — 실제로 `pnpm --filter @motungi/core typecheck`가 이를 증명했다. FSD/아키텍처 경계 위반 없음(단일 파일 내부 가시성 변경).

**테스트 전략**: 기존 `scoring.test.ts`(32 테스트)가 4개 식별자를 이름으로 참조하지 않아 수정 불필요 — export 범위 축소는 동작 변경이 아니므로 신규 테스트도 불필요. `pnpm --filter @motungi/core test`로 회귀 없음 확인.

## 변경 내용
- **수정** `packages/core/src/scoring.ts` — `ScoreWeights`·`DEFAULT_WEIGHTS`·`ScoredOpportunity`·`distanceKm` 4곳에서 `export` 키워드 제거(module 내부 전용으로 좁힘). `scoreOpportunity`·`scoreAll`·`pickTop`·`nearestAnchorKm` 등 실사용 함수는 그대로 유지.
- diff는 이슈 하나(M-038)에만 집중돼 있다 — 무관한 변경 없음.

## 검증
```
$ pnpm --filter @motungi/core typecheck
tsc --noEmit  →  에러 없음

$ pnpm --filter @motungi/core test
 Test Files  10 passed (10)
      Tests  263 passed (263)

$ bash scripts/gate.sh
── typecheck
   ✅ typecheck
── test
@motungi/core:test:  Test Files  10 passed (10)  Tests  263 passed (263)
@motungi/mobile:test:  Test Files  17 passed (17)  Tests  91 passed (91)
@motungi/web:test:  Test Files  36 passed (36)  Tests  279 passed (279)
   ✅ test
── lint
@motungi/web:lint: ✔ No ESLint warnings or errors
   ✅ lint
── secrets
   ✅ secrets

GATE: PASS — push 허용
```

## 리스크 & 후속
- **`M-052` 3일 연속 블록**: 지도/산책로 표시 기능이 사람의 아키텍처 결정 없이는 더 이상 진행 불가. 세 옵션(react-native-maps 신규 의존성 / NAVER 정적지도 백엔드 프록시 / 키리스 서드파티 정적지도) 중 하나를 사람이 승인하거나, 우선순위를 낮춰 다른 medium 이슈가 생길 때까지 씨앗 경합에서 빼는 것을 권장한다 — 현재 상태로는 매일 밤 동일한 재검증 사이클이 반복된다.
- **오케스트레이터 재량 사용 고지**: 이번 밤은 트리거 프롬프트의 "empty queue → audit, never idle" 정신을 확장 적용해 planner/architect 단계로 알려진-불가능한 씨앗(`M-052`)을 밀어넣지 않고 오케스트레이터가 사전에 제외·재시딩했다. `.claude/agents/planner.md`·`architect.md`는 이런 "3일 연속 확정 차단" 상황에서의 재시딩 절차를 명시하지 않는다 — 문서에 없는 처리이므로 사람이 검토해 이 재량이 적절했는지, 혹은 규칙으로 명문화할지 판단이 필요하다.
- **다음 밤 후보**: `M-043`(culture_info 산문 백필, low, 요청제한 대응 필요·자체적으로 복잡도 있음) · `M-054`(모바일 탐색 로딩 스켈레톤, low, 소규모). scope 겹침이 없어 각각 단독 클러스터가 될 가능성이 높다.
