# Nightly Report — 2026-08-02

## TL;DR

- 백로그 게이트: `docs/backlog/backlog.yml`의 `status:todo`가 14건(≥2) → **구현 모드**로 정상 진행.
- high 우선순위 이슈가 M-025/M-026/M-027/M-030/M-034/M-035 총 6개 동률 → tie-break 원칙(동순위 시 id 오름차순)대로 **M-025**를 정상적으로 집었다(어젯밤 2026-08-01처럼 tie-break를 벗어날 사유가 없었다 — dev HEAD가 이미 깨끗했음).
- planner가 `dev@978951e` 기준 실코드로 재검증한 결과, M-025 notes의 주장이 100% 사실로 확인됐다: `apps/mobile/app/opportunity.tsx:17`의 `catalog.find((x) => x.id === id) ?? catalog[0]`가 카탈로그가 비어있지 않은 한 not-found 분기를 도달 불가능하게 만들어, 존재하지 않는 id로 진입해도 무작위(첫 번째) 활동이 마치 요청한 활동인 것처럼 표시됐다. `onShare`도 공유 텍스트에 딥링크 URL이 없었다.
- `apps/mobile/hooks/useOpportunity.ts`(신규)를 웹의 `apps/web/src/hooks/useOpportunity.ts` 패턴 그대로 이식하고, `apps/mobile/data/opportunities.ts`에 `fetchOpportunityById` 래퍼·`OpportunityResult` 재노출을 추가, `opportunity.tsx`가 이 훅을 쓰도록 교체, `onShare` 메시지에 `${SITE_URL}/opportunity?id=${o.id}` 딥링크를 포함시켰다. 신규 테스트 2파일(훅 5건 + 화면 7건, 이 중 "카탈로그 2건·미스매치 id일 때 catalog[0] 제목이 렌더되지 않음"을 명시적으로 검증하는 회귀 테스트 포함).
- 검증: **typecheck 클린(4패키지)**, **core 215/mobile 36/web 122 테스트 전부 통과**(mobile +12 신규: 훅 5 + 화면 7), diff가 계획된 5개 파일(`apps/mobile` 내부만)과 정확히 일치, 시크릿 매치 0건, `supabase/` 무변경. qa 판정: **PUSH GATE: PASS**.
- 이번 구현은 `origin/dev` 위에 직접 쌓인 트렁크 커밋(`e4f0b58`)이다. 이 리포트 자체도 같은 방식으로 `dev`에 직접 커밋되며, PR은 열지 않는다.

## 집은 이슈

- **M-025** — 모바일 활동 상세: id 미스매치 시 엉뚱한 활동 표시 + 공유에 딥링크 URL 누락 (priority: high, scope: `apps/mobile`, status: todo → **doing**)
- planner가 `dev@978951e` 기준 실코드 대조로 done_when 4개 항목이 전부 실제 미충족 상태임을 확인(노트의 주장과 100% 일치):
  - `apps/mobile/app/opportunity.tsx:17`이 `catalog.find((x) => x.id === id) ?? catalog[0]`로, 카탈로그가 비어있지 않은 한 `if (!o)` not-found 분기가 도달 불가능.
  - `onShare`(당시 47-49행)가 `RNShare.share({ message: ... })`에 활동 제목만 넣고 URL이 전혀 없음.
  - 웹은 이미 `apps/web/src/hooks/useOpportunity.ts` + `packages/core/src/catalog.ts:188-205`의 `fetchOpportunityById`로 단건 조회 패턴을 갖고 있어 이관 대상 구현이 이미 존재함(신규 core 로직 불필요).
  - 웹은 `apps/web/src/lib/kakao.ts`의 모든 공유 폴백 경로가 URL을 포함하는데 모바일만 빠져 있어 플랫폼 간 기능 격차였음.

## 아키텍처 검토

- architect 판단: **frontend-impl 단독**으로 충분(`packages/core`의 `fetchOpportunityById`는 이미 구현·테스트·export돼 있어 core 변경 불필요, `supabase/` 무관). FSD 경계 위반 없음 — `packages/core → apps/mobile` import 방향은 기존 `fetchOpportunities`와 동일하게 이미 확립된 패턴.
- 파일 계획 확정(5개, 모두 `apps/mobile` 내부):
  1. `apps/mobile/data/opportunities.ts`(수정) — 웹 `apps/web/src/data/opportunities.ts`의 래퍼 패턴 그대로 미러링.
  2. `apps/mobile/hooks/useOpportunity.ts`(신규) — 웹 훅 로직 그대로 이식(캐시 우선 조회 → 미스 시 단건 네트워크 폴백, 언마운트 후 setState 방지 가드 포함).
  3. `apps/mobile/hooks/useOpportunity.test.ts`(신규, **architect가 planner 원안에 추가**) — 웹 훅 테스트를 미러링하되 모바일 기존 컨벤션(`vi.hoisted` 가변 state + `useAppStore` 셀렉터 shim, `useEnsureCatalog.test.ts` 패턴)을 따름. 훅 단위 테스트가 없으면 화면 테스트가 이중 책임을 지고 언마운트 경쟁조건 가드가 직접 검증되지 않는다는 이유로 추가.
  4. `apps/mobile/app/opportunity.tsx`(수정) — `catalog.find(...) ?? catalog[0]` 제거하고 `useOpportunity(id ?? null)`로 교체, 로딩 분기(`ActivityIndicator`, `loading.tsx` 컨벤션과 동일 토큰) 추가, 기존 not-found 블록은 카피 변경 없이 조건만 정확해짐, `onShare`에 `SITE_URL`(`EXPO_PUBLIC_SITE_URL` env, 기본값 `https://motungi.app`) 딥링크 삽입.
  5. `apps/mobile/app/opportunity.test.tsx`(신규) — 캐시 히트·**id 미스매치 회귀 케이스**(catalog[0] 제목 미노출을 명시적으로 assert)·not-found→탐색 이동·id 없음·로딩 상태·공유 URL 포함·공유 실패 무시, 총 7건.
- 범위 밖 명시: `apps/web`(참조만), `useEnsureCatalog`/`useAppStore`(무변경), `supabase/*`(무관), 웹의 카카오 SDK/`navigator.share` 다중 폴백을 모바일에 이식하지 않음(RN `Share.share` 네이티브 시트에 URL 텍스트만 포함하면 충분, 범위 밖 리팩터 방지).
- 테스트 전략: unit(훅) + component(RTL, 모바일 기존 mocking 컨벤션) 레벨. e2e 불필요(로컬 카탈로그·훅 계약 수준 버그).

## 변경 내용

5개 파일 변경(커밋 `e4f0b58`, 418줄 추가/7줄 삭제), 모두 `apps/mobile` 내부:

- `apps/mobile/data/opportunities.ts` — `OpportunityResult` 재노출 추가, `fetchOpportunityById(id)`가 core의 `fetchOpportunityById(supabase, id)`를 위임 호출하는 래퍼 신설.
- `apps/mobile/hooks/useOpportunity.ts`(신규) — `useOpportunity(id: string | null)` 훅. 카탈로그 스토어에서 캐시 우선 조회, 없으면 `fetchOpportunityById` 네트워크 폴백, 언마운트 후 setState 방지 가드 포함. `idle|loading|ok|empty|error|unconfigured` 상태 반환.
- `apps/mobile/hooks/useOpportunity.test.ts`(신규) — 캐시 재사용(재조회 없음)·캐시 미스 네트워크 조회·id 없음·존재하지 않는 id→empty·언마운트 취소 경쟁조건, 5건.
- `apps/mobile/app/opportunity.tsx` — `catalog.find(...) ?? catalog[0]` 및 미사용 `catalog` 셀렉터 제거, `useOpportunity` 훅 사용으로 교체. 로딩 분기(`ActivityIndicator size="large" color={C.primary}`, `loading.tsx`와 동일 토큰) 신설. `onShare` 메시지에 `${SITE_URL}/opportunity?id=${o.id}` 딥링크 추가(`SITE_URL = process.env.EXPO_PUBLIC_SITE_URL ?? "https://motungi.app"`), 기존 `.catch(() => {})` 에러 삼킴 유지.
- `apps/mobile/app/opportunity.test.tsx`(신규) — 캐시 히트 렌더·**id 미스매치+카탈로그 비어있지 않음 회귀 케이스**(not-found 텍스트 노출 AND catalog[0] 제목 미노출을 동시 assert)·not-found→`router.replace("/explore")`·id 없음·로딩 상태(`ActivityIndicator`)·공유 메시지 URL 포함(`RNShare.share` spy)·공유 실패 시 예외 미전파, 7건.

범위 밖 파일(`packages/core`, `apps/web`, `supabase/`, `.env`)은 무변경임을 `git diff --stat 978951e e4f0b58`로 확인.

## 검증

qa가 실제로 돌린 명령과 그 출력(리뷰어가 재확인):

```
$ git show --stat e4f0b58
 apps/mobile/app/opportunity.test.tsx     | 187 ++++++++++++++++++++++++++++
 apps/mobile/app/opportunity.tsx          |  34 +++++-
 apps/mobile/data/opportunities.ts        |  16 ++-
 apps/mobile/hooks/useOpportunity.test.ts | 124 ++++++++++++++++++
 apps/mobile/hooks/useOpportunity.ts      |  64 +++++++++
 5 files changed, 418 insertions(+), 7 deletions(-)
```

```
$ npx turbo run typecheck --force   (캐시 우회, 4패키지)
Tasks:    4 successful, 4 total
exit 0
```

```
$ npx turbo run test --force   (캐시 우회)
@motungi/core:   215/215 passed (13 files)
@motungi/mobile:  36/36 passed (10 files, +12 신규: useOpportunity.test.ts 5 + opportunity.test.tsx 7)
@motungi/web:    122/122 passed (16 files)
Tasks:    3 successful, 3 total
exit 0
```

```
$ npx vitest run app/opportunity.test.tsx hooks/useOpportunity.test.ts   (apps/mobile, 격리 재실행)
2 files, 12 tests, 전부 통과
```

- id 미스매치 회귀 테스트(`opportunity.test.tsx:114-126`)를 qa가 직접 읽어 확인: 카탈로그에 2건("op-1","op-2")이 있고 요청 id가 `"nope"`일 때 not-found 텍스트("활동을 찾을 수 없어요")가 렌더되고 **catalog[0]의 제목("망원 한강 러닝 클래스")이 렌더되지 않음**을 명시적으로 assert — 단순히 not-found 텍스트 존재만 확인하는 게 아니라 버그 자체(잘못된 활동 표시)가 고쳐졌음을 증명하는 테스트.
- onShare 테스트(`opportunity.test.tsx:158-171`)도 qa가 직접 읽어 확인: `RNShare.share` spy의 호출 인자 `message`에 `/opportunity?id=op-1` 문자열이 포함됨을 assert.
- diff 오염 검사: `git diff --name-only 978951e e4f0b58` → 계획된 5개 파일만, `supabase/` diff 빈 값 확인.
- 시크릿 grep(`SUPABASE_SERVICE_ROLE`/`SECRET`/`API_KEY`/`password`/`token`): 매치 0건.
- QA 판정: **"PUSH GATE: PASS — safe to push e4f0b58 to origin/dev."**
- M-025 done_when 4개 항목 전부, 구현자 주장이 아니라 qa의 실제 diff 정독 + 명령 재실행 출력 대조로 ✅ 확인됨.

## 리스크 & 후속

1. **네트워크 폴백 비용**: 딥링크로 직접 진입하거나 로컬 300건 배치에 없는 id로 접근할 때만 `fetchOpportunityById` 네트워크 왕복이 1회 추가된다. `useEnsureCatalog`가 이미 네트워크 요청을 하는 패턴이라 새로운 리스크는 아니지만, 실기기에서 지연 체감은 다음 밤 모니터링 대상.
2. **M-025는 tie-break 상 다음 후보였을 M-026(high, ingest Edge Function 무인증)이 여전히 todo로 남아 있다** — 보안 관련 이슈라 우선순위가 높다. 다음 밤 tie-break 정상 진행 시 M-026이 유력 후보.
3. status는 `todo → doing`으로만 갱신했다 — `done` 승격은 사람의 최종 리뷰 몫이다.
4. **문서 모순 기록 없음**: 이번 밤은 트리거 프롬프트와 repo 문서(`CLAUDE.md`, `.claude/agents/*.md`, `docs/nightly/PIPELINE.md`, `.claude/rules/workflow/nightly-pipeline.md`) 사이에 발견된 모순이 없었다 — 모두 dev 트렁크 모델·qa 푸시 게이트·audit 게이트 조항이 일치했다(2026-07-24 `M-024`에서 이미 정정 완료된 상태 유지 확인).

## dev 직접 커밋 (PR 없음)

이번 밤도 트렁크 기반 모델을 따랐다: 브랜치를 파지 않고 `origin/dev` 위에 직접 커밋·push하며 PR은 열지 않는다. 구현 커밋(`e4f0b58`)과 이 리포트·backlog 커밋 모두 `dev`에 직접 쌓였고, qa PASS 판정에 따라 `git push origin dev`로 push한다. `dev → main` 승격은 사람이 주기적으로 직접 판단하며, 이는 야간 파이프라인의 역할이 아니다 — main은 이번 밤에도 건드리지 않았다.
