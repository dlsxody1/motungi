# Nightly Report — 2026-09-07

## TL;DR

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `docs/backlog/backlog.yml`의 `status: todo`는 **6건**(M-052/M-097 medium, M-079/M-088/M-092/M-093 low)이었다 — `todo ≥ 2`이므로 구현 모드로 진행했다.
- **연속 report-only 경보 해당 없음.** 직전 밤(09-06)은 M-091/M-096이 shipped된 정상 구현 밤이었다(커밋 `cdb2308`). 스트릭 0 — 오늘 밤도 shipped로 이어져 스트릭은 계속 0이다.
- **M-052, 26일 연속 동일 사전제외 (계속).** 08-13부터 오늘(09-07)까지 26일 연속 씨앗 단계에서 재검증 후 제외. (아래 §리스크&후속에 사람 개입 요청 유지, 날짜 산정은 §M-052 섹션 참조 — 계산을 직접 다시 해 확인함.)
- **클러스터 = M-097 단독, architect가 즉시 분리해 M-098로 shipped.** M-097의 `done_when`은 서로 무관한 두 절반(neighborhoods 좌표 백필 vs opportunities.dong_name 표기 정규화)이었다. 좌표 백필은 이 sandbox에서 blocked-human으로 판정돼 M-097에 남고(todo 유지), 표기 정규화 3개 done_when은 신규 이슈 **M-098**로 분리해 오늘 밤 완료했다.
- `bash scripts/gate.sh` **PASS**(qa 독립 실행 + 리뷰어가 재차 독립 재실행, 아래 §검증에 실제 출력 인용) — typecheck 4/4 · test(core 391 · mobile 159 · web 381) · lint clean · secrets clean.
- **되돌린 이슈 없음.** 미룬 이슈: M-079/M-088/M-092/M-093(low, scope 겹침 0) — M-052는 blocked-human으로 계속 제외.

## 집은 클러스터

- **씨앗 선정**: `status: todo` 6건 중 priority 최상위는 `medium`(M-052·M-097, high 없음). id 오름차순 최소는 M-052지만, 26일 연속 재확인된 blocked-human 사유(§리스크&후속)로 씨앗에서 사전 제외. 다음 medium인 **M-097**을 씨앗으로 선정.
- **M-097 내부 분리(스코프 겹침이 아니라 done_when 내부 무관성)**: planner·architect가 M-097의 `done_when` 4개를 검토한 결과, 1번(`neighborhoods.coord_level='dong'` 좌표 백필)과 2~4번(`opportunities.dong_name` 표기 정규화 + 적재 시점 적용 + 마이그레이션 추가전용)이 서로 다른 테이블·다른 성격의 문제임을 확인했다 — 전자는 지리 데이터 조달(외부 API/데이터셋 필요), 후자는 순수 문자열 처리(기존 `normalizeGu` 로직을 배선만 하면 됨). 하나로 묶여 있으면 조달 불가 절반 때문에 배선만 하면 되는 절반까지 todo에 갇힌다.
  - **1번(좌표 백필)**: 아래 §이슈별 결과에서 blocked-human으로 기각, M-097에 남기고 todo 유지.
  - **2~4번(표기 정규화)**: 실제로 shippable했다. architect 승인으로 **신규 이슈 M-098**로 분리해 오늘 밤 클러스터의 유일한 작업 대상으로 진행.
- **다른 todo와의 scope 겹침**: M-098의 실제 scope(`packages/core/src/adapters/gu-fallback.ts`, `supabase/functions/ingest/index.ts`, `supabase/migrations`)는 나머지 low priority todo(M-079/M-088/M-092/M-093, 전부 `packages/tokens`·`packages/core/src/adapters/{seoul-jobs,util}.ts` 등 무관 파일)와 겹치지 않아 클러스터에 추가하지 않았다. app-fallback도 적용 대상이 아니다 — M-052 제외 이후 나머지 todo 중 M-097/M-098과 같은 priority(medium)가 없기 때문(폴백은 "파일 겹침 0 + 동일 priority"가 조건인데 medium이 이제 M-097/M-098 자신뿐).
- **상한 3개 중 1개만 참** — 인원 부족이 아니라 애초에 겹치는 후보가 없었다(scope 겹침 0). 상한을 채우려 low priority를 끌어오지 않았다.
- **미룬 이슈**: M-079·M-088·M-092·M-093(low, 다음 밤 씨앗 후보). M-052는 blocked-human으로 계속 제외.

## 이슈별 결과

### M-097 — 부분 기각(좌표 백필), todo 유지
`done_when` 1번(neighborhoods 426행 중 대다수 `coord_level='dong'`, 같은 구 안에서 좌표가 서로 다름)을 planner·architect가 실제로 조사했다(가짜 데이터 없이):
- `.env`가 이 sandbox에 없다(`.env.example`만 존재, 모든 값 공란) — data.go.kr/Seoul Open API 인증키가 없다.
- repo 안에 벤더링된 지오코딩 데이터셋이 없다.
- 일반 outbound internet이 프록시 정책으로 막혀 있다 — 리뷰어가 재확인: `curl -m 8 https://apis.data.go.kr/` → 연결 자체가 거부됨(organization policy에 의한 차단, `000`/`connect_rejected` 패턴).
- `scripts/gen-neighborhoods.ts`(1-29행) 자신의 헤더 주석이 이미 인정하는 사실: 현재 426행 중 418행이 `coord_level='sigungu'`(구청 위치 근사)이고, 실측 좌표를 가진 건 `SEED_OVERRIDES`(69-78행)의 8행(마포구 4 + 성동구 4)뿐이다. `coord_level='dong'`은 이 코드베이스 어디에도 아직 한 번도 쓰인 적이 없다.
**판정: blocked-human.** 그럴듯해 보이는 좌표를 지어내는 건 현재의 정직한 근사치(gu-center 근사임을 이미 문서화한 상태)보다 더 나쁘다고 명시적으로 기각했다 — 사람이 데이터 출처(예: 행안부 행정동 중심좌표 공개 데이터, 또는 data.go.kr 인증키 발급)를 제공해야 진행 가능. M-052와 같은 등급의 차단이다.
- **backlog.yml 갱신**: M-097의 `done_when`을 이 1개 항목만 남기고 나머지 3개는 M-098로 분리, 다운 노트에 blocked-human 사유·증거를 기록. `status: todo` 유지(미착수/차단 — doing으로 바꾸면 다음 밤이 재시도 대상에서 빠진다).

### M-098(신규) — shipped (`0f4c0dc`)
M-097의 원래 done_when 2~4번(opportunities.dong_name 표기 정규화)을 분리해 오늘 밤 완료.
- **문제**: 적재 소스가 dong_name 표기를 통일하지 않아 같은 구가 "종로구"와 "서울 종로구"로 분열돼 있다(0018 마이그레이션 주석이 이미 확인: 종로구 288행 · 서울 종로구 242행). `packages/core`에 `normalizeGu`가 이미 존재했지만(view.ts, private 복제본 gu-fallback.ts) 적재 파이프라인(`supabase/functions/ingest/index.ts`)에 배선돼 있지 않았다.
- **architect의 scope 교정**: 원래 M-097 scope에 있던 `supabase/functions/ingest/adapters.ts`는 실제로 손댈 필요가 없었고(이 파일은 dong_name 원본을 만들 뿐 정규화 위치가 아님), 대신 실제 choke point인 `supabase/functions/ingest/index.ts`(원 scope에 없었던 파일)가 정규화를 적용해야 할 지점이었다. 이 정정을 거쳐 M-098의 scope를 실코드 기준으로 다시 잡았다.
- **구현(backend-impl, 5개 파일, 1커밋)**:
  - `packages/core/src/adapters/gu-fallback.ts` — 기존 private `normalizeGu`를 `export`로 바꿈(로직 무변경, 4줄짜리 순수 문자열 처리).
  - `packages/core/src/adapters/gu-fallback.test.ts` — `normalizeGu("종로구") === normalizeGu("서울 종로구")` 직접 단언 테스트 추가.
  - `supabase/functions/ingest/index.ts` — `upsertRows()`의 최종 payload에서만 `normalizeGu`를 적용. `applyGuCoordFallback`은 raw(시/도 접두어 포함) `dong_name`으로 `neighborhoods.sigungu`와 매칭해야 하므로 그 호출 **이후**에 정규화한다 — 순서를 바꾸면 그 매칭이 깨진다.
  - `supabase/migrations/0020_opportunities_dong_name_normalize.sql`(신규, 추가만) — 기존 행 백필 UPDATE. 서울/인천 중구 명칭 충돌 캐비어트를 SQL 주석에 명시(아래 참조).
  - `packages/core/src/adapters/index.ts` — `export * from "./gu-fallback"`를 명시적 재export 목록으로 좁혀 `normalizeGu`를 **제외**했다. `view.ts`가 이미 같은 이름을 export하는데 `export *`로 겹치면 `index.ts`에서 **TS2308(모호한 재export)로 typecheck가 실제로 깨진다** — 스타일 선택이 아니라 실패 확인 후 조치. 두 실 소비처(`ingest/index.ts`, `gu-fallback.test.ts`)는 leaf 파일을 상대경로로 직접 import하므로 barrel 노출이 필요 없다.
- **리뷰어 직접 재확인(rubber-stamp 아님)**:
  - `packages/core/src/index.ts`가 `export * from "./adapters"` 다음에 `export * from "./view"`를 두고 있어, `adapters/index.ts`가 `normalizeGu`를 더는 재export하지 않는 지금은 패키지 루트 `@motungi/core`도 모호성 없이 `view.ts`의 `normalizeGu` 하나만 노출한다 — 실제로 이 심볼을 쓰는 4곳(`apps/web/src/components/hero-carousel.tsx`, `apps/web/src/app/explore/(list)/page.tsx`, `apps/web/src/app/explore/[gu]/page.tsx`, `apps/mobile/app/(tabs)/explore.tsx`)은 전부 `@motungi/core`에서 import하고 있어 아무 영향이 없다.
  - `git diff cdb2308 0f4c0dc --stat`로 위 5개 파일 외 변경 없음 확인. `supabase/migrations/0001`~`0019`는 무변경. `0020`은 추가전용 UPDATE 하나뿐이고 DROP/DELETE 없음.
  - `bash scripts/gate.sh`를 리뷰어가 다시 독립 실행 — 아래 §검증 참조, PASS.
- **서울/인천 중구 캐비어트(architect 판정, 새 리스크 아님)**: 접두사 제거만으로는 "인천 중구"와 "서울 중구"가 둘 다 "중구"로 합쳐진다(인천·서울 모두 실제 "중구"라는 자치구를 가짐, 다른 구 이름은 겹치지 않음). 이미 `0018` 백필과 `gu-fallback.ts`의 `applyGuCoordFallback`이 매칭용으로 같은 위험을 감수하고 있던 것과 동일 등급이라, 방어적 재구분 로직을 추가하지 않고(architect 결정) `0020` SQL 주석에 캐비어트만 명시했다. 필요해지면 별도 이슈로 다룬다.
- **done_when 3개 전부 충족.** 되돌림 없음.

## 아키텍처 검토

- **M-097 분리 결정**: `done_when` 내부에 스코프·난이도가 무관한 두 절반이 섞여 있는 걸 방치하면, 조달 불가한 절반(좌표 백필) 때문에 실제로 shippable한 절반(표기 정규화)까지 영구히 todo에 묶인다. architect가 분리를 승인하고 `M-098`을 새로 등재하는 방식으로 처리 — M-097 자체의 id·history(위치 지도류 blocked-human 선례와의 연속성)는 보존하면서, 완료 가능한 작업은 별도로 흘려보냈다.
- **scope 교정**: 원 M-097 scope의 `supabase/functions/ingest/adapters.ts`는 손댈 필요가 없다는 걸 구현 전에 재확인했고, 실제 choke point `supabase/functions/ingest/index.ts`를 scope에 넣었다. planner가 처음 짠 scope를 architect가 실코드 대비로 다시 검증한 사례.
- **담당·경계**: `normalizeGu`는 계속 `packages/core`(순수 로직) 소유. Edge Function(`supabase/functions/ingest`)은 그 함수를 배선만 하는 쪽 — 로직 복제 없이 `packages/core`를 상대경로로 import(Deno 배포 제약상 barrel을 거치지 않는 기존 패턴, `gu-fallback.ts` 헤더 주석에 이미 문서화돼 있던 관행을 그대로 따름).
- **테스트 전략**: 새 export에 대한 최소 단언(정규화 동치성) 하나만 추가 — 로직 자체는 기존 `normalizeGu`/`view.ts` 쪽 테스트가 이미 커버하므로 중복 테스트를 만들지 않았다.
- **barrel 충돌은 발견된 실제 버그다**: `adapters/index.ts`에서 `normalizeGu`를 export했다면 `packages/core/src/index.ts` 레벨에서 `view.ts`의 동명 export와 충돌해 TS2308로 typecheck가 깨졌을 것 — qa 게이트가 이걸 잡아냈고, backend-impl이 barrel에서 배제하는 방식으로 해결했다. 이건 스타일이 아니라 정합성 이슈였다.

## 변경 내용

커밋 `0f4c0dc` (`fix(core): M-098 opportunities.dong_name 표기 정규화(적재 시점+백필)`), 5개 파일:

| 파일 | 변경 |
|---|---|
| `packages/core/src/adapters/gu-fallback.ts` | private `normalizeGu` → `export` (로직 무변경, 1줄) |
| `packages/core/src/adapters/gu-fallback.test.ts` | `normalizeGu` 직접 단언 테스트 추가 |
| `packages/core/src/adapters/index.ts` | barrel에서 `normalizeGu` 제외, 나머지 심볼은 명시적 재export로 전환 |
| `supabase/functions/ingest/index.ts` | `upsertRows()` 최종 payload에 `normalizeGu(dong_name)` 적용 (`applyGuCoordFallback` 이후) |
| `supabase/migrations/0020_opportunities_dong_name_normalize.sql` | 신규, 추가전용 백필 UPDATE + 중구 명칭 충돌 캐비어트 주석 |

backlog.yml: M-097 `done_when` 축소 + blocked-human 노트 추가(`status: todo` 유지), M-098 신규 등재(`status: doing`).

## 검증

qa가 실행한 `bash scripts/gate.sh`(base `origin/dev`) 결과와, 리뷰어가 독립적으로 재실행한 결과가 일치한다(PASS):

```
── typecheck   ✅ typecheck
── test        ✅ test   (core 391 · mobile 159 · web 381, 전부 passed)
── lint        ✅ lint   (`next lint --dir src` — No ESLint warnings or errors)
── secrets     ✅ secrets (시크릿 패턴 없음)

GATE: PASS — push 허용
```

추가로 리뷰어가 직접 확인한 것:
- `git diff cdb2308 0f4c0dc --stat` → 위 5개 파일만 변경, `supabase/migrations/0001`~`0019` 무변경.
- `0020` 마이그레이션에 DROP/DELETE 없음 — 추가전용 UPDATE 하나.
- `normalizeGu`의 4개 실 소비처(`apps/web` 3곳, `apps/mobile` 1곳) 전부 `@motungi/core` 패키지 루트에서 import — barrel 배제 이후에도 모호성 없이 `view.ts`의 심볼로 resolve됨을 소스 직접 확인.
- 커밋 메시지가 Conventional Commits(`fix(core):`) + 백로그 id(`M-098`)를 포함 — 추적성 규율 충족.

## 리스크 & 후속

### M-052 — 26일 연속 동일 사전제외 (계속, 사람 개입 필요)
`M-052`(모바일 상세 화면 지도·산책로 폴리라인)는 **2026-08-13부터 오늘(09-07)까지 26일 연속** 씨앗 단계에서 재검증 후 사전 제외되고 있다(리뷰어가 날짜를 직접 다시 계산해 확인 — 08-13~08-31이 19일, 09-01~09-07이 7일, 합 26일). 오늘 밤 재검증도 동일: `grep -i map apps/mobile/package.json` 공백, `apps/mobile` 코드 전체에 `MapView`/`react-native-maps` 참조 0건, `.env.example`·`security-policy.md`에 지도 관련 항목 없음 — 변화 없음. 08-13이 제시한 3개 구현 경로(react-native-maps 신규 네이티브 의존성 / NAVER 정적지도 백엔드 프록시 신설 / 키리스 서드파티 정적지도) 모두 사람의 정책 승인 없이는 무인 야간 파이프라인이 뚫을 수 없다. 08-21부터 여러 밤이 이미 3택1(경로 승인 / priority 하향 / `blocked-human` 상태 도입)을 요청했고 09-05·09-06은 PushNotification까지 보냈다 — 아직 처리되지 않았다. 오늘 밤은 추가 알림을 보내지 않는다(직전 두 밤이 이미 보냈고, 상태 변화가 없다).

### M-097도 이제 M-052와 같은 blocked-human 등급 — backlog 상태 스키마에 정식 도입이 필요하다는 신호가 하나 더 늘었다
오늘 밤 M-097의 좌표 백필 절반이 M-052와 완전히 같은 사유(사람의 데이터/정책 제공 없이는 무인 파이프라인이 뚫을 수 없음)로 기각됐다. 지금 backlog.yml 스키마엔 `todo | doing | done` 세 상태만 있고 `blocked-human`이 없어서, 두 이슈 모두 매일 밤 씨앗 경쟁에는 계속 들어오고(`medium` priority가 유지되는 한) 매번 재검증 로그만 쌓인다. M-052 하나였을 때도 여러 밤(08-17·08-24·08-28 등)이 이미 이 상태 도입을 건의했는데, 이제 두 번째 사례가 생겼다는 것은 이게 M-052만의 특이 사례가 아니라 이 파이프라인이 반복적으로 마주치는 패턴이라는 뜻이다. **사람이 판단할 것을 다시 권고**: (a) backlog.yml 스키마에 `status: blocked-human`을 정식 도입해 씨앗 경쟁에서 자동 제외하거나, (b) M-052·M-097(좌표 백필)의 priority를 낮춰 자연스럽게 밀려나게 하거나, (c) 둘 중 하나에 실제 승인/데이터를 제공해 언블록. 지금처럼 매 밤 재검증 로그만 backlog.yml에 계속 쌓이면 그 자체가 파일을 무겁게 만든다(현재 2500줄 근접).

### 서울/인천 중구 명칭 충돌 (신규 리스크 아님, 관찰만)
`0020` 마이그레이션과 `gu-fallback.ts`의 접두사 제거 규칙은 "인천 중구"와 "서울 중구"를 둘 다 "중구"로 합친다. 오늘 밤 새로 만든 위험이 아니라 `0018`·`applyGuCoordFallback`이 이미 감수하던 것과 동일 등급이며, SQL 주석에 캐비어트를 남겨뒀다. 실제 데이터에서 두 지역의 활동이 뒤섞여 노출되는 사례가 관찰되면 별도 이슈로 다룰 것.
