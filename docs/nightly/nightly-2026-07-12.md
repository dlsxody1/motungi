# Nightly Report — 2026-07-12

## TL;DR

- 백로그 최우선 이슈는 **M-002**(high, 목업→Supabase 읽기 전환, `apps/web`)였으나, 동일 스코프의 미병합 draft PR이 이미 2개(#6, #7) 열려 있어 세 번째 중복을 피하고자 **M-003**(medium, `packages/core` scoring.ts 엣지케이스 테스트 보강)으로 전환.
- `packages/core/src/scoring.test.ts`에 cost·time·distance 3축 엣지케이스 테스트 12개(신규 assertion 기준)를 추가. **로직 코드(scoring.ts)는 수정 없음** — 손계산 검증 결과 기존 구현에 버그 없음.
- 검증: `pnpm --filter @motungi/core test` **71/71 통과**, `pnpm --filter @motungi/core typecheck` **에러 0**. qa가 브랜치를 직접 체크아웃해 재실행하고, 12개 수용 기준 값을 전부 수동 재계산해 대조 — 전부 일치.
- 변경 파일은 `packages/core/src/scoring.test.ts` 1개, +75줄 (diff 확인 완료, 다른 파일 변경 없음).
- PR: draft, main 대상 — 오케스트레이터가 GitHub MCP로 생성 예정 (아래 PR 섹션 참조).

## 집은 이슈

- **M-002** — 목업 데이터 → Supabase 읽기 전환 (웹) (priority: high, scope: `apps/web`, status: todo)
  - planner가 백로그에서 최우선으로 확인했으나, 아래 사유로 이번 밤은 **스킵**.
- **M-003** — scoring.ts cost+time 가중치 엣지케이스 테스트 보강 (priority: medium, scope: `packages/core`, status: todo → **doing**)
  - planner가 M-002 대신 실제로 집은 이슈. 이번 밤 실행 대상.

## M-002를 스킵한 이유 (판단 근거)

- `apps/web` 스코프의 M-002를 다루는 **미병합 draft PR이 이미 2개** 열려 있음: `#6`(`nightly/2026-07-10`), `#7`(`nightly/2026-07-11`).
- 두 PR 모두 사실상 동일 작업(vitest 인프라 + `fetchOpportunities` 4상태 테스트 + 카탈로그 렌더 테스트)을 다루고 있고, 실데이터 바인딩 로직 자체는 이미 커밋 `7b180a6`에서 완료된 상태.
- 이 상태에서 세 번째로 같은 스코프를 또 건드리면 **중복 PR/충돌 위험**이 커짐 — draft PR 3개가 서로 다른 브랜치에서 동일 파일을 건드릴 경우 사람이 리뷰·머지할 때 병합 충돌 정리 비용만 늘어남.
- PR #7 리뷰어 메모에 "다음 백로그 후보: M-003"이 명시되어 있어, 이를 따라 M-003으로 전환.
- **M-002는 `docs/backlog/backlog.yml`에서 이번 밤 건드리지 않고 `status: todo`로 유지**. 사람이 기존 draft PR #6/#7 중 하나를 병합 또는 정리한 뒤, 다음 밤 파이프라인이 재평가하도록 남겨둠. (M-002를 `doing`으로 바꾸지 않은 이유: 이미 doing 상태나 다름없는 PR이 2개나 열려 있어 상태 필드로는 실제 상황을 더 잘 반영하기 어렵고, 사람의 정리가 선행돼야 하는 이슈이기 때문.)

## 아키텍처 검토

- architect가 `packages/core/src/scoring.ts`, `scoring.test.ts`를 실제 코드와 대조: `AFTERWORK_WINDOW={18,22}`, `COST_CEILING_KRW=50000` 등 상수가 실재함을 확인.
- FSD/의존성 방향 문제 없음 — `packages/core`는 apps에 의존하지 않는 순수 계층이므로 스코프(`packages/core`) 위반 없음.
- 담당은 backend-impl 단독으로 확정 (별도 프론트엔드 영향 없음).
- planner 스펙 예시 중 "부분겹침 20~23시=1시간"이 **산술 오류**임을 architect가 발견해 정정: 실제로는 `{21,23}` 구간에서 `overlapHours = min(22,23) - max(18,21) = 1`, `ratio = 1/(22-18) = 0.25` (planner가 원래 예로 든 20~23시 구간은 실제로 2시간 겹침이라 표현이 부정확했음). 정정된 기대값을 backend-impl에 전달.
- architect가 scoring.ts 실제 계산을 수동 트레이스한 결과 모든 수용 기준(AC)이 기존 구현과 일치 — **로직 수정 불필요, 테스트 전용 스코프**로 사전에 확정. 이 예측은 실제 구현·qa 검증에서도 그대로 들어맞음(코드 수정 0줄).

## 변경 내용

- `packages/core/src/scoring.test.ts` (+75줄, 신규 코드 수정 없음): 8개 신규 `it()` 블록 추가
  - **distance**: 집이 회사보다 가까운 반대 방향 min 적용(→1), home만/work만 있을 때 하위호환(→각 1), 앵커·좌표 모두 없을 때 중립값(→0.5)
  - **time**: 퇴근후 코어(18~22시) 완전일치(→1), 완전 무겹침(→0), 부분겹침 `{21,23}`(→0.25), `timeWindow` 없을 때 중립값(→0.5)
  - **cost**: 무료(0원) 만점(→1), `COST_CEILING_KRW`(50,000원) 이상 완전 0(경계값+초과값 둘 다)
- `packages/core/src/scoring.ts`: **변경 없음** — 기존 로직이 모든 신규 AC를 이미 충족함을 손계산으로 확인했기 때문.
- `git diff origin/main...HEAD --stat` 재확인 결과 위 1개 파일 외 변경 없음(리뷰어가 직접 재확인 완료).

## 검증

리뷰어가 직접 재실행한 결과(qa 판정과 일치):

```
$ pnpm --filter @motungi/core test
 RUN  v2.1.9 /home/user/motungi/packages/core

 ✓ src/scoring.test.ts (17 tests) 9ms
 ✓ src/adapters/seoul-jobs.test.ts (11 tests) 8ms
 ✓ src/view.test.ts (16 tests) 29ms
 ✓ src/adapters/seoul-culture.test.ts (6 tests) 7ms
 ✓ src/adapters/util.test.ts (12 tests) 7ms
 ✓ src/adapters/trail.test.ts (5 tests) 5ms
 ✓ src/adapters/culture-info.test.ts (4 tests) 6ms

 Test Files  7 passed (7)
      Tests  71 passed (71)
```

- `pnpm --filter @motungi/core typecheck`: 에러 0 (backend-impl, qa 양쪽 모두 확인).
- qa는 브랜치를 직접 체크아웃해 `pnpm install --frozen-lockfile` 성공을 확인한 뒤 위 테스트/타입체크를 재실행했고, `scoring.test.ts`를 직접 읽어 신규 12개 수용 기준 각각의 assert 값을 수동 계산으로 재검증 — **12개 전부 통과**.
- `git diff origin/main...nightly/2026-07-12 --stat`: `scoring.test.ts`만 변경됨을 qa·reviewer 양쪽에서 재확인. 시크릿 노출·파괴적 SQL 없음.
- 종합 판정: **통과 (pass)**.

## 리스크 & 후속

- **경미 지적 (qa 발견, 실패 아님)**: 기존(이번 diff 이전부터 있던) "work-closer" 테스트가 `toBeGreaterThan(0.9)`로 다소 느슨함. 실제 계산값은 정확히 `1`이므로 `toBe(1)`이 더 엄격한 단언이었을 것. 이번 diff 범위 밖이라 수정하지 않았으나, 다음 테스트 정리 시 함께 타이트닝 권장.
- **M-002 처리 필요**: 사람이 draft PR `#6`(`nightly/2026-07-10`)과 `#7`(`nightly/2026-07-11`) 중 하나를 검토·병합(또는 둘 다 정리)한 뒤에야 M-002를 다음 밤 파이프라인에 다시 태울 수 있음. 방치하면 계속 스킵될 위험.
- **HANDOFF.md 갱신**: 이전 밤(2026-07-10) 리포트에서 이미 지적된 `docs/HANDOFF.md` §2 stale 문제가 아직 해결되지 않음 — 후속 필요.
- **scoring.ts 로직 자체는 이번 밤 검증 대상이 아니었음**: 테스트만 보강했으므로, 실제 서비스 사용자 데이터로 스코어링 결과가 기대와 맞는지는 별도 통합/실사용 검증이 필요(범위 밖).
- **다음 밤 후보**: M-002(사람 정리 후 재평가) 또는 M-004(low, 진단 플로우 a11y 점검).

## PR

https://github.com/dlsxody1/motungi/pull/8 (draft, main 대상). 오케스트레이터가 GitHub MCP 도구로 생성했다 — 리뷰어는 `gh pr create` 실행 권한이 없어 브랜치 커밋 + push까지만 수행했다.
