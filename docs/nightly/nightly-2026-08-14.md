# Nightly Report — 2026-08-14

## TL;DR
클러스터 = **M-055 단독**(모바일 상세가 M-044 LLM 근거(useWhyReasons)를 소비하지 않음). `bash scripts/gate.sh`(typecheck·test·lint·시크릿 스캔, 4패키지 전체) **PASS**(typecheck 4/4 ✅, test core 263+mobile 91+web 279=633/633 ✅, lint ✅, secrets ✅) — **shipped, dev로 나갔다**(커밋 `d4e98ae`). revert 없음.

씨앗은 원래 `M-052`(모바일 지도·산책로 렌더, 유일한 medium todo)였으나, 08-13 야간이 이미 3개 구현 경로(react-native-maps 신규 네이티브 의존성 / NAVER 정적지도 백엔드 프록시 신설 / 키리스 서드파티 정적지도) 전부를 무인 실행 부적합으로 판정해 제외한 바 있다. 오늘 밤 그 판단이 여전히 유효한지 직접 재확인(`.env.example`·`security-policy.md`·`apps/mobile/package.json` 재검토, 08-13 이후 변경 없음)했고, 사람의 승인·키 제공이 없어 **동일하게 제외**했다. `M-052`는 `todo`로 유지된다.

연속 report-only 0회(§0단계 참조 — 08-10~08-13 전부 shipped) — 경고 불필요.

## 집은 클러스터
- 씨앗: `M-052`(priority medium, 유일한 medium todo) → **architect 단계에서 재차 제외**(사유는 위 TL;DR·아래 §아키텍처 검토).
- `M-052` 제외 후 파일 겹침으로 남는 유일한 후보가 `M-055`(scope 동일: `apps/mobile/app/opportunity.tsx`)였다 — 클러스터에 그대로 남겨 단독 진행.
- 나머지 todo(`M-038`[packages/core], `M-043`[supabase/functions/ingest, scripts/], `M-054`[apps/mobile/app/(tabs)/explore.tsx])는 `M-055`와 scope가 겹치지 않아 클러스터에 넣지 않았다. 상한 3개를 채우려고 낮은 priority를 끌어오지 않는다는 규칙(클러스터 규칙 §)을 따라 **패딩하지 않았다** — 1이슈 밤은 정상 결과.
- 상한 3개로 밀린 이슈는 없다(애초에 겹치는 후보가 1개뿐이었다).

## 이슈별 결과
| id | 결과 | 비고 |
|---|---|---|
| M-052 | **제외**(구현 안 함, `todo` 유지) | 08-13과 동일 사유 재확인 — 사람 승인 없이 채택 가능한 지도 구현 경로가 없음 |
| M-055 | **shipped** | 커밋 `d4e98ae`, dev push 완료, qa 게이트 PASS |

## 아키텍처 검토
**담당**: frontend-impl(mobile) 단독. `apps/mobile/hooks/useWhyReasons.ts`(신규) + `apps/mobile/app/opportunity.tsx`(수정) + 대응 테스트 2건.

**M-052 재제외 상세**: `docs/backlog/backlog.yml:1272-1278`(08-13 기록)이 이미 3개 접근을 다음 근거로 기각했다 — (1) `react-native-maps`는 신규 네이티브 의존성으로 Expo prebuild·EAS 빌드 설정에 영향을 줘 사람 승인 필요, (2) NAVER 정적지도 백엔드 프록시 신설은 스코프 초과(신규 API route + 시크릿 배선 필요), (3) 키리스 서드파티 정적지도는 미프로비저닝 + `security-policy.md:8`("NAVER 시크릿은 `/api/geo` 프록시 뒤에 숨긴다")의 NAVER-only 서버프록시 원칙과 어긋날 소지. 오늘 밤은 이 판단을 그대로 수용하지 않고 **직접 재검증**했다: `.env.example`에 지도 관련 신규 키가 추가되지 않았고, `apps/mobile/package.json`에 지도 의존성이 없으며, `security-policy.md`도 변경이 없다 — 08-13 이후 사람의 결정·제공이 전혀 없었다는 뜻이므로 동일하게 제외하는 것이 맞다고 판단했다. 다음 밤이 또 씨앗으로 집기 전에 사람이 위 세 옵션 중 하나를 승인하거나 새 옵션을 제시해야 이 반복이 끝난다.

**M-055 설계**: 모바일엔 `@tanstack/react-query`가 없다(`apps/mobile/package.json` 확인, 웹 `useWhyReasons.ts`는 이 의존성 기반) — 웹 훅을 그대로 옮기지 않고 `apps/mobile/lib/geo.ts`의 기존 패턴("상대경로 `/api`가 없으니 `EXPO_PUBLIC_WEB_ORIGIN` 경유 + plain useState/useEffect")을 재사용했다(M-045가 이미 쓴 전략, backlog notes 지시대로). FSD 경계 위반 없음 — `apps/mobile/hooks/`는 기존에도 `useOpportunity`·`useEnsureCatalog`가 있는 자리이고, `@motungi/core`의 순수 함수(`whyReasons`·`scoreOpportunity`·`timeRangeLabel`)만 가져다 썼다.

**Rules of Hooks 주의점**: `apps/mobile/app/opportunity.tsx`는 `!o`(로딩 중)일 때 조기 return한다. 웹 `opportunity-detail.tsx:74-77`이 "early return보다 앞에서 훅을 불러야 한다"고 주석으로 명시한 것과 동일한 이유로, `useWhyReasons` 호출을 조기 return **이전**으로 배치했다(초안에서 이후에 뒀다가 직접 재배치).

**테스트 전략**: 훅 레벨 단위테스트(`useWhyReasons.test.ts`, RTL `renderHook`)로 6가지 계약을 고정 — 즉시 규칙기반 반환(블로킹 없음) · `opp=null`이면 fetch 없음 · `WEB_ORIGIN` 미설정이면 fetch 없이 규칙기반 유지(오늘 실제 환경과 동일) · fetch 성공 시 LLM 산문 교체 · 서버 `fallback:true` 시 규칙기반 유지 · 네트워크 실패 시 규칙기반 유지(예외 전파 없음). 화면 레벨(`opportunity.test.tsx`)은 `useWhyReasons`를 얇게 목업해 화면 로직만 검증(기존 컨벤션 — `useOpportunity`·`useEnsureCatalog`도 동일하게 목업).

## 변경 내용
- **신규** `apps/mobile/hooks/useWhyReasons.ts` — 모바일판 "왜 맞을까요" LLM 근거 훅. `whyReasons()` 즉시 반환 + `EXPO_PUBLIC_WEB_ORIGIN`이 설정되고 `/api/why-reasons` 호출이 성공하면 LLM 산문으로 교체.
- **신규** `apps/mobile/hooks/useWhyReasons.test.ts` — 6케이스(위 §아키텍처 검토 참조).
- **수정** `apps/mobile/app/opportunity.tsx` — `whyReasons()` 직접 호출 → `useWhyReasons(o, answers, anchors)`로 교체, `anchors` 스토어 셀렉터 추가, 조기 return 이전으로 훅 위치 재배치.
- **수정** `apps/mobile/app/opportunity.test.tsx` — `@/hooks/useWhyReasons` 목업 추가, 스토어 mock state에 `anchors: {}` 추가(회귀 없음 확인 — 기존 15개 테스트 전부 그대로 통과).
- diff는 이슈 하나(M-055)에만 집중돼 있다 — 무관한 변경 없음.

## 검증
```
$ bash scripts/gate.sh
── typecheck
   ✅ typecheck
── test
@motungi/core:test:  Test Files  10 passed (10)  Tests  263 passed (263)
@motungi/mobile:test:  Test Files  17 passed (17)  Tests  91 passed (91)
@motungi/web:test:  Test Files  36 passed (36)  Tests  279 passed (279)
   ✅ test
── lint
   ✅ lint
── secrets
   (diff 없음)
   ✅ secrets

GATE: PASS — push 허용
```
- `pnpm --filter @motungi/mobile typecheck`, `pnpm --filter @motungi/mobile test -- opportunity`, `pnpm --filter @motungi/mobile test -- whyReasons` 개별로도 먼저 실행해 확인 후 전체 게이트를 돌렸다.
- 모바일엔 별도 `build` 스크립트가 없다(Expo, `apps/mobile/package.json` scripts에 `build` 항목 없음) — qa.md의 "프론트 변경이면 해당 앱 pnpm build"는 해당 없음.
- `main`이 `dev`보다 앞서 있는지 확인(`git log --oneline origin/dev..origin/main`) — 비어 있어 되받을 것 없이 그대로 진행했다.

## 리스크 & 후속
- **`M-052`(모바일 지도) — 이제 2일 연속 같은 이유로 제외됐다.** 사람이 다음 세 옵션 중 하나를 결정해야 이 반복이 끝난다: (1) `react-native-maps` 신규 네이티브 의존성 승인, (2) NAVER 정적지도 백엔드 프록시 신설 스코프 승인 + 배선, (3) 키리스 서드파티 정적지도 서비스 지정. 결정이 없으면 다음 밤도 같은 결론을 반복할 가능성이 높다 — 매 밤 재검증 자체는 규율(스테일 백로그 방지)이지만, 아무 진전 없이 반복되면 그것도 낭비다. **제안**: 사람이 결정하기 전까지는 `M-052`의 priority를 `low`로 낮추거나 별도 `blocked` 라벨을 두는 걸 고려(현재 backlog 스키마엔 `blocked` status가 없다 — 이것도 스키마 개선 후보).
- **`M-055` done_when 4번째 항목("실제 Gemini 응답 경로는 사람이 검증")은 미해결.** `GEMINI_API_KEY`(서버)와 `EXPO_PUBLIC_WEB_ORIGIN`(모바일) 둘 다 이 환경에 등록돼 있지 않다(.env.example 확인) — 에이전트는 키를 발급·등록할 수 없다. 두 값을 등록한 뒤 실기기/시뮬레이터에서 상세 화면 진입 → LLM 산문이 실제로 뜨는지 사람이 최종 확인해야 한다.
- **repo 문서 모순 발견 (이 트리거 프롬프트의 PRECEDENCE 규칙에 따라 기록)**: `.claude/agents/reviewer.md:16`은 "qa 게이트(`pnpm typecheck`+`pnpm test`)를 통과한 것만 dev에 올라간다"라고 적혀 있는데, `docs/nightly/PIPELINE.md:22`·`.claude/rules/workflow/nightly-pipeline.md:13,40-48`은 게이트가 `bash scripts/gate.sh`(typecheck+test+**lint**+**시크릿 스캔**)로 이미 2026-08-05에 단일화됐다고 명시한다. 이번 밤은 후자(더 최신·더 구체적인 문서, 그리고 이 트리거 프롬프트의 PRECEDENCE 원칙)를 따라 `gate.sh` 전체를 돌렸다. `reviewer.md:16`을 `gate.sh` 기준으로 갱신 필요.
- 다음 밤 후보(scope 겹침 없어 각각 단독 클러스터 후보): `M-038`(low, `packages/core` 미사용 export 정리), `M-043`(low, `culture_info` 산문 백필), `M-054`(low, 모바일 탐색 로딩 스켈레톤).
