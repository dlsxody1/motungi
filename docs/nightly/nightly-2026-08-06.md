# Nightly Report — 2026-08-06

## TL;DR
클러스터 = **M-035**(모바일 location.tsx 테스트 공백) + **M-045**(보관함이 카탈로그 창 밖 저장 id를 조용히 버림) + **M-046**(보관함이 실패를 "저장한 게 없음"으로 표시). 셋 다 `bash scripts/gate.sh`(typecheck·test·lint·시크릿 스캔)를 통과해 **셋 다 dev로 나갔다**. revert 없음. 연속 report-only 0회(§0 참조) — 경고 불필요.

## 0. 헛도는 밤 감지
`ls docs/nightly/`로 최근 노트를 직접 열어 확인: 2026-08-05(M-030+M-034 shipped), 08-04(M-027 shipped), 08-03(M-026 shipped), 08-02(M-025 shipped), 08-01(M-039 shipped) — 오늘 밤 직전까지 **5개 밤 연속 코드가 나갔다**. report-only(코드 무변경)로 끝난 가장 최근 밤은 2026-07-31(audit 모드, 14건 등재만 하고 코드는 안 건드림)인데, 그 다음 밤(08-01)부터 계속 구현이 나갔으므로 **연속 report-only 스트릭은 0**. 경고 문구 불필요.

## 집은 클러스터
- **씨앗**: `status: todo` 이슈 17건(≥2 → 구현 모드) 중 priority 최상위(high) 그룹 — `M-035`/`M-045`/`M-046`이 동순위(high)로 최소 id인 **M-035**.
- **파일 겹침 판정**: M-035의 `scope`는 `[apps/mobile]`로 앱 전체 단위라 파일·좁은 디렉토리 겹침 후보 0건(planner·architect 독립 재확인, 둘 다 0건 확인).
- **폴백 적용**: 후보 0건이므로 같은 앱 경계(`apps/mobile`) + 같은 priority(`high`)인 todo를 id 오름차순으로 최대 2개까지 추가 → **M-045**(`scope: [apps/mobile/app/(tabs)/saved.tsx, apps/mobile/hooks]`), **M-046**(`scope: [apps/mobile/app/(tabs)/saved.tsx]`) 둘 다 해당. 다른 high 우선순위 apps/mobile todo는 없어 상한 3건에 정확히 맞춰졌고, 상한 초과로 미룬 이슈는 없다.
- **참고**: M-045와 M-046은 서로 `apps/mobile/app/(tabs)/saved.tsx`를 실제로 공유한다(`planner.md`가 파일 겹침의 예시로 직접 드는 그 사례). 다만 이 둘이 클러스터에 들어온 경로는 **씨앗(M-035)과의 직접 파일 겹침이 아니라 폴백 규칙**이다 — 씨앗 M-035는 이 둘과 파일을 공유하지 않는다. 클러스터 구성 근거를 정확히 하기 위해 명시해둔다.

## 이슈별 결과

### M-035 — shipped (커밋 `6ab023d`)
- **문제**: `apps/mobile/app/location.tsx`(위치 권한/디바운스 검색 진입 플로우)에 테스트가 0건. 웹 대응 화면(`apps/web/src/app/location/page.test.tsx`)은 이미 커버돼 있어 모바일만 예외적으로 비어 있었다.
- **범위 경계**: 신규 `apps/mobile/app/location.test.tsx`(189줄, 6케이스: 위치 권한 허용/거부/예외, 검색어 디바운스 250ms, 디바운스 취소, 빈 입력 즉시 닫힘)만 추가. `location.tsx` 소스는 **한 줄도 건드리지 않음**(diff 0줄, 직접 확인) — 현재 동작이 정상이라 판단해 테스트만 고정.
- **되돌림 없음**.

### M-045 — shipped (커밋 `291e0e8`)
- **문제**: `saved.tsx:53-56`이 `savedIds.map(id => catalog.find(...)).filter(Boolean)`로만 해소해, `useEnsureCatalog`가 반경으로 좁힌 카탈로그 창 밖의 저장 id는 조용히 보관함에서 사라졌다(웹의 동일 버그와 같은 원인, 웹은 이미 수정됨).
- **범위 경계**: 신규 `apps/mobile/hooks/useSavedOpportunities.ts`(85줄) + `.test.ts`(119줄, 5케이스) 추가, `apps/mobile/app/(tabs)/saved.tsx`는 해소 로직만 이 훅으로 교체(8줄 변경). `packages/core`·`useEnsureCatalog.ts`는 무변경(직접 diff 확인, 0줄).
- **구현 방식**: 웹의 `apps/web/src/hooks/useSavedOpportunities.ts`를 계약(catalog에 있으면 그대로, 없으면 단건 조회, savedIds 순서 보존, `status`+`retry()` 노출) 참조로만 삼고 그대로 이식하지 않았다 — 웹은 `@tanstack/react-query`의 `useQueries` 기반인데 모바일엔 해당 의존성이 없어, 새 훅은 plain `useState`/`useEffect`로 재구현했다(아키텍처 결정, 자세한 내용은 §아키텍처 검토).
- **검증 포인트(qa가 직접 확인)**: 비동기 응답이 순서 없이 도착해도 `savedIds` 순서를 보존함을 단위 테스트로 명시적으로 검증.
- **되돌림 없음**.

### M-046 — shipped (커밋 `381f54c`)
- **문제**: `saved.tsx`의 `ListEmptyComponent` 하나가 로딩·에러·진짜 무저장을 전부 "아직 저장한 활동이 없어요"로 뭉뚱그렸다. M-045 훅이 `status`(loading/ok/empty/error)를 이미 구분해 내보내는데도 UI가 이를 쓰지 않고 있었다.
- **범위 경계**: `apps/mobile/app/(tabs)/saved.tsx`만 편집(49줄 변경) — 훅·테스트 외 다른 파일 무변경.
- **변경 내용**: 로딩(ActivityIndicator+안내 문구) / 에러(안내 문구+"다시 시도" 버튼, 훅의 `retry()`를 실제 호출) / 진짜 무저장(기존 문구+둘러보기 CTA 유지) 3분기로 나눔. 개수 라벨("N개")은 로딩·에러 중엔 **렌더 자체를 하지 않음**(hidden이 아니라 unmounted) — "0개"로 오독될 수 없게.
- **qa 검증 포인트**: "다시 시도" 버튼을 눌렀을 때 fetch mock 호출 횟수가 1→2로 실제로 증가함을 테스트로 확인(버튼이 장식이 아님을 기능적으로 검증) — 렌더 여부만 보는 얕은 테스트가 아니었음을 직접 diff로 재확인.
- **되돌림 없음**.

## 아키텍처 검토
- **FSD/경계**: 클러스터 전체가 순수 `apps/mobile` 프론트엔드. `packages/core`·`supabase/`는 어디에도 손대지 않았다(architect 확인). `fetchOpportunityById`(`packages/core/src/catalog.ts`)는 이미 존재하는 것을 그대로 재사용했고 마감 필터 없음 특성도 그대로 유지(의도적).
- **담당**: frontend-impl 단독. backend-impl 불필요.
- **시퀀싱**: M-045(훅+status 신설)가 M-046(그 status를 소비해 UI 분기)보다 먼저 커밋돼야 하는 하드 의존성이 있어 architect가 순서를 명시했고, 실제 커밋 순서(`6ab023d` → `291e0e8` → `381f54c`)가 이를 그대로 따랐다.
- **참조 vs 이식**: architect가 `apps/web/src/hooks/useSavedOpportunities.ts`를 M-045의 의미론적 참조로 지정했으나(동일 버그를 웹이 이미 해결한 사례), 코드를 그대로 포팅하지 않았다 — 웹 구현은 `@tanstack/react-query`의 `useQueries`에 의존하는데 모바일엔 이 패키지가 없어, 계약(순서 보존, catalog 우선, status 4종, retry)만 유지하고 구현은 plain React 훅으로 새로 짰다.
- planner→architect 바운스 없음. line-number drift 없이 스펙대로 승인.

## 변경 내용
| 커밋 | 이슈 | 파일 |
|---|---|---|
| `6ab023d` | M-035 | `apps/mobile/app/location.test.tsx`(신규, 189줄, 6케이스) |
| `291e0e8` | M-045 | `apps/mobile/hooks/useSavedOpportunities.ts`(신규)+`.test.ts`(신규, 5케이스), `apps/mobile/app/(tabs)/saved.tsx`(해소 로직 교체), `apps/mobile/app/(tabs)/saved.test.tsx`(버그를 고정하던 기존 테스트를 수정 후 동작 검증으로 재작성) |
| `381f54c` | M-046 | `apps/mobile/app/(tabs)/saved.tsx`(ListEmptyComponent 3분기+count 라벨 조건부 렌더), `apps/mobile/app/(tabs)/saved.test.tsx`(로딩/에러/재시도 기능 테스트 추가) |

총 diff: 5개 파일, +495/-21 (`git diff origin/dev..HEAD --stat`).

## 검증
- **frontend-impl 자체 검증**: 커밋마다 `pnpm --filter mobile typecheck`/`test` green 확인 + 최종 모노레포 전체 `pnpm typecheck`(4/4)+`pnpm test`(core 291 / web 247 / mobile 54, 전부 green) sanity check.
- **qa 독립 재검증**(구현자 자기 보고를 신뢰하지 않고 재실행): `bash scripts/gate.sh`를 3커밋 적용 상태에서 fresh 실행 → **typecheck 4/4, test core 291/291 + mobile 54/54 + web 247/247, lint clean(web `next lint`), secret scan clean — PASS**.
  - M-035: `location.tsx` 소스 diff가 dev 대비 완전히 빈 것을 직접 확인(테스트 전용 커밋).
  - M-045: 훅이 비동기 응답 순서 뒤바뀜에도 `savedIds` 순서를 보존함을 단위 테스트로 명시 검증됨을 확인. `useEnsureCatalog.ts`·`packages/core` diff가 빈 것을 확인.
  - M-046: "다시 시도" 버튼이 fetch mock 호출 횟수를 1→2로 실제 증가시킴을 테스트가 assert함(장식 버튼 아님)을 확인. 로딩·에러 중엔 count 라벨이 숨김이 아니라 아예 렌더되지 않아 "0개" 오독이 구조적으로 불가능함을 확인.
- **reviewer(본인) 최종 재확인**: `bash scripts/gate.sh` 재실행 → 동일하게 **PASS**. `git diff origin/dev..HEAD --stat` 및 3커밋의 실제 hunk를 직접 재독해 위 qa 결과와 일치함을 확인. 시크릿 패턴(`service_role`/`SUPABASE_*_KEY`/PEM/AWS 키/`sk-`류) grep 0건.
- **커밋 메시지 규율**: 3커밋 모두 Conventional Commits(`test(mobile):`/`fix(mobile):`/`feat(mobile):`) + 제목에 `M-NNN` id 포함 확인(`git log` 직접 확인).

## 트리거-repo 문서 정합성 점검 (PRECEDENCE 절)
`.claude/agents/planner.md`, `docs/nightly/PIPELINE.md`, `.claude/rules/workflow/nightly-pipeline.md` 3개 문서를 직접 읽고 오늘 밤 실제로 적용한 CLUSTER RULE·BACKLOG GATE와 대조했다. **모순 발견 없음** — 세 문서 모두 클러스터(1~3개, scope 겹침 기준, 파일 겹침 0건 시 "같은 앱+같은 priority" 폴백 2개까지) 규칙과 `todo≥2 → 구현 모드` 백로그 게이트를 동일하게 서술하고 있고, `planner.md`가 예시로 드는 파일 겹침 사례("M-045+M-046, 둘 다 `saved.tsx`")가 오늘 밤 실제 클러스터 구성과 정확히 일치했다.

부가로, reviewer 본인이 `git log --oneline origin/dev..origin/main`을 확인한 결과 커밋 1개(`744411d`, PR #21 스쿼시)가 잡혔으나, 트리 내용을 대조(`git diff <구dev HEAD> origin/main --stat`)한 결과 실제 파일 차이는 `.claude/commands/commit.md`·`.claude/rules/workflow/nightly-pipeline.md` 2개뿐이었고 그 차이도 **dev가 main보다 더 최신**(dev의 직전 커밋 `6c7c7c9`가 PR #21 충돌 교훈으로 "main 되받기" 규칙 자체를 dev에 갓 추가한 것)이었다. 즉 코드 드리프트는 없고, squash-merge 워크플로우에서 기대되는 정상적인 비대칭(main은 사람이 승격할 때만 갱신)일 뿐이다. 문서 모순도, 실질적 main/dev 코드 드리프트도 없음을 확인했다.

## 리스크 & 후속
- **(qa 발견, non-blocking)** `saved.tsx`의 `ListEmptyComponent`는 FlatList의 `data`가 비어 있을 때만 렌더된다. 혼합 상태(일부 id는 catalog에서 즉시 해소, 일부는 아직 loading/error)에서는 `items`가 비어 있지 않으므로 로딩/에러 배너가 뜨지 않고 개수 라벨 억제만 적용된다. 현재 테스트는 전부 "전량 미해소" 케이스만 다뤄 M-046의 명시된 수용 기준을 위반하지는 않지만, 혼합 상태 UX는 아직 검증되지 않은 사각지대다. 다음 밤 후속 백로그 후보로 남긴다(코드 변경은 하지 않았다).
- 남은 apps/mobile 관련 todo(우선순위 high 기준 이번 클러스터로 소진): 현재 backlog에 다른 high apps/mobile 이슈는 없음 — 다음 밤 씨앗은 다른 high todo 또는 medium/low 그룹에서 나올 것.
- backlog `status: doing`으로 갱신된 M-035·M-045·M-046은 사람이 dev→main 승격 후 `done`으로 전환 필요.
