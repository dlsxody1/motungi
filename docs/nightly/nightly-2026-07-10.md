# 야간 리포트 — 2026-07-10

## TL;DR

- 백로그 최우선 이슈 **M-001**(culture/active 어댑터 구현)을 집었으나, **이미 완료된 작업**이었음 — 백로그가 stale.
- 실제 코드 변경 없음(report-only). M-001을 `done`으로 정정하고, 발견 사실을 기록.
- 검증: `pnpm --filter @motungi/core test` **61/61 통과**. done_when 3개 모두 충족 확인.
- PR: 이 리포트 + 백로그 정정을 담은 PR (아래 참조).

## 집은 이슈

- **M-001** — culture/active 어댑터 구현 및 단위 테스트 (priority: high, scope: `packages/core`)
- 선정 근거: `docs/backlog/backlog.yml`에서 `status: todo` 중 최우선(high, 최소 id).

## 아키텍처 검토 / 판정

planner·검증 단계에서 **M-001의 done_when이 이미 origin/main에서 충족됨**을 확인:

- `packages/core/src/adapters/`에 어댑터가 이미 존재:
  - `seoul-culture.ts` → `culture` (서울시 문화행사)
  - `culture-info.ts` → `culture` (한눈에보는문화정보, 전국 보조)
  - `trail.ts` → `active` (두루누비 걷기길)
- 각 어댑터에 대응하는 테스트 존재: `seoul-culture.test.ts`, `culture-info.test.ts`, `trail.test.ts`
- `packages/core/src/adapters/index.ts` 주석에 카테고리 매핑이 문서화됨.

따라서 신규 구현이 불필요 → **report-only로 확정.** 범위 밖 확장(예: `sports_facility` 어댑터 신규 구현)은 백로그에 없으므로 하지 않음(scope creep 방지).

## 변경 내용

report-only — 제품 코드 변경 없음. 문서/백로그만 정정:

- `docs/backlog/backlog.yml` — M-001 `status: todo → done`, notes에 발견 사실 기록.
- `docs/nightly/nightly-2026-07-10.md` — 이 리포트.

## 검증

```
$ pnpm --filter @motungi/core test
 ✓ src/scoring.test.ts (7 tests) 3ms
 ✓ src/adapters/seoul-jobs.test.ts (11 tests) 3ms
 ✓ src/adapters/trail.test.ts (5 tests) 7ms
 ✓ src/adapters/seoul-culture.test.ts (6 tests) 4ms
 ✓ src/adapters/util.test.ts (12 tests) 6ms
 ✓ src/adapters/culture-info.test.ts (4 tests) 5ms
 ✓ src/view.test.ts (16 tests) 15ms

 Test Files  7 passed (7)
      Tests  61 passed (61)
```

done_when 항목별 판정:
- ✅ culture·active 어댑터가 Raw* 응답을 OppRow로 매핑 — seoul-culture/culture-info/trail 존재
- ✅ 각 어댑터에 대응하는 *.test.ts 통과 — 위 출력
- ✅ `pnpm --filter @motungi/core test` 통과 — 61/61

## 리스크 & 후속

- **근본 원인**: `docs/HANDOFF.md` §2 '진행 중/미완'이 stale해서 이미 끝난 작업을 백로그에 등재. → HANDOFF §2 갱신이 후속 과제.
- **다음 밤**: 백로그 정정으로 M-002(목업 → Supabase 읽기 전환, apps/web, high)가 다음 차례가 됨. 이건 실제 코드 작업이 있는 이슈.
- **운영 이슈(중요)**: 이번 실행은 클라우드 routine 환경에서 GitHub **쓰기 권한 부재**로 원격 push/draft PR 생성에 실패함(`Resource not accessible by integration` 403 — Claude GitHub App 미설치). 파이프라인 로직은 정상 작동했고 산출물은 로컬에서 재현·수거함. 자동 PR을 원하면 로컬 cron 또는 GitHub Actions로의 전환이 필요.

## PR

이 리포트 + 백로그 정정을 담은 PR. (수동 수거분 — 클라우드 세션이 push하지 못해 로컬에서 재현해 올림.)

---

## 2차 실행 — M-002 (같은 UTC 날짜, 21:00 KST 정기 스케줄)

> 위 섹션은 2026-07-10 이른 아침(UTC 기준 같은 날짜)에 진행된 **1차 실행**의 기록이며, 이미 PR #5로 origin/main에 병합됐다. 그 실행은 성격상 수동/설정 테스트에 가까웠다(M-001 report-only, 게다가 GitHub 쓰기 권한 부재로 원격 push조차 실패해 로컬에서 수거함). **이번은 21:00 KST 정기 스케줄로 돌아간 당일 2번째, 그리고 사실상 첫 "정식" 야간 파이프라인 실행**이다. `nightly/2026-07-10` 브랜치는 1차 PR 머지 이후 origin/main 최신 커밋(4ddde13) 기준으로 재생성되었고, 이번 작업은 1차와 완전히 무관한 새 이슈(M-002)를 다룬다.

### TL;DR

- 집은 이슈: **M-002** — 목업 데이터 → Supabase 읽기 전환 (웹) (priority: high, scope: `apps/web`)
- planner가 확인한 바, 실데이터 읽기 자체(`fetchOpportunities`가 `supabase.from("opportunities").select` 사용, ok/empty/error/unconfigured 4상태 반환)는 이미 커밋 7b180a6(2026-07-09)에서 구현 완료 — done_when 1·2번은 착수 전부터 충족 상태였음.
- 실제로 막혀 있던 것은 done_when 3번("apps/web typecheck 통과, 관련 컴포넌트 테스트 통과") — apps/web에는 애초에 vitest 등 테스트 인프라가 전무했음.
- architect가 frontend-impl 단독 담당으로 확정, Next.js App Router + vitest 통합상 함정(플러그인 누락·path alias·next/navigation mock·zustand 싱글턴 리셋·supabase mock 분리)을 미리 지적해 구현 지침에 반영.
- frontend-impl이 vitest+RTL+jsdom 인프라와 4개 신규 테스트 파일(3개 케이스: ok/empty/error + unconfigured 1건 + 화면 렌더 4상태)을 작성, 커밋 081b4c6.
- qa 실행 결과와 reviewer가 독립적으로 재실행한 결과가 일치: typecheck 클린, `vitest run` 3 test files / 8 tests 전부 통과, `apps/web build` 성공, 범위 밖 변경 없음.
- **판정: 승인.** 실패 없음 — 있는 그대로 기록.

### 집은 이슈 (2차)

- **M-002** — 목업 데이터 → Supabase 읽기 전환 (웹)
  - priority: high, scope: `apps/web`
  - done_when: (1) apps/web 화면이 목업 대신 opportunities 테이블을 읽는다 (2) 로딩/에러/빈 상태가 처리된다 (3) apps/web typecheck 통과, 관련 컴포넌트 테스트 통과
  - planner 발견: (1)(2)는 커밋 7b180a6(2026-07-09)에서 이미 구현되어 있었음 — `fetchOpportunities()`가 실제로 Supabase를 조회하고 ok/empty/error/unconfigured 4가지 상태를 반환. 백로그가 stale했던 M-001과 유사한 패턴이나, 이번엔 (3)이 실제로 미충족 상태였다는 점이 달랐음.

### 아키텍처 검토 (2차)

- **담당**: frontend-impl 단독. 백엔드/스키마/다른 앱(apps/mobile, packages/core) 변경 불필요 — scope는 순수하게 apps/web 테스트 인프라 구축으로 한정.
- **경계**: 기존 `fetchOpportunities` 구현 로직 자체는 건드리지 않음(이미 완료된 부분에 손대지 않는다는 원칙). 오직 그 동작을 검증하는 테스트와 이를 실행할 인프라만 추가.
- **테스트 전략 / 사전 지적된 함정**:
  - `@vitejs/plugin-react` 누락 시 JSX 변환 실패 → devDependency로 명시 추가.
  - `tsconfig`의 `@/*` path alias를 vitest에서도 동일하게 해석하도록 `vitest.config.ts`에 `resolve.alias` 별도 설정 필요(Next.js 자체 alias 해석과 vitest/vite 해석은 별개).
  - App Router 컴포넌트가 `next/navigation`의 `useRouter` 등을 쓰는 경우 jsdom 환경에서 mock 없이는 렌더 실패 → `vi.mock("next/navigation", ...)` 필요.
  - zustand 스토어가 모듈 싱글턴이라 테스트 간 상태가 누수됨 → 각 테스트 `beforeEach`에서 `useAppStore.setState(INITIAL_STATE)`로 리셋 필요.
  - `@/lib/supabase` 싱글턴 mock은 ok/empty/error 케이스(`supabase` 객체 mock)와 unconfigured 케이스(`supabase: null` mock)의 mock 형태가 서로 달라 한 파일에서 공존시킬 수 없음 → 파일을 분리(`opportunities.test.ts` vs `opportunities.unconfigured.test.ts`).
  - msw는 이번 범위에서 불필요하다고 판단해 제외(Supabase 클라이언트 자체를 모듈 레벨에서 mock하는 것으로 충분).

### 변경 내용 (2차)

커밋 `081b4c6` (`test: apps/web 테스트 인프라 구축 + opportunities 읽기 검증 (M-002)`):

- `apps/web/package.json` — devDependency 추가: `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `@vitejs/plugin-react`, `jsdom`. `"test": "vitest run"` 스크립트 추가.
- `apps/web/vitest.config.ts` (신규) — jsdom 환경, `@vitejs/plugin-react` 플러그인, `@/*` path alias, setup 파일 등록.
- `apps/web/vitest.setup.ts` (신규) — `@testing-library/jest-dom/vitest` import 한 줄.
- `apps/web/src/data/opportunities.test.ts` (신규) — `fetchOpportunities` ok/empty/error 3케이스.
- `apps/web/src/data/opportunities.unconfigured.test.ts` (신규) — supabase 미설정(unconfigured) 1케이스. mock 형태 차이로 별도 파일 분리.
- `apps/web/src/app/report/page.test.tsx` (신규) — `catalogStatus`(ok/empty/error/unconfigured) 4상태별 실제 화면 렌더 검증(원픽 카드, 빈 상태 문구, 실패 상태 문구·재시도 버튼).
- `docs/backlog/backlog.yml` — M-002 `notes`에 "이미 (1)(2) 구현됨 + 실제 범위는 테스트 인프라 구축이었음" 정황 기록.
- `pnpm-lock.yaml` — 위 devDependency 추가에 따른 lockfile 갱신(520줄, 자동 생성).
- apps/mobile, packages/core, supabase(마이그레이션/함수) 디렉터리에는 변경 없음(diff 확인 결과 0줄) — scope 이탈 없음.

### 검증 (2차)

qa가 직접 실행한 명령과 출력, 그리고 reviewer가 동일 명령을 독립적으로 재실행해 일치 확인한 결과:

```
$ pnpm --filter @motungi/web typecheck
> @motungi/web@0.1.0 typecheck /home/user/motungi/apps/web
> tsc --noEmit
(클린 통과, 에러 없음)

$ pnpm --filter @motungi/web test
> @motungi/web@0.1.0 test /home/user/motungi/apps/web
> vitest run

 RUN  v2.1.9 /home/user/motungi/apps/web

 ✓ src/data/opportunities.unconfigured.test.ts (1 test) 3ms
 ✓ src/data/opportunities.test.ts (3 tests) 6ms
 ✓ src/app/report/page.test.tsx (4 tests) 233ms

 Test Files  3 passed (3)
      Tests  8 passed (8)
```

- `pnpm --filter @motungi/web build` — qa 기록: 15/15 페이지 정적 생성 성공 (reviewer는 시간 관계상 typecheck/test만 재실행, build는 qa 출력을 신뢰).
- `git diff origin/main...HEAD -- apps/mobile packages/core supabase` — 0줄, 범위 밖 변경 없음(reviewer 재확인).
- 시크릿 노출 없음, diff 오염 없음.
- done_when 3개 전부 충족: (1)(2)는 기존 구현으로 이미 충족, (3)은 이번 작업으로 충족.
- **최종 판정: 승인.**

### 리스크 & 후속 (2차)

- **테스트 커버리지는 최소 범위**: 이번엔 done_when 3번을 충족시키는 데 필요한 최소 테스트만 추가했다(`fetchOpportunities` 4상태 + report 페이지 4상태). apps/web의 다른 화면(온보딩·저장·프로필 등)은 아직 테스트가 없다 — 다음 밤 후보로 남겨둘 만함.
- **lockfile diff가 큼(520줄)**: devDependency 추가에 따른 정상적인 변경이지만, 리뷰어 입장에서 실제 코드 diff와 lockfile diff를 혼동하지 않도록 주의가 필요. 이번 건에서는 문제 없음을 확인.
- **msw 미도입 판단**: frontend-impl이 "supabase 클라이언트 모듈 mock으로 충분"이라 판단해 msw를 제외했음. 향후 API 라우트/서버 액션 테스트가 늘어나면 msw 도입을 재검토할 필요.
- **1차/2차 실행 혼선 방지**: 같은 UTC 날짜에 브랜치 `nightly/2026-07-10`이 두 번 쓰이고 각각 별도 PR로 나가는 구조라, 리포트 파일명이 겹친다(`nightly-2026-07-10.md`). 이번엔 섹션을 나눠 한 파일에 이어 쓰는 방식으로 투명성을 확보했으나, 하루 2회 이상 파이프라인이 도는 게 상시화되면 파일명에 실행 회차(예: `-run2`)나 타임스탬프를 넣는 컨벤션 도입을 검토할 만함.
- **다음 밤 후보**: 백로그 기준 M-003(scoring.ts cost+time 가중치 엣지케이스 테스트 보강, packages/core, medium)이 다음 순위.

### PR (2차)

- 이번 2차 실행에 대한 신규 draft PR. base=`main`, head=`nightly/2026-07-10`. 1차 PR(#5, 이미 병합됨)과는 무관한 별도 PR.
