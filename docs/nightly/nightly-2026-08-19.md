# Nightly Report — 2026-08-19

## TL;DR

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `docs/backlog/backlog.yml`의 `status: todo` 개수는 **18건**(전날 08-18 audit이 M-056~M-072 17건을 새로 등재한 결과) — `todo ≥ 2`이므로 정상 구현 모드로 진행했다.
- **클러스터: M-056 + M-058** (둘 다 shipped). 둘 다 `apps/mobile`, priority `high`.
- qa 게이트(`bash scripts/gate.sh`) **PASS** — typecheck 4/4, test(core 286/286·web 279/279·mobile 125/125) 전체 통과, lint 통과, 시크릿 없음.
- 연속 report-only 경보 해당 없음: 직전 밤(08-18)은 audit 모드(report-only 1회)였지만 그 전날(08-17)은 M-054 shipped 구현 밤이었다 — 연속 2회 미만.

## 집은 클러스터

**씨앗**: priority 최상위 todo 중 id 오름차순 1개 — `high` 3건(M-056/M-057/M-058) 중 최소 id인 **M-056**(`apps/mobile/lib/auth.ts`).

**scope 겹침 탐색**: M-056(`apps/mobile/lib/auth.ts`)과 실제 파일/좁은 디렉토리를 공유하는 todo 이슈를 전체 18건에서 찾았으나 **0건**(어떤 todo도 `apps/mobile/lib/`를 건드리지 않음).

**폴백 적용**: 파일 겹침 후보가 0개이므로 클러스터 규칙의 폴백 — "같은 앱 경계 + 같은 priority" todo를 id 오름차순 2개까지 추가. `high` + `apps/mobile`인 todo는 **M-058**(`apps/mobile/ui/components.tsx` 외 4개 화면, a11y 스윕) 하나뿐이었다(M-057은 `apps/web`이라 앱 경계 불일치로 제외). 그래서 클러스터 = **M-056 + M-058** (상한 3개 중 2개 사용, 더 채울 후보 없음 — 수를 채우려 낮은 priority를 끌어오지 않았다).

**제외된 이슈**: M-052(medium, `apps/mobile/app/opportunity.tsx`, 6일째 지도/산책로 구현 방식에 대한 사람 결정 대기 중 — 08-13부터 매일 재확인 후 제외돼 온 이슈)는 이번에도 재확인 후 제외했다. `apps/mobile/package.json`에 지도 관련 의존성 없음, `.env.example`에 신규 지도 키 없음, `security-policy.md`에 지도 관련 내용 없음 — 08-13~08-18과 동일 결과, 변경 사항 없음. priority도 medium이라 애초에 high 우선 씨앗/폴백 대상이 아니었다.

## 이슈별 결과

### M-056 — 모바일 OAuth/세션 흐름(auth.ts) 테스트 0건 → **shipped**

- 커밋: `da3a79f` `test(mobile): auth.ts OAuth/세션 흐름 테스트 추가 (M-056)`
- 변경: `apps/mobile/lib/auth.test.ts` 신규 1개 파일, 410줄. **`apps/mobile/lib/auth.ts` 자체는 0-line diff**(로직 미변경, 순수 테스트 추가) — `git diff 8f53278 a84d076 -- apps/mobile/lib/auth.ts` 결과 빈 출력으로 재확인.
- 수용 기준(planner 산출, qa 재검증):
  - ✅ `signInWithKakao` 5개 에러 분기(supabase 미설정 / oauth 에러·URL 없음 / 취소 vs 기타 실패 / code 없음·비문자열 / exchangeCodeForSession 실패) + 성공 경로
  - ✅ `promoteLocalToAccount`: `savedIds.length>0` upsert 호출 / `===0` 스킵, `profiles.upsert` anchors 필드 매핑
  - ✅ `initAuthListener`: 서버·로컬 saved id Set 병합(중복 제거), `userId==null` 분기(setUser(null)만 호출), `user_metadata.name` 비문자열 런타임 가드
  - ✅ `pnpm --filter mobile test` 통과 — `lib/auth.test.ts (25 tests)` 전부 통과

### M-058 — 모바일 핵심 인터랙션 accessibilityRole 누락 스윕 → **shipped**

- 커밋: `a84d076` `fix(mobile): 핵심 인터랙션 요소 accessibilityRole 누락 스윕 (M-058)`
- 변경: 5개 소스 파일(각 속성 추가 1줄, 스타일/onPress 로직 무변경) + 대응 테스트 5개 파일, 총 66행 추가/4행 삭제.
  - `apps/mobile/ui/components.tsx` — 공용 `Button`
  - `apps/mobile/app/diagnosis.tsx` — 다음/결과 보기 CTA
  - `apps/mobile/app/opportunity.tsx` — 보러 가기 CTA
  - `apps/mobile/app/(tabs)/report.tsx` — 재진단·오늘의 원픽 히어로·함께보면좋아요 행(3곳)
  - `apps/mobile/app/(tabs)/explore.tsx` — `ActivityItem` Pressable
- 수용 기준:
  - ✅ 5개 파일 지정 위치 전부 `accessibilityRole="button"` 추가 확인(qa가 각 hunk 직접 diff로 재검증)
  - ✅ 각 파일 대응 테스트에 `getByRole`/role 노출 검증 추가
  - ✅ 기존 통과 중이던 접근성 테스트(Chip, diagnosis 옵션 카드, opportunity 북마크 토글) 회귀 없음 — 삭제된 테스트 코드 없음, 전부 그대로 통과
  - ✅ `pnpm --filter mobile test` 전체 통과 — 19 파일/125개 테스트 전부 통과
  - ✅ diff가 backlog 명시 5개 파일(+테스트)로 한정됨

## 아키텍처 검토

- 담당: **frontend-impl 단독**(둘 다 `apps/mobile` 전용, backend-impl 불필요 — DB 마이그레이션·packages/core 변경 없음).
- FSD 경계: `apps/mobile`은 entities/features/widgets/shared 계층을 쓰지 않는 구조(`app/`·`ui/`·`lib/`·`store/`·`hooks/`·`data/`) — 위반 소지 자체가 없음을 확인.
- 테스트 전략: component test(vitest + RTL, react-native-web). M-056은 이 리포에 처음 필요해진 `expo-linking`/`expo-web-browser` mock을 새로 작성했고, `@/lib/supabase` mock은 기존 `apps/mobile/data/opportunities.test.ts`의 getter 기반 패턴을 재사용했다. M-058은 신규 테스트 파일 없이 기존 `*.test.tsx` 5개를 보강.
- 스킬 라우팅: M-058은 시각적 변경·인터랙션 로직 변경이 0이고 이미 6곳에 확립된 `accessibilityRole="button"` 컨벤션을 나머지 누락 지점에 기계적으로 맞추는 작업이라 판단해 `impeccable`/`ux` 스킵, `frontend-patterns`·`react-testing`만 적용 — architect 단계에서 사전에 이 판단을 확정하고 근거를 남겼다.
- planner 스펙 반려 없음 — architect가 두 이슈 모두 파일·행 번호를 실측 재확인 후 승인.

## 변경 내용

```
$ git diff 8f53278 a84d076 --stat
 apps/mobile/app/(tabs)/explore.test.tsx |  9 +++++++++
 apps/mobile/app/(tabs)/explore.tsx      |  6 +++++-
 apps/mobile/app/(tabs)/report.test.tsx  | 14 ++++++++++++++
 apps/mobile/app/(tabs)/report.tsx       |  5 +++--
 apps/mobile/app/diagnosis.test.tsx      |  6 ++++++
 apps/mobile/app/diagnosis.tsx           |  1 +
 apps/mobile/app/opportunity.test.tsx    | 12 ++++++++++++
 apps/mobile/app/opportunity.tsx         |  1 +
 apps/mobile/lib/auth.test.ts            | 410 +++++++++++++++++++++++++++++
 apps/mobile/ui/components.test.tsx      | 15 ++++++++++++-
 apps/mobile/ui/components.tsx           |  1 +
 11 files changed, 470 insertions(+), 4 deletions(-)
```

`apps/mobile/**` 밖으로 새 나간 변경 없음. 시크릿·`.env`·마이그레이션 없음.

## 검증

qa가 실제로 돌린 게이트(`bash scripts/gate.sh`, dev 트리 `a84d076` 기준):

```
── typecheck
 Tasks:    4 successful, 4 total   (core, mobile, tokens, web)
   ✅ typecheck
── test
@motungi/mobile:test:  ✓ lib/auth.test.ts (25 tests) 299ms
@motungi/mobile:test:  ✓ app/(tabs)/explore.test.tsx (12 tests) 751ms
@motungi/mobile:test:  ✓ app/(tabs)/report.test.tsx (4 tests) 167ms
@motungi/mobile:test:  ✓ app/diagnosis.test.tsx (5 tests) 533ms
@motungi/mobile:test:  ✓ app/opportunity.test.tsx (16 tests) 1045ms
@motungi/mobile:test:  ✓ ui/components.test.tsx (3 tests) 155ms
@motungi/mobile:test:  Test Files  19 passed (19) / Tests 125 passed (125)
@motungi/core:test:  Test Files  11 passed (11) / Tests 286 passed
@motungi/web:test:  Test Files  36 passed (36) / Tests 279 passed
 Tasks:    3 successful, 3 total
   ✅ test
── lint
@motungi/web:lint: ✔ No ESLint warnings or errors
 Tasks:    1 successful, 1 total
   ✅ lint
── secrets
   (시크릿 패턴 없음)
   ✅ secrets

GATE: PASS — push 허용
```

lint는 `apps/web`에만 스크립트가 있어(core·tokens·mobile엔 lint 스크립트 없음, 기존에 이미 기록된 known gap) turbo가 1/4 패키지에서만 실행 — 게이트 오작동 아님, 재확인만.

qa는 `git diff 8f53278 a84d076 -- apps/mobile/lib/auth.ts`가 빈 출력임을 직접 재확인(구현자 자기보고를 신뢰하지 않고 재검증)했고, M-058의 5개 소스 파일 hunk를 전부 직접 읽어 속성 추가 외 변경이 없음을 확인했다. `scripts/scan-secrets.sh`를 8f53278 기준으로 독립 재실행해도 동일하게 클린.

두 이슈 모두 revert 대상 없음 — 전부 push.

## 리스크 & 후속

- **M-052 여전히 블록 상태(7일째, 08-13~08-19)**: 모바일 상세 화면 지도·산책로 표시 기능이 사람의 구현 방식 승인(react-native-maps 신규 네이티브 의존성 vs 정적 지도 프록시 vs 서드파티) 없이는 진행 불가. 이번 밤도 재확인 후 제외했다. **사람 개입 요청 반복**: (a) 구현 방식 승인, (b) priority 강등, (c) `blocked-human` 상태 도입 중 하나를 결정해 달라 — 08-14부터 매일 동일 요청이 쌓이고 있다.
- **M-057 다음 밤 유력 씨앗**: 이번 밤 high 3건 중 M-057(`apps/web/src/hooks/useReportFallback.ts`)만 남았다. `apps/web` 단독 scope라 다음 밤 씨앗이 되면 scope 겹침 탐색이 먼저 필요하다(현재 todo 16건 중 `apps/web` 단독 scope인 것들과 겹치는지 재확인 필요 — 이번 밤엔 확인하지 않았다).
- **모바일 lint 스크립트 부재**: 기존 known gap 재확인(신규 아님). core·mobile엔 lint 스크립트가 없어 게이트의 lint 단계가 web만 검사한다.
- **M-058 접근성 노트**: 구현 단계에서 `report.tsx`의 히어로 카드처럼 내부에 여러 텍스트/뷰가 중첩된 큰 Pressable에 role을 추가할 때 스크린리더 중복 낭독 가능성이 있어 `accessibilityLabel` 추가 검토가 제안됐으나, 이는 이번 done_when 범위 밖이라 반영하지 않았다 — 백로그 후보로 남길 만하다(별도 이슈화는 이번 밤에 하지 않음).

## PR

없음. `docs/nightly/PIPELINE.md`의 dev 직접 커밋(트렁크) 모델에 따라 브랜치·PR 없이 `dev`에 직접 커밋·push한다.
