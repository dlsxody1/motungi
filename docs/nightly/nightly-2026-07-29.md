# Nightly Report — 2026-07-29

## TL;DR

- **오늘 밤도 report-only다 — 코드 변경 없음.** `origin/dev` 기준 `docs/backlog/backlog.yml` 이슈 19개(M-001~M-019)를 전수 확인한 결과 `status: todo`인 항목이 **2026-07-23부터 이레째(7일 연속)** 하나도 없다.
- 구성은 어제(07-28)와 동일: `done` 16개, `doing` 3개(M-014, M-015, M-017).
- planner 규칙(`.claude/agents/planner.md` §"할 일" 1번)에 따라 "todo가 없으면 그 사실만 보고하고 종료"해야 하므로, architect/구현/qa 단계로 진행하지 않고 이 리포트만 남긴다.

## 집은 이슈

- **없음.** `origin/dev`(fetch 후 `git checkout -B dev origin/dev`, tip `0feaba8`)의 `backlog.yml` 전체 스캔 결과:
  - `status: done` — M-001, M-002, M-003, M-004, M-005, M-006, M-007, M-008, M-009, M-010, M-011, M-012, M-013, M-016, M-018, M-019 (16개)
  - `status: doing` — M-014("a11y: 구조·모션·확대 결함 수정", 2026-07-20 doing 전환, **오늘로 9일째**), M-015("웹/모바일 패리티 정합", 2026-07-21 doing 전환, **8일째**), M-017("RN 모바일 화면 렌더 테스트 도입", 2026-07-22 doing 전환, **7일째**)
  - `status: todo` — 0개
- ⚠️ **주의(재확인)**: 이번 밤도 저장소를 얕은 클론(`--depth 1`, 기본 브랜치 `main`)한 직후 `docs/backlog/backlog.yml`을 먼저 읽었는데, 그 스냅샷에는 M-015·M-017이 `status: todo`로 보였다(main tip `f537639`, PR #17 머지 시점 기준이라 stale). `origin/dev`를 별도로 fetch·checkout해 재확인한 결과 실제로는 둘 다 `doing`이었다. **계획은 반드시 `origin/dev` 기준으로 세워야 하며 `main` 스냅샷을 backlog 판단 근거로 쓰면 안 된다** — 지난 엿새의 리포트가 남긴 경고가 오늘도 유효했고, 오늘도 동일한 순서(main 클론 → origin/dev 별도 fetch → 재확인)로 오판을 피했다.
- 신규 이슈는 사람이 `backlog.yml`에 직접 추가해야 공급된다(밤 에이전트는 status만 갱신, 신규 이슈 추가 권한 없음 — `CLAUDE.md` §백로그).

## 아키텍처 검토

- 해당 없음 — planner 단계에서 조기 종료했으므로 architect/frontend-impl/backend-impl/qa 단계를 실행하지 않았다.

## 변경 내용

- **코드 변경 없음.** 이 리포트 파일(`docs/nightly/nightly-2026-07-29.md`) 추가만 dev에 커밋한다. `packages/*`, `apps/*`, `supabase/*` 어디도 건드리지 않았다.
- `docs/backlog/backlog.yml`도 갱신하지 않는다 — todo→doing으로 바꿀 새 이슈가 없었기 때문이다.

## 검증

- 실행한 명령과 결과:
  ```
  $ git clone --depth 1 https://github.com/dlsxody1/motungi /workspace/motungi   # 기본 브랜치(main) 클론
  $ git fetch origin dev:refs/remotes/origin/dev --depth 1
  * [new branch]      dev        -> origin/dev
  $ git checkout -B dev origin/dev
  # 로컬 dev HEAD = 0feaba8 (origin/dev tip과 동일, 최신 기준선 확인)
  $ grep -n "status:" docs/backlog/backlog.yml
  # → status: done × 16, status: doing × 3 (M-014, M-015, M-017), status: todo 0건
  $ git log --oneline main..dev
  0feaba8 docs(nightly): 2026-07-28 report-only — backlog에 status:todo 이슈 없음 (6일째)
  $ git log --oneline dev..main
  f537639 dev → main: 상세페이지 개선 + 히어로 캐러셀·동네설정 UX (누적) (#17)
  $ git status --short
  (출력 없음 — 클린)
  ```
- typecheck/test/build는 실행하지 않았다 — 코드 변경이 없어 push 게이트(qa) 대상 자체가 없기 때문이다(억지로 돌려 "검증했다"고 부풀리지 않음).

## 리스크 & 후속

1. **backlog 공급 고갈 이레째**: `status: todo` 항목이 2026-07-23부터 오늘(07-29)까지 0개다. 사람이 새 이슈를 추가하지 않으면 다음 밤도 동일하게 report-only로 종료될 가능성이 높다.
2. **`doing` 3건 적체, 최장 9일째**: M-014(2026-07-20~, 9일째)·M-015(2026-07-21~, 8일째)·M-017(2026-07-22~, 7일째)이 `doing`에 머물러 있다. `dev`가 `main`(PR #17 머지 시점, `f537639`) 대비 여러 커밋 앞서 있어 격차가 계속 벌어지는 중이다. `dev → main` 승격 PR을 사람이 열어 리뷰·머지하고 해당 이슈들을 `done`으로 확정하면 재작업 여지가 줄어든다.
3. **`main` 기준 backlog 판단 금지 재확인**: 이레 연속(07-23~07-29) 같은 함정이 반복되고 있다 — 얕은 클론 기본 브랜치가 `main`이라 `docs/backlog/backlog.yml`을 최초로 읽을 때마다 stale 스냅샷을 보게 된다. `origin/dev`를 명시적으로 fetch·checkout해서 재확인하는 절차가 실제로 매번 오판을 막아왔다 — 다음 밤들도 반드시 이 순서를 지킬 것.
4. 다음 밤을 위해: 사람이 (a) `doing` 3건을 검토해 `main` 승격 여부를 판단하고, (b) 새 `todo` 이슈를 backlog에 추가해주기를 권장한다. 이 상태가 계속되면 파이프라인이 매일 밤 report-only만 반복하게 된다.
