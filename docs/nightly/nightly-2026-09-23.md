# Nightly Report — 2026-09-23

## TL;DR

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `docs/backlog/backlog.yml`의 `status: todo`
  9건(엄밀 카운트 — 참고: `grep -c "status: todo"`는 notes 본문 인용까지 잡아 15로 나오므로
  실제 이슈 파싱으로 다시 셌다) → `todo ≥ 2`이므로 구현 모드. audit 모드로 전환하지 않았다.
- **연속 report-only 경보 해당 없음** — 직전 다섯 밤(09-15·09-17·09-19·09-21·09-22) 모두
  코드가 나갔다. 오늘 밤도 코드가 나가 스트릭이 이어진다.
- **씨앗 M-097(최우선 medium, id 최소)은 이번에도 blocked-human이라 제외**하고 다음 씨앗
  **M-114**로 밤을 짰다. **클러스터는 M-114 1건**(scope에 실제 파일 2개가 있어 CLUSTER RULE
  폴백(파일·좁은 디렉토리가 전혀 없을 때만 발동) 조건이 성립하지 않는다 — 어제 밤(09-22)이
  같은 이유로 M-113을 단독 클러스터로 짠 것과 동일한 판단).
- **결과: SHIPPED.** M-114 — `packages/core/src/index.ts`의 `export *`(12줄)를 앱이 실제로
  쓰는 54개 심볼만 명시적으로 재export하도록 바꾸고, `gu-summary` 계열 6개 심볼(SEO 구
  페이지 전용, mobile 미사용)을 `packages/core`에서 `apps/web/src/lib/`로 승격했다.
  `bash scripts/gate.sh` PASS(typecheck·test·lint·secrets·pure-tests 5/5) + web build 27/27
  라우트 성공. 커밋 `f272049`를 `dev`에 push. reverted 이슈 없음.
- ⚠️ **CLUSTER RULE 폴백 조건 불일치는 어제 밤 기록된 것과 동일하게 아직 미해결**
  (트리거 프롬프트 vs `.claude/rules/workflow/nightly-pipeline.md`/`docs/nightly/PIPELINE.md`/
  `.claude/agents/planner.md`). 오늘 밤은 M-114의 scope에 파일이 2개(`index.ts`·
  `gu-summary.ts`) 있어 **양쪽 해석이 같은 결론(폴백 미발동 → 1이슈 클러스터)에 도달**했으므로
  실제 클러스터 크기에는 영향이 없었다. 사람 조치는 여전히 필요 — 다음에 "scope에 파일은
  있지만 겹치는 게 하나도 없는" 이슈가 씨앗이 되면 다시 갈릴 수 있다.

## 집은 클러스터

**씨앗 선정**: `status: todo` 9건 중 priority 최상위는 `medium`(high 없음). medium은
M-097·M-114·M-115(id 오름차순) 3건. M-113은 어제 밤 SHIPPED로 `doing` 전환됐다.
규칙대로면 최소 id인 **M-097**이 씨앗이어야 한다.

**M-097을 씨앗에서 재차 제외한 이유**: M-097(`동 단위 좌표 백필`)은 2026-09-07·09-09·09-22
밤이 이미 `blocked-human`으로 판정해 "씨앗에서 사전 제외"를 notes에 남겨둔 이슈다
(`docs/backlog/backlog.yml`). 오늘 밤 재검증:
```
$ ls -la .env            → No such file or directory (.env.example만 존재, 전 항목 공란)
$ curl -v https://apis.data.go.kr/
  < HTTP/1.1 403 Forbidden
  * CONNECT tunnel failed, response 403
```
`.env`가 없고 `DATA_GO_KR_SERVICE_KEY`가 공란이며, outbound network가 조직 정책으로
`apis.data.go.kr`를 차단한다(전날과 동일한 403). 좌표를 지어내는 건 근사치보다 나쁘다는
전날 판단을 유지한다. `status: todo` 유지(재시도 대상). **네 번째 밤째 같은 사유로 막혀
있다** — 사람이 API 키를 발급하거나 network 예외를 허용하지 않으면 계속 이 자리에서
막힐 것이다.

**다음 씨앗: M-114**(`packages/core 공개 API 경계 명시`, priority medium, id 최소 중 유효).
scope: `[packages/core/src/index.ts, packages/core/src/gu-summary.ts]`.

**done_when 재검증(planner 규율 — dev 실코드로 이미 충족됐는지 먼저 확인)**: 어젯밤
M-113이 `packages/core/src/geo.ts`를 신설하며 `index.ts`에 `export * from "./geo"`를
추가했다(12번째 줄) — M-114의 done_when 1번("export * 11줄 → 명시적 재export")이 가리키는
줄 수는 감사 시점(2026-09-11) 기준이라 실제로는 12줄이었다. 전수조사로 다시 세어 진행했고,
이 사실은 아래 "변경 내용"에 기록한다. 나머지 done_when(gu-summary 이동·WEB_ONLY 분류
문서화·typecheck/test 통과)은 코드에 반영된 바 없어 그대로 유효했다.

**scope 겹침으로 묶이는 다른 todo가 있는지 확인**(파일 단위):
- M-115 scope `[apps/mobile/ui/theme.ts, packages/tokens/src/index.ts]` — M-114가 건드리는
  파일(`packages/core/src/index.ts`, `gu-summary.ts`)과 겹치지 않는다.
- M-088·M-092·M-093·M-102·M-103·M-110(low priority) — 전부 다른 파일. 겹치지 않는다.
- (M-097은 씨앗 후보에서 이미 제외됨 — 겹침 검토 대상이 아니다.)
→ **파일 겹침 후보 0건.**

**폴백 여부**: M-114의 scope에는 `packages/core/src/index.ts`·`packages/core/src/gu-summary.ts`
**실제 파일 2개**가 포함돼 있다. 트리거 프롬프트의 CLUSTER RULE 3번("seed의 scope에 파일이
하나도 없고 좁은 디렉토리도 없을 때만" 폴백)과 repo 3개 문서("파일 겹침 후보가 0개라는
사실"이 기준)가 이 지점에서는 **같은 결론에 도달한다** — 어느 해석으로 읽어도 M-114의
scope엔 이미 파일이 있으므로 "파일이 전혀 없을 때"(트리거 기준)에도, "파일이 섞여 있든
무관하되 결국 파일 겹침 후보 0개"(repo 문서 기준)에도 폴백 발동 여부가 갈리지 않는다(둘 다
파일 겹침을 먼저 보는데 0건이고, 트리거 기준은 여기서 추가로 "scope에 파일 자체가 없어야"라는
조건이 있어 아예 발동 조건 미충족). **오늘 밤은 두 해석이 우연히 합의했다** — 트리거와 repo
문서의 실제 차이가 드러나는 경우는 "scope에 파일이 있는데 아무 데도 안 겹치는" M-114 같은
사례가 아니라, 09-22 밤의 M-113처럼도 결국 같았다. 실제로 갈리는 사례는 아직 관측되지
않았을 수 있다 — 사람이 트리거 프롬프트와 repo 문서 중 뒤처진 쪽을 정리해 달라는 요청은
09-22 리포트의 기록을 그대로 유지한다.
- 상한 3개로 미룬 이슈: 없음(클러스터가 애초에 1건).

## 이슈별 결과

### M-114 — SHIPPED

- **제목**: packages/core 공개 API 경계 명시
- **커밋**: `f272049` (dev 직접 커밋, 트렁크 모델, PR 없음)
- **수용 기준 판정**:
  - ✅ "index.ts의 export * 11줄(실측 12줄)을 명시적 재export로 바꿔 공개 API를 의도적으로
    고른다" — `apps/web`·`apps/mobile` 전체를 `from "@motungi/core"` import/re-export
    기준으로 전수조사(스크립트로 정규식 매칭, import문과 re-export문 둘 다)해 실제 사용
    심볼 58개(이동 전 기준)를 뽑고, `index.ts`를 모듈별 `export type {}`/`export {}`로
    재작성했다. `isolatedModules: true`라 타입/값을 섞으면 번들 단계에서 실패하므로
    분리를 강제로 검증받았다(실수하면 typecheck가 아니라 vitest/next build에서 터진다는
    걸 확인 — 실제로 한 번도 안 섞였다).
  - ✅ "WEB_ONLY 18개(실측 15개, 축소 후 9개)를 'mobile 미사용' vs 'web 전용'으로 분류해
    문서에 남긴다" — `docs/architecture/core-public-api.md` 신설. BOTH 44 · WEB_ONLY
    9(core 잔류) · web으로 내린 6 · MOBILE_ONLY 1로 표를 만들고 각 항목의 판정 근거를
    적었다. 특히 `MockOpportunity`·`GeoPoint`·`CATEGORY_LABEL`은 "mobile이 이름을 직접
    import하지 않지만 구조적으로 이미 BOTH"임을 밝혔다(TS 구조적 타이핑 — `CatalogResult`·
    `rowToMock`을 통해 이미 의존한다).
  - ✅ "web 전용으로 판정된 것(gu-summary가 1순위)을 apps/web으로 내린다" —
    `packages/core/src/gu-summary.ts`(+테스트)를 `apps/web/src/lib/gu-summary.ts`로
    `git mv`. import를 상대경로(`./catalog`, `./view`)에서 `@motungi/core`로 교체.
    소비처 4곳(`app/page.tsx`·`app/sitemap.ts`·`app/explore/[gu]/page.tsx`, 그리고
    `MockOpportunity` 타입만 core에 남긴 채 분리)의 import를 `@/lib/gu-summary`로 갱신.
  - ✅ "pnpm typecheck·test가 web·mobile 양쪽에서 통과한다" — 아래 "검증" 참조.
  - **범위를 넘어 다음 밤 후보로 남긴 것**: `buildWhyReasonsPrompt`(web `/api/why-reasons`
    전용, `WhyReasonsPromptInput` 타입은 여전히 BOTH라 분리 손이 더 간다),
    `POPULAR_NEIGHBORHOODS`·`diagnosisSummaryChips`·`parseGpxPoints`("mobile 아직 미사용"이지
    "web 전용"은 아니라 지금 옮기면 왕복 비용). 문서에 근거와 함께 명시했다.

## 아키텍처 검토

- **담당**: packages/core(공개 API 표면) + apps/web(gu-summary 이동처) — 오케스트레이터가
  이슈 규모(1개, 리팩터 성격)를 감안해 별도 architect/frontend-impl 서브에이전트 분리 없이
  직접 계획·구현·검증을 수행했다.
- **레이어 경계**: `apps/web/src/lib/gu-summary.ts`는 여전히 순수하다(react-web.md 규율) —
  옮긴 뒤에도 import하는 건 `@motungi/core`뿐, react·store·hooks·components를 참조하지
  않는다. `apps/web/.eslintrc.json`의 `import/no-restricted-paths`는 `app/ → lib/` 방향을
  허용하므로 위반 없음.
- **예상 밖 파급 — TS2742 "portable type" 에러**: `index.ts`를 명시적 재export로 바꾸자
  `apps/web/src/lib/supabase.ts`의 `export const supabase = url && publishableKey ?
  createClient<Database>(...) : null`에서 `tsc`가 "추론된 타입을 이름 붙일 수 없다"고
  새로 실패했다(`Database`의 재export 경로가 한 단계 깊어지며 선언 emit이 참조 경로를 못
  찾은 것으로 추정). `export const supabase: SupabaseClient<Database> | null = ...`로
  명시적 타입 주석을 달아 해소했다 — 이건 잠재해 있던 타입 취약점을 이번 리팩터가
  드러낸 것이지, 새로 만든 문제가 아니다(추론에 기대던 게 우연히 통과하고 있었을 뿐).
- **데이터 흐름**: 순수 재배치(로직 변경 없음)라 마이그레이션 불필요. `gu-summary.ts`의
  함수 시그니처·동작은 그대로 옮겼다 — 회귀 테스트(콜로케이션 이동한 20케이스)가 전부
  무변경으로 통과해 동작이 안 바뀌었음을 증명한다.
- **테스트 전략**: unit(신규 없음 — 이동만) + 기존 계약 테스트 재배치(20케이스,
  `packages/core` → `apps/web/src/lib`로 콜로케이션 유지). `check-pure-tests.sh`가
  새 위치에서도 짝 테스트를 요구해 계속 강제한다.

## 변경 내용

| 파일 | 요약 |
|---|---|
| `packages/core/src/index.ts` | `export *` 12줄 → 모듈별 `export type {}`/`export {}` 54개 심볼 명시적 재export |
| `apps/web/src/lib/gu-summary.ts` (이동, `packages/core/src/`에서) | import를 `@motungi/core`로 교체, 이동 근거 주석 추가 |
| `apps/web/src/lib/gu-summary.test.ts` (이동) | import를 `@motungi/core`로 교체 |
| `apps/web/src/lib/supabase.ts` | `supabase` export에 `SupabaseClient<Database> \| null` 명시적 타입 주석(TS2742 해소) |
| `apps/web/src/app/page.tsx` | `summarizeGu` import를 `@motungi/core` → `@/lib/gu-summary` |
| `apps/web/src/app/sitemap.ts` | `summarizeGu` import를 `@motungi/core` → `@/lib/gu-summary`(`fetchOpportunities`는 core 유지) |
| `apps/web/src/app/explore/[gu]/page.tsx` | `GU_MIN_ACTIVITIES`·`guFaqs`·`isSeoulGu`·`summarizeGu`·`summarySentence`·`GuSummary` import를 `@/lib/gu-summary`로, `MockOpportunity`는 core 유지 |
| `docs/architecture/core-public-api.md` (신규) | BOTH/WEB_ONLY/MOBILE_ONLY 전수 분류와 판정 근거, 후속 후보 |

## 검증

모두 최신 `dev`(`510e543` 기준, 커밋 후 `f272049`) 위에서 실제 실행. 출력 원문 발췌:

```
$ pnpm install --frozen-lockfile
Done in 17.1s

$ pnpm typecheck   (커밋 전, index.ts만 바꾼 1차 시도)
@motungi/web:typecheck: src/lib/supabase.ts(12,14): error TS2742: The inferred type of
  'supabase' cannot be named without a reference to
  '../../node_modules/@motungi/core/src/database.types'. This is likely not portable.
  A type annotation is necessary.
 ELIFECYCLE  Command failed with exit code 2.
                                             ↓ SupabaseClient<Database> 명시적 타입 주석 추가
$ pnpm typecheck   (수정 후)
   • Packages in scope: @motungi/core, @motungi/mobile, @motungi/tokens, @motungi/web
 Tasks:    4 successful, 4 total

$ pnpm test
@motungi/core:test:  Test Files  16 passed (16) / Tests  417 passed (417)   ← 이동 전 17/437에서 gu-summary.test.ts(20케이스) 이동분만큼 감소, 정확히 일치
@motungi/mobile:test: Test Files  23 passed (23) / Tests  184 passed (184)
@motungi/web:test:   Test Files  49 passed (49) / Tests  415 passed (415)  ← 이동 전 48/395에서 +1파일/+20케이스, 정확히 일치
 Tasks:    3 successful, 3 total

$ pnpm --filter @motungi/web build
✓ Generating static pages (27/27)
(라우트 27/27 성공 — /explore/[gu]·/sitemap.xml 포함, gu-summary 이동으로 인한 빌드 실패 없음)

$ bash scripts/gate.sh   (커밋 후, 실제 diff 대비 시크릿 스캔 포함)
── typecheck   ✅
── test        ✅
── lint        ✅ (경고만 — 기존 max-lines-per-function/complexity, 이번 변경과 무관)
── secrets     ✅ (시크릿 패턴 없음)
── pure-tests  ✅ (순수 레이어 테스트 누락 없음, 면제 6건)
GATE: PASS — push 허용
```

push 전 머지 체크: `git log --oneline origin/dev..origin/main` → 비어 있음(main이 dev를
앞서지 않음, 되받을 것 없음). `git merge-base --is-ancestor origin/dev HEAD` → OK
(fast-forward, push 시점까지 dev에 동시 작업 없음 확인).

## 리스크 & 후속

- **M-097(좌표 백필)은 네 번째 밤째 blocked-human.** `.env`에 `DATA_GO_KR_SERVICE_KEY`를
  채우거나 outbound network 예외를 허용해야 다음 밤이 진행할 수 있다. 매 밤 씨앗 후보로
  재검토하는 비용이 쌓이고 있다 — 계속 막혀 있을 거면 우선순위를 낮추거나 "사람 조치 대기"로
  분류해 씨앗 후보에서 자동 제외되게 하는 걸 고려해 달라(09-22 리포트에서도 같은 요청).
- **CLUSTER RULE 폴백 조건 불일치**는 09-22 리포트에 기록된 채 아직 사람 조치가 없다.
  오늘 밤은 두 해석이 우연히 같은 결론(폴백 미발동)에 도달해 실질적 영향은 없었지만,
  다음에 "scope에 파일은 있는데 하나도 안 겹치는" 씨앗이 나오면 다시 갈릴 수 있다.
- **`buildWhyReasonsPrompt` core→web 이동 후보** — `WhyReasonsPromptInput` 타입은 BOTH라
  분리 작업이 gu-summary보다 손이 간다. 다음에 core 경계 관련 이슈가 씨앗이 되면 이어서
  다룰 만하다(`docs/architecture/core-public-api.md` 참조).
- **`lint`가 이미 걸고 있는 사전 경고**(이번 변경과 무관, 기존 상태 유지): `diagnosis/page.tsx`·
  `explore/(list)/page.tsx`·`location/page.tsx`·`saved/page.tsx`·`neighborhood-menu.tsx`·
  `opportunity-detail-desktop.tsx`·`opportunity-detail-mobile.tsx`·`report-desktop.tsx`·
  `web-landing.tsx`가 `max-lines-per-function`/`complexity` 경고를 낸다(M-102가 이미 이 중
  2파일을 백로그에 등재해둠). 오늘 밤 범위 밖이라 손대지 않았다.
