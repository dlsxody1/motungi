# Nightly Report — 2026-07-21

## TL;DR

- 집은 이슈는 **M-015**(medium, "웹/모바일 패리티 정합: 라우팅·누락 섹션·마이크로카피 통일", scope `apps/web`, `apps/mobile`, `packages/core`). M-017(medium, id 더 큼)과 우선순위가 동률이라 낮은 id인 M-015를 선택했다. planner가 `dev@726c3e5` 기준으로 재검증한 결과 done_when 5개 중 4개가 미충족 상태였다(이전 몇몇 이슈에서 있었던 "백로그 문구가 이미 충족된 stale 상태" 패턴은 이번엔 해당 없음).
- `apps/web`의 report/opportunity/diagnosis 3개 화면과 `apps/mobile`의 index/diagnosis 2개 화면, 총 6개 파일(테스트 1개 포함)을 수정했다. `packages/core`는 손대지 않았다 — architect가 문구들을 페이지 로컬 리터럴로 판단해 core 승격이 불필요하다고 결정했다.
- 검증: **typecheck 클린(4패키지)**, **core 207/mobile 14/web 114 테스트 전부 통과(web 신규 2건 포함)**, **build 20/20 라우트 성공**, diff가 예상 6개 파일과 정확히 일치, 시크릿 매치 0건. qa 판정: **PASS**.
- 이번 구현 커밋(`4409846`)은 `origin/dev` 위에 직접 쌓인 트렁크 커밋이다. 이 리포트 자체도 같은 방식으로 `dev`에 직접 커밋되며, PR은 열지 않는다.

## 집은 이슈

- **M-015** — 웹/모바일 패리티 정합: 라우팅·누락 섹션·마이크로카피 통일 (priority: medium, scope: `apps/web`, `apps/mobile`, `packages/core`, status: todo → **doing**)
- M-017("RN 모바일 화면 렌더 테스트 도입", medium)도 후보였으나 id가 더 크고 우선순위는 동일해, planner가 낮은 id인 M-015를 선정하는 타이브레이크 규칙을 적용했다. M-017은 오늘 밤 손대지 않고 여전히 todo로 남아 있다.
- planner가 `dev@726c3e5` 기준 실코드 대조로 확인한 결과, done_when 5개 항목 중 아래 4개가 실제로 미충족 상태임을 확인:
  - 웹 report `ReportEmpty`의 `onRetry`가 `isError` 분기 없이 항상 동일 경로로 이동 → 미충족.
  - 모바일 온보딩 보조 CTA가 기본 버튼과 동일하게 `/location`으로 이동해 완전 중복 → 미충족.
  - 웹 opportunity 모바일(`md:hidden`) 레이아웃에 `whyReasons` 카드 부재 → 미충족.
  - disclaimer·원픽 CTA·진단 Q2 옵션 카피가 웹·모바일 간 문구 불일치 → 미충족.
  - (5번째 typecheck·test 통과는 done_when이 아니라 게이트 조건이라 planner 사전검증 대상은 아님)
- 다른 일부 과거 이슈에서 나타났던 "백로그 문구가 이미 충족돼 stale"한 사례와 달리, 이번 M-015는 5건 모두 실제 작업 대상이었다.

## 아키텍처 검토

- architect가 M-015를 **frontend-impl 단독** 작업으로 배정했다 — `backend-impl`은 불필요.
- `packages/core` 변경도 불필요하다고 판단: 이번에 통일한 문구들(disclaimer, 원픽 CTA 라벨, 진단 Q2 옵션 title/desc)은 각 페이지 로컬 리터럴이며 core가 소유해야 할 공유 도메인 로직이 아니라고 결론지었다. done_when 문구의 "가능하면 core 공유 소스"는 선택적 권고로 해석해 이번엔 페이지 로컬 상수 통일로 충분하다고 판단.
- FSD 경계 위반 없음 확인 — 이 저장소는 `entities`/`features`/`widgets` 같은 리터럴 폴더 대신 `app/` + `components`/`hooks`/`lib`/`data`/`store` 구조를 쓰며, 이번 변경은 그 경계를 넘지 않는다.
- 테스트 전략: 웹은 RTL 컴포넌트 테스트를 기존 파일 컨벤션(`page.test.tsx`)에 맞춰 추가. 모바일은 이번에 손댄 리프 스크린(index/diagnosis)에 대해 typecheck만으로 검증하기로 결정 — RN 화면 렌더 테스트 도입 자체는 별도 백로그 M-017의 범위이므로 이번 밤에는 손대지 않았다(의도적으로 배제, 누락 아님).
- 원픽 CTA 라벨을 "자세히 보기"로 통일하기로 한 근거를 architect가 명시: 웹 두 곳·모바일 한 곳 중 이미 모바일에서 쓰이던 값이고, 세 후보 중 가장 짧으며, 웹 카카오톡 공유 타이틀 문구와도 일치해 추가 카피 충돌이 없다.
- 진단 Q2 옵션 카피는 architect가 정확한 canonical 문자열을 사전 확정해 구현자에게 전달: weekend desc는 "토·일 오전/오후", flexible title은 "유동적". 웹·모바일 양쪽 모두 이 문자열로 맞췄다.

## 변경 내용

6개 파일 변경(커밋 `4409846`):

- `apps/web/src/app/report/page.tsx` — `ReportEmpty`의 `onRetry`를 `isError` 기준으로 분기(에러→`/loading`, 미진단→`/diagnosis`), 원픽 CTA 라벨 2곳을 "자세히 보기"로 통일.
- `apps/web/src/app/report/page.test.tsx` — 위 라우팅 분기를 검증하는 신규 테스트 2건 추가(에러→`/loading`, 빈상태→`/diagnosis`), 기존 테스트는 무변경.
- `apps/mobile/app/index.tsx` — 온보딩 보조 CTA를 `/location`(기본 버튼과 완전 중복)에서 `/report`로, 라벨을 "로그인 없이 바로 시작"으로 수정.
- `apps/web/src/app/opportunity/page.tsx` — 모바일(`md:hidden`) 레이아웃에 기존 `why` 변수를 재사용한 `whyReasons` 카드 추가, 모바일·데스크탑 두 곳의 disclaimer 문구를 "보러 가기를 누르면..."으로 통일.
- `apps/web/src/app/diagnosis/page.tsx` — 진단 Q2 weekend desc를 "토·일 오전/오후"로 수정(모바일과 통일).
- `apps/mobile/app/diagnosis.tsx` — 진단 Q2 flexible title을 "유동적"으로 수정(웹과 통일).

범위 밖 파일(`packages/core`, `apps/mobile/app/opportunity.tsx`, `web-landing.tsx`, `supabase/` 등)은 무변경임을 확인했다.

## 검증

qa가 실제로 돌린 명령과 그 출력(리뷰어가 재확인):

```
$ pnpm install --frozen-lockfile
lockfile up to date
```

```
$ pnpm typecheck   (turbo, 4 packages)
Tasks: 4 successful, 4 total
exit 0
```

```
$ pnpm test   (turbo)
@motungi/core:  207/207 passed
@motungi/mobile: 14/14 passed
@motungi/web:   114/114 passed  (report/page.test.tsx 4 tests, including the 2 new ones)
Tasks: 3 successful, 3 total
exit 0
```

```
$ pnpm --filter @motungi/web build
✓ Compiled successfully
✓ Generating static pages (20/20)
exit 0
```

- diff 오염 검사: 예상된 6개 파일만 변경됨, 그 외 변경 없음.
- 시크릿 grep(`SUPABASE_SERVICE_ROLE`/`SECRET`/`API_KEY`/`password`/`token`): 매치 0건.
- `supabase/` 및 마이그레이션 파일 무변경.
- QA 판정: **"PUSH GATE: PASS"**.
- M-015 done_when 5개 항목 전부, 구현자 주장이 아니라 실제 diff와 명령 출력 대조로 ✅ 확인됨.

## 리스크 & 후속

1. **모바일 화면 렌더 테스트 부재**: `apps/mobile/app/index.tsx`, `(tabs)/report.tsx`, `diagnosis.tsx` 등 모바일 화면에는 스크린 렌더 테스트가 없어 이번 변경은 typecheck만으로 검증됐다. RN 렌더 테스트 도입은 별도 백로그 **M-017**(오늘 밤 다루지 않음, 여전히 todo)의 범위로 남아 있다.
2. **opportunity 모바일 why-card 전용 테스트 없음**: `apps/web/src/app/opportunity/page.tsx`에 추가한 신규 모바일 `why-card`에는 전용 RTL 테스트가 붙지 않았다 — done_when이 요구하지 않았고 architect가 선택사항으로 명시했기 때문이다. 후속 커버리지로 고려할 만하다.
3. **원픽 CTA 라벨 선정 근거 기록**: 세 곳 중 "자세히 보기"를 canonical로 택한 이유는 (a) 이미 모바일에서 쓰이던 값이고, (b) 세 후보 중 가장 짧으며, (c) 웹 카카오톡 공유 타이틀 문구와도 일치하기 때문 — 아침 리뷰 시 재론 없이 이 근거를 참고할 수 있도록 여기 남긴다.
4. status는 `todo → doing`으로만 갱신했다 — `done` 승격은 사람의 최종 리뷰 몫이다.

## dev 직접 커밋 (PR 없음)

이번 밤도 트렁크 기반 모델을 따랐다: 브랜치를 파지 않고 `origin/dev` 위에 직접 커밋·push하며 PR은 열지 않는다. 구현 커밋(`4409846`)과 이 리포트·backlog 커밋 모두 `dev`에 직접 쌓였고, qa PASS 판정에 따라 `git push origin dev`로 push한다. `dev → main` 승격은 사람이 주기적으로 직접 판단하며, 이는 야간 파이프라인의 역할이 아니다 — main은 이번 밤에도 건드리지 않았다.
