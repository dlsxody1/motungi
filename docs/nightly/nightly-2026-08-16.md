# Nightly Report — 2026-08-16

## TL;DR
⚠️ **`M-052`가 오늘 밤으로 4일 연속(08-13·08-14·08-15·08-16) 동일 사유로 재제외됐다** — 사람 개입이 필요한 반복 패턴이다(§리스크&후속 최상단 참조).

클러스터 = **M-043 단독**(`culture_info` 154행 산문 백필 — detail2 배치 로직·픽스처 테스트). `bash scripts/gate.sh`(typecheck·test·lint·시크릿 스캔, 4패키지 전체) **PASS**(typecheck 4/4 ✅, test core 286(신규 23)+web 279+mobile 91=656/656 ✅, lint ✅, secrets ✅) — **shipped, dev로 나갔다**(커밋 `06e66c1`). revert 없음. 오늘 밤은 로직·픽스처 테스트까지만 진행했고(샌드박스에 실 시크릿·네트워크 없음), 실 데이터 대상 0%→50%+ 커버리지 검증은 architect 결정에 따라 사람/QA가 실 자격증명 있는 환경에서 후속 수행한다 — 이는 오늘 밤의 실패가 아니다.

씨앗 후보는 원래 `M-052`(유일한 medium priority todo, 모바일 상세 지도·산책로 렌더)였다. planner가 실행되기 전 오케스트레이터가 먼저 재검증했고, 08-13부터 08-15까지 이미 3일 연속 동일하게 제외돼 있던 상태가 오늘 밤도 그대로였다(`grep -i map apps/mobile/package.json` 여전히 공백, `.env.example`에 신규 지도 관련 키 없음, `security-policy.md`에 지도 관련 내용 0). **동일하게 제외**했다(§아키텍처 검토·§리스크&후속 참조). `M-052`는 `todo`로 유지된다.

연속 report-only 0회 — `docs/nightly/nightly-2026-08-11.md`~`nightly-2026-08-15.md` 5개 노트를 직접 열어 TL;DR을 확인했고 전부 실제 코드/문서 산출물이 dev로 나간 밤이었다(08-11 M-044+M-049, 08-12 M-048, 08-13 M-050+M-051+M-053, 08-14 M-055, 08-15 M-038). 경고 불필요.

## 집은 클러스터
- **0단계 백로그 게이트**: 밤 시작 시점 `status:todo` 3건(M-043 low, M-052 medium, M-054 low) → ≥2, **구현 모드**로 정상 진행(audit 모드 미발동).
- 원 씨앗 `M-052`(priority medium, 유일한 medium todo) → 08-13~08-15와 동일 사유로 **오케스트레이터가 planner 실행 전 사전 제외**(재검증 상세는 §아키텍처 검토).
- `M-052` 제외 후 후보 풀 = {`M-043`[supabase/functions/ingest, scripts/], `M-054`[apps/mobile/app/(tabs)/explore.tsx]}. 둘 다 priority `low`, 파일/디렉토리 겹침 0(scope 교집합 없음).
- **재시딩**: 남은 두 후보가 동순위(low)라 id 오름차순으로 `M-043`을 씨앗으로 선택.
- `M-043` 기준 파일 겹침 후보 0개. 앱 단위 폴백(같은 앱 경계 + 같은 priority)도 시도 — `supabase`(edge function) 경계와 `apps/mobile`(M-054) 경계가 달라 폴백도 0개.
- 클러스터 = `M-043` 단독. **1이슈 밤은 정상 결과**(패딩 없음).
- 상한 3개로 밀린 이슈는 없다(애초에 겹치는 후보가 없었다). `M-054`는 그냥 이번 밤 씨앗 경합에서 안 뽑혔을 뿐 다음 밤 유력 후보로 남는다(`todo` 그대로).
- planner가 `origin/dev`==`HEAD`==`147491d`임을 확인(`git log origin/dev..origin/main` 비어 있음 — main/dev 간 되받을 것 없음)하고, M-043·M-054 둘 다 done_when이 실제로 미충족임을 코드로 재확인(culture_info 설명 커버리지 0% 코드 주석으로 확인, explore.tsx에 로딩 스켈레톤 분기 없음을 직접 읽어 확인)한 뒤에 진행했다.

## 이슈별 결과
| id | 결과 | 비고 |
|---|---|---|
| M-052 | **제외**(구현 안 함, `todo` 유지) | 08-13~08-15와 동일 사유 4연속 재확인 — 사람 승인 없이 채택 가능한 지도 구현 경로가 없음 |
| M-054 | **미채택**(구현 안 함, `todo` 유지) | 클러스터에 포함 안 됨(scope 겹침 없음) — 다음 밤 유력 씨앗 후보 |
| M-043 | **shipped** | 커밋 `06e66c1`, dev push 완료, qa 게이트 PASS. 실 커버리지 검증은 사람/QA 후속(architect 결정) |

## 아키텍처 검토
**담당**: backend-impl 단독(프론트 관여 없음).

**M-052 4일 연속 재제외 상세**: `docs/backlog/backlog.yml`(M-052 notes)에 08-13·08-14 두 밤의 판정이 기록돼 있고, 08-15 밤도 오케스트레이터가 동일 재검증 후 제외했다(단, 08-15 밤은 이 사실을 backlog 노트에는 남기지 못하고 그날 리포트에만 남겼다는 공백을 오늘 밤 재검증 중 발견해 backlog.yml에 소급 기록했다 — 상세는 M-052 notes 참조). 오늘 밤도 판정을 그대로 수용하지 않고 직접 재검증했다: `grep -i map apps/mobile/package.json` 여전히 공백, `.env.example`에 지도 관련 신규 env 키 없음(기존 `NAVER_MAP_CLIENT_ID`/`NAVER_MAP_Client_SECRET`는 역지오코딩·웹 SDK용이지 모바일 네이티브 지도용이 아님), `.claude/rules/core/security-policy.md`에 지도 관련 내용 0건. 08-13이 제시한 3개 구현 경로(react-native-maps 신규 네이티브 의존성 / NAVER 정적지도 백엔드 프록시 신설 / 키리스 서드파티 정적지도) 전부 여전히 사람 승인·프로비저닝 대기 상태다.

**4일 연속이 의미하는 것**: 08-15에 이미 한 번 지적했던 "오케스트레이터 단계 사전 제외 워크어라운드가 `.claude/agents/planner.md`/`architect.md` 어디에도 성문화돼 있지 않다"는 문제가 오늘 밤도 반복됐다 — 이번엔 08-15와 마찬가지로 planner 단계까지 가지도 않고 씨앗 선정 시점에 즉시 배제했다. 이 워크어라운드 자체가 매일 밤 반복되는 "관행"이 되고 있는데 문서에는 없다. 사람이 (a) 세 경로 중 하나를 승인하거나, (b) `M-052`를 medium에서 내려 씨앗 경합에서 자연히 빠지게 하거나, (c) 이 사전 제외 절차를 planner.md/architect.md에 정식으로 문서화하는 것 중 하나를 결정해야 한다.

**M-043 설계**: 스코프 협소화(로직·픽스처 테스트만) 결정 — 이 샌드박스에 `DATA_GO_KR_SERVICE_KEY`/Supabase 시크릿이 전혀 없음을 확인해 라이브 API 호출은 애초에 불가능·미시도. (1) 순수 백필 로직은 `packages/core/src/adapters/culture-info-detail.ts`(신규)에 두고, 이를 소비하는 신규 독립 오케스트레이션 스크립트 `scripts/backfill-culture-info-detail.ts`(신규)를 일일 cron edge function과 완전히 분리했다 — `supabase/functions/ingest/index.ts`는 손대지 않아 cron 런타임에 구조적으로 영향이 없다. edge function 내부에 detail2 로직을 넣는 안은 기각했다: (a) seq당 순차 호출 ≈1200회가 일일 1회 cron 실행 예산 안에서 런타임을 과도하게 늘리고, (b) 그 디렉터리의 Deno 테스트는 `gate.sh`에서 자체 테스트 파일 헤더 주석으로 명시적으로 제외돼 있어 거기 둔 로직은 push 게이트가 절대 검증하지 못한다. (2) 신규 DB 마이그레이션 없음 — 기존 nullable `description` 컬럼(마이그레이션 0015가 이미 추가했고, 그 주석 자체가 이 백필-by-`IS NULL` 설계를 미리 예견하고 있었다)을 재개 체크포인트로 재사용한다: 스크립트가 행 하나 성공할 때마다 즉시 description을 write하므로, `WHERE description IS NULL`이 어느 재실행에서든 이미 끝난 행을 자연히 배제한다 — 별도 상태 테이블 불필요.

**담당·범위**: backend-impl 단독 배정(프론트엔드 관여 없음). 실 프로덕션 데이터 대상 0%→50%+ 커버리지 검증은 사람/QA가 실 자격증명 있는 환경에서 나중에 수행하도록 명시적으로 이연했고, 이는 오늘 밤의 실패 조건으로 세지 않는다(architect 결정).

**scope 필드 참고**: backlog의 M-043 `scope`는 `[supabase/functions/ingest, scripts/]`였지만 실제로는 `packages/core/src/adapters/`가 주 변경지였다(architect가 위 (a)(b) 이유로 의도적으로 그렇게 결정) — `supabase/functions/ingest`는 diff에 전혀 등장하지 않는다. 의도된 이탈이고 QA·reviewer 둘 다 diff를 직접 읽어 이 결정이 정당함을 확인했지만, backlog scope 필드 자체는 실제 변경지와 어긋난 채로 남아 있다는 점은 다음 밤을 위해 기록해둔다.

## 변경 내용
- **신규** `packages/core/src/adapters/culture-info-detail.ts`(107줄) — `buildDetail2Url`(serviceKey/seq로 detail2 URL 조립, period2와 동일 인증 컨벤션) · `parseDetail2Description`(`contents1` 추출, 기존 `parseXmlItems`/`joinDescription` 재사용) · `isRateLimitError`(실측 `LIMITED_NUMBER_OF_SERVICE_REQUESTS_EXCEEDS_ERROR` 문자열 판정) · `nextRetryDelayMs`(지수 백오프, 60초 상한) · `selectPendingSeqs` · `summarizeBackfillOutcomes`. fetch/Supabase 호출 없는 순수 함수만.
- **신규** `packages/core/src/adapters/culture-info-detail.test.ts`(182줄) — 위 6개 함수 전부 픽스처/인메모리 데이터로 커버(vitest, 네트워크 없음), 23테스트.
- **수정** `packages/core/src/adapters/index.ts` — barrel export 한 줄 추가(`export * from "./culture-info-detail"`).
- **신규** `scripts/backfill-culture-info-detail.ts`(175줄) — 실 fetch + `@supabase/supabase-js` 배선하는 오케스트레이션. `DATA_GO_KR_SERVICE_KEY`/`SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`는 `process.env`만 신뢰(CLI 인자 폴백 없음), 키 값은 로그에 남기지 않는다. `--dry-run` 지원. 행 하나 성공할 때마다 즉시 description을 write(체크포인트).
- diff는 이슈 하나(M-043)에만 집중돼 있다 — `supabase/functions/ingest/*`·`scoring.ts`(읽기 전용 타입 import 1건 제외)·`view.ts`·`apps/web`·`apps/mobile`에 무관한 변경 없음. 신규 마이그레이션 없음.

## 검증
QA가 독립적으로 재실행하고 붙여넣은 실제 출력(구현자 주장을 그대로 신뢰하지 않고 재확인):
```
$ pnpm --filter @motungi/core typecheck
tsc --noEmit  →  에러 없음

$ pnpm --filter @motungi/core test
 Test Files  11 passed (11)
      Tests  286 passed (286)   (신규 23)

$ bash scripts/gate.sh
── typecheck
   ✅ typecheck (4/4 패키지)
── test
@motungi/core:test:  Test Files  11 passed (11)  Tests  286 passed (286)
@motungi/web:test:   Test Files  36 passed (36)  Tests  279 passed (279)
@motungi/mobile:test: Test Files  17 passed (17)  Tests  91 passed (91)
   ✅ test
── lint
@motungi/web:lint: ✔ No ESLint warnings or errors
   ✅ lint
── secrets
   (scan-secrets.sh 클린)
   ✅ secrets

GATE: PASS — push 허용
```
- QA가 커밋이 정확히 claimed 4개 파일만 건드렸는지, `supabase/functions/ingest/*`·`scoring.ts`·`view.ts`(읽기전용 타입 import 제외)·`apps/web`·`apps/mobile`에 오염이 없는지, 신규 마이그레이션이 없는지 diff로 직접 확인. 신규 파일 전부 전문을 읽고 주장된 로직과 실제 코드가 일치함을 확인(커밋 메시지만 신뢰하지 않음).
- QA가 7개 acceptance criteria 전부 판정: 6개 ✅ + 기준1(실 커버리지 상승)은 architect의 명시적 이연 근거로 ✅(미달성으로 카운트 안 함).
- **reviewer(이 단계)가 다시 한번 `git show 06e66c1caeb1958b1372fb945af13528eba0df6a` 전체 diff를 직접 읽고 최종 확인**: 통계상으로도 정확히 4개 파일·465줄 추가(삭제 0)만 존재. `parseXmlItems`(ingest-fetch.ts:10)·`joinDescription`/`stripHtml`(util.ts:108,129)·`OpportunityRow`(view.ts) import가 실제로 해당 위치에 존재함을 grep으로 재확인. 시크릿 리터럴·JWT·AWS 키·PEM 패턴 스캔 결과 이 4개 파일에서 매치 0건. 마이그레이션 0015가 실제로 nullable `description` 컬럼과 그 컬럼 주석("description이 null인 행을 정상 케이스로 다뤄야 한다")을 갖고 있음을 확인 — architect 설명이 사실과 일치. 파괴적 SQL 없음(SQL 자체가 없음, Supabase client만). 스타일·리스크 관점에서 추가로 지적할 사항 없음 — QA 판정을 그대로 확정.
- `git log origin/dev..origin/main` 재확인 — 비어 있어 main과의 divergence 없음.

## 리스크 & 후속
- **⚠️ `M-052` 4일 연속 블록(08-13~08-16)**: 지도/산책로 표시 기능이 사람의 아키텍처 결정 없이는 더 이상 진행 불가능한 상태가 굳어졌다. 세 옵션(`react-native-maps` 신규 네이티브 의존성 승인 / NAVER 정적지도 백엔드 프록시 신설 승인·배선 / 키리스 서드파티 정적지도 서비스 지정) 중 하나를 사람이 승인하거나, `M-052`의 priority를 medium에서 낮춰 씨앗 경합에서 자연히 빠지게 할 것을 강하게 권장한다. **또한 이 오케스트레이터 단계 사전 제외 워크어라운드(08-15에 처음 즉흥적으로 도입, 오늘 밤 그대로 반복) 자체가 `.claude/agents/planner.md`/`architect.md` 어디에도 문서화돼 있지 않다** — 사람이 이 재량이 적절했는지 검토하고, 계속 쓸 것이면 두 문서에 정식 절차로 성문화할 필요가 있다.
- **QA 비차단 운영 노트 — 재시도가 반응형 전용**: `nextRetryDelayMs`/`isRateLimitError` 기반 백오프는 쿼터 초과 응답을 받은 *뒤에만* 작동하고, 연속 성공 호출 사이에 선제적 고정 간격 스로틀이 없다. 코드 결함은 아니고 어떤 acceptance criterion도 fail시키지 않지만, 실 프로덕션 첫 실행(seq당 ≈1200회 순차 호출) 때 쿼터 초과가 얼마나 자주 발생하는지 지켜볼 가치가 있다 — 필요하면 후속 이슈로 선제적 스로틀(예: 호출 사이 고정 지연)을 추가하는 것을 고려.
- **M-043 실 데이터 커버리지 검증 미완**: done_when 1번("0%에서 50%+로 상승")은 이 샌드박스에 `DATA_GO_KR_SERVICE_KEY`/Supabase 자격증명이 없어 오늘 밤 실행 불가능했다(architect가 명시적으로 이연, 실패로 카운트하지 않음). `status: doing`이지만 사람이 실 자격증명 있는 환경에서 `scripts/backfill-culture-info-detail.ts`를 실행해 실제 커버리지 상승을 확인해야 이 이슈가 완결된다.
- **트리거 프롬프트 PRECEDENCE 절 점검 결과 — 오늘 밤은 모순 없음**: 이 트리거 프롬프트가 지정한 원칙과 현재 repo 문서(`docs/nightly/PIPELINE.md`, `.claude/rules/workflow/nightly-pipeline.md`)를 대조했다. 두 문서 모두 이미 현재의 클러스터 규칙(파일 겹침 기반, 상한 3개, 패딩 금지)과 0단계 백로그 게이트(audit 모드 전환 조건)를 정확히 반영하고 있어 **오늘 밤은 2026-07-23~07-30 사건(트리거와 repo 문서가 8일간 서로 모순돼 밤이 조기 종료됐던 사고, M-024로 사후 해소)과 달리 무엇도 override할 필요가 없었다.**
- **다음 밤 후보**: `M-054`(모바일 탐색 로딩 스켈레톤, low, 유일하게 남은 non-M-052 todo) — scope 겹침이 없어 단독 클러스터가 될 가능성이 높다. `M-052`는 여전히 사람 결정 대기 중이라 다음 밤도 사전 제외 대상일 가능성이 높다(위 리스크 항목 참조).
