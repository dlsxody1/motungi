# Nightly Report — 2026-08-30

## TL;DR

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `docs/backlog/backlog.yml`의 `status: todo`는 **22건**(M-052 + M-073~M-093, 어젯밤 2026-08-29 audit 모드가 등재)이었다 — `todo ≥ 2`이므로 구현 모드로 진행했다.
- **연속 report-only 경보 해당 없음.** 직전 밤 2026-08-29는 report-only(audit 모드, todo<2라 코드 무변경)였지만 그 전날 2026-08-28은 M-070이 shipped된 정상 구현 밤이었다. 즉 report-only 연속 횟수는 **1**(어젯밤 하루)뿐이라 경보 기준(2회 이상)에 못 미친다. `ls docs/nightly/`와 두 파일 TL;DR을 직접 열어 확인했다.
- **브랜치 동기화 점검(신규 규칙, nightly-pipeline.md 6번) 수행함**: `git log --oneline origin/dev..origin/main`이 비어 있었다(origin/main의 tip `744411d`가 이미 `origin/dev`의 조상) — 되받을 게 없어 no-op으로 그냥 진행했다.
- **클러스터 = M-073(씨앗) + M-089 + M-090, 3건 전부 shipped.** 되돌린 이슈 없음.
- `bash scripts/gate.sh` **PASS** (typecheck 4/4 · test core 314/314·mobile 146/146·web 311/311 · lint web 통과 · 시크릿 없음) — 리뷰어가 최종 트리에서 재실행해 재확인함.
- **doc 모순 2건 발견**(아래 리스크 참조): `.claude/agents/architect.md`의 FSD 언급이 `constitution.md`와 충돌, `.claude/agents/reviewer.md`의 게이트 서술이 구식.

## 집은 클러스터

- **씨앗**: M-073 (priority `high`, high 등급 todo 중 최소 id — M-076/M-082/M-085보다 앞섬).
- **M-089 추가** — 이유: **파일 완전 겹침**. M-073과 M-089 둘 다 `apps/mobile/app/(tabs)/saved.tsx`를 직접 건드린다(M-073은 `SavedItem`의 Pressable에 `accessibilityRole` 추가, M-089는 같은 `SavedItem`에 `Thumbnail` 추가).
- **M-090 추가** — 이유: **좁은 디렉토리 겹침**. M-073이 `apps/mobile/ui/hero-carousel.tsx`(=`apps/mobile/ui/` 하위)를 건드리고, M-090도 같은 `apps/mobile/ui/` 하위의 `components.tsx`를 건드린다. (`apps/mobile` 전체 겹침이 아니라 `apps/mobile/ui/`라는 좁은 디렉토리 겹침이라 클러스터 규칙의 기준을 충족한다.)
- **상한 3개에 정확히 도달** — 더 미룬 이슈 없음. 겹침 후보가 애초에 이 2건뿐이었고 폴백(같은 앱+같은 priority)도 필요 없었다(실제 파일/디렉토리 겹침이 있었으므로).
- 남은 high priority todo(M-076, M-082, M-085)는 이번 클러스터의 씨앗 파일과 겹치지 않아 포함하지 않았다 — 다음 밤 후보.

## 이슈별 결과

- **M-073 (shipped, `8e5373d`)** — `fix(mobile): accessibilityRole 스윕 saved/my/hero-carousel (M-073)`. saved.tsx 4곳, my.tsx 재진단+메뉴 6항목, hero-carousel.tsx HeroCard에 `accessibilityRole="button"` 추가. saved.test.tsx에 4개 컨트롤 role 검증 추가, my.test.tsx·hero-carousel.test.tsx 신규 작성. done_when 5개 항목 전부 diff+테스트로 확인됨(`getByRole("button")` 쿼리로 saved/my/hero-carousel 3개 화면 각각 검증, `pnpm --filter mobile typecheck`/`test` 통과).
- **M-089 (shipped, `0cf9a37`)** — `feat(mobile): 보관함 카드 썸네일 추가 (M-089)`. saved.tsx `SavedItem`에 explore.tsx와 동일한 `Thumbnail`(64x64, tone 매핑) 추가, thumbnail.tsx 자체는 무수정 재사용. imageUrl 있음/없음/로드 실패 3가지 폴백과 고정 64x64(레이아웃 시프트 없음)를 saved.test.tsx에 추가. done_when 3개 항목 전부 충족.
- **M-090 (shipped, `9f50bfa`)** — `chore(mobile): 죽은 Card 프리미티브 제거 (M-090)`. `apps/mobile/ui/components.tsx`의 미사용 `Card` export 5줄 삭제. word-boundary grep으로 소비처 0곳 재확인. 테스트 커버리지가 없던 컴포넌트라 테스트 변경 없음(=계약 없음). done_when 3개 항목(제거 완료, grep 무매치, typecheck·test 통과) 전부 충족.

## 아키텍처 검토

- **담당**: frontend-impl 단독 지정. 3건 전부 `apps/mobile` UI 레이어만 건드리고 `packages/core`/`supabase` 접촉이 없어 backend-impl 불필요.
- **M-090 방향 결정**: done_when이 "제거 또는 배선" 양자택일이었는데, architect가 **제거**로 확정했다 — word-boundary grep으로 소비처 0곳을 재확인했고, 배선(wiring)은 이번 클러스터 범위 밖의 새 UX 판단이 필요해 무인 야간 파이프라인 권한 밖으로 판단.
- **독립 revert 가능성 확인**: saved.tsx에서 M-073과 M-089의 변경이 서로 다른 hunk임을 diff로 직접 확인했다 — M-073은 `Pressable` 속성(`accessibilityRole="button"`) 추가, M-089는 새 import(`Thumbnail`)+JSX 자식 요소+스타일 추가. 겹치는 줄이 없어 한쪽만 되돌려도 다른 쪽이 안전하다.
- **파일 구조 컨벤션**: FSD 계층이 아니라 평면 구조(`app/`·`ui/`) 준수 확인 — constitution.md 규칙과 일치, 위반 없음.
- **테스트 전략**: 3건 모두 component-level RTL(getByRole/getByTestId류) + typecheck. e2e 불필요 범위(순수 UI prop/렌더 변경).

## 변경 내용

- `apps/mobile/app/(tabs)/saved.tsx` — M-073(accessibilityRole 4곳) + M-089(Thumbnail 추가) 순차 적용.
- `apps/mobile/app/(tabs)/saved.test.tsx` — M-073 role 검증 + M-089 썸네일 폴백 3종 테스트 추가.
- `apps/mobile/app/(tabs)/my.tsx` — M-073, 재진단·메뉴 6항목 Pressable에 accessibilityRole 추가.
- `apps/mobile/app/(tabs)/my.test.tsx` (신규) — M-073 role 검증.
- `apps/mobile/ui/hero-carousel.tsx` — M-073, HeroCard Pressable에 accessibilityRole 추가.
- `apps/mobile/ui/hero-carousel.test.tsx` (신규) — M-073 role+접근 가능 이름 검증.
- `apps/mobile/ui/components.tsx` — M-090, 미사용 `Card` export 5줄 삭제.
- `git diff origin/dev..HEAD --stat`로 위 7개 파일 외 아무것도 건드리지 않았음을 재확인(`apps/web`·`packages/core`·`supabase`·`.env`·CI 설정 무접촉).

## 검증

리뷰어가 최종 3커밋 트리에서 `bash scripts/gate.sh`를 직접 재실행해 qa 결과를 재확인함:

```
── typecheck: ✅ (core/tokens/mobile/web 4/4 packages, cache hit)
── test: @motungi/core:test   Test Files 11 passed (11)  Tests 314 passed (314)
         @motungi/mobile:test Test Files 21 passed (21)  Tests 146 passed (146)
         @motungi/web:test    Test Files 40 passed (40)  Tests 311 passed (311)
         ✅ test
── lint: @motungi/web:lint → "✔ No ESLint warnings or errors" (mobile/core/tokens는 lint 스크립트 자체가 없음 — 기존부터의 상태, 이번 클러스터와 무관)
── secrets: (시크릿 패턴 없음) ✅
GATE: PASS — push 허용
```

- qa가 별도로 `pnpm --filter mobile test -- --run`(146/146)과 `pnpm --filter mobile typecheck`(clean)도 독립 재실행해 동일 결과를 재확인했다.
- `git diff origin/dev..HEAD --stat`로 architect가 승인한 7개 파일만 건드렸음을 확인 (리뷰어도 동일 명령으로 재검증).
- 3개 이슈의 done_when 항목 전부 실제 diff+테스트 출력 대조로 ✅ 확인됨(qa가 항목별로 확인, 리뷰어가 saved.tsx의 M-073/M-089 hunk를 직접 diff로 재확인).

## 리스크 & 후속

- **jsdom 콘솔 경고(follow-up a11y 넛지, done_when 실패 아님)**: `saved.test.tsx` 실행 시 `Warning: validateDOMNesting(...): <button> cannot appear as a descendant of <button>.`가 stderr에 찍힌다. M-073이 `SavedItem`의 카드 전체 `Pressable`에 `accessibilityRole="button"`을 추가했는데, 그 안에 이미 M-031이 넣은 저장 취소 토글용 `role="button"` 컨트롤이 들어있어(RN-web에서 `role="button"`이 실제 `<button>` 태그로 렌더됨) 버튼 안에 버튼이 중첩되는 모양이 됐다. 테스트 12건은 전부 통과(콘솔 경고일 뿐 실패 아님)하고 3개 이슈 어느 done_when도 DOM nesting 유효성을 요구하지 않았으므로 이번 게이트를 막지는 않았다. **다음 밤 후보로 backlog에 등재를 권함**: 중첩 interactive role 정리(예: 카드 바깥 Pressable을 role 없는 wrapper로 두고 안쪽 저장취소 버튼만 role="button" 유지, 또는 반대로 안쪽을 별도 접근성 그룹으로 분리).
- **문서 모순 1 — `.claude/agents/architect.md`**: 9번째 줄("...TypeScript · pnpm · turbo. FSD 아키텍처.")과 18번째 줄("**FSD 경계**: entities/features/widgets/shared 계층 위반 없나. import 방향 맞나.") 모두 FSD 계층을 전제하는데, `.claude/rules/core/constitution.md` 24번째 줄은 "FSD(entities/features/widgets/shared) 디렉토리는 실재하지 않는다(옛 룰의 잔재였다)"라고 명시한다. 이번 밤 architect 단계는 이번 실행의 명시적 override 지시에 따라 FSD 점검을 건너뛰고 평면 구조 점검으로 대체했다(위반 없음). **권고**: 사람 또는 다음 밤이 `architect.md:9`와 `:18`의 FSD 언급을 지운다. 단, `nightly-pipeline.md`가 스스로 경고하듯 repo 문서만 고쳐서는 클라우드 트리거(`trig_013KmkrrdUit9DhPPHpq873y`)에 반영되지 않으므로, 실제로 고치려면 트리거 프롬프트도 같이 손봐야 한다.
- **문서 모순 2 — `.claude/agents/reviewer.md`**: 16번째 줄이 push 게이트를 "qa 게이트(`pnpm typecheck`+`pnpm test`)"라고 서술하는데, `nightly-pipeline.md`의 2026-08-05 갱신은 실제 게이트가 `scripts/gate.sh`(typecheck+test+lint+시크릿 스캔, 4종)라고 명시한다. 이번 밤은 `nightly-pipeline.md` 쪽이 우선한다는 이번 실행의 지시에 따라 올바른 4종 게이트를 사용했다. `reviewer.md:16`도 같은 이유로 갱신 권고.
- **다음 밤 후보**: 남은 high priority todo M-076/M-082/M-085(이번 클러스터와 파일 겹침 없어 제외됨), 그리고 M-052(모바일 지도, 사람 결정 대기 — 2026-08-29 리포트에서 PushNotification으로 이미 에스컬레이션됨. 이번 밤은 M-052 상태를 건드리지 않았다).
- 시크릿 노출·인증·파괴적 마이그레이션 관점 최종 훑기: 이번 클러스터는 UI-only 변경(accessibilityRole/Thumbnail prop/데드코드 제거)이라 시크릿·인증·SQL 접촉 없음. `scripts/scan-secrets.sh` 결과도 매치 없음.
