# Nightly Report — 2026-08-24

## TL;DR

⚠️ **M-052가 오늘로 12일 연속(08-13~08-24) 동일 사유로 씨앗 단계에서 사전 제외됐다 — 사람 개입 필요.** (연속 report-only 경보는 해당 없음. 아래 참조.)

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `status: todo` 개수는 **7건**(M-052, M-060, M-066, M-067, M-070, M-071, M-072) — `todo ≥ 2`이므로 구현 모드로 진행했다.
- **클러스터 = M-072 단독 1건.** 씨앗 후보였던 M-052(유일한 medium priority)는 12일째 재제외됐고, 남은 유일한 medium인 M-072가 씨앗이 됐다. 나머지 5건(M-060·M-066·M-067·M-070·M-071)은 파일도 다르고 전부 low priority라 파일 겹침도 앱+동일priority 폴백도 성립하지 않아 합류하지 못했다.
- qa 게이트(`bash scripts/gate.sh`) **PASS** — typecheck 4/4, test(core 303/303 · mobile 132/132 · web 307/307), lint 통과, 시크릿 없음. reviewer가 커밋 이후 게이트를 fresh 재실행해 동일 수치로 재확인.
- **shipped: M-072**(카탈로그 반경 사다리 정책을 packages/core로 승격), 커밋 `c207fb0`. 되돌린 이슈 없음.
- 연속 report-only 경보 해당 없음: `docs/nightly/`의 08-19~08-23을 직접 열어 확인 — 다섯 밤 전부 shipped 구현 밤이었다(08-19: M-056+M-058, 08-20: M-057+M-064+M-065, 08-21: M-059, 08-22: M-061+M-062+M-063, 08-23: M-068+M-069). report-only 연속 0회.

## 집은 클러스터

**M-072 단독.** 씨앗 선정 과정:
1. 정상적인 씨앗은 priority 최상위 medium인 M-052지만, 12일 연속(08-13~08-24) 재제외 대상이라 애초에 씨앗 후보에서 빠졌다(사유는 "리스크 & 후속" 참조).
2. 남은 medium priority todo는 M-072 하나뿐이라 씨앗이 됐다.
3. scope 겹침 확인: 나머지 5건(M-060·M-066·M-067·M-070·M-071) 전부 M-072의 scope(`packages/core/src/catalog.ts`, `apps/web/src/app/api/opportunities/route.ts`, `apps/mobile/hooks/useEnsureCatalog.ts`)와 파일이 하나도 겹치지 않았다:
   - M-060: `apps/web/src/components/icons.tsx` + `landing-icons.tsx` (low)
   - M-066: `apps/web/src/app/api/why-reasons/route.ts` (low)
   - M-067: `packages/tokens/src/index.ts` + `apps/mobile` 전반 (low)
   - M-070: `packages/core/src/store.ts` (low, catalog.ts와 같은 패키지지만 다른 파일)
   - M-071: `apps/web/src/components/neighborhood-menu.tsx` (low)
4. **앱 단위 + 동일 priority 폴백도 성립하지 않는다** — 폴백 조건은 "같은 앱 + 같은 priority"인데 M-072는 medium이고 나머지 5건은 전부 low라 priority가 다르다. 폴백 요건 자체가 충족되지 않아 자동 미룸.
5. 상한 3개 룰과 무관하게, 원천적으로 묶일 후보가 0개라 **1이슈 클러스터**로 확정. `nightly-pipeline.md`가 명시하듯 "겹치는 게 없으면 1이슈 밤이고, 그것도 정상 결과"에 해당한다.

미룬 이슈: M-060, M-066, M-067, M-070, M-071 (전부 low, scope 무관, 다음 밤 재검토 대상). M-052는 위 사유로 재차 미룸.

## 이슈별 결과

### M-072 — shipped
"카탈로그 반경 사다리 정책이 core가 아니라 web/mobile에 각각 중복 구현됨"

- `packages/core/src/catalog.ts`에 `RADII`([5,10,20])·`MIN_RESULTS`(20)·`NO_ANCHOR_LIMIT`(300) 상수와 `loadCatalogByRadiusLadder()` 순수 오케스트레이션 함수(+ 타입 `RadiusFetch`/`NoAnchorFetch`/`CatalogLadderResult`)를 신설.
- `apps/web/src/app/api/opportunities/route.ts`와 `apps/mobile/hooks/useEnsureCatalog.ts`가 각자 리터럴로 갖고 있던 동일한 반경 확장 루프를 제거하고, 콜백(`fetchAtRadius`/`noAnchorFetch`)만 주입해 core 함수를 호출하도록 배선.
- 신규 단위 테스트 5건(core) + 기존 web/mobile 테스트 갱신 1건 + 신규 회귀 테스트 1건(mobile, error 즉시중단 동작 고정).
- done_when 5개 항목 전부 충족 확인(아래 "검증" 참조).
- 커밋: `c207fb0 refactor(core): 카탈로그 반경 사다리 정책을 core로 승격 (M-072)`.

되돌린 이슈: 없음.

## 아키텍처 검토

- **담당 분리**: backend-impl이 `packages/core/src/catalog.ts` + 테스트를 먼저(신규 export가 프론트 두 곳의 의존 대상이므로), frontend-impl이 `route.ts` + `useEnsureCatalog.ts` + 두 테스트 파일을 그다음 순서로 진행. architect가 `loadCatalogByRadiusLadder`의 정확한 시그니처(`point, fetchAtRadius, noAnchorFetch` → `{ result, radiusKm }`)를 사전에 확정해 두 구현자가 어긋나지 않게 함.
- **경계**: core 함수는 React/취소(cancellation)/캐시(unstable_cache, react-query) 개념을 전혀 모르는 순수 오케스트레이션으로 유지했다. mobile의 언마운트 취소는 주입 콜백 내부(`cancelled` 체크 후 합성 `{ data: [], status: "empty" }` 반환)에서 처리해, core가 React 생명주기를 알 필요가 없게 했다.
- **행동 통일 결정 (architect 판단)**: 사다리가 `status: "error"`를 만나면 즉시 멈추는 정책을 web/mobile 양쪽에 통일 적용했다. 과거 mobile은 error가 나도 다음 반경까지 계속 시도했고 web은 이미 즉시 중단이었다 — 이 차이에 git 히스토리·UI 코드를 추적해봐도 mobile 쪽 동작을 의도적으로 다르게 둘 근거가 없음을 확인, 안전한 동작 개선(회귀 아님)으로 판단하고 이를 고정하는 신규 테스트를 요구했다. (`apps/mobile/hooks/useEnsureCatalog.test.ts`의 "첫 반경(5km) 조회가 error면 즉시 멈춘다" 테스트)
- **테스트 전략**: web 테스트는 `fetchOpportunities`만 모킹하고 `loadCatalogByRadiusLadder`는 `importActual`로 실물을 통과시켜, 라우트가 새 core 함수와 실제로 올바르게 배선됐는지까지 검증하도록 설계(단순 모킹으로 배선 실수를 놓치는 것을 방지).

### 문서 불일치 발견 (사람 조치 필요)
- `.claude/agents/architect.md:9`와 `:18`이 "FSD 아키텍처"·"FSD 경계: entities/features/widgets/shared 계층 위반 없나"를 언급한다. 그러나 `.claude/rules/core/constitution.md`는 "**FSD(entities/features/widgets/shared) 디렉토리는 실재하지 않는다**(옛 룰의 잔재였다)"고 명시하며 실제 규칙은 평면 구조(app/components/hooks/lib/data/store)다. architect가 이번 밤 검증 과정에서 이 모순을 발견했다 — **architect.md의 FSD 언급을 constitution.md와 일치하도록 제거/수정할 필요가 있다.** 코드 영향은 없었다(architect가 constitution.md를 우선해 판단했고 결과는 문제없음)지만, 이 stale 문서가 다음 밤 architect를 오도할 위험이 있으니 사람이 손볼 것을 권고.

### 새로 발견된 세 번째 중복 (백로그 후보)
- architect가 이번 스코프를 검증하던 중, 반경 사다리 패턴의 **세 번째 중복**을 발견해 명시적으로 스코프 밖으로 뒀다: `apps/web/src/hooks/useReportFallback.ts:58-61`에 자체 반경 루프가 있다(`[10, 20]`을 순회하며 `REPORT_SIZE`(=6)를 채우면 멈춤). M-072의 `MIN_RESULTS`(=20)와 멈춤 기준값 자체가 달라 `loadCatalogByRadiusLadder` 전체를 그대로 재사용할 순 없지만, `RADII`(반경 배열) export는 재사용 가능하다. **다음 밤 백로그 후보로 등재 권고**: `useReportFallback.ts`가 하드코딩한 `[5, 10, 20]` 대신 core의 `RADII`를 import해 쓰도록 정리(오케스트레이션 함수까지 통합하려면 정지 조건을 파라미터화하는 추가 설계가 필요 — 별도 판단 대상).

## 변경 내용

커밋 `c207fb0` (6개 파일, +195/-53):
- `packages/core/src/catalog.ts` — `RADII`/`MIN_RESULTS`/`NO_ANCHOR_LIMIT`/`loadCatalogByRadiusLadder`/`RadiusFetch`/`NoAnchorFetch`/`CatalogLadderResult` 신규 export.
- `packages/core/src/catalog.test.ts` — `loadCatalogByRadiusLadder` 단위 테스트 5건 신규(5km 충족/10km로 넘어감/20km도 미달/앵커없음/error 즉시중단).
- `apps/web/src/app/api/opportunities/route.ts` — 로컬 상수·루프 제거, core 함수 호출로 대체. `status:"error"`면 캐시 오염 방지용 throw는 사다리가 끝난 뒤 한 번만.
- `apps/web/src/app/api/opportunities/route.test.ts` — `@motungi/core` 모킹을 `importOriginal` 기반으로 바꿔 `fetchOpportunities`만 모킹, `loadCatalogByRadiusLadder`는 실물 통과. 기존 14개 케이스 assertion 변경 없이 그대로 통과.
- `apps/mobile/hooks/useEnsureCatalog.ts` — 로컬 상수·루프 제거, core 함수 호출로 대체. 언마운트 취소는 콜백 내부에서 처리.
- `apps/mobile/hooks/useEnsureCatalog.test.ts` — error 즉시중단 동작을 고정하는 신규 테스트 1건 추가.

reviewer가 diff를 직접 읽고 확인한 점: scope는 정확히 backlog M-072의 scope 3개 파일 + 대응 테스트 파일로 한정돼 있고, 스코프 크립(scope creep) 없음. 마이그레이션 없음. 시크릿 리터럴 없음(`bash scripts/gate.sh`의 secrets 단계 + 육안 확인 둘 다 클린). 신규 의존성 추가 없음(`package.json` 변경 없음, diff에 미포함). commit 메시지가 Conventional Commits(`refactor(core):`) 형식이며 제목에 `M-072`가 포함돼 있어 추적성 요건 충족.

## 검증

reviewer가 커밋 이후 `bash scripts/gate.sh`를 다시 fresh 실행해 재확인한 실제 출력:

```
 Tasks:    4 successful, 4 total
   ✅ typecheck

@motungi/core:test:  Test Files  11 passed (11)
@motungi/core:test:       Tests  303 passed (303)
@motungi/mobile:test:  Test Files  19 passed (19)
@motungi/mobile:test:       Tests  132 passed (132)
@motungi/web:test:  Test Files  40 passed (40)
@motungi/web:test:       Tests  307 passed (307)
 Tasks:    3 successful, 3 total
   ✅ test

 Tasks:    1 successful, 1 total
   ✅ lint

   (시크릿 패턴 없음)
   ✅ secrets

GATE: PASS — push 허용
```

qa가 밤 동안 검증한 M-072 done_when 5개 항목(reviewer가 diff 대조로 재확인):
1. RADII/MIN_RESULTS/NO_ANCHOR_LIMIT + 사다리 정책을 core 순수 함수로 승격 — `loadCatalogByRadiusLadder`가 단일 반경 fetch를 콜백으로 주입받는 형태로 구현됨(react-query/unstable_cache를 모름). ✅
2. web·mobile 둘 다 core 함수를 호출 — route.ts·useEnsureCatalog.ts 둘 다 로컬 루프 제거하고 `loadCatalogByRadiusLadder` 호출로 대체. ✅
3. web `unstable_cache` 메모이제이션(M-041)과 mobile 세션 캐치업은 각 앱 레이어 유지 — route.ts의 `unstable_cache` 래핑, useEnsureCatalog.ts의 `lastFetchedKey` 모듈 스코프 캐시 둘 다 diff에서 손대지 않음. ✅
4. core 정책 함수 단위 테스트(5km 충족/10km로 넘어감/20km도 미달/앵커없음 분기) + 회귀 없이 기존 web·mobile 테스트 통과 — catalog.test.ts 5건 신규(4개 분기 + error 분기 1개 추가), web 307/307·mobile 132/132 전체 통과. ✅
5. `npx turbo run typecheck --force` 통과 — `bash scripts/gate.sh`의 typecheck 단계(4/4 workspaces) 통과로 충족. ✅

추가로 `pnpm --filter @motungi/web build` 성공(qa 보고, reviewer 미재실행 — gate.sh 범위 밖의 부가 확인이었으므로 재실행 생략, gate.sh 자체가 push 게이트의 전부이므로 문제 없음).

## 리스크 & 후속

### ⚠️ M-052 — 12일 연속 사람 개입 대기 (최우선 확인 요망)
2026-08-13일 밤부터 오늘(08-24)까지 **12일 연속** 동일 사유로 씨앗 단계에서 사전 제외됐다:
- 08-13: architect가 3개 구현 경로(react-native-maps 신규 네이티브 의존성 / NAVER 정적지도 백엔드 프록시 신설 / 키리스 서드파티 정적지도) 전부를 무인 야간 실행에 부적합으로 판정.
- 이후 매 밤(08-14~08-24, 오늘 포함) `apps/mobile/package.json`에 지도 관련 의존성 없음, `.env.example`에 신규 지도 관련 env var 없음을 재확인 — 사람의 결정/승인이 전혀 반영되지 않은 채 동일 벽에 부딪히고 있다.
- 08-16·08-17 노트가 이미 이 반복 패턴 자체가 `blocked-human` 같은 정식 상태로 성문화될 필요가 있다고 관찰했고, 08-21·08-22·08-23이 연달아 사람에게 3택1을 요청했다: **(a) 3개 경로 중 하나를 승인하고 필요한 키/승인을 제공, (b) M-052의 priority를 medium에서 내려 씨앗으로 재선정되는 빈도를 낮춤, (c) `blocked-human` 같은 정식 status를 backlog 스키마에 도입해 씨앗 선정 로직 자체에서 배제.**
- 오늘 밤도 위 사유를 재검증(변경 없음 확인)하고 backlog.yml에 12번째 소급 노트를 추가했을 뿐, **사람의 결정 없이는 13일째도 동일하게 반복될 것**이다. **reviewer로서 강력히 권고: 이 3택1 중 하나를 이번 주 안에 처리해달라.** 12일은 "밤이 알아서 처리하지 못하는 블로커가 있다"는 신호치고는 길다.

### 문서 불일치
- `.claude/agents/architect.md:9,18`의 FSD 언급이 `constitution.md`와 모순 — 위 "아키텍처 검토"에 상세. 사람이 architect.md를 constitution.md와 일치하도록 정리할 것을 권고(다음 밤 architect의 판단을 흐릴 위험).

### 새 백로그 후보
- `apps/web/src/hooks/useReportFallback.ts:58-61`의 세 번째 반경 루프 중복 — 위 "아키텍처 검토"에 상세. 다음 밤(또는 다음 audit 사이클)에 backlog 이슈로 정식 등재할 것을 권고. (오늘 밤은 M-072 스코프 밖이라 등재하지 않음 — architect가 발견해 리포트에만 남김.)

### 그 외
- 남은 low priority todo 5건(M-060·M-066·M-067·M-070·M-071)은 이번 밤 스코프 밖. 다음 밤 씨앗 후보.
- M-072의 `status`를 `todo → doing`으로 갱신(shipped 확정). M-052는 `todo` 유지(재시도 대상이므로 상태 불변, notes만 추가).
