# Nightly Report — 2026-09-25

## TL;DR

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `docs/backlog/backlog.yml`을 이슈 단위로
  파싱해 다시 센 결과 `status: todo` **8건**(`grep -c "status: todo"`는 notes 본문 인용까지
  잡아 다른 숫자로 나올 수 있어 스크립트로 이슈 단위 재파싱함) → `todo ≥ 2`이므로 구현 모드.
  audit 모드로 전환하지 않았다.
- **연속 report-only 경보 해당 없음** — 직전 다섯 밤(09-19·09-21·09-22·09-23, 그리고
  오늘) 모두 코드가 나갔거나 나갈 예정이다. 스트릭 이어짐.
- **씨앗 M-097(최우선 medium, id 최소)은 이번에도 blocked-human이라 제외**하고 다음 씨앗
  **M-115**로 밤을 짰다. **클러스터는 M-115 + M-092, 2건** — 둘 다 실제로 같은 파일
  (`packages/tokens/src/index.ts`)을 건드려 CLUSTER RULE의 "같은 파일 = 겹침" 기준을
  그대로 충족한다(폴백 불필요, 진짜 파일 겹침).
- **결과: SHIPPED 2/2.** M-115 — DESIGN.md가 이미 문서화한 "모바일 타이포는 웹보다 약간
  크다"는 의도를 확인하고, 그 값을 `packages/tokens`로 토큰화해 `apps/mobile/ui/theme.ts`의
  손 재작성을 없앴다(시각 변화 없음, 거짓 주석 정정). M-092 — 어디서도 참조되지 않는
  `tokens`/`Tokens` 집계 export를 제거했다(M-067 선례와 동일 처리 방향). `bash scripts/gate.sh`
  PASS(typecheck·test·lint·secrets·pure-tests 5/5, 각 이슈 커밋 후 재확인). 커밋 `e6e30e0`
  (M-115) · `3fad509`(M-092)를 `dev`에 push. reverted 이슈 없음.
- **부수 발견(이번 밤 범위 밖, 코드 안 건드림)**: `docs/backlog/backlog.yml`이 엄밀한 YAML이
  아니다 — M-111 항목의 `scope: [apps/web/src/app/explore/[gu]/page.tsx, ...]`처럼 경로에
  대괄호(`[gu]`)가 리터럴로 들어가 있어 PyYAML 같은 엄격한 파서가 flow-sequence 파싱에서
  깨진다(`origin/dev` 기준으로도 이미 그렇다 — 오늘 밤이 만든 문제 아님, 재현: 아래 검증
  로그). 지금까지의 grep 기반 파싱엔 영향 없지만, 백로그를 스크립트로 다루는 도구를
  붙일 계획이 있다면 참고할 것.

## 집은 클러스터

**씨앗 선정**: `status: todo` 8건 중 priority 최상위는 `medium`(high 없음). medium은
`M-097`·`M-115`(id 오름차순) 2건뿐. 규칙대로면 최소 id인 **M-097**이 씨앗이어야 한다.

**M-097을 씨앗에서 재차 제외한 이유**: M-097(`동 단위 좌표 백필`)은 2026-09-07·09-09·09-22·
09-23 밤이 이미 `blocked-human`으로 판정해 "씨앗에서 사전 제외"를 notes에 남겨둔 이슈다.
오늘 밤 재검증:
```
$ ls -la .env
ls: cannot access '.env': No such file or directory   (.env.example만 존재, 전 항목 공란)

$ curl -sS -o /dev/null -w "%{http_code}\n" https://apis.data.go.kr/
curl: (56) CONNECT tunnel failed, response 403
```
`.env`가 없고 `DATA_GO_KR_SERVICE_KEY`가 공란이며, outbound network가 조직 정책으로
`apis.data.go.kr`를 여전히 차단한다. 좌표를 지어내는 건 근사치보다 나쁘다는 기존 판단을
유지한다. `status: todo` 유지(재시도 대상). **다섯 번째 밤째 같은 사유로 막혀 있다** — 사람이
API 키를 발급하거나 network 예외를 허용하지 않으면 계속 이 자리에서 막힐 것이다. 우선순위를
낮추거나 "사람 조치 대기"로 분류해 씨앗 후보에서 자동 제외되게 하는 걸 다시 요청한다
(09-22·09-23 리포트에서도 같은 요청).

**다음 씨앗: M-115**(`mobile 타이포가 tokens를 import하지 않고 손으로 재작성`, priority
medium, id 최소 중 유효). scope: `[apps/mobile/ui/theme.ts, packages/tokens/src/index.ts]`.

**done_when 재검증(planner 규율)**: M-115의 done_when #1("모바일이 웹보다 큰 게 의도인지
결정")부터가 미해결 상태였다 — `dev` 실코드로 이미 충족된 부분은 없었다. 나머지도 코드에
반영된 바 없어 그대로 유효했다.

**scope 겹침으로 묶이는 다른 todo 확인(파일 단위)**:
- `M-092` scope `[packages/tokens/src/index.ts]` — M-115의 scope 파일 중 하나와
  **정확히 같은 파일**. 겹침 확정 → 클러스터 합류.
- `M-088`(`apps/mobile/app/(tabs)/report.tsx`)·`M-093`(`packages/core/src/adapters/*`)·
  `M-102`(`apps/web/src/app/{diagnosis,location}/page.tsx`)·`M-103`
  (`apps/web/.eslintrc.json`)·`M-110`(`packages/core/src/view.ts`) — 전부 다른 파일. 겹치지
  않는다.
- (M-097은 씨앗 후보에서 이미 제외됨 — 겹침 검토 대상 아님.)
→ **파일 겹침 후보 1건(M-092). 폴백 불필요** — 진짜 scope 겹침으로 클러스터가 자연스럽게
2건이 됐다.
- 상한 3개 중 미룬 이슈: 없음(겹치는 candidate가 M-092 하나뿐이었다).

## 이슈별 결과

### M-115 — SHIPPED

- **제목**: mobile 타이포가 tokens를 import하지 않고 손으로 재작성 — 7개 역할 중 6개가
  웹과 어긋남
- **커밋**: `e6e30e0` (dev 직접 커밋, 트렁크 모델, PR 없음)
- **결정(done_when #1)**: **의도된 차이.** `DESIGN.md`의 "Typography" 절(현재 96행)이
  이미 "모바일(RN)은 약간 큰 대응값(display 32/40 등, `ui/theme.ts`의 `T`)"이라고
  명시하고 있었다 — 09-11 감사가 놓친 1차 출처다. 값 자체(웹 대비 각 역할 +2px류)도
  DESIGN.md 표의 웹 수치와 정확히 대응해, "잘못 베낀 값"이 아니라 "문서엔 있지만 코드에
  토큰화되지 않은 값"으로 확인했다.
- **수용 기준 판정**:
  - ✅ done_when #1 — 위 결정.
  - ✅ done_when #2(의도 분기) — `packages/tokens/src/index.ts`의 `typography`에
    `mobile` 서브스케일(display·h1·h2·headline·body·bodySm·label·caption, 기존
    theme.ts 값과 숫자 동일)을 추가하고, `shadow`에 `mobileCard`(color/opacity/radius/
    offset/elevation)를 추가했다. `apps/mobile/ui/theme.ts`는 이제 `T = typography.mobile`,
    `cardShadow`는 `shadow.mobileCard` 필드를 그대로 재노출한다. `theme.ts:47`의 거짓
    주석("웹 typography 스케일과 맞춤")을 "웹 대비 +2px 스케일(의도된 차이, DESIGN.md
    참조)"로 정정했다.
  - (done_when #3 "의도 아님" 분기는 해당 없음 — 의도로 판정.)
  - ✅ done_when #4(shadow 출처 이전) — 위와 동일 작업으로 충족.
  - ✅ done_when #5(DESIGN.md-코드 일치 확인) — 수치가 동일해 그대로 일치. DESIGN.md
    본문 수정 불필요.
  - **키 이름은 바꾸지 않았다**: `Txt` 컴포넌트(`apps/mobile/ui/components.tsx:180-185`)가
    `T[preset]`로 `h1`·`h2`·`headline`·`body`·`bodySm`·`label`·`caption`을 약 19개
    호출부에서 쓰고 있어(`grep 'preset="'` 결과: h1 5·label 5·bodySm 4·headline 3·
    caption 1·body 1), 웹 쪽 이름(`heading1`/`body1` 등)으로 통일하면 그 19곳을 전부
    고쳐야 한다. done_when에 리네임 요구가 없어 범위를 넘는 변경으로 보고 하지 않았다 —
    `typography.mobile`의 키는 theme.ts의 기존 키(`h1`/`h2`/`body`/`bodySm`)를 그대로
    썼다.
  - **시각 회귀 없음**: 토큰으로 옮긴 숫자는 옮기기 전 `theme.ts`의 하드코딩 값과
    1:1 동일. `letterSpacing`도 웹의 `tracking`(em) 값을 재계산하지 않고 기존 px 값을
    그대로 보존했다(재계산하면 예: heading1 -0.01em×24px=-0.24px로 원래 -0.4px와
    달라져 미세한 드리프트가 생겼을 것 — 값 보존을 우선했다).

### M-092 — SHIPPED

- **제목**: packages/tokens의 집계 export tokens/Tokens — 개별 토큰만 쓰이고 통합 객체는
  미참조
- **커밋**: `3fad509` (dev 직접 커밋, 트렁크 모델, PR 없음)
- **방향 결정(done_when #1, "사람/planner 판단")**: **제거.** 리포 전체
  (`apps/web`·`apps/mobile`·`packages/*`)를 `from "@motungi/tokens"` 기준으로 grep한
  결과 실제 import는 `apps/web/src/components/venue-map.tsx`(`color`만)와
  `apps/mobile/ui/theme.ts`(이번 커밋 후 `color`·`radius`·`shadow`·`space`·`typography`)
  뿐이었고, 둘 다 개별 named export만 가져다 쓴다. `tokens`/`Tokens` 집계는 정의부 외
  어디서도 이름으로 참조되지 않았다(M-092 notes의 2026-08-29 판정과 동일). 강제로 한
  곳에 배선하면 기존 named-import 스타일과 어긋나는 인위적 사용처가 생기므로,
  M-067 선례(강제 배선 대신 정리)와 같은 방향을 택했다.
- **수용 기준 판정**:
  - ✅ "제거하거나 배선한다" — 제거.
  - ✅ "pnpm --filter @motungi/tokens typecheck 통과, web/mobile typecheck 회귀 없음" —
    "검증" 절 참조, 4개 패키지 전부 통과.

## 아키텍처 검토

- **담당**: `packages/tokens`(값의 단일 출처) + `apps/mobile`(소비처) — 이슈 2개 모두
  리팩터 성격에 파일 2개짜리 소규모라, 오케스트레이터가 별도 architect/frontend-impl
  서브에이전트 분리 없이 직접 계획·구현·검증했다.
- **레이어 경계**: `packages/tokens`는 여전히 "플랫폼 무관한 primitive만 유지"(파일 헤더
  주석) 원칙을 지켰다 — `typography.mobile`은 RN Text 스타일 형태(fontSize/fontWeight
  문자열/letterSpacing px)를 쓰지만, 이는 이미 `shadow`가 웹 CSS 문자열로 플랫폼 특화돼
  있는 것과 같은 선례를 따른 것이지 새 원칙 위반이 아니다(플랫폼별 값은 있어도 되고,
  "어디서 관리하는가"만 단일화하면 된다).
- **레이어 경계(웹)**: 이번 클러스터는 `apps/web`을 건드리지 않았다(웹은 이미
  `typography`/`shadow`의 데스크톱 값을 그대로 쓰고 있었다). `import/no-restricted-paths`
  영향 없음.
- **예상 밖 파급**: 없음. 두 이슈 모두 값 재배치/미사용 코드 제거라 런타임 동작이 바뀌지
  않는다.
- **테스트 전략**: 신규 유닛 테스트는 추가하지 않았다 — 값 재배치이며 mobile
  typecheck·test(23개 파일, 184케이스 그대로)가 회귀를 잡는 안전망이다.
  `scripts/check-pure-tests.sh`가 "면제 6건"으로 통과했고 이번 변경으로 새로 걸린
  누락은 없다.

## 변경 내용

| 파일 | 요약 |
|---|---|
| `packages/tokens/src/index.ts` | `typography.mobile`(8개 역할)·`shadow.mobileCard` 추가(M-115); `tokens`/`Tokens` 집계 export 제거(M-092) |
| `apps/mobile/ui/theme.ts` | `T`가 `typography.mobile`을, `cardShadow`가 `shadow.mobileCard`를 재노출하도록 변경. 거짓 주석 정정 |
| `docs/backlog/backlog.yml` | M-115·M-092 `status: todo → doing`, notes에 오늘 밤 판단 근거 추가 |

## 검증

모두 최신 `dev`(`4023cee` 기준, 커밋 후 `e6e30e0`→`3fad509`) 위에서 실제 실행. 출력 원문 발췌:

```
$ pnpm install --frozen-lockfile
Done in 14.5s

$ bash scripts/gate.sh   (M-115 커밋 후, M-092 변경 포함 전)
── typecheck   ✅
── test        ✅ (core 16/16 · mobile 23/23 · web 49/49 파일, 전부 통과)
── lint        ✅ (경고만 — 기존 max-lines-per-function/complexity, 이번 변경과 무관)
── secrets     ✅ (diff 없음)
── pure-tests  ✅ (순수 레이어 테스트 누락 없음, 면제 6건)
GATE: PASS — push 허용

$ bash scripts/gate.sh   (M-092 변경 후, 최종 트리)
── typecheck   ✅ (cache hit — 동일 내용 재확인)
── test        ✅
── lint        ✅
── secrets     ✅ (시크릿 패턴 없음)
── pure-tests  ✅
GATE: PASS — push 허용

$ pnpm test 최종 합계
@motungi/core:test:   Test Files 16 passed (16) / Tests 417 passed (417)
@motungi/mobile:test:  Test Files 23 passed (23)
@motungi/web:test:    Test Files 49 passed (49) / Tests 415 passed (415)
```

push 전 머지 체크: `git log --oneline origin/dev..origin/main` → 비어 있음(main이 dev를
앞서지 않음, 되받을 것 없음). `git fetch origin dev` 후 `git log --oneline origin/dev..HEAD`
/`HEAD..origin/dev` → 로컬 커밋 2개만 앞서 있고 원격에 동시 작업 없음 확인.

## 리스크 & 후속

- **M-097(좌표 백필)은 다섯 번째 밤째 blocked-human.** 09-22·09-23 리포트와 동일한 요청을
  반복한다 — `.env`에 `DATA_GO_KR_SERVICE_KEY`를 채우거나 outbound network 예외를
  허용해야 다음 밤이 진행할 수 있다. 계속 막혀 있을 거면 우선순위를 낮추거나 "사람 조치
  대기"로 분류해 씨앗 후보에서 자동 제외되게 하는 걸 고려해 달라.
- **backlog.yml이 엄밀한 YAML이 아니다** — M-111의 `scope: [apps/web/src/app/explore/[gu]/page.tsx, ...]`
  처럼 대괄호가 든 경로 리터럴이 flow-sequence 파싱을 깬다(PyYAML로 재현, `origin/dev`
  기준 사전 존재 — 이번 밤이 만든 문제 아님). 오늘 밤은 이슈 카운트를 스크립트로 직접
  파싱해 우회했다. grep 기반 사용엔 지장 없으니 이번 밤의 범위(M-115·M-092)에서는
  손대지 않았지만, 백로그를 정식 YAML 파서로 다루는 도구를 붙일 계획이라면 이 파일
  전체를 인용부호 처리하는 정리 작업이 필요하다.
- **CLUSTER RULE 폴백 조건 불일치**(트리거 프롬프트 vs repo 3개 문서, 09-22 리포트부터
  반복 기록)는 오늘 밤은 발동 여부에 영향이 없었다 — M-115의 scope에 이미 파일 2개가
  있고, M-092와 정확히 같은 파일로 실겹침이 나서 폴백 자체가 필요 없었다(트리거 기준·
  repo 기준 모두 "진짜 겹침 있음"으로 같은 결론). 여전히 사람 조치는 필요하다.
- **`Txt` 프리셋 키 통일(h1/h2/body/bodySm → heading1/heading2/body1/body2)은 이번 밤
  범위 밖으로 미뤘다** — 웹과 이름을 맞추면 읽기 편해지지만 콜사이트 19곳을 함께 고쳐야
  해서 오늘의 done_when(값 출처 통합)보다 큰 변경이다. 다음에 mobile 타이포 관련 이슈가
  나오면 후보로 남긴다.
