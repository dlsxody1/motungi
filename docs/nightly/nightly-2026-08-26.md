# Nightly Report — 2026-08-26

## TL;DR
⚠️ **M-052가 오늘로 14일 연속(08-13~08-26) 동일 사유로 씨앗 단계에서 사전 제외됐다 — 사람 개입 필요.** 08-22·08-23·08-24·08-25 밤이 이미 사람에게 3택1(구현 경로 승인 / priority 하향 / `blocked-human` 상태 도입)을 직접 알림까지 보냈으나 아직 반영되지 않았다. 이 패턴은 더 이상 매일 밤 재검증만 하고 넘어갈 문제가 아니다 — 아래 "리스크 & 후속" 참조.

(연속 report-only 경보는 해당 없음 — 아래 §0 참조.)

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `status: todo` 개수는 **4건**(M-052, M-066, M-067, M-070) — `todo ≥ 2`이므로 구현 모드로 진행했다.
- **클러스터 = M-066 단독 1건.** 씨앗 후보였던 M-052(유일한 medium priority)는 14일째 재제외됐고, 남은 3건(M-066·M-067·M-070) 전부 low priority에 파일/디렉토리 겹침이 0이며 앱 경계 폴백(M-066은 apps/web, M-067/M-070은 apps/mobile·packages/core·packages/tokens)도 공유 앱이 없어 성립하지 않았다 — id 오름차순 최소인 M-066만 남아 1이슈 클러스터가 됐다.
- qa 게이트(`bash scripts/gate.sh`) **PASS** — typecheck 4/4, test(web 311/311 · core 303/303 · mobile 132/132), lint 통과, 시크릿 스캔 경고 1건은 조사 후 확인된 기존 base 커밋 리터럴(오탐)로 판정.
- **shipped: M-066**(`/api/why-reasons` category 멤버십 검증 도입), 커밋 `5290c07`. 되돌린 이슈 없음.

## 0. 헛도는 밤 감지
`ls docs/nightly/ | sort | tail -15`로 최근 노트를 확인했다. 08-11~08-25 전부 파일이 존재하고, 08-21~08-25의 TL;DR을 직접 열어 확인한 결과 전부 코드가 나간 구현 밤이었다(08-21: M-059, 08-22: M-061+M-062+M-063, 08-23: M-068+M-069, 08-24: M-072, 08-25: M-060+M-071). **연속 report-only 0회** — 헛도는 밤 패턴 아님. 경고 불필요.

## 집은 클러스터
- **씨앗 후보였으나 사전 제외**: M-052 (유일한 medium priority todo) — 14일 연속 동일 사유(사람 아키텍처 결정 대기)로 씨앗 단계에서 배제. 상세는 아래 "리스크 & 후속".
- **집은 클러스터: M-066 단독.**
  - 남은 3개 todo(M-066: `apps/web/src/app/api/why-reasons/route.ts` / M-067: `packages/tokens/src/index.ts` + `apps/mobile` / M-070: `packages/core/src/store.ts`)는 서로 파일도 디렉토리도 겹치지 않는다.
  - **앱 경계 폴백도 성립하지 않음**: 폴백은 "같은 앱 + 같은 priority"인데, M-066은 apps/web, M-067은 apps/mobile(+packages/tokens), M-070은 packages/core로 세 이슈가 모두 다른 앱/패키지 경계에 있다. 공유하는 앱이 하나도 없어 폴백 조건 자체가 무의미하다.
  - **정확한 표현 확인**: M-067·M-070은 "상한 3개 초과로 미뤄진" 것이 **아니다** — 애초에 M-066과 클러스터링될 조건(파일 겹침도, 앱+priority 폴백도)을 전혀 충족하지 못해 후보에도 들지 못했다. 클러스터 상한에 걸려 잘린 게 아니라, 규칙상 애초에 M-066과 묶일 수 없었다는 점을 리포트에 명시해둔다.

## 이슈별 결과
### M-066 — shipped
`/api/why-reasons`가 요청 body의 `category`를 `typeof === "string"` 검사만으로 통과시킨 뒤 `WhyReasonsPromptInput["category"]`로 바로 캐스트하고 있었다. 유효하지 않은 문자열(예: 피벗 이전 레거시 값 `"subsidy"`)이 그대로 `CATEGORY_LABEL[category]` 조회를 거쳐 `undefined`가 섞인 채 외부 Gemini LLM 프롬프트로 나갈 수 있는 조용한 데이터 오염 경로였다. 공개 POST 라우트라 앱의 `useWhyReasons.ts` 호출자 외에 임의 HTTP 클라이언트도 도달 가능.

`@motungi/core`에 이미 존재하던 타입가드 `isOpportunityCategory`(`v is OpportunityCategory`)로 교체해 문자열 체크와 멤버십 검증을 한 번에 흡수했다. 가드 통과 후 `category`는 `OpportunityCategory`로 좁혀지므로 기존의 `as` 캐스팅도 제거했다. 테스트 2건 추가(무효 category → 400, `fetch` 미호출 / 유효 category(`"culture"` 외) → 200 통과). 커밋 `5290c07`.

## 아키텍처 검토
architect가 M-066 스펙을 **수정 없이 승인**했다.
- FSD 해당 없음 확인(constitution.md 기준 — 이 저장소는 flat 파일-종류별 배치이며 FSD 디렉토리는 실재하지 않음).
- 담당: frontend-impl (apps/web route handler).
- 대상 파일: `apps/web/src/app/api/why-reasons/route.ts` + `apps/web/src/app/api/why-reasons/route.test.ts`.
- 테스트 전략: route-handler 단위 Vitest(POST 핸들러 직접 호출, `fetch` 스텁).

## 변경 내용
- `apps/web/src/app/api/why-reasons/route.ts` (+8/-3): `isOpportunityCategory` import 및 `parseInput()`의 category 검증 교체, 캐스팅 제거.
- `apps/web/src/app/api/why-reasons/route.test.ts` (+27): 무효/유효 category 테스트 2건 추가.
- 총 diff: 2개 파일, 35 insertions(+), 3 deletions(-). 의도한 두 파일 외 오염 없음(qa·reviewer 양쪽 확인).

## 검증
- qa가 실행한 `bash scripts/gate.sh` 결과:
  - **typecheck**: ✅ 4/4 워크스페이스 클린.
  - **test**: ✅ web 40개 파일/311 테스트 통과(신규 route.test.ts 16개 포함), core 303/303, mobile 132/132.
  - **lint**: ✅ `next lint` 클린.
  - **시크릿 스캔**: ⚠️ → 조사 결과 **확인된 기존 오탐**. 신규 테스트 줄의 `process.env.GEMINI_API_KEY = "test-key"`를 스캐너가 플래그했으나, `git show 9b4f8d4:apps/web/src/app/api/why-reasons/route.test.ts`로 대조한 결과 동일 리터럴이 base 커밋(9b4f8d4)의 96·143행에 이미 존재했다 — 이번 변경이 추가한 게 아니라 스캐너가 diff의 *추가된 줄*만 검사하는 특성상 지금 처음 걸린 것뿐이다. 실제 자격증명이 아니므로 nightly-pipeline.md의 오탐 처리 지침대로 게이트를 끄거나 우회하지 않고 사유만 기록하고 통과 처리했다.
- qa는 8개 done_when 인수기준을 전부 command 출력 근거와 함께 ✅로 확인(무효 category → 400 `{error:"invalid_body"}` + fetch 미호출, `"culture"` 외 유효 category 통과, 기존 테스트 전부 무수정 통과 등).
- reviewer(본인)도 `git show 5290c07` 전체 diff를 직접 재검토: 스코프는 스펙과 정확히 일치하는 2개 파일, 신규 의존성 없음, 시크릿 없음, 캐스트 제거가 오히려 타입 안전성을 강화하는 방향(타입가드로 좁힌 뒤 원래 타입 그대로 반환)이라 리스크 낮음으로 판단. `git log -1 5290c07`으로 커밋 메시지에 `M-066`이 제목에 포함돼 있음을 직접 확인 — 커밋 메시지 규율(추적성) 통과.
- **qa PASS 판정, reviewer 독립 재검토도 동일 결론 — dev로 push 확정.**

## 리스크 & 후속

### M-052 — 14일 연속 사전 제외, 더 이상 매일 밤 재검증만 하고 넘어갈 문제가 아니다
`apps/mobile`에 위치 지도·산책로 표시가 없다는 이슈로, **2026-08-13부터 오늘(2026-08-26)까지 14일 연속** 씨앗 단계에서 사전 제외됐다. 오늘 밤도 재검증했다(`grep -i map apps/mobile/package.json` 여전히 공백, `MapView`/`react-native-maps` 참조 0건) — 변화 없음, 동일 사유로 재제외.

근본 원인은 변한 적이 없다: 구현에 세 가지 경로가 있는데(① `react-native-maps` 신규 네이티브 의존성 추가 — Expo prebuild·EAS 빌드에 영향 ② NAVER 정적지도 서버 프록시 신설 ③ 키리스 서드파티 정적지도 사용) 셋 중 무엇을 선택할지가 **무인 야간 파이프라인이 승인할 수 없는 사람의 결정**이다.

이미 **08-22, 08-23, 08-24, 08-25 나흘 연속** 리포트가 이 사실을 기록했고, 08-22 이후로는 backlog 노트뿐 아니라 사람에게 직접 알림(push)까지 보내 3택1(① 세 경로 중 하나 승인 ② M-052 priority 하향 ③ `blocked-human` 같은 정식 상태 도입)을 요청했다. **아직 아무것도 반영되지 않았다.**

**분명히 말한다**: 이 패턴은 이제 멈춰야 한다. 매일 밤 동일한 3옵션을 재상신하는 것은 정보 가치가 없다 — 이미 나흘 전에 다 전달됐다. 사람이 실제로 이번 주 안에 위 셋 중 하나를 정해야, 15일째·16일째로 같은 문단이 반복되는 것을 멈출 수 있다. 아침에 이 리포트를 보는 사람에게: 결정을 미루는 것 자체가 결정(= priority medium인 이슈를 사실상 영구 보류시키는 것)이라는 점을 감안해달라.

### M-067 — 문서·코드 불일치, 방향 결정 필요 (M-052보다 낮은 우선순위)
`packages/tokens`의 `MIN_HIT_TARGET` 상수가 정의만 있고 실제 코드에서 import된 곳이 0건인데, `docs/ux/ux-design.md:87`와 이전 백로그 서술은 이 상수로 44px 히트 타겟이 "적용됨"이라 말하고 있다. 두 방향이 있다:
1. `apps/mobile`의 하드코딩된 `hitSlop` 값들(예: `saved.tsx:39`, `ui/components.tsx:119`)을 실제로 `MIN_HIT_TARGET` 기준 계산으로 배선한다.
2. 강제할 의도가 없다면 export와 문서·백로그의 "적용됨" 서술을 함께 정리해 실제 상태와 맞춘다.

M-052처럼 다수 밤이 막힌 이력은 없지만(오늘이 사실상 첫 재확인), scope가 `apps/mobile` 전체에 걸쳐 있어 어느 방향이 맞는지는 사람이 한 번 정해주는 편이 다음 밤의 판단 낭비를 줄인다. 시급하지는 않으므로 M-052보다 후순위로 남긴다.

### 그 외
- M-070(zustand persist rehydrate 검증 공백)은 이번 밤 클러스터에 들지 못해 `todo`로 그대로 남는다 — 다음 밤이 재시도.
- backlog `todo` 잔여 3건(M-052 제외 시 2건: M-067, M-070) — 다음 며칠은 todo < 2로 떨어질 수 있어 audit 모드 진입 가능성 있음. M-052가 계속 medium으로 todo에 남아 카운트에 잡히는 한 `todo ≥ 2` 문턱은 유지되지만, 실질적으로 구현 가능한 이슈 풀은 점점 얕아지고 있다 — M-052 해소가 늦어질수록 이 효과도 커진다.
