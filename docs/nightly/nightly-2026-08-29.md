# Nightly Report — 2026-08-29

## TL;DR

- **⚠️ M-052가 오늘로 16일 연속(08-13~08-29) todo에 머물러 있고, 사람의 결정 없이는 이 파이프라인이 스스로 뚫을 수 없는 블로커임을 08-13부터 최소 8개 밤이 리포트에 반복 기록했다.** 리포트 안에서만 반복돼 사람에게 실제로 닿았는지 알 수 없었던 것으로 보인다 — 이번 밤은 리포트 기록에 더해 **PushNotification으로 직접 알림을 보냈다**(선택지: (a) 모바일 지도 구현 경로 3안 중 하나 승인 — react-native-maps 신규 네이티브 의존성 / NAVER 정적지도 백엔드 프록시 신설 / 키리스 서드파티 정적지도, (b) M-052 priority를 low로 낮춰 클러스터 씨앗에서 자연스럽게 밀려나게 하기, (c) `blocked-human` 같은 정식 상태를 backlog 스키마에 도입).
- **BACKLOG GATE 판정: audit 모드.** 밤 시작 시 `docs/backlog/backlog.yml`의 `status: todo`를 셌더니 **1건**(M-052)뿐이었다(`todo < 2`) — 트리거 프롬프트 BACKLOG GATE에 따라 구현 모드(planner/architect/impl/qa) 대신 **audit 모드**를 실행했다. 제품 코드는 전혀 건드리지 않았다.
- `origin/dev` HEAD(`21c28ff`, 2026-08-28 야간 산출물까지 포함)를 8축(a11y·perf·보안·타입안전·의존성경계·테스트커버리지·UX 일관성(web↔mobile)·데드코드)으로 병렬 감사(축당 에이전트 1개, 총 8개)했다. 원 findings **23건** → 오케스트레이터가 각 finding을 직접 `Read`/`grep`으로 file:line 재확인하는 적대검증 → **23건 전부 생존**(허위양성 없음, 단 1건은 원 finding의 프레이밍이 부정확해 정정) → 동일 결함 패턴(모바일 accessibilityRole 누락) 3건을 병합해 **21개 이슈(M-073~M-093)**로 `backlog.yml`에 등재.
- `bash scripts/gate.sh`(typecheck·test·lint·시크릿 스캔) — 코드 무변경이므로 예상대로 **전부 PASS**(typecheck 4/4, test core/web/mobile 전체 351개 통과, lint 통과, 시크릿 없음). audit 모드는 이 게이트를 "코드가 안 깨졌다"는 확인용으로만 돌렸다.
- **연속 report-only 경보 해당 없음**: 직전 두 밤(08-27 M-067 shipped, 08-28 M-070 shipped)은 정상 구현 밤이었다. 이번이 그 뒤 첫 audit 밤이라 "2회 이상 연속" 경보 조건에 해당하지 않는다.
- **PRECEDENCE 절 점검**: 트리거 프롬프트의 BACKLOG GATE·CLUSTER RULE을 `docs/nightly/PIPELINE.md`·`.claude/rules/workflow/nightly-pipeline.md`와 대조했다. 두 문서 모두 이미 동일한 0단계 백로그 게이트(todo<2→audit)와 클러스터 규칙을 담고 있어 override가 필요한 모순은 없었다.

## M-052 — 16일째 블록, 이번 밤 처음으로 사람에게 직접 알림

기존 관행: BACKLOG GATE가 구현 모드로 판정되면 planner가 M-052를 씨앗으로 집었다가 architect/오케스트레이터 사전검증에서 매번 제외해왔다(08-13~08-25, backlog.yml의 M-052 notes 참조). 오늘 밤은 애초에 todo가 1건(M-052 자신)뿐이라 **구현 모드 자체에 진입하지 않아 "제외" 절차가 없었다** — 그래서 이번 리포트에는 M-052 notes에 새 `[2026-08-29]` 항목을 추가하지 않았다(제외 판정을 안 내렸으므로).

대신 이번 밤이 처음으로 확인한 사실: **08-21·08-22·08-23·08-25가 각각 "사람에게 직접 알림을 보낸다"고 리포트에 적었지만, 그 알림은 실제로는 이 nightly report 파일에 적힌 문장이었을 뿐 사람의 휴대폰/이메일에 닿는 실제 알림 채널이 아니었을 가능성이 높다.** 이번 밤은 실행 환경에 `PushNotification` 도구가 있어 실제로 알림을 보냈다 — 이게 이 블로커가 사람에게 닿는 첫 시도일 수 있다.

**요청 사항 (사람 결정 필요, 3택1 중 하나 또는 조합)**:
1. 모바일 지도 구현 경로 승인 — `react-native-maps`(신규 네이티브 의존성, Expo prebuild/EAS 빌드 영향) / NAVER 정적지도 백엔드 프록시 신설 / 키리스 서드파티 정적지도. 세 경로 모두 무인 야간 파이프라인 권한 밖 결정(신규 의존성 승인·시크릿 프록시 신설·서드파티 신뢰)이라는 게 08-13 architect 판정.
2. M-052의 priority를 `medium`에서 `low`로 낮추기 — 그러면 클러스터 씨앗 우선순위에서 자연히 밀려나 이 사전제외 워크어라운드 자체가 불필요해진다.
3. backlog 스키마에 `blocked-human`(또는 유사) status를 도입해 이런 케이스를 `todo`와 구분 — 08-16 리포트가 이미 이 필요성을 지적함.

## 감사 범위와 방법

8개 에이전트를 축별로 병렬 fan-out했다(각 read-only, 제품 코드 미수정, `dev` HEAD 기준). 각 에이전트에게 `docs/backlog/backlog.yml` 전체(M-001~M-072, 대부분 doing/done)를 먼저 읽고 이미 다뤄진 결함과 겹치지 않는 것만 찾도록 지시했다.

| 축 | 담당 에이전트 findings | 검증 후 채택 |
|---|---|---|
| a11y | 3건 | 3건 → 1개로 병합(M-073) |
| perf | 2건 | 2건 (M-074, M-075) |
| 보안 | 3건 | 3건 (M-076, M-077, M-078) |
| 타입안전 | 1건 | 1건 (M-079) |
| 의존성 경계 | 2건 | 2건 (M-080, M-081) |
| 테스트 커버리지 | 3건 | 3건 (M-082, M-083, M-084) |
| UX 일관성(web↔mobile) | 5건 | 5건 (M-085~M-089) |
| 데드코드 | 4건 | 4건 (M-090~M-093) |
| **합계** | **23건** | **21개 이슈로 등재** |

## 적대검증 상세 (오케스트레이터 직접 수행)

각 에이전트는 이미 스스로 backlog와 대조·재검증했다고 보고했지만, 트리거 프롬프트가 요구하는 적대검증은 오케스트레이터가 직접 하는 것이므로 **23건 전부를 내가 직접 `Read`/`grep`으로 file:line을 다시 읽어 재확인**했다:

- **a11y(M-073)**: 원 finding이 "저장 취소 버튼이 상세이동 버튼 안에 중첩돼 역할도 없다"고 주장했으나, 직접 읽어보니 `saved.tsx:37-45`의 저장 취소 `Pressable`은 이미 `accessibilityRole="button"`+`accessibilityLabel="저장 취소"`가 있었다 — **원 finding을 정정**하고, 실제 결함(카드 바깥 Pressable + 재진단/다시시도/둘러보기 3곳 + my.tsx 2곳 + hero-carousel.tsx 1곳, 총 3개 파일에 role 누락)만 채택했다.
- **perf(M-074)**: `opportunity-detail.tsx:49`의 주석은 "initial이 있으면 재조회 없이 렌더"라 적혀 있어 처음엔 의심했으나, `useOpportunity.ts:24-30`을 직접 읽어 `enabled: id != null`로 initial 유무와 무관하게 항상 쿼리가 실행됨을 확인 — 주석이 사용자 체감(화면엔 안 보임)과 실제 네트워크 동작을 혼동하고 있었다. finding 확정.
- **perf(M-075)**: `useSavedOpportunities.ts`가 정말 개별 `fetchOpportunityById`를 쓰는지, core의 `fetchOpportunitiesByIds`가 실제 존재하고 웹이 이미 쓰는지 grep으로 교차 확인. 확정.
- **보안(M-076)**: `/api/geo/route.ts:35-62`를 직접 읽어 lat/lng 미스냅 확인, `/api/opportunities/route.ts:35,47-48`의 `GRID=0.01`/`snap()`과 나란히 대조해 "형제 라우트는 이미 처리했다"는 주장을 검증. `/api/why-reasons/route.ts`도 레이트리밋·길이제한 부재 확인.
- **보안(M-077)**: `adapters.ts:93`의 `cta_url: raw.ORG_LINK || null`에 검증 없음, web/mobile 양쪽 `hasLink` 판정에 프로토콜 체크 없음을 직접 확인. `trail-route/route.ts:72`의 기존 방어 패턴과 대조.
- **보안(M-078)**: `route.ts:68`의 `?key=${apiKey}` 쿼리스트링 사용, `sentry.server.config.ts`에 breadcrumb 스크러빙 부재 직접 확인.
- **타입안전(M-079)**: `store.ts:156-165`의 `merge()`가 `answers`만 재검증하고 `savedIds`/`anchors`는 통과시킴을 직접 확인.
- **의존성경계(M-080, M-081)**: `explore-filters.ts`/`explore.tsx`의 `FILTERS` 리터럴 중복, `report/page.tsx`의 로컬 `formatDeadline` vs `packages/core/src/view.ts`의 `deadlineLabel` 중복을 grep으로 직접 확인.
- **테스트커버리지(M-082, M-083, M-084)**: `opportunity-detail.render-isolation.test.tsx`가 deadline 없는 fixture만 씀을 확인, `course-guide.tsx`에 대응 테스트 파일이 아예 없음을 `ls`로 확인, `login/page.tsx:33-34`의 오픈 리다이렉트 방어 코드와 부재한 테스트 파일 확인, `useTrailRoute`가 소비처마다 `vi.mock`되고 전용 테스트가 없음을 확인.
- **UX 패리티(M-085~M-089)**: 모바일 `explore.tsx:158`의 `/location` 이동과 `location.tsx`의 `start()`가 항상 `/diagnosis`로 감을 직접 확인, `location.tsx`의 권한 거부 처리(86-114행)가 단일 문구뿐임을 확인, `report.tsx` 전체에 `Thumbnail`/`imageUrl`/`deadline`/`Share` 참조가 0건임을 grep으로 확인, `!onePick` 분기가 idle을 error와 구분 안 함을 확인, `saved.tsx`에 `Thumbnail`/`imageUrl` 참조 0건 확인.
- **데드코드(M-090~M-093)**: 4건 모두 word-boundary grep으로 정의처 외 참조 0건 재확인(단, `poster-ring.tsx`의 동명 로컬 `type Card`는 무관함을 별도 확인해 오탐 배제).

**23건 전원 생존**(1건은 프레이밍만 정정, 반증으로 완전히 탈락한 finding은 없음).

## 등재된 이슈 (M-073 ~ M-093)

| id | priority | scope | 요약 |
|---|---|---|---|
| M-073 | high | 모바일 3개 화면 | accessibilityRole 스윕 2차(M-058 범위 밖이었던 saved/my/hero-carousel) |
| M-074 | medium | `apps/web/src/hooks/useOpportunity.ts` | 상세 페이지 SSR 데이터를 클라이언트가 즉시 중복 재조회 |
| M-075 | medium | `apps/mobile/hooks/useSavedOpportunities.ts` | 모바일 보관함 카탈로그 밖 항목 N+1 조회 |
| M-076 | high | `apps/web/src/app/api/geo/route.ts` 외 | 공개 프록시 레이트리밋·좌표 스냅 부재 |
| M-077 | medium | `supabase/functions/ingest/adapters.ts` 외 | 외부 cta_url 프로토콜 검증 없음 |
| M-078 | medium | `apps/web/src/app/api/why-reasons/route.ts` 외 | Gemini 키가 쿼리스트링으로 전송, Sentry 노출 위험 |
| M-079 | low | `packages/core/src/store.ts` | rehydrate 시 savedIds/anchors 미검증 |
| M-080 | medium | `apps/web/src/lib/explore-filters.ts` 외 | 탐색 FILTERS 목록 web/mobile 중복 |
| M-081 | medium | `apps/web/src/app/report/page.tsx` | 로컬 formatDeadline이 core deadlineLabel 재구현 |
| M-082 | high | `apps/web/src/components/opportunity-detail.tsx` 외 | 상세 화면 핵심 로직 테스트 0건 |
| M-083 | medium | `apps/web/src/app/login/page.tsx` | 오픈 리다이렉트 방어 테스트 0건 |
| M-084 | medium | `apps/web/src/hooks/useTrailRoute.ts` | 항상 mock, 실구현 미검증 |
| M-085 | high | `apps/mobile/app/(tabs)/explore.tsx` 외 | 모바일 동네 변경이 진단 리셋 강제 |
| M-086 | medium | `apps/mobile/app/location.tsx` | 위치 권한 영구거부 복구 안내 없음 |
| M-087 | medium | `apps/mobile/app/(tabs)/report.tsx` | 원픽 이미지·마감배지·공유 없음 |
| M-088 | low | `apps/mobile/app/(tabs)/report.tsx` | 로딩/빈상태 미구분 |
| M-089 | low | `apps/mobile/app/(tabs)/saved.tsx` | 보관함 카드 썸네일 없음 |
| M-090 | low | `apps/mobile/ui/components.tsx` | 미사용 Card 컴포넌트 |
| M-091 | medium | `apps/web/src/components/ui.tsx` | Txt/InfoBox/Card 테스트만 있고 미사용 |
| M-092 | low | `packages/tokens/src/index.ts` | 집계 export tokens/Tokens 미사용 |
| M-093 | low | `packages/core/src/adapters/*` | 어댑터 미사용 export 2건 |

우선순위 분포: high 4 · medium 11 · low 6.

## 병합 사유

- **M-073**: a11y 축의 3개 독립 finding(saved.tsx·my.tsx·hero-carousel.tsx)이 전부 "RN Pressable에 accessibilityRole 누락"이라는 동일 결함 패턴이었다. M-058(같은 패턴을 다중 파일에 걸쳐 한 스윕 이슈로 등재) 선례를 따라 병합.
- 나머지 20건은 서로 독립적인 결함(다른 파일·다른 메커니즘)이라 각각 별도 이슈로 유지했다. 데드코드 4건(M-090~M-093)은 파일·패키지가 전부 달라(모바일 ui / 웹 ui / tokens / core adapters) 억지로 하나로 묶으면 향후 CLUSTER RULE의 scope-overlap 판정이 무의미해지므로 병합하지 않았다.

## qa 게이트 (audit 모드 — 코드 무변경 확인용)

```
$ bash scripts/gate.sh
── typecheck   ✅ (4/4 워크스페이스)
── test        ✅ (core/web/mobile 전체 351개 통과)
── lint        ✅ (web ESLint 경고/에러 없음)
── secrets     ✅ (커밋 diff에 시크릿 패턴 없음)
GATE: PASS — push 허용
```

## 이번 밤 산출물

- `docs/backlog/backlog.yml`에 M-073~M-093 21개 신규 이슈 등재(커밋 `96666e3`).
- 이 리포트(`docs/nightly/nightly-2026-08-29.md`).
- **제품 코드는 한 줄도 건드리지 않았다.** planner/architect/impl/qa 단계는 실행하지 않았다(audit 모드 규칙대로).
- M-052를 포함해 backlog `status`는 전혀 변경하지 않았다 — reviewer의 todo→doing 전환은 구현 모드 산출물에만 적용되며 이번 밤엔 shipped 이슈가 없다.

## 리스크 & 후속

- **M-052가 다음 며칠 안에도 사람 결정이 없으면 todo 총량이 계속 낮게 유지되어 audit 모드가 반복될 수 있다.** audit 모드 자체는 정상 동작(백로그를 계속 채운다)이지만, 사람이 M-052를 처리하지 않는 한 "매일 audit만 하고 구현은 못 한다"는 새로운 정체 패턴으로 이어질 위험이 있다 — 오늘 밤 등재한 21건이 todo를 22건으로 늘렸으므로 내일 밤은 정상적으로 구현 모드(클러스터)에 진입할 것으로 예상되지만, M-052 자체의 해결은 여전히 사람 손에 달려 있다.
- M-091(웹 `ui.tsx`의 `Txt`/`InfoBox`/`Card`)은 우선순위를 medium으로 매겼다 — 단순 데드코드가 아니라 "테스트가 있어 쓰이고 있다는 착각을 준다"는 유지비 함정이라 다른 데드코드(low)보다 위험도가 높다고 판단했다.
