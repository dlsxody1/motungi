# Nightly Report — 2026-09-06

## TL;DR

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `docs/backlog/backlog.yml`의 `status: todo`는 **8건**이었다 — `todo ≥ 2`이므로 구현 모드로 진행했다.
- **연속 report-only 경보 해당 없음.** 직전 밤(09-05)은 M-081/083/084가 shipped된 정상 구현 밤이었다. 스트릭 0.
- **브랜치 동기화 점검(nightly-pipeline.md 6번) 수행함**: `git log --oneline origin/dev..origin/main`이 비어 있어 되받을 것 없이 그대로 진행.
- **클러스터 = M-091(씨앗) + M-096, `apps/web/src/components/` narrow-directory 겹침으로 2건 전부 shipped.** 되돌린 이슈 없음.
- `bash scripts/gate.sh` **PASS**(qa 독립 재실행) — typecheck 4/4 · test(core 391·mobile 159·web 381) · lint clean · secrets clean. `pnpm --filter web build` 27/27 라우트 성공.
- **환경 이슈 재발 확인(2번째 연속)**: 이 세션도 09-05와 동일하게 `/home/user`가 빈 디렉터리로 시작해 `mcp__Claude_Code_Remote__list_repos`로 `dlsxody1/motungi`를 찾아 직접 clone해야 했다. 09-05 리포트는 "일회성인지 불확실"이라 적었으나, 2일 연속 동일 증상이 나왔으므로 **이제는 우연이 아니라 트리거/환경 설정의 구조적 문제로 판단**한다. 사람에게 알린다(아래 참조).
- **doc 모순 3연속 미해결**: `.claude/agents/architect.md:3,9,18,28`이 "FSD 아키텍처"·"FSD 경계: entities/features/widgets/shared"를 언급하는데 `.claude/rules/core/constitution.md`("파일 구조는 기술 종류별 평면 배치다... FSD 디렉토리는 실재하지 않는다")와 정면으로 모순된다. 09-04·09-05 리포트에 이미 기록됐으나 여전히 미수정이다. 이번 밤도 constitution.md(평면 구조)를 따랐다 — 새 디렉토리를 만들지 않고 기존 `components/`·`app/`에만 작업했다.

## ⚠️ 사람 개입 필요

### 1. M-052, 25일 연속 동일 사전제외 (계속)
`M-052`(모바일 상세 화면 지도·산책로 폴리라인)는 **2026-08-13부터 오늘(09-06)까지 25일 연속** 씨앗 단계에서 재검증 후 사전 제외되고 있다. 오늘 밤 재검증도 동일: `grep -i map apps/mobile/package.json` 공백, `apps/mobile` 코드 전체에 `MapView`/`react-native-maps` 참조 0건, `.env.example`·`security-policy.md`에 지도 관련 항목 없음 — 변화 없음. 08-13이 제시한 3개 구현 경로(react-native-maps 신규 네이티브 의존성 / NAVER 정적지도 백엔드 프록시 신설 / 키리스 서드파티 정적지도) 모두 사람의 정책 승인 없이는 무인 야간 파이프라인이 뚫을 수 없다. 09-05 리포트가 이미 PushNotification으로 알렸고 3택1(경로 승인 / priority 하향 / blocked-human 상태 도입)을 요청했다.

### 2. 트리거 실행 환경에 repo가 사전 배선되지 않음 — 2일 연속 재현, 구조적 문제로 격상
09-05·09-06 두 밤 연속으로 세션이 **완전히 빈 작업 디렉터리**(`/home/user`)에서 시작해, `docs/backlog/backlog.yml`은커녕 repo 자체가 없는 상태로 파이프라인을 시작했다. 두 밤 다 `mcp__Claude_Code_Remote__list_repos`로 `dlsxody1/motungi`를 찾아 수동 clone 후 `origin/dev` 체크아웃으로 복구했지만, 이건 이번 파이프라인 지시문에 있는 "복구 절차"가 아니라 즉흥 대응이다. 트리거가 이 repo를 세션에 자동으로 붙이지 않고 있다는 뜻이며, 언젠가 복구에 실패하면(예: repo 이름을 못 찾거나 권한 문제) 그 밤은 아예 시작도 못 하고 조용히 실패할 위험이 있다. **사람이 클라우드 트리거/환경 설정에서 이 repo가 세션 시작 시 자동으로 연결되도록 고쳐야 한다.**

**본 세션이 위 두 건을 PushNotification으로 사람에게 알린다** — 특히 2번은 09-05 리포트 시점엔 "일회성인지 불확실"이었던 것이 오늘로 확정됐다는 새 정보다.

## 집은 클러스터

- **씨앗 선정**: `status: todo` 8건 중 priority 최상위는 `medium`(M-052/M-091/M-096/M-097, high 없음). 최소 id는 `M-052`이나, 위 "사람 개입 필요 §1"대로 25일째 사전 제외 사유가 재확인돼 씨앗에서 배제. 다음 최소 id인 **M-091**을 씨앗으로 선정.
- **파일/좁은 디렉토리 겹침**: M-091(`apps/web/src/components/ui.tsx`)과 M-096(`apps/web/src/components/web-landing.tsx`)이 같은 좁은 디렉토리 `apps/web/src/components/`를 공유한다 — 08-25의 M-060+M-071, 08-18/23의 M-068+M-069와 동일한 narrow-directory 겹침 선례를 따름(둘 다 별개 파일이지만 같은 디렉토리, 앱 전체 단위가 아님).
- **앱 단위 폴백은 사용하지 않음** — 파일 겹침 후보가 이미 1건(M-096) 있었으므로 폴백 조건(겹침 0건)이 성립하지 않는다.
- **상한 3개 중 2개만 참** — 세 번째 후보(M-097, medium, `supabase/migrations`·`supabase/functions/ingest/adapters.ts`·`packages/core/src/catalog.ts`)는 M-091 scope와 파일/디렉토리 겹침이 전혀 없어 클러스터에 들어오지 않았다(상한 도달로 미룬 것이 아니라 애초에 겹침 후보가 아니었음 — 낮은 priority를 끌어올 이유도 없었다).
- **미룬 이슈**: M-097(medium, 다음 밤 씨앗 후보), M-079·M-088·M-092·M-093(low). M-052는 위 사유로 계속 제외.

## 이슈별 결과

### M-091 — shipped (`19b126e`)
`apps/web/src/components/ui.tsx`의 `Txt`/`InfoBox`/`Card`가 export만 되고 `ui.test.tsx` 외 실사용처가 0이라 "테스트가 있으니 쓰이고 있다"는 거짓 신뢰를 유발하던 문제.
- **기준선 재확인**: `origin/dev` 실코드에 word-boundary grep으로 세 심볼 모두 정의·테스트 파일 외 매치 0건 재확인(`poster-ring.tsx`의 동명 로컬 `type Card`는 무관) — `done_when` 미충족.
- **구현 방향**: 배선 대신 삭제. 선례는 M-090(모바일 `apps/mobile/ui/components.tsx`의 동일 패턴 `Card`)이 이미 삭제로 해소돼 있음을 확인하고 동일 방향을 택했다.
- **변경**: `ui.tsx`에서 `Txt`·`InfoBox`·`Card` 함수 정의(및 관련 JSDoc/섹션 주석) 제거. `ui.test.tsx`에서 세 `describe` 블록과 관련 import 제거. 같은 파일의 `text` 타이포 프리셋 const는 `done_when`에 없어 손대지 않음(별도 데드코드 후보로 남을 수 있으나 이번 scope 밖).
- **검증**: `grep -rn -w "Txt" apps/web/src` 0건, `"InfoBox"` 0건, `"Card"`는 `poster-ring.tsx`에서만(무관 타입). `pnpm --filter web typecheck`·`test` 통과.
- **done_when 3개 전부 충족.** 되돌림 없음.

### M-096 — shipped (`b575fc2`)
`docs/AEO.md` §3(랜딩 답변 블록 + 구 링크 + `/explore` ItemList) 구현. 랜딩이 "퇴근하고 뭐하지?"를 질문으로만 갖고 있고 답 산문이 없던 문제, `/explore`가 클라이언트 렌더라 크롤러에 구조화 데이터가 전혀 안 나가던 문제.
- **기준선 재확인**: `web-landing.tsx`에 산문 답변 블록·구 링크 목록 부재, `/explore` 라우트에 ItemList JSON-LD 없음을 직접 확인 — `done_when` 미충족.
- **scope 보정**: backlog의 `apps/web/src/app/explore/page.tsx`는 라우트 그룹 재구성(`explore/(list)/`, `explore/[gu]/`)으로 이미 존재하지 않는 경로다. 신규 `apps/web/src/app/explore/(list)/layout.tsx`로 대체 — `(list)` 그룹에만 적용돼 이미 자체 구조화 데이터를 가진 `/explore/[gu]/*`에는 영향이 없다(Next.js route-group layout scoping으로 구조적 보장, 빌드 출력에서도 `/explore`만 `revalidate: 6h`를 받고 `/explore/[gu]`는 별도임을 확인).
- **"24개 구" 하드코딩 회피**: done_when 문구의 "24개"는 검증되지 않은 수치다(M-095가 실측 결과 25개 구 중 22개만 임계(`GU_MIN_ACTIVITIES`)를 넘겨 실제 페이지가 있음을 이미 확인한 선례). `page.tsx`가 `summarizeGu`(이미 `packages/core`에 존재, 신규 구현 아님)로 실측한 리스트를 `guNames`로 `WebLanding`에 내려주고, 링크 목록·문구의 개수 모두 `guNames.length`에서 뽑는다. 리터럴 숫자·gu 이름 배열 하드코딩 없음(grep으로 재확인).
- **구현**: `web-landing.tsx`에 FAQ 섹션 뒤 신규 섹션 — "퇴근하고 뭐하지? — 모퉁이의 답" 산문 4문단(기존 `LANDING_FAQS`의 사실만 재사용, 새 사실 추가 없음, 평서형 유지) + 실측 구 링크 목록(`guNames` 비면 섹션째 미렌더). `lib/seo.ts`에 `itemListJsonLd` 헬퍼 신설(`eventJsonLd`/`faqJsonLd`와 동일한 `safeJson` 이스케이프·빈 목록→`null` 규율) + 단위 테스트. `page.tsx`에 `revalidate = 21_600`(sitemap·`[gu]`와 동일 주기) 추가, 모바일 히어로(`md:hidden` 트리)는 diff로 무변경 확인.
- **테스트**: `web-landing.test.tsx`에 산문 개수·링크 개수(`guNames.length`와 일치)·빈 목록 시 미렌더 테스트 추가. `apps/web/src/app/explore/(list)/layout.test.tsx` 신설(데이터 있음/빈 목록/fetch 에러 3케이스 + children 항상 통과). `seo.test.ts`에 `itemListJsonLd` 빈 목록→null·이스케이프·필드 매핑 테스트 추가.
- **done_when 4개 항목 판정**: (1) 산문 3~4문단 서버렌더 — 충족(4문단, `web-landing.tsx`/`page.tsx` 어디에도 `"use client"` 추가 없음). (2) "24개 구 링크 목록" — 문구의 리터럴 숫자는 폐기하고 정신(실측 링크 목록)만 충족, 사유는 위에 명시. (3) `/explore` ItemList JSON-LD 서버 렌더 — 충족, `/explore/[gu]`는 영향 없음을 구조·빌드 출력 둘 다로 확인. (4) 모바일 히어로 무손상 — 충족.
- 되돌림 없음.

## 아키텍처 검토

- 두 이슈 모두 **frontend-impl 단독 담당** — 순수 웹 프론트엔드(컴포넌트·서버 컴포넌트 레이아웃·헬퍼·테스트) 변경이고 DB 마이그레이션·Edge Function 접점이 없다. `packages/core`는 M-096에서도 **미변경**이다(`summarizeGu`가 M-073에서 이미 만들어져 있어 소비만 함).
- **경계 배치 판단(M-096)**: ItemList JSON-LD를 기존 `apps/web/src/app/explore/layout.tsx`(모든 `/explore/*`에 적용)가 아니라 신규 `explore/(list)/layout.tsx`(라우트 그룹 스코프)에 둔 것이 이번 밤의 유일한 아키텍처 결정이었다. 공유 레이아웃에 두면 `/explore/[gu]/*`가 이미 가진 구조화 데이터와 중복·충돌했을 것 — 라우트 그룹으로 격리하는 게 Next.js App Router의 기존 관용구(이미 `explore/layout.tsx`가 "page.tsx가 use client라 메타데이터를 못 export해 레이아웃으로 감싼다"는 같은 이유로 존재)와 일치한다.
- FSD 계층 위반 없음 — 새 디렉토리 없이 기존 `components/`(평면) + `app/explore/(list)/`(App Router 라우트 그룹, 기존 컨벤션) 안에서만 작업했다. `.claude/rules/core/constitution.md`의 "FSD 디렉토리는 실재하지 않는다"를 따름 — 위 TL;DR의 doc 모순 참조.
- 렌더 격리 관점: `WebLanding`은 이미 `md:` 이상에서만 렌더되는 데스크톱 전용 컴포넌트라 별도 격리 조치 불필요. 신규 섹션은 정적 prop(`guNames`) 기반이라 리렌더 이슈 없음.

## 변경 내용

**M-091** (커밋 `19b126e`):
- `apps/web/src/components/ui.tsx` — `Txt`/`InfoBox`/`Card` 제거(60줄)
- `apps/web/src/components/ui.test.tsx` — 해당 describe 블록·import 제거(67줄)

**M-096** (커밋 `b575fc2`):
- `apps/web/src/lib/seo.ts` / `seo.test.ts` — `itemListJsonLd` 헬퍼 + 단위 테스트 신설
- `apps/web/src/app/page.tsx` — `summarizeGu` 기반 `guNames` 도출 + `WebLanding`에 전달 + `revalidate` 추가
- `apps/web/src/components/web-landing.tsx` / `web-landing.test.tsx` — 산문 답변 섹션 + 구 링크 목록 신설
- `apps/web/src/app/explore/(list)/layout.tsx`(신규) / `layout.test.tsx`(신규) — `/explore` 전용 ItemList JSON-LD

## 검증

qa 에이전트가 구현자 보고를 신뢰하지 않고 동일 HEAD(`b575fc2`, 두 커밋 모두 적용)에서 독립 재실행:

```
bash scripts/gate.sh
── typecheck   ✅ (core·mobile·tokens·web 4/4)
── test        ✅ (core 391·mobile 159·web 381, 전부 통과)
── lint        ✅ (next lint — ✔ No ESLint warnings or errors)
── secrets     ✅ (시크릿 패턴 없음)
GATE: PASS — push 허용

pnpm --filter web build
✓ Compiled successfully in 9.8s
✓ Generating static pages (27/27)
├ ○ /explore        13.7 kB   316 kB   6h   1y   ← 신규 (list) 레이아웃의 revalidate
├ ● /explore/[gu]    1.78 kB  283 kB        ← revalidate 컬럼 없음(영향 없음 확인)
```

M-091 acceptance grep 재확인: `Txt`/`InfoBox` 0건, `Card`는 `poster-ring.tsx`(무관 로컬 타입)에서만.
`git diff` 스코프 점검: 예상된 9개 파일(M-091 2개 + M-096 7개) 외 변경 없음. 시크릿·파괴적 변경 없음(순수 추가/삭제성 UI·카피·JSON-LD 코드).

두 커밋 모두 개별적으로 revert 가능하며, 최종 트리(2커밋 전부 반영)로 gate.sh + build를 재실행해 green을 확인한 뒤 push한다.

## 리스크 & 후속

- **M-052(모바일 지도) 25일 연속 블로킹** — 위 "사람 개입 필요 §1" 참조.
- **트리거 환경에 repo 미배선, 2일 연속 재현** — 위 "사람 개입 필요 §2" 참조. 09-05엔 "일회성인지 불확실"이었으나 오늘로 구조적 문제로 확정.
- `architect.md`(FSD 언급, 3·9·18·28행) ↔ `constitution.md`(FSD 디렉토리 부재 명시) 모순 3일 연속(09-04·09-05·09-06) 미해결 — 재수정 권고 반복.
- M-096에서 `text`(ui.tsx 타이포 프리셋)가 `Txt` 제거로 내부 소비처를 잃어 M-093과 같은 "export-과다개방" 패턴 후보가 됐다 — 이번 scope 밖이라 손대지 않았으나 향후 audit 축에서 걸릴 수 있음, 참고용으로 남김.
- 다음 밤 씨앗 후보: M-097(medium, 동 단위 좌표 백필 — supabase 마이그레이션 포함이라 backend-impl 필요, DB 백필은 특히 신중한 계획이 필요).
