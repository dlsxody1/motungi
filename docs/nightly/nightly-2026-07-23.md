# Nightly Report — 2026-07-23

## TL;DR

- **오늘 밤은 report-only다 — 코드 변경 없음.** `docs/backlog/backlog.yml`의 이슈 19개(M-001~M-019)를 전수 확인한 결과 `status: todo`인 항목이 **하나도 없다**. 나머지는 전부 `done`(13개) 또는 `doing`(3개: M-014, M-015, M-017)이며, `doing`은 지난 밤들이 이미 골라 dev에 push했으나 아직 사람이 `dev → main` 승격·`done` 확정을 하지 않은 상태다.
- planner 규칙(`.claude/agents/planner.md` §"할 일" 1번)에 따라 "todo가 없으면 그 사실만 보고하고 종료"해야 하므로, architect/구현/qa 단계로 진행하지 않고 이 리포트만 남긴다.
- `dev`(로컬 `160bc98`, `origin/dev` tip과 일치 확인)와 `main`(`f537639`, PR #17 머지 후 tip)을 비교하면 dev가 여러 커밋 앞서 있다 — 최근 승격 주기가 늦어지며 `doing` 이슈가 3개까지 쌓인 것으로 보인다.

## 집은 이슈

- **없음.** `backlog.yml` 전체 스캔 결과:
  - `status: done` — M-001, M-002, M-003, M-004, M-005, M-006, M-007, M-008, M-009, M-010, M-011, M-012, M-013, M-016, M-018, M-019 (16개)
  - `status: doing` — M-014("a11y: 구조·모션·확대 결함 수정", 2026-07-20 야간에 doing 전환), M-015("웹/모바일 패리티 정합", 2026-07-21 야간에 doing 전환 — done_when 5개 항목 모두 구현·검증 완료 기록 확인), M-017("RN 모바일 화면 렌더 테스트 도입", 2026-07-22 야간에 doing 전환)
  - `status: todo` — 0개
- 다음 후보가 될 신규 이슈는 사람이 `backlog.yml`에 직접 추가해야 한다(밤 에이전트는 status만 갱신 가능, 신규 이슈 추가 권한 없음 — `CLAUDE.md` §백로그).

## 아키텍처 검토

- 해당 없음 — planner 단계에서 조기 종료했으므로 architect/frontend-impl/backend-impl/qa 단계를 실행하지 않았다.

## 변경 내용

- **코드 변경 없음.** 이 리포트 파일(`docs/nightly/nightly-2026-07-23.md`) 추가만 dev에 커밋한다. `packages/*`, `apps/*`, `supabase/*` 어디도 건드리지 않았다.
- `docs/backlog/backlog.yml`도 갱신하지 않는다 — todo→doing으로 바꿀 새 이슈가 없었기 때문이다(reviewer 규칙은 "집은 이슈의 status 갱신"을 전제하는데, 오늘은 이슈를 고르지 않았다).

## 검증

- 실행한 명령과 결과:
  ```
  $ git fetch origin dev && git ls-remote origin refs/heads/dev
  160bc98d22d8261c98a2f891341fc6b962f27fe8	refs/heads/dev
  $ git checkout -B dev origin/dev
  # 로컬 dev HEAD = 160bc98 (origin/dev와 동일, 최신 기준선 확인)
  $ awk '/^  - id: M-0/{id=$0} /status:/{print id, $0}' docs/backlog/backlog.yml
  # → status:todo 0건, status:doing 3건(M-014·M-015·M-017), 나머지 done
  $ git log --oneline origin/main -5
  f537639 dev → main: 상세페이지 개선 + 히어로 캐러셀·동네설정 UX (누적) (#17)
  ```
- typecheck/test/build는 실행하지 않았다 — 코드 변경이 없어 push 게이트(qa) 대상 자체가 없기 때문이다(`pnpm typecheck`/`pnpm test`를 억지로 돌려 "검증했다"고 부풀리지 않음).

## 리스크 & 후속

1. **backlog 공급 고갈**: `status: todo` 항목이 0개다. 사람이 `docs/backlog/backlog.yml`에 새 이슈를 추가하지 않으면 다음 밤도 동일하게 report-only로 종료된다.
2. **`doing` 3건 적체**: M-014(2026-07-20~)·M-015(2026-07-21~)·M-017(2026-07-22~)이 며칠째 `doing`에 머물러 있다. 각 이슈의 notes를 보면 구현·검증(typecheck/test/build)이 이미 완료된 상태로 기록돼 있어, 코드 자체는 문제 없어 보인다. `dev`가 `main`(#17 머지 시점) 대비 여러 커밋 앞서 있으므로, `dev → main` 승격 PR을 사람이 열어 리뷰·머지하고 해당 이슈들을 `done`으로 확정하면 이후 재작업 여지도 줄어든다.
3. 다음 밤을 위해: 사람이 (a) `doing` 3건을 검토해 `main` 승격 여부를 판단하고, (b) 새 `todo` 이슈를 backlog에 추가해주기를 권장한다.
