# Nightly Report — 2026-08-25

## TL;DR

⚠️ **M-052가 오늘로 13일 연속(08-13~08-25) 동일 사유로 씨앗 단계에서 사전 제외됐다 — 사람 개입 필요. 오늘 밤은 backlog 노트 반복만으로는 부족하다고 판단해 세션 알림(push)을 직접 발송한다.**

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `status: todo` 개수는 **6건**(M-052, M-060, M-066, M-067, M-070, M-071) — `todo ≥ 2`이므로 구현 모드로 진행했다.
- **클러스터 = M-060 + M-071** (2건). 씨앗 후보였던 M-052(유일한 medium priority)는 13일째 재제외됐고, 나머지 전부가 low priority라 최소 id인 M-060이 씨앗이 됐다. M-071이 `apps/web/src/components/` narrow-directory 겹침으로 합류(M-068+M-069 선례와 동일 패턴). M-066·M-067·M-070은 파일도 디렉토리도 겹치지 않아 미룸.
- qa 게이트(`bash scripts/gate.sh`) **PASS** — typecheck 4/4, test(core/mobile/web 전체), lint 통과, 시크릿 없음. 오케스트레이터가 커밋 이후 게이트를 fresh 재실행해 재확인(아래 "검증" 참조).
- **shipped: M-060**(미사용 아이콘 export 11개 정리, 커밋 `d48ed17`), **M-071**(동네선택 다이얼로그 aria-current 추가, 커밋 `9cef49f`). 되돌린 이슈 없음.
- 연속 report-only 경보 해당 없음 — 지난밤(08-24)도 shipped였고 오늘도 shipped다.
- 부수 확인: 클론 직후 main(`744411d`)이 dev보다 앞서 있는 것처럼 보였으나(얕은 clone이라 공통 조상이 안 보였을 뿐), `git fetch --unshallow` 후 재확인 결과 `git log origin/dev..origin/main`이 비어 있어 실제로는 dev가 이미 그 커밋을 포함하고 있었다(오탐). nightly-pipeline.md 신규 규칙(6번 — "main이 dev보다 앞서 있으면 되받고 시작") 자체는 정상 작동했고, 이번엔 merge가 불필요했다는 것만 확인.

## 집은 클러스터

**M-060 + M-071.**
1. 정상적인 씨앗은 priority 최상위 medium인 M-052지만, 13일 연속(08-13~08-25) 재제외 대상이라 씨앗 후보에서 애초에 빠졌다(사유는 "리스크 & 후속" 참조, 08-13~08-24와 변경 없음을 오늘 밤도 재검증함).
2. 남은 todo 5건이 전부 low priority라 priority 동률 → id 오름차순으로 **M-060**이 씨앗.
3. scope 겹침 확인: M-060의 scope는 `apps/web/src/components/icons.tsx` + `apps/web/src/components/landing-icons.tsx` — 둘 다 실제 파일. 나머지 4건 중 **M-071**(`apps/web/src/components/neighborhood-menu.tsx`)이 같은 디렉토리(`apps/web/src/components/`)를 공유한다. M-066(`apps/web/src/app/api/why-reasons/route.ts`)·M-067(`packages/tokens/src/index.ts` + `apps/mobile`)·M-070(`packages/core/src/store.ts`)은 디렉토리도 다르다.
4. "같은 디렉토리 겹침"으로 합류시킨 근거는 08-23 밤의 M-068+M-069(둘 다 `apps/web/src/lib/`) 선례와 동일한 판단 기준을 따랐다 — 서로 다른 파일이지만 같은 좁은 디렉토리를 공유하면 겹침으로 인정.
5. 상한 3개 이내(2건). 더 채울 수 있는 후보가 없어(나머지는 디렉토리도 다르고, priority가 전부 동일 low라 "앱 단위+동일 priority" 폴백을 적용할 이유도 없음 — 애초에 파일 겹침으로 이미 클러스터가 성립했으므로 폴백 규칙 자체가 적용 대상이 아니다) 2이슈로 확정.

미룬 이슈: M-066, M-067, M-070 (전부 low, 다음 밤 재검토 대상). M-052는 위 사유로 재차 미룸.

## planner 재검증 (기준선: origin/dev `ce9c961`)

- `git fetch --depth 1 origin dev` 후 `origin/dev`(`ce9c961`, 08-24 야간 리포트 커밋)로 checkout. 이후 `git fetch --unshallow`로 전체 히스토리 확보(main-vs-dev divergence 점검용).
- M-060: `apps/web/src/components/icons.tsx`·`landing-icons.tsx`의 11개 심볼(ArrowRightIcon·FireIcon·SparkleIcon·BoltIcon·SavingsIcon·ShieldIcon·PinIcon·TargetIcon·SparkIcon·FlashIcon·CompassMiniIcon) 전부 여전히 정의만 있고 호출부 0건 — `grep -rn -w`로 `apps packages supabase scripts docs .claude` 전체 재확인. 미해결 확인.
- M-071: `neighborhood-menu.tsx`에 `aria-current`/`aria-selected` 여전히 0건(grep 확인). 미해결 확인.
- 둘 다 이미 충족된 상태가 아니어서 그대로 스펙 진행.

## 이슈별 결과

### M-060 — shipped (커밋 `d48ed17`)
"apps/web 아이콘 컴포넌트 미사용 export 11개 정리"
- 삭제 전 11개 심볼 전부를 word-boundary grep(`apps packages supabase scripts docs .claude`)으로 재확인 — 정의 위치 1건씩만 매치, 호출부 0건.
- `icons.tsx`의 `ArrowRightIcon`·`FireIcon`·`SparkleIcon`·`BoltIcon`·`SavingsIcon`·`ShieldIcon` 6개, `landing-icons.tsx`의 `PinIcon`·`TargetIcon`·`SparkIcon`·`FlashIcon`·`CompassMiniIcon` 5개 함수 전체 삭제(100줄 순삭제).
- 남은 아이콘(UserIcon·InfoIcon·TimerIcon 등, PinIcon 삭제 후 남은 SearchMiniIcon 등)은 손대지 않음.
- done_when 4개 전부 충족: 삭제 ✅, 재확인 grep ✅, `pnpm typecheck` 클린 ✅(아래 검증).

### M-071 — shipped (커밋 `9cef49f`)
"웹 동네선택 다이얼로그의 현재 선택 상태가 스크린리더에 노출 안 됨"
- `neighborhood-menu.tsx`의 `POPULAR_NEIGHBORHOODS` 렌더 루프에서 `currentDong === n.dongName`(`active`)인 항목의 `<button>`에 `aria-current={active ? "true" : undefined}` 추가. M-013/M-014가 `bottom-nav`에 적용한 `aria-current="page"` 선례와 같은 속성 — 값만 "페이지"가 아니라 "true"로 뒀다(페이지 링크가 아니라 목록 내 현재 선택이라는 의미 차이를 반영).
- `neighborhood-menu.test.tsx`에 테스트 2건 추가: (1) `useAppStore.setState({ anchors: { home: { dongName: "망원동", ... } } })`로 앵커를 목록 중 하나와 일치시켰을 때 그 버튼만 `aria-current="true"`이고 다른 버튼엔 없음, (2) 앵커가 비어 있는 기본 상태에서 5개 항목 전부 `aria-current` 없음.
- 기존 4개 테스트(두 갈래 분기·검색 디바운스)는 무변경으로 계속 통과.
- done_when 3개 전부 충족: aria-current 추가 ✅, RTL 접근성 쿼리 테스트 ✅, `pnpm --filter web test` 통과 ✅(아래 검증).

되돌린 이슈: 없음.

## 아키텍처 검토

- **담당**: 둘 다 frontend-impl 단독(`apps/web/src/components/*` 순수 프론트, backend-impl 불필요 — supabase/DB 미변경). 오케스트레이터가 직접 구현(이슈 규모가 각각 파일 삭제/속성 1개 추가 수준으로 작아 별도 계획-구현 왕복 없이 진행).
- **경계 확인**: `apps/web/src/components/`는 이 리포의 실제 규칙(FSD 아님, 평면 구조 + kebab-case 파일명 접두사)에 맞는 위치. 새 디렉토리 계층을 만들지 않았다. 계층 위반 없음.
- **데이터 흐름**: M-060은 순수 삭제라 데이터 흐름 영향 없음. M-071은 기존 `useAppStore` 셀렉터(`s.anchors.home?.dongName`)를 그대로 재사용, 신규 상태·구독 추가 없음.
- **테스트 전략**: M-060은 기존 무테스트 dead export라 테스트 추가 대상이 없음(삭제 후 typecheck로 충분). M-071은 RTL `getByRole("button", { name })` + `toHaveAttribute("aria-current", ...)`로 스크린리더가 실제로 인식하는 속성을 직접 검증(시각적 클래스 대신 접근성 트리 관점).
- **문서 불일치 재확인 (사람 조치 아직 미반영)**: `.claude/agents/architect.md:9,18`이 여전히 "FSD 아키텍처"·"FSD 경계: entities/features/widgets/shared 계층 위반 없나"를 언급한다 — `.claude/rules/core/constitution.md`가 "FSD 디렉토리는 실재하지 않는다"고 명시한 것과 모순되는 상태가 08-24 발견 이후 그대로다. 오늘 밤도 constitution.md를 우선해 판단했고 실제 코드 영향은 없었지만, 다음 밤 architect 단계를 오도할 위험이 계속 남아 있다. **재차 권고: architect.md:9,18의 FSD 언급을 정리할 것.**

## 변경 내용

| 커밋 | 이슈 | 파일 |
|---|---|---|
| `d48ed17` | M-060 | `apps/web/src/components/icons.tsx`, `apps/web/src/components/landing-icons.tsx` |
| `9cef49f` | M-071 | `apps/web/src/components/neighborhood-menu.tsx`, `apps/web/src/components/neighborhood-menu.test.tsx` |

스코프 크립 없음(각 커밋이 계획된 파일만 건드림). 마이그레이션 없음. 신규 의존성 없음(`package.json` 무변경). 시크릿 리터럴 없음.

## 검증

오케스트레이터가 qa 단계로 직접 재실행(구현자 자체 보고를 신뢰하지 않고 독립 재확인) — 두 커밋 모두 반영된 상태에서 `bash scripts/gate.sh` fresh 실행:

```
── typecheck
   ✅ typecheck
── test
   ✅ test
── lint
   ✅ lint
── secrets
   ✅ secrets

GATE: PASS — push 허용
```

세부: `pnpm typecheck`(4패키지 전체) clean. `pnpm test` — `@motungi/core` 전량 통과·`@motungi/mobile` 19 files/132 tests 통과·`@motungi/web` 40 files/309 tests 통과(`neighborhood-menu.test.tsx` 4→6 tests, 신규 2건 포함). `pnpm lint`(web) 통과. `scripts/scan-secrets.sh`는 커밋 전엔 `HEAD` 기준이라 "(diff 없음)"으로 오탐 통과했었는데, 두 이슈를 커밋한 뒤 재실행하니 실제 diff(`d48ed17`+`9cef49f`)를 스캔해 정상적으로 "✅ secrets"로 통과 — 이 스캔은 **커밋 이후에 재실행해야 의미가 있다는 점을 다음 밤을 위해 기록**(작업 트리 diff가 아니라 `BASE...HEAD` 커밋 diff를 본다).

`neighborhood-menu.test.tsx` 단독 실행으로 신규 2건 개별 확인:
```
✓ NeighborhoodMenu > 현재 선택된 동네를 스크린리더에 aria-current로 노출한다 (M-071)
✓ NeighborhoodMenu > 집 앵커가 인기 동네 목록에 없으면 어떤 항목도 aria-current를 갖지 않는다
Test Files  1 passed (1)
     Tests  6 passed (6)
```

push 게이트 판정: **두 이슈 모두 PASS** → revert 없이 그대로 dev에 push.

## 리스크 & 후속

### ⚠️ M-052 — 13일 연속 사람 개입 대기 (오늘 세션 알림 발송)
2026-08-13부터 오늘(08-25)까지 **13일 연속** 동일 사유로 씨앗 단계에서 사전 제외됐다. 08-16·08-17이 이 반복 패턴을 성문화 필요 사안으로 지목했고, 08-21~08-24가 연달아 사람에게 3택1을 요청했다:
**(a)** 3개 구현 경로(react-native-maps 신규 네이티브 의존성 / NAVER 정적지도 백엔드 프록시 신설 / 키리스 서드파티 정적지도) 중 하나 승인 + 필요 키/설정 제공,
**(b)** M-052의 priority를 medium에서 내려 씨앗 재선정 빈도를 낮춤,
**(c)** `blocked-human` 같은 정식 status를 backlog 스키마에 도입해 씨앗 선정 로직에서 배제.

`grep -i map apps/mobile/package.json`·`apps/mobile` 전체 MapView/react-native-maps 참조·`.env.example`·`security-policy.md` 모두 오늘 밤도 재확인, 변경 없음. **backlog.yml에 13번째 소급 노트를 남기는 것만으로는 사람에게 닿지 않는다고 판단해, 이번엔 오케스트레이터가 세션 알림(push notification)을 직접 발송한다.** 13일은 "밤이 알아서 처리하지 못하는 블로커"라는 신호로는 충분히 길다.

### 문서 불일치 (2일 연속 미반영)
- `.claude/agents/architect.md:9,18`의 FSD 언급이 `constitution.md`와 모순 — 08-24에 이어 오늘도 재확인, 변경 없음. 위 "아키텍처 검토" 참조.

### 그 외
- 남은 low priority todo 3건(M-066·M-067·M-070)은 이번 밤 스코프 밖. 다음 밤 씨앗 후보(동률이면 M-066이 최소 id).
- M-060·M-071의 `status`를 `todo → doing`으로 갱신(shipped 확정). M-052는 `todo` 유지(재시도 대상이므로 상태 불변, notes만 추가).
