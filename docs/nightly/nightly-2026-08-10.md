# Nightly Report — 2026-08-10

## TL;DR
클러스터 = **M-037**(활동 상세 "즐기는 방법(steps)" dead UI 제거) + **M-042**(`/explore`·`/report` loading.tsx 경계 추가) + **M-047**(모바일 동네 검색 IME 요청 폭주 gate + 실제 요청 취소). 셋 다 `bash scripts/gate.sh`(typecheck·test·lint·시크릿 스캔, 4패키지 전체)를 통과해 **셋 다 dev로 나갔다**. revert 없음.

heartbeat 체크: `ls docs/nightly/ | sort | tail -15` 실행 결과 최근 리포트는 …07-29/07-30/07-31/08-01~08-06/08-08/08-09이며 08-07 하루만 파일이 비어 있고(단발성 공백으로 추정, 원인 미조사) 그 외에는 연속 report-only 스트릭이 없다. 어젯밤(08-09)도 M-031/M-032/M-033 shipped였고 오늘 밤도 실코드 3건이 shipped됐으므로 ⚠️ 경고는 불필요.

## 집은 클러스터
- **0단계 백로그 게이트**: 밤 시작 시점 `status:todo` 8건(M-037·M-038·M-042·M-043·M-044·M-047·M-048·M-049) → todo≥2, **구현 모드**로 진행.
- **씨앗**: todo 중 priority가 존재하는 것은 전부 `medium`(high 없음) — 동순위이므로 id 오름차순 최소인 **M-037**을 씨앗으로.
- **파일 겹침 판정(CLUSTER RULE 2단계)**: planner가 M-037의 실제 타깃 파일(`packages/core/src/catalog.ts`, `apps/mobile/app/opportunity.tsx`)을 나머지 7개 todo 이슈의 실제 타깃 파일과 대조 — 진짜 겹침 0건.
- **앱 경계 폴백(3단계)**: M-037의 선언된 `scope`가 `[packages/core, apps/web, apps/mobile]`로 굵어(단일 파일·좁은 디렉토리가 아님) 폴백 발동. 같은 앱 경계 + 같은 priority(medium)를 공유하는 후보를 id 오름차순으로 나열하면 M-042·M-044·M-047·M-048·M-049. 기계적 규칙만 따랐다면 앞 2개인 M-042·M-044가 뽑혔을 것.
- **M-042는 그대로 채택** — planner+architect가 실제 코드로 재확인한 결과 여전히 필요하고 스코프도 적절한 작업으로 판단.
- **M-044는 의도적으로 제외하고 M-047로 대체**(architect도 독립적으로 동일 결론) — 이유:
  - M-044("LLM 계층 — 추천 근거 생성 + 자연어 의도 파싱")는 `.env.example`에 없는 신규 서버 시크릿 `GEMINI_API_KEY`가 필요한데, 시크릿 프로비저닝은 하드룰로 금지된 작업이라 오늘 밤 안에 조달 불가능.
  - `done_when`의 검증 기준 중 하나("근거 문자열 유니크 비율이 개선된다")는 실제 모델 출력 없이는 정직하게 검증할 수 없다 — mock으로 통과시키면 verification theater가 된다.
  - 이슈 제목은 두 기능(추천 근거 생성 + 자연어 의도 파싱)을 약속하지만 `done_when`은 한 기능만 커버 — 스펙 자체가 하룻밤 스코프로는 불완전.
  - → M-044는 `todo`로 유지 권고. 후속 planner가 (a) mock 가능한 fallback 서브이슈 vs (b) 사람이 게이트하는 실모델 검증 서브이슈로 쪼개거나, 시크릿이 실제 조달될 때까지 대기해야 한다.
- **M-048은 기계적 순위와 무관하게 애초에 제외** — 백로그 본문 자체가 "이 이슈는 조사·분류용이다 — 여기서 구현하지 말고 개별 이슈로 쪼갤 것"이라고 명시한 투자/triage 이슈라 구현 모드 클러스터에 들어갈 대상이 아님.
- **최종 클러스터: M-037 + M-042 + M-047**(3건, 상한 준수, 채우기용 패딩 없음 — 각 항목이 독립적으로 정당화됨).
- **백로그 문서 노후화 2건을 planner가 스펙 grounding 중 발견·정정**(트리거-저장소 문서 간 PRECEDENCE 모순은 아님, 단순 백로그 텍스트 노후화):
  - M-037 백로그 항목이 `apps/web/src/app/opportunity/page.tsx`를 dead code 위치로 지목했으나 실제로는 redirect shim일 뿐이고, 진짜 dead code는 모바일(`apps/mobile/app/opportunity.tsx`)에만 있었다.
  - M-042 백로그 항목이 loading/error/not-found 경계가 "0개"라고 주장했으나 실제로는 error.tsx·global-error.tsx·not-found.tsx는 이미 존재했고 진짜로 없던 것은 loading.tsx뿐이었다.
  - 사람이 백로그 텍스트 자체를 나중에 정리하면 좋을 항목 — 별도 M-NNN 이슈로 만들 필요까지는 없는 footnote.
- **M-047 스코프가 백로그 선언보다 넓어짐** — 백로그는 `apps/mobile/app/location.tsx` 단독 스코프를 선언했으나, 실제 fetch 레벨 abort 배관은 `apps/mobile/lib/geo.ts`에 있어 그쪽까지 손대야 했다. architect가 이는 scope creep이 아니라 필요한 확장이라고 확인 — location.tsx만 고쳤다면 증상만 가리는 결과가 됐을 것.
- **QA가 발견한 운영 갭(코드 결함 아님)**: `scripts/scan-secrets.sh`의 기본 base ref가 `origin/dev`인데 이 로컬 체크아웃은 shallow single-branch clone이라 `origin/dev` ref 자체가 없다 — 그 결과 자동 스캔 범위가 `HEAD~1` 하나로 조용히 축소됐다. QA가 이를 수동으로 메꿔 `27d1447...HEAD` 3커밋 전체 diff에 동일 정규식 패턴을 재실행(0건, 클린 확인). 다음 밤을 위한 툴링 갭으로 리스크 절에 플래그 — 이 자체로 백로그 이슈가 필요한지는 사람 판단.

## 이슈별 결과

### M-037 — 활동 상세 "즐기는 방법(steps)" dead UI 제거 (shipped, 커밋 b5a03be)
- **문제**: `packages/core/src/catalog.ts`의 옵셔널 `steps` 필드가 실데이터로 채워진 적이 없어, 상세 화면의 "즐기는 방법" 섹션이 항상 빈 채로 렌더되던 도달 불가능 코드.
- **구현**: 미사용 타입 필드 및 죽은 조건부 렌더 블록, 그에 딸린 스타일시트 키까지 함께 제거.
- **변경 파일**: `apps/mobile/app/opportunity.tsx`(-22), `packages/core/src/catalog.ts`(-2) — 2 files changed, 24 deletions(순수 삭제, 신규 로직 없음).

### M-042 — `/explore`·`/report` loading.tsx 경계 추가 (shipped, 커밋 20abf0c)
- **문제**: 두 라우트 모두 Next.js `loading.tsx`가 없어 클라이언트 페이지 컴포넌트가 마운트되기 전 빈 화면 플래시가 발생 — 라우트 전환과 hydration 사이 CLS 유발.
- **구현**:
  - `explore/loading.tsx` 신설 — 모바일 행 스켈레톤 + 데스크톱 카드 그리드, 기존 `isLoading` a11y 패턴(`aria-busy`/`aria-live`)과 일치시킴.
  - `report/page.tsx`에 있던 `ReportSkeleton`을 `components/report-skeleton.tsx`로 추출, `dongName`을 옵셔널(기본값 "우리 동네")로 만들어 `loading.tsx`가 앵커를 모르는 상태에서도 렌더 가능하게 함 — `report/page.tsx`는 계속 명시적 `dongName`을 전달.
  - `report/loading.tsx` 신설, 추출된 스켈레톤 재사용.
- **테스트**: `explore/loading.test.tsx`, `report/loading.test.tsx`, `report-skeleton.test.tsx` 신설.
- **변경 파일**: 7 files changed, +254/-79.

### M-047 — 모바일 동네 검색 IME 요청 폭주 gate + 취소 (shipped, 커밋 ebaeb27)
- **문제**: `app/location.tsx`가 디바운스는 했지만 실제 in-flight fetch를 취소하지 않았다 — `cancelled` boolean은 응답을 무시할 뿐이라 빠른 타이핑(또는 한글 IME 조합)마다 실제 네트워크 요청이 하나씩 나갔다. 또한 한 글자만 입력해도 쿼리가 나가 "검색 결과가 없어요" 드롭다운이 오해를 유발했다.
- **구현**:
  - `lib/geo.ts`: `searchNeighborhoods`가 옵셔널 `AbortSignal`을 받아 fetch에 전달(하위 호환 — signal 없이 호출하던 기존 코드는 영향 없음). `apps/web/src/lib/geo.ts`의 동일 배관을 미러링(단, 모바일에는 없는 `reportError` 텔레메트리는 제외).
  - `app/location.tsx`: 디바운스 effect와 `showDropdown`에 `MIN_QUERY_LEN=2` 게이트 추가, `cancelled` boolean 패턴을 실제 `AbortController`로 교체(effect cleanup에서 abort).
  - `app/location.test.tsx`: 기존 call-arg 검증 2건을 새 signal 인자에 맞춰 업데이트 + 빠른 연타 시 단일 호출 케이스, 최소 길이 미만 무호출 케이스 신규 추가.
- **변경 파일**: 3 files changed, +71/-11.

## 아키텍처 검토
- 담당: 3건 모두 **frontend-impl**(M-037·M-047은 apps/mobile, M-042는 apps/web). backend-impl 불필요(서버·DB 변경 없음, 신규 시크릿 없음).
- FSD 경계: 위반 없음 — 모퉁이는 FSD를 쓰지 않는다(constitution.md, 평면 구조). M-042의 `components/report-skeleton.tsx` 추출도 기존 `apps/web/src/components/` 평면 구조를 그대로 따름.
- 데이터 흐름: 신규 core 로직 없음. M-047은 기존 `searchNeighborhoods` 시그니처를 옵셔널 파라미터로만 확장해 하위 호환 유지.
- 스코프 판단: M-047의 `lib/geo.ts` 확장은 백로그 선언 스코프 밖이었지만 architect가 필요성을 확인(위 "집은 클러스터" 참조) — location.tsx 단독 수정은 증상만 가렸을 것.
- 테스트 전략: 전부 기존 컴포넌트 스모크 레벨 컨벤션(Vitest/RTL) 유지, 신규 테스트 파일 3개(explore/loading, report/loading, report-skeleton) + 기존 파일 확장 1개(location.test.tsx).
- 파일 겹침: 3개 커밋 간 실제 파일 겹침 없음(M-037: catalog.ts/opportunity.tsx, M-042: apps/web 전용, M-047: location.tsx/geo.ts) — 순차 커밋에 충돌 리스크 없었음.

## 변경 내용
- **M-037**: `apps/mobile/app/opportunity.tsx`, `packages/core/src/catalog.ts` — 2 files changed, +0/-24.
- **M-042**: `apps/web/src/app/explore/loading.tsx`(신규), `apps/web/src/app/explore/loading.test.tsx`(신규), `apps/web/src/app/report/loading.tsx`(신규), `apps/web/src/app/report/loading.test.tsx`(신규), `apps/web/src/app/report/page.tsx`(수정, 스켈레톤 추출), `apps/web/src/components/report-skeleton.tsx`(신규), `apps/web/src/components/report-skeleton.test.tsx`(신규) — 7 files changed, +254/-79.
- **M-047**: `apps/mobile/app/location.tsx`, `apps/mobile/app/location.test.tsx`, `apps/mobile/lib/geo.ts` — 3 files changed, +71/-11.
- `docs/backlog/backlog.yml`: M-037/M-042/M-047 `status: todo→doing`(이 리뷰어 커밋에서 반영, 다른 이슈는 손대지 않음).

## 검증
베이스는 `27d1447`(어젯밤 shipped 커밋). 3개 구현 커밋을 모두 쌓은 최종 트리에서 QA가 전체 게이트 실행:

```
bash scripts/gate.sh
── typecheck   ✅ (core/mobile/tokens/web 4패키지 전체)
── test        ✅ (576 tests, core/mobile/web 전체 통과)
── lint        ✅ (경고 0건)
── secrets     ✅ (패턴 없음)
GATE: PASS — push 허용
```

시크릿 스캔 보강: `scripts/scan-secrets.sh`의 기본 base ref `origin/dev`가 이 shallow 체크아웃에 없어 자동 스캔이 `HEAD~1` 한 커밋으로 조용히 축소됐던 것을 QA가 발견 — `27d1447...HEAD` 3커밋 전체 diff에 대해 동일 정규식 패턴을 수동으로 재실행해 0건(클린)을 재확인했다. 리뷰어도 3개 커밋 `git show --stat`를 육안 재확인 — 스코프·스타일·리스크 이상 없음, 놀랄 만한 변경 없음, revert 없음.

## 리스크 & 후속
- **M-044(LLM 계층)는 여전히 `todo`, 재도전 전 스펙 분리 필요**: `GEMINI_API_KEY` 미조달 + `done_when`이 라이브 모델 출력 없이는 검증 불가능한 기준을 포함 + 제목이 약속하는 두 기능 중 하나만 `done_when`이 커버. 다음 planner는 (a) mock 가능한 fallback 서브이슈와 (b) 사람이 게이트하는 실모델 검증 서브이슈로 쪼개거나, 시크릿이 실제 조달될 때까지 착수를 보류해야 한다.
- **M-048(앱↔웹 간극 정리)은 여전히 `todo`, 사람의 triage 필요**: 백로그 본문이 스스로 "조사·분류용, 여기서 구현하지 말고 개별 이슈로 쪼갤 것"이라 명시 — 구현 모드 클러스터 대상이 아니라 사람이 개별 M-NNN 이슈로 쪼개는 작업이 선행돼야 한다.
- **`scripts/scan-secrets.sh` 툴링 갭**: 기본 base ref `origin/dev`가 shallow single-branch clone에는 없어 자동 스캔 범위가 조용히 `HEAD~1`로 축소된다(에러 없이 축소 — 발견하기 쉽지 않음). 오늘 밤은 QA가 수동으로 메꿨지만, 스크립트 자체가 fetch 실패/ref 부재 시 명시적으로 경고하거나 로컬 브랜치 첫 커밋까지 스캔 범위를 넓히도록 고치는 게 근본 해결. 후속 이슈화 여부는 사람 판단에 맡김.
- **백로그 텍스트 노후화 2건**: M-037이 지목한 dead code 파일 위치(`apps/web/.../opportunity/page.tsx`)가 실제로는 redirect shim이었고, M-042가 "경계 0개"라 주장했지만 실제로는 loading.tsx만 없었다. 둘 다 트리거-저장소 문서 간 모순(PRECEDENCE 발동 대상)은 아니고 단순 백로그 본문 노후화 — planner가 스펙 grounding 중 정정해 구현에는 영향 없었다. 사람이 백로그 텍스트 자체를 다음에 정리하면 좋음.
- **M-047 스코프 확장 기록**: `apps/mobile/lib/geo.ts`까지 건드린 것은 scope creep이 아니라 필요한 확장이었음(위 "아키텍처 검토" 참조) — 백로그의 `scope` 필드가 실제 필요 범위보다 좁게 선언돼 있었다는 점은 다음에 M-047 관련 후속 작업을 스코핑할 때 참고.
- **트리거 vs repo 문서 모순**: 이번 밤 실행 중 발견한 모순 없음(PRECEDENCE 조항 발동 사례 없음).
