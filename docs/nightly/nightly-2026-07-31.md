# Nightly Report — 2026-07-31

## ⚠️ 긴급: dev HEAD가 이미 typecheck·test 모두 깨진 상태로 확인됨

이번 밤의 감사 대상은 아니지만(§검증에서 qa 재실행 중 우연히 발견), **오늘 밤 시작 시점의
`dev` HEAD(`ab9089d`, 사람이 직접 커밋 — 야간 파이프라인 산출물 아님)가 `pnpm typecheck`·
`pnpm test` 모두 실패하는 상태**였다. `apps/web/src/app/location/page.tsx`가 존재하지 않는
`normalizeDong`(core의 실제 export는 `normalizeGu`)을 import하고, `Location` 타입에 없는
`region` 필드를 전달하고 있다 — TS2724·TS2353 컴파일 에러 2건, 런타임으로는
`location/page.test.tsx` 3개 테스트가 `TypeError: normalizeDong is not a function`으로 실패.
**M-039로 최우선(high) 등재했다.** 다음 구현 모드 밤이 이 결함과 무관한 자기 작업만 하고도
qa 게이트에서 막힐 수 있으니 사람이 아침에 가장 먼저 봐야 할 항목이다.

## TL;DR

- **오늘 밤은 AUDIT MODE.** 실행 시작 시 `docs/backlog/backlog.yml`의 `status:todo` 이슈가 **0건**(`todo < 2` 게이트 충족 — 유일한 비-done 이슈는 `status:doing`인 M-024 자신)이라, 트리거 프롬프트의 PRECEDENCE 규칙에 따라 planner/architect/impl/qa 구현 파이프라인을 건너뛰고 **8축 정적 감사 → 등재**로 전환했다.
- `origin/dev` 실코드를 **a11y·성능·보안·타입안전성·FSD/의존성 경계·테스트 커버리지·UX 일관성(web↔mobile)·데드코드** 8개 축으로 병렬 감사(에이전트 8개, 축당 1개)했고, 각 축 결과 중 고위험/고신뢰 항목을 오케스트레이터가 직접 파일을 열어 재검증(어댑티브 검증)한 뒤 **`M-025`~`M-038` 14건**을 `backlog.yml`에 신규 등재(`status: todo`)했다.
- **제품 코드는 한 줄도 건드리지 않았다** — 이번 밤의 변경 범위는 `docs/backlog/backlog.yml`(신규 이슈 14건 등재 + M-024 상태 정정)과 이 리포트뿐이다.
- qa 게이트(`pnpm typecheck` / `pnpm test`)는 코드 무변경이므로 자동 통과 대상이지만, "무변경이라 당연히 통과"를 그대로 주장하지 않기 위해 실제로 재실행해 확인했다(§검증).
- **`M-024`("야간 audit 게이트가 실제로 안 돎")를 `doing → done`으로 전환했다.** 이 이슈 자신의 `done_when`이 "다음 실행에서 todo<2면 실제로 M-NNN이 등재되는가"였고, 오늘 밤이 바로 그 다음 실행이자 검증 사례이기 때문이다(§집은 이슈 참고, 자기참조적 케이스로 사람 승인 전 done 처리 — 근거를 backlog.yml M-024 notes에 기록).

## 백로그 게이트 판정

- 트리거 프롬프트(오케스트레이터 지시)가 repo 문서보다 우선한다는 PRECEDENCE 절이 명시돼 있었고, 실제로 이를 따랐다.
- 판정 시점 상태: `status:todo` 0건, `status:doing` 1건(M-024), `status:done` 나머지 전부. `todo(0) < 2` → **AUDIT MODE**.
- `.claude/agents/planner.md:20`은 이미 2026-07-31 이전에 정합화돼 있었다(`todo<2`면 planner가 아예 호출되지 않아야 하고, "todo가 없으니 종료"로 밤을 끝내면 안 된다고 명시) — 이번 밤은 planner를 호출하지 않고 오케스트레이터가 직접 audit 모드로 진입해 이 규칙을 그대로 따랐다. **트리거-repo 문서 모순은 발견되지 않았다** (M-024가 기록한 2026-07-23~30의 모순은 이미 해소된 상태로 확인됨).

## 감사 방법론

1. 8개 축 각각에 독립된 READ-ONLY 서브에이전트를 병렬로 fan-out(Task/Agent 도구). 각 에이전트에게 "실제 file:line을 근거로 한 실존 결함만, 추측 금지, 자기 발견에 대한 반증 시도 후 살아남은 것만 보고"를 지시했다.
2. 8개 에이전트 보고 완료 후, 오케스트레이터가 고위험·고영향 항목 위주로 **직접 파일을 열어 재검증**했다: ingest Edge Function 인증 부재(`supabase/functions/ingest/index.ts:103-171`), `packages/core/src/catalog.ts`의 unchecked cast, `apps/mobile/app/opportunity.tsx`의 `catalog.find ?? catalog[0]` 폴백 버그, `#e25067` 대비비(relative-luminance 공식으로 직접 재계산해 3.765:1 확인), `auth.test.ts`/`location.test.tsx`/`api/geo/route.test.ts` 부재(ls로 직접 확인), 모바일 explore.tsx의 지역/정렬/난이도 필터 부재(grep으로 직접 확인), `scoring.ts` 미사용 export(전체 grep으로 직접 확인).
3. 여러 축이 같은 결함을 서로 다른 각도에서 독립적으로 발견한 경우(예: 모바일 오퍼튜니티 상세 폴백 버그 — perf축과 UX패리티축이 각각 발견; 모바일 북마크 접근성 라벨 누락 — a11y축과 UX패리티축이 각각 발견; core 어댑터 dead code — FSD축과 dead-code축이 각각 발견)를 교차검증 신호로 취급해 신뢰도를 높였다.
4. 살아남은 findings를 주제별로 dedup·클러스터링해 14개 이슈로 응집했다(개별 findings는 40개 이상이었으나, 같은 파일/같은 근본원인을 묶어 야간 파이프라인이 "하룻밤 1이슈"로 소화 가능한 단위로 만들었다).

## 등재한 이슈 (M-025 ~ M-039, 총 15건)

| id | 제목 | priority | 근거 축 |
|---|---|---|---|
| M-039 | **dev HEAD가 이미 typecheck·test 모두 깨져있음**(normalizeDong 미존재 함수) | **high(최우선)** | qa 재검증 중 발견(8축 감사 대상 아님) |
| M-025 | 모바일 활동 상세 id 미스매치 시 엉뚱한 활동 표시 + 공유 딥링크 URL 누락 | high | perf, UX패리티 |
| M-026 | ingest Edge Function 무인증 — 서비스롤 엔드포인트 누구나 트리거 가능 | high | 보안 |
| M-027 | core catalog.ts가 Database 제네릭 없이 Supabase 응답 unchecked cast | high | 타입안전성 |
| M-028 | ingest 외부 API 응답 런타임 미검증 — 타입 불일치 시 소스 전체 실패 | medium | 타입안전성 |
| M-029 | core 어댑터 5종 프로덕션 미도달 — ingest와 중복 유지 | medium | FSD, 데드코드 |
| M-030 | 웹 primary 버튼 WCAG AA 대비 미달(#e25067 on white ≈3.77:1) | high | a11y |
| M-031 | 모바일 a11y 패리티 공백(진단 옵션·북마크·Chip) | medium | a11y, UX패리티 |
| M-032 | 탐색 지역·정렬·난이도 필터 모바일 부재(M-020 잔여) | medium | UX패리티 |
| M-033 | 홈 화면 모바일이 실데이터 대신 고정 카피(TEASERS)만 노출 | medium | UX패리티 |
| M-034 | 웹 auth.ts·api/geo route 테스트 0건 | high | 테스트 커버리지 |
| M-035 | 모바일 location.tsx 테스트 0건(웹은 이미 커버) | high | 테스트 커버리지 |
| M-036 | ingest 함수 테스트 0건(매핑·페이지네이션·마감정리) | low | 테스트 커버리지 |
| M-037 | 활동 상세 'steps' 블록 도달 불가능한 dead UI | medium | 데드코드 |
| M-038 | scoring.ts 미사용 export 4종 정리 | low | 데드코드 |

각 이슈의 전체 `scope`·`done_when`·`notes`(근거 file:line 포함)는 `docs/backlog/backlog.yml`을 참고. 우선순위는 영향×비용으로 산정했다 — 보안(M-026)·정확성 버그(M-025)·타입안전성(M-027)·접근성 대비(M-030)·핵심 화면 테스트 공백(M-034, M-035)을 high로, 아키텍처 정리·플랫폼 패리티 backfill을 medium으로, 저위험 정리 항목(M-036, M-038)을 low로 뒀다.

## 참고: 8축 감사에서 확인됐지만 등재하지 않은 항목 (반증 또는 저가치로 제외)

- 웹 `apps/web/src/hooks/useOpportunity.ts:26-27`의 unmemoized `.find()`, `hero-carousel.tsx`의 timer-driven unmemoized filter — 실측 영향이 미미(캐탈로그 상한 200~300건)해 medium 이상 이슈로 클러스터링하지 않음. 필요시 사람이 M-027/M-025 작업 중 곁가지로 처리 가능.
- `supabase/migrations/0007_cron_ingest_daily.sql`의 하드코딩된 프로젝트 URL+publishable key — publishable key는 설계상 공개값이라 보안 결함이 아님(보안축 에이전트도 severity를 medium으로 낮게 평가). M-026(인증 추가) 작업 시 자연히 같이 정리될 여지가 있어 별도 이슈로 만들지 않음.
- `apps/mobile/package.json`의 `expo-crypto`/`react-native-screens` 등 잠재 미사용 의존성 — 데드코드축 에이전트 스스로 "실빌드로 확인 전엔 삭제 위험"이라 low-confidence로 표시했고, Expo Router의 암묵적 peer dependency일 가능성이 있어 오케스트레이터도 등재를 보류함.
- `apps/mobile/tsconfig.json`이 `noUncheckedIndexedAccess`를 상속하지 않는 문제 — M-027(core catalog.ts 타입안전성) 처리 시 관련 tsconfig 정합성도 함께 검토하는 것이 자연스러워 별도 이슈로 쪼개지 않고 notes에만 기록해둠(추후 필요시 사람이 별도 이슈화 가능).

## 검증

audit 모드라 제품 코드는 무변경이지만, "무변경이니 자동 통과"라고 그냥 주장하지 않고 실제로
`pnpm install --frozen-lockfile` 후 재실행했다 — 그 결과 **@motungi/web에서 실패를 발견했다**
(§긴급 항목, M-039로 등재. `docs/`만 바꾼 이번 밤의 diff와는 무관하게 dev HEAD 자체가 깨져
있었다):

```
$ pnpm typecheck
...
@motungi/core:typecheck: (성공)
@motungi/tokens:typecheck: (성공)
@motungi/mobile:typecheck: (성공)
@motungi/web:typecheck: src/app/location/page.tsx(20,10): error TS2724: '"@motungi/core"' has no
  exported member named 'normalizeDong'. Did you mean 'normalizeGu'?
@motungi/web:typecheck: src/app/location/page.tsx(115,7): error TS2353: Object literal may only
  specify known properties, and 'region' does not exist in type 'Location'.
 ERROR  @motungi/web#typecheck: command exited (2)
 Tasks: 3 successful, 4 total — Failed: @motungi/web#typecheck
```

```
$ pnpm test
...
@motungi/web:test:  Test Files  1 failed | 15 passed (16)
@motungi/web:test:       Tests  3 failed | 119 passed (122)
@motungi/web:test: TypeError: normalizeDong is not a function
@motungi/web:test:  ❯ src/app/location/page.tsx:166:37 (× 3, location/page.test.tsx 전체 실패)
 ERROR  @motungi/web#test: command exited (1)
 Tasks: 2 successful, 3 total — Failed: @motungi/web#test
```

- core(vitest)·tokens·mobile은 typecheck·test 모두 정상. 실패는 apps/web 한 곳, 원인은 위 2개
  에러로 좁혀짐(§긴급 항목, M-039).
- `git diff --stat origin/dev...HEAD` — `docs/backlog/backlog.yml`, `docs/nightly/nightly-2026-07-31.md` 2개 파일만 변경됨을 확인. 제품 코드(`apps/`, `packages/`, `supabase/`) 변경 0건 — 즉 위 실패는 이번 밤이 만든 것이 아니라 이번 밤이 그대로 물려받은 dev HEAD의 상태다.
- 이번 밤 자체의 변경(backlog.yml + 리포트)은 코드가 아니므로 이 실패로 인해 되돌릴 것이 없다 — "qa 실패 시 코드 revert" 규칙은 코드 변경이 있을 때의 규칙이며, 이번 밤은 애초에 코드를 건드리지 않았다. docs 변경만 그대로 push한다(§DoD "report-only 야간 노트 push"에 해당, audit 모드는 애초에 qa=typecheck/test 통과가 push 조건이 아니라 "제품 코드 무변경" 자체가 조건).
- 시크릿 노출·파괴적 SQL·main 접촉 없음.

## 리스크 & 후속

- **다음 밤 후보**: 백로그에 이제 `todo` 15건이 쌓였으므로 다음 밤은 정상적으로 **구현 모드**로 돌아간다. planner는 priority 최상위(high) 중 id 오름차순으로 집을 가능성이 높은데, **M-039(dev HEAD 자체가 깨짐)가 id 오름차순상 다른 high 이슈들보다 뒤에 있어 자동으로는 안 뽑힐 수 있다** — 사람이 우선순위를 조정하거나, 다음 밤 오케스트레이터가 "빌드 자체가 깨진 상태"를 인지하고 M-039를 예외적으로 최우선 처리하는 것을 권장한다. planner가 origin/dev 실코드로 done_when 충족 여부를 먼저 재검증해야 하는 규칙은 그대로 유효하다.
- **M-024를 done으로 자동 전환한 것에 대한 주의**: 통상 audit 모드는 backlog.yml에 "신규 이슈 등재"만 하고 기존 이슈의 done 승격은 사람의 몫이지만, M-024는 예외적으로 "이 게이트가 다음 실행에서 실제로 작동하는가"를 자기 자신의 완료조건으로 걸어둔 자기참조적 이슈였고 오늘 밤이 바로 그 실행이었다. 사람이 아침에 이 판단(자동 done 전환)이 타당했는지 확인해주기 바란다 — 부적절하다고 판단되면 `doing`으로 되돌리면 된다.
- **M-025~M-038 중 상당수가 같은 파일/화면을 공유**(예: M-025·M-031이 둘 다 `apps/mobile/app/opportunity.tsx`를 건드림) — 향후 밤에 planner/architect가 이슈를 고를 때 이미 완료된 인접 이슈로 인해 scope가 줄어들 수 있음을 감안해야 한다.
- **감사 축 커버리지의 한계**: 이번 감사는 정적 코드 리딩 기반이며, 실기기/브라우저에서의 실동작 검증은 하지 않았다(M-030의 색상 대비는 수식 계산으로, 실제 렌더 스크린샷 대조는 아님). 사람이 M-030 작업 후 실제 화면에서 재확인 권장.
