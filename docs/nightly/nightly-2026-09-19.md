# Nightly Report — 2026-09-19

## TL;DR

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `status: todo` 12건(`grep -cE "^    status: todo" docs/backlog/backlog.yml` = 12 — 참고: 느슨한 `grep -c "status: todo"`는 notes 본문 속 문구까지 걸려 15로 잘못 셈됐다) → `todo ≥ 2`이므로 구현 모드.
- **연속 report-only 경보 해당 없음** — 직전 두 밤(09-15: M-101/M-112 shipped, 09-17: M-107 shipped) 모두 코드가 나갔다.
- **세션이 빈 컨테이너로 시작** — `mcp__Claude_Code_Remote__list_repos`로 `dlsxody1/motungi`를 찾아 clone 후 `git fetch origin dev` + `git checkout -B dev origin/dev`로 시작했다(09-17과 동일 상황).
- **`main`이 `dev`보다 앞서 있지 않음을 확인**(`git log origin/dev..origin/main` 비어 있음) — 되받기 절차 불필요.
- **클러스터 = M-109 단독(1건).** 아래 "집은 클러스터" 참조.
- **M-109 shipped (커밋 `fb663f6`).** `packages/core/src/store.ts`의 catalog 슬라이스 주석을 "mobile 전용"으로 명시. 코드 동작 변경 없음(문서 전용 diff).
- **되돌린 이슈 없음.** `bash scripts/gate.sh` 전체 PASS, 최종 dev 트리 green.
- **문서 불일치 재확인(6번째 지적)** — 아래 "레포 문서와의 불일치" 참조. 이번 밤은 영향 없었지만(폴백 조건 자체가 성립 안 함) 사람이 정정 필요.

## 집은 클러스터

### 씨앗 선정
- `status: todo` 12건 중 priority 분포: medium 6건(M-097·M-109·M-111·M-113·M-114·M-115),
  low 6건(M-088·M-092·M-093·M-102·M-103·M-110), high 0건.
- priority 최상위는 medium. medium 중 id 최소는 **M-097**이지만 notes에 `blocked-human`으로
  명시돼 있어(.env 없음·data.go.kr/서울 열린데이터광장 인증키 없음·outbound network 프록시
  차단 — 09-07·09-09·09-11·09-15·09-17 밤이 반복 재검증) 씨앗에서 제외했다. 오늘 밤도
  같은 사실을 다시 확인했다: `.env`는 여전히 `.env.example`만 존재하고 모든 값이 공란,
  `curl https://apis.data.go.kr/` → `connect_rejected`(조직 정책). 변화 없음 → 계속 제외.
- 다음 최소 id는 **M-109** → 씨앗. scope: `packages/core/src/store.ts`(구체 파일 1개).

### scope 겹침 검토 (M-109 scope: `packages/core/src/store.ts`)

| id | scope | 겹침 판정 |
|---|---|---|
| M-088 | `apps/mobile/app/(tabs)/report.tsx` | 겹침 없음 |
| M-092 | `packages/tokens/src/index.ts` | 겹침 없음(다른 패키지) |
| M-093 | `packages/core/src/adapters/seoul-jobs.ts`, `.../util.ts` | 겹침 없음(같은 패키지지만 `adapters/` 하위 다른 파일 — store.ts와 파일도 좁은 디렉토리도 공유 안 함) |
| M-097 | `supabase/migrations`, `supabase/functions/ingest/adapters.ts`, `packages/core/src/catalog.ts` | 겹침 없음(blocked-human으로 씨앗 후보에서도 제외됨) |
| M-102 | `apps/web/src/app/diagnosis/page.tsx`, `.../location/page.tsx` | 겹침 없음 |
| M-103 | `apps/web/.eslintrc.json` | 겹침 없음 |
| M-110 | `packages/core/src/view.ts` | 겹침 없음(다른 파일) |
| M-111 | `apps/web/src/app/explore/[gu]/page.tsx`, `.../lib/seo.ts` | 겹침 없음 |
| M-113 | `apps/web/src/lib/geo.ts`, `apps/mobile/lib/geo.ts`, `packages/core/src` | 겹침 없음(`packages/core/src`는 패키지 전체라 좁은 디렉토리 아님 — 09-17 리포트와 동일 판단) |
| M-114 | `packages/core/src/index.ts`, `.../gu-summary.ts` | 겹침 없음(다른 파일) |
| M-115 | `apps/mobile/ui/theme.ts`, `packages/tokens/src/index.ts` | 겹침 없음 |

파일/좁은 디렉토리 겹침 후보 0건.

**폴백 미적용**: 이 프롬프트(트리거)의 폴백 발동 조건은 "씨앗 scope에 파일도 좁은
디렉토리도 전혀 없을 때만"이다. M-109의 scope는 이미 구체 파일 1개(`packages/core/src/store.ts`)를
담고 있으므로 이 조건 자체가 성립하지 않는다 — 폴백을 쓰지 않았다.

### 레포 문서와의 불일치 (6번째 지적)
`.claude/rules/workflow/nightly-pipeline.md:58`와 `docs/nightly/PIPELINE.md`의 폴백 조건은
"파일 겹침 후보가 0개면"으로 트리거 프롬프트보다 더 넓게 쓰여 있다 — 이 문서 그대로라면
오늘 밤도 폴백 후보(같은 medium priority인 M-111/M-113/M-114/M-115 중 id 오름차순 2개,
즉 M-111·M-113)를 클러스터에 더했을 것이다. 09-09·09-11·09-15·09-17 리포트가 이미
네 번 지적한 것과 동일한 지점이며, 이번 밤도 PRECEDENCE 절에 따라 트리거 프롬프트의
좁은 조건("씨앗 scope에 파일도 좁은 디렉토리도 전혀 없을 때만")을 우선했다. 오늘 밤은
M-109의 scope가 이미 구체 파일을 담고 있어 두 조건 모두 "폴백 미적용"으로 결론은 같았지만,
씨앗의 scope가 파일 없이 앱/패키지 단위로만 적힌 밤이 오면 두 문서가 서로 다른 결과를
낼 것이다. **여섯 번째 지적: 사람이 트리거 프롬프트 또는 두 repo 문서 중 한쪽을 정정해야
한다.**

**결과: 클러스터 = M-109 단독. 1건.** 상한 3개 미달이지만 겹치는 이슈가 없고 폴백도
발동하지 않아 억지로 채우지 않았다 — 정상 결과다.

## 이슈별 결과

| 이슈 | 결과 |
|---|---|
| M-109 | shipped (커밋 `fb663f6`) |

되돌린 이슈 없음. 미룬 이슈 없음(상한 3개 미달).

## 아키텍처 검토

- **경계 배치**: `packages/core`(순수 도메인 로직) 전용 변경 → **backend-impl** 담당.
  `apps/web`·`apps/mobile` 코드는 건드리지 않는다(둘 다 `AppState<TOpportunity,
  TCatalogStatus>` 제네릭 인터페이스를 그대로 쓰고, 구현체 필드 자체는 변경이 없어
  하위 앱 타입에 영향 없음 — JSDoc 주석만 바뀜).
- **FSD 경계**: `.claude/agents/architect.md:9,18`은 "FSD 아키텍처"·"entities/features/
  widgets/shared 계층 위반" 검사를 요구하지만, `.claude/rules/core/constitution.md`의
  불변 규칙은 "**FSD(entities/features/widgets/shared) 디렉토리는 실재하지 않는다**
  (옛 룰의 잔재였다). 새 계층 디렉토리를 임의로 만들지 마라"라고 명시한다. 이 프롬프트의
  PRECEDENCE 절에 따라 repo 문서보다 트리거 프롬프트를 우선하되, 트리거 프롬프트 자체도
  "FSD/의존성 경계"를 감사 축 이름으로만 쓰고 구현 모드 규칙에는 FSD를 요구하지 않으므로,
  이 밤은 실제 파일구조 규율(기술 종류별 평면 배치·파일명 접두사)로 판단했다. 오늘 변경은
  디렉토리 구조에 아무 영향이 없다(기존 파일 내 주석 수정뿐). **불일치를 기록**:
  `architect.md:9`("FSD 아키텍처"), `architect.md:18`("FSD 경계: entities/features/
  widgets/shared 계층 위반 없나")가 `constitution.md:16`("FSD 디렉토리는 실재하지
  않는다")와 정면으로 어긋난다 — 사람이 `architect.md`를 고쳐야 한다.
- **데이터 흐름**: 타입 시그니처 변경 없음(주석만). core → web/mobile 전파 영향 없음.
  DB 마이그레이션 불필요.
- **테스트 전략**: 순수 문서(JSDoc) 변경이라 신규 단위 테스트는 불필요 — 기존
  `packages/core` 테스트 스위트(429개)가 회귀 없이 통과하는지로 충분하다고 판단.
  `pnpm --filter @motungi/core test`로 확인.

## 변경 내용

- `packages/core/src/store.ts` — `AppState.catalog`·`AppState.catalogStatus` 필드의
  JSDoc 주석에 "이 슬라이스는 mobile 전용이다" 문구와 M-108(2026-09-11, TanStack
  비도입 결정)·M-109 근거를 명시. `setCatalog`는 2026-08-24(`c207fb0`)에 이미
  "mobile 전용" 문구가 있어 변경하지 않았다. **동작 변경 없음** — 필드 타입·시그니처·
  런타임 로직은 그대로다.

## 검증 (실제 출력)

```
$ pnpm --filter @motungi/core typecheck
> @motungi/core@0.1.0 typecheck /home/user/motungi/packages/core
> tsc --noEmit
(exit 0, 에러 없음)

$ pnpm --filter @motungi/core test
 Test Files  16 passed (16)
      Tests  429 passed (429)

$ bash scripts/gate.sh
── typecheck   ✅
── test        ✅ (core 429 / mobile 184 / web 395)
── lint        ✅ (신규 경고 없음 — apps/web의 기존 max-lines/max-lines-per-function/
                  complexity 경고만 잔존, 이번 diff와 무관한 파일들)
── secrets     ✅ (diff 없음)
── pure-tests  ✅ (누락 없음, 면제 6건 그대로)
GATE: PASS — push 허용
```

done_when 3개 판정:
1. ✅ "M-108 결정에 종속" — M-108(2026-09-11) 결정이 이미 내려져 있음을 확인·인용.
2. ✅ "가지 않기로 하면 core 스토어 주석에 '이 슬라이스는 mobile 전용'이라고 명시" —
   M-108이 비도입으로 결정됐으므로 이 분기 적용, catalog·catalogStatus 주석에 명시함.
3. ✅ "pnpm typecheck·test 통과(web·mobile 양쪽)" — gate.sh 전체 PASS로 web(395)·
   mobile(184) 테스트 포함 확인.

diff 오염 없음(`git diff --stat` = `store.ts` 1개 파일만). 시크릿 노출 없음. 파괴적
마이그레이션 없음(DB 변경 자체가 없음).

## 리스크 & 후속

- **낮은 리스크.** 이번 변경은 JSDoc 주석뿐이라 런타임 회귀 가능성이 없다. 사람 리뷰는
  문구의 정확성(M-108 판단 근거 요약이 실제 결정과 일치하는지)만 확인하면 된다.
- **M-097(동 단위 좌표 백필)이 여전히 blocked-human이다.** 09-07부터 다섯 밤 연속 재검증
  결과가 같다 — `.env` 값·data.go.kr 인증키·지오코딩 데이터셋 중 하나를 사람이 제공해야
  다음 밤이 이걸 씨앗으로 잡을 수 있다.
- **문서 정정 필요 (누적, 사람 개입 요청)**:
  1. `.claude/rules/workflow/nightly-pipeline.md:58` / `docs/nightly/PIPELINE.md`의
     클러스터 폴백 조건("파일 겹침 후보가 0개면")을 트리거 프롬프트의 조건("씨앗 scope에
     파일도 좁은 디렉토리도 전혀 없을 때만")과 일치시킬 것 — 09-09부터 이번까지 6회 지적.
  2. `.claude/agents/architect.md:9,18`의 FSD 계층 검사 문구를 삭제하거나
     `constitution.md`가 명시한 실제 파일구조 규율(평면 배치·파일명 접두사)에 맞게
     재작성할 것 — 이번 밤 첫 지적.
- **backlog.yml**: M-109 → `doing`. 사람 최종 리뷰 후 `done` 승격.

## 커밋

- `fb663f6` — `docs(core): catalog 슬라이스가 mobile 전용임을 스토어 주석에 명시 (M-109)`
