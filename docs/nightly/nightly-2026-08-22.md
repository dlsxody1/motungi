# Nightly Report — 2026-08-22

## TL;DR

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `docs/backlog/backlog.yml`의 `status: todo` 개수는 **12건**(≥2) — 정상 구현 모드로 진행했다.
- **클러스터: M-061 + M-062 + M-063** (셋 다 shipped, 되돌린 것 없음). 씨앗 M-061의 scope가 파일 1개(`apps/mobile/tsconfig.json`)뿐이라 파일 겹침 후보 0개 → **앱 단위(apps/mobile) + 동일 priority(medium) 폴백**으로 M-062·M-063을 id 오름차순 2개까지 합류시켰다.
- qa 게이트(`bash scripts/gate.sh`) **PASS** — typecheck 4/4(추가로 mobile은 turbo 캐시를 우회한 순정 `npx tsc --noEmit`로 독립 재검증), test(core 298/298 · web 290/290 · mobile 131/131) 전체 통과, lint 통과, 시크릿 없음. 독립 qa 서브에이전트가 diff·file:line 근거로 4개 판정 항목 모두 재확인.
- **M-052가 오늘로 10일째(08-13~08-22) 연속 사전 제외됐다.** 08-21 노트가 이미 사람에게 3택1(구현 경로 승인 / priority 하향 / `blocked-human` 상태 도입)을 요청했으나 아직 반영되지 않았다 — 이번 밤도 동일 사유로 재확인 후 제외했다. 이 노트를 계기로 사람에게 직접 알림을 보낸다(당직 지침).
- **정직하게 기록**: M-063은 backlog 원문 `done_when`의 절반만 충족했다(리마운트 시 재요청 버그는 고침, 저밀도 앵커의 콜드 사다리 캐싱은 미해결·`data/opportunities.ts` 미변경). 아래 §이슈별 결과에 상세.
- **연속 report-only 경보 해당 없음**: 직전 세 밤(08-19·08-20·08-21) 전부 shipped 구현 밤이었다(`ls docs/nightly/`로 직접 확인). 헛도는 밤 패턴이 아니다.

## 집은 클러스터

씨앗 선정: `status:todo` 12건 중 priority 최상위는 medium(high 없음). medium 중 id 최하위는 M-052(0단계 사전 제외 대상 — 아래 §M-052 참조)이므로, 그다음 id인 **M-061**을 씨앗으로 삼았다.

- **파일 겹침 시도**: M-061의 scope는 `apps/mobile/tsconfig.json` 단일 파일이라, 다른 어떤 todo와도 같은 파일·좁은 디렉토리를 공유하지 않는다 → 겹침 후보 0개.
- **앱 단위 폴백 적용**: 겹침 0이므로 규칙대로 "같은 앱 경계(`apps/mobile`) + 같은 priority(medium)"인 todo를 id 오름차순으로 2개까지 추가했다. 후보는 M-052(제외 대상이라 건너뜀) → **M-062**(medium, apps/mobile) → **M-063**(medium, apps/mobile). 다음 후보였을 M-072(medium, `apps/mobile/hooks/useEnsureCatalog.ts` 포함하지만 web·core에도 걸침)는 **상한 3개**로 인해 이번 밤은 미룬다.
- **최종 클러스터**: M-061(씨앗) + M-062 + M-063, 셋 다 `apps/mobile` 안에서 서로 다른 파일을 건드려 이슈당 1커밋으로 독립 revert 가능함을 확인 후 진행.

## 이슈별 결과

| id | 결과 | 요약 |
|---|---|---|
| M-061 | ✅ shipped | `apps/mobile/tsconfig.json`에 `noUncheckedIndexedAccess:true` 직접 추가(extends 체인 불변경). 신규 타입 에러 2건(테스트 파일 mock 인덱싱)을 기존 관용구(`!`)로 수정. done_when 3개 전부 충족. |
| M-062 | ✅ shipped | `apps/mobile/app/opportunity.tsx`에 `o.summary` 렌더 + `isWeekendOuting(o)` "주말 나들이" 배지 추가(웹 opportunity-detail.tsx와 동등). 신규 테스트 5건. done_when 4개 전부 충족. |
| M-063 | ⚠️ shipped, **부분 완료** | `useEnsureCatalog.ts`의 리마운트 재요청 버그(모듈 스코프 승격)만 고침. 저밀도 앵커의 콜드 사다리 캐싱(원문 done_when의 나머지 절반, `data/opportunities.ts` 포함 스코프)은 미해결 — backlog.yml에 정직하게 기록. |

되돌린 이슈는 없다 — qa 게이트가 3건 전부 PASS.

## 아키텍처 검토

- **경계 배치**: 세 이슈 모두 `apps/mobile` 프론트엔드 영역이라 frontend-impl 단독 담당으로 확정. backend-impl/DB 마이그레이션 불필요.
- **FSD 경계**: constitution.md대로 이 리포는 FSD 디렉토리(entities/features/widgets)를 쓰지 않는 평면 구조다 — 위반 없음. 세 이슈 모두 기존 파일 내 수정으로 새 디렉토리 계층을 만들지 않았다.
- **범위 확정**: architect 관점에서 재검증한 실제 파일 — `apps/mobile/tsconfig.json`, `apps/mobile/app/opportunity.tsx`(+test), `apps/mobile/lib/auth.test.ts`(M-061의 부수 타입에러 수정), `apps/mobile/hooks/useEnsureCatalog.ts`(+test). 모두 architect가 사전에 grep/read로 실존을 확인한 경로.
- **M-063 스코프 판단**: 원문 done_when이 요구하는 "웹처럼 `/api/opportunities` 경유 또는 앱 자체 요청 캐시"의 완전한 해법(모바일 전용 백엔드 프록시 신설 또는 콜드 사다리 자체의 캐싱)은 하룻밤 스코프를 넘어선다고 판단해, 실제 확인된 버그(리마운트 시 캐시 무시)만 좁혀 처리했다 — planner 단계의 의도적 축소이며 qa가 이 축소를 그대로 판정 기준으로 받아 PASS시켰다. 사람이 원문 그대로의 완전한 done_when을 요구한다면 이 이슈는 `todo`로 되돌려야 한다는 점을 명시한다(이번 밤은 `doing`으로 표기하고 상세를 backlog notes에 남김).
- **테스트 전략**: 셋 다 unit/component 레벨(Vitest+RTL, mobile 컨벤션)로 충분히 커버됨. e2e 불필요(순수 UI/훅 변경).

## 변경 내용

- `apps/mobile/tsconfig.json:9` — `noUncheckedIndexedAccess: true` 추가.
- `apps/mobile/app/opportunity.test.tsx:279`, `apps/mobile/lib/auth.test.ts:367` — `noUncheckedIndexedAccess` 활성화로 드러난 `T|undefined` 인덱싱 2곳을 `!`로 수정.
- `apps/mobile/app/opportunity.tsx:12,100-104,111,217-220` — `isWeekendOuting` import, "주말 나들이" 배지(`pillComfortable` 톤 재사용), `o.summary` 렌더, 관련 스타일 추가.
- `apps/mobile/app/opportunity.test.tsx:311-368` — summary 있음/없음, 배지(trail/durationMin/일반) 케이스 5건 추가.
- `apps/mobile/hooks/useEnsureCatalog.ts:22` — `useRef` 제거, 모듈 스코프 `let lastFetchedKey`로 승격(리마운트 시 캐시 유지). 주석으로 이유 명시.
- `apps/mobile/hooks/useEnsureCatalog.test.ts:153-178` — 리마운트 시 재요청 안 함을 입증하는 회귀 테스트 신규(구 코드 대비 실패 확인 후 신 코드로 통과 확인).

커밋 3개(dev 직접, PR 없음):
1. `dcadfdc` — `fix(mobile): enable noUncheckedIndexedAccess to match other 3 workspaces (M-061)`
2. `9f8a0d5` — `feat(mobile): add activity summary + weekend-outing badge to detail screen (M-062)`
3. `86f6051` — `fix(mobile): make catalog session-cache survive screen remounts (M-063)`

## 검증

**frontend-impl 자체 검증** — `pnpm typecheck`(4/4 성공) · `pnpm test`(core 298/298 · web 290/290 · mobile 131/131).

**qa 독립 재검증** — 별도 서브에이전트가 구현자의 보고를 신뢰하지 않고 직접 실행:
- `git diff origin/dev..HEAD --stat` — 정확히 예상된 6개 파일만 `apps/mobile/` 아래에서 변경됨 확인. `.env`/시크릿 패턴 grep 결과 없음. SQL 무변경.
- `npx tsc --noEmit`(turbo 캐시 우회, mobile 워크스페이스 직접) — exit 0.
- `bash scripts/gate.sh` 전체 실행:
  ```
  ── typecheck   ✅ (4/4 패키지)
  ── test        ✅ core 298/298 · web 290/290 · mobile 131/131 (19 파일)
  ── lint        ✅
  ── secrets     ✅ (시크릿 패턴 없음)
  GATE: PASS — push 허용
  ```
- 4개 acceptance criteria를 이슈별로 file:line 근거로 개별 판정(✅/✅/✅, M-063은 원문 대비 부분 충족을 명시) — 위 §이슈별 결과·§아키텍처 검토 참조.
- 3개 커밋의 독립 revert 가능성도 확인: 서로 겹치는 diff hunk 없음(파일은 opportunity.test.tsx가 M-061·M-062 양쪽에서 수정되지만 far-apart hunk라 개별 revert 안전), 기능적 결합 없음.

revert 대상 없음 — 최종 트리는 3개 커밋 그대로 green.

## 리스크 & 후속

- **M-052, 오늘로 10일째(08-13~08-22) 블록 — 사람 개입 필요, 이번 밤 직접 알림 발송.** 08-13부터 매일 동일한 3개 구현 경로(react-native-maps 신규 네이티브 의존성 / NAVER 정적지도 백엔드 프록시 신설 / 키리스 서드파티 정적지도)가 전부 무인 야간 파이프라인의 권한 밖 결정을 요구해 사전 제외돼 왔다. 08-21 노트가 제안한 3택1이 아직 처리되지 않았다.
- **M-063 부분 완료**: 리마운트 재요청 버그는 고쳤으나 저밀도 앵커의 콜드 사다리 캐싱(원문 done_when 나머지 절반, `apps/mobile/data/opportunities.ts` 포함)은 미해결. 사람이 (a) 이대로 `done` 승격 + 별도 이슈 신규 등재, (b) `doing`에 두고 다음 밤이 좁은 스코프로 재작업, 둘 중 하나를 결정해야 한다.
- **다음 밤 씨앗 후보**: `todo`는 이제 9건(M-052 제외 시 8건 사실상 가용). M-052를 계속 빼면 다음 medium 최상위는 M-068(`apps/web/src/lib/kakao.ts`)이며, id 오름차순으로 M-069(`apps/web/src/lib/geo.ts`)·M-072(`apps/web` 포함)와 앱 단위(apps/web) 폴백 겹침 가능성이 있다 — 다음 밤 planner가 처음부터 재스캔할 것.
- 모바일 lint 스크립트 부재는 기존 known gap — 이번 밤 새로 발견한 것 아님.
- backlog `status: doing`으로 갱신된 M-061·M-062·M-063은 사람이 dev→main 승격 후 `done`으로 전환 필요.
