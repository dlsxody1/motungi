# Nightly Report — 2026-09-04

## TL;DR

- **BACKLOG GATE 판정: 구현 모드.** 밤 시작 시 `docs/backlog/backlog.yml`의 `status: todo`는 **13건**이었다 — `todo ≥ 2`이므로 구현 모드로 진행했다.
- **연속 report-only 경보 해당 없음.** `ls docs/nightly/` 최근 파일(08-30~09-02) 확인 결과 직전 두 밤(09-01·09-02)은 각각 M-082/M-074/M-077, M-085/M-080/M-086이 shipped된 정상 구현 밤이었다. 09-03 날짜의 리포트는 존재하지 않지만, `git log`로 확인한 결과 그날은 자동 파이프라인이 아니라 **사람(`dlsxody1`)이 직접 카피 수정 커밋**(`37d2258`)을 올린 것이었다(Co-Authored-By 없음, M-NNN 참조 없음) — 파이프라인이 헛돈 report-only 실패가 아니라 애초에 그 밤은 자동 실행되지 않았던 것으로 보인다. 연속 report-only 스트릭은 0으로 판단.
- **브랜치 동기화 점검(nightly-pipeline.md 6번) 수행함**: `git log --oneline origin/dev..origin/main`에 `a9c2986`(PR #23 스쿼시) 1건이 있어 `git merge origin/main`으로 되받았다(`7a51f33`). `git diff origin/dev..origin/main --stat`가 비어 있어 실제 파일 차이는 없었다(dev가 이미 PR #23과 동일 내용을 갖고 있었음) — 충돌 없는 no-op 성격 머지.
- **클러스터 = M-075(씨앗, medium) + M-087(medium, 앱 단위 폴백), 2건 전부 shipped.** 되돌린 이슈 없음.
- `bash scripts/gate.sh` **PASS**(2회 재실행으로 재확인) — typecheck 4/4(core·mobile·tokens·web) · test(mobile 21파일 159/159, web 45파일 360/360) · lint clean(`next lint` — `✔ No ESLint warnings or errors`) · 시크릿 스캔 clean(`(시크릿 패턴 없음)`).
- **씨앗 선정 시 M-052를 사전 제외했다** — `docs/backlog/backlog.yml`의 M-052 notes에 2026-08-13부터 오늘까지 누적된 기록대로, 모바일 지도 구현 방식(react-native-maps 신규 의존성 / NAVER 정적지도 프록시 / 서드파티 정적지도) 3안 모두 사람 승인이 아직 없음을 오늘 밤도 재확인(`grep -i map apps/mobile/package.json` 공백, `.env.example`·`security-policy.md`에 지도 관련 항목 없음, `apps/mobile` 전체에 MapView/react-native-maps 참조 0건)했다. **선례와 달리 오늘은 M-052가 medium priority 중 최소 id(52)라 실제로 씨앗 후보였다.** 다음 최소 id인 M-075를 대신 씨앗으로 잡았다.
- **doc 모순 1건 신규 발견**(파이프라인 지시에 따라 여기 기록 — 사람이 고쳐야 함): `.claude/agents/architect.md:3,9,18,28`이 "FSD 아키텍처"·"entities/features/widgets/shared 계층"을 검증 대상으로 명시하는데, 항상 로드되는 `.claude/rules/core/constitution.md:24`는 "FSD(entities/features/widgets/shared) 디렉토리는 실재하지 않는다(옛 룰의 잔재였다)"라고 명시적으로 반대로 말한다. 오늘 밤은 트리거 PRECEDENCE 원칙에 따라 constitution.md(항상 로드되는 상위 규칙)를 따라 FSD 디렉토리를 만들지 않고 기존 평면 구조(`app/`·`hooks/`·`data/`)에만 작업했다. architect.md를 constitution.md와 정합하게 고칠 것을 권고.

## 집은 클러스터

- **씨앗**: M-075 (priority `medium`, M-052 사전 제외 후 최소 id). "모바일 보관함이 카탈로그 창 밖 저장 항목을 여전히 건별 개별 조회(N+1)".
- **파일 겹침 후보 0개** — M-075의 scope(`apps/mobile/hooks/useSavedOpportunities.ts`, `apps/mobile/data/opportunities.ts`)와 실제 파일이 겹치는 다른 todo가 없었다.
- **앱 단위 폴백으로 M-087 추가** — 이유: `apps/mobile` + `medium` priority가 M-075와 일치하는 todo 중 id 오름차순 최소가 M-087이었다(M-052는 blocked-human으로 사전 제외, 대상에서 빠짐). scope는 `apps/mobile/app/(tabs)/report.tsx`로 M-075와 실제 파일 겹침은 없다 — 순수 폴백 클러스터링.
- **상한 3개 중 2개만 채움** — 폴백 대상(같은 앱 `apps/mobile` + 같은 priority `medium`)이 M-052(제외)·M-087 둘뿐이라 더 못 채웠다. **수를 채우려 다른 priority를 끌어오지 않았다.**
- **미룬 이슈**: 다른 medium todo(M-081·M-083·M-084·M-091·M-096·M-097)는 전부 `apps/web`·`supabase`·`packages/*` scope라 이번 클러스터(apps/mobile)와 앱 경계부터 다름. low priority(M-079·M-088·M-092·M-093)는 애초에 seed priority(medium)와 달라 폴백 대상이 아니었다.

## 이슈별 결과

### M-075 — shipped (`67187d7`)
모바일 보관함(saved.tsx)이 카탈로그 반경 창 밖 저장 항목을 개별 `fetchOpportunityById` N회로 조회하던 것을 벌크 조회 1회로 교체.
- **기준선 재확인**: `origin/dev` 실코드로 `done_when` 미충족 확인 — `useSavedOpportunities.ts`가 `missingIds`를 순회하며 개별 fetch, `data/opportunities.ts`엔 `fetchOpportunitiesByIds` 재노출이 없음을 직접 grep으로 확인.
- **구현**: `apps/mobile/data/opportunities.ts`에 core `fetchOpportunitiesByIds`를 supabase 클라이언트 주입 래퍼로 추가(웹과 동등). `useSavedOpportunities.ts`를 id별 fetch map → 단일 bulk fetch state(`{status, data: Map}`)로 재작성 — `missingIds` 전체를 한 번에 요청하고, `retry()`도 `missingIds`(=catalog로 못 푼 것)만 재요청해 "부분 실패 시 실패분만 재조회" 계약을 유지.
- **테스트**: `useSavedOpportunities.test.ts` 8건 전면 재작성(신규: 12건 창 밖 시나리오에서 호출 1회 검증). 같은 훅을 쓰는 `saved.test.tsx`의 mock도 `fetchOpportunityById` → `fetchOpportunitiesByIds`로 갱신(회귀 방지를 위해 같은 커밋에 포함 — scope는 M-075가 명시한 두 파일이 핵심이고 saved.test.tsx는 그 결과로 깨진 소비처 테스트를 고친 것).
- **done_when 5개 전부 충족**: 벌크 래퍼 추가·1회 호출로 N+1 해소·10건 이상 시나리오 테스트·기존 계약 유지·mobile typecheck/test 통과.
- 되돌림 없음.

### M-087 — shipped (`7b4e8e0`)
모바일 리포트 화면(`report.tsx`) 원픽 히어로 카드가 텍스트뿐이던 것을 상세 화면과 동등하게 이미지·마감 배지·공유로 보강.
- **기준선 재확인**: `origin/dev` 실코드로 `report.tsx` 전체에 `Thumbnail`/`imageUrl`/`deadline`/`Share` 참조 0건임을 grep으로 확인 — `done_when` 미충족.
- **구현**: `Thumbnail`(`ui/thumbnail.tsx` 재사용)을 히어로 카드 상단에 추가. `deadlineLabel`(core)로 계산한 D-day를 카테고리 `Tag` 옆에 로컬 `DdayPill`(3톤: 지남/임박≤3일/여유 — `opportunity.tsx`의 동명 로컬 컴포넌트와 동일 규칙, scope가 report.tsx로 한정돼 공유 컴포넌트로 승격하지 않음)로 렌더. 공유 버튼(`RNShare.share` + 딥링크 URL)을 추가하되 **히어로 Pressable의 자식이 아니라 형제로 절대위치 배치**했다 — 상세 이동용 Pressable 안에 또 다른 Pressable을 중첩하면 웹에서 M-014가 이미 고친 것과 같은 "중첩 인터랙티브" 결함이 재발하기 때문.
- **테스트**: `report.test.tsx`에 5건 추가 — 이미지 있음(img 태그+src 검증)/없음(레이아웃 안 깨짐), 마감 있음(D-day 텍스트)/없음(배지 미노출, 레이아웃 안 깨짐), 공유 버튼 클릭 시 `RNShare.share` 메시지에 제목+딥링크 URL 포함 검증.
- **done_when 4개 전부 충족**: 썸네일·마감 배지·공유 버튼 추가, 3항목 부재 시 회귀 없음 + `pnpm --filter mobile test` 통과.
- 되돌림 없음.

## 아키텍처 검토

- 두 이슈 모두 architect 단계에서 **frontend-impl 단독 담당**으로 확정 — 순수 프론트엔드(RN 화면·훅) 변경이고 DB/Edge Function 접점이 없다. FSD 경계 검증은 위 TL;DR의 doc 모순 항목대로 constitution.md(평면 구조, FSD 디렉토리 없음)를 따랐다 — 새 디렉토리 계층을 만들지 않고 기존 `hooks/`·`data/`·`app/(tabs)/`에만 작업했다.
- M-075: `apps/mobile/data/opportunities.ts`는 core 함수에 앱별 supabase 클라이언트를 주입하는 "얇은 래퍼" 파일이라는 기존 패턴(파일 최상단 주석에 이미 명시)을 그대로 따라 `fetchOpportunitiesByIds` 한 함수만 추가했다 — 새 추상화 없음. 훅 내부 상태 모델(`FetchEntry` map → `BulkState`)이 바뀌었지만 훅의 공개 시그니처(`useSavedOpportunities(savedIds, catalog): SavedView`)는 무변경이라 소비처(`saved.tsx`)는 손대지 않았다(테스트만 수정).
- M-087: 새 의존성·새 네이티브 모듈 없음 — 기존 `Thumbnail`·`deadlineLabel`·`RNShare`(RN 내장) 재사용뿐이다. 유일한 설계 판단은 공유 버튼을 형제 오버레이로 뺀 것(위 상세 참조) — 중첩 Pressable을 피하는 게 새 컴포넌트를 만드는 것보다 싸고 M-014 선례와 일관됐다.
- 두 이슈 다 `packages/core`를 건드리지 않았다 — 필요한 함수(`fetchOpportunitiesByIds`, `deadlineLabel`)가 이미 존재해 소비만 했다.

## 변경 내용

**M-075** (커밋 `67187d7`):
- `apps/mobile/data/opportunities.ts` — `fetchOpportunitiesByIds` 래퍼 추가
- `apps/mobile/hooks/useSavedOpportunities.ts` — N+1 루프 → 벌크 fetch 1회로 재작성
- `apps/mobile/hooks/useSavedOpportunities.test.ts` — 8건 재작성(신규: 1회 호출 회귀 테스트)
- `apps/mobile/app/(tabs)/saved.test.tsx` — mock을 `fetchOpportunitiesByIds`로 갱신(소비처 회귀 방지)

**M-087** (커밋 `7b4e8e0`):
- `apps/mobile/app/(tabs)/report.tsx` — Thumbnail·DdayPill·공유 버튼 추가
- `apps/mobile/app/(tabs)/report.test.tsx` — 5건 추가

**브랜치 동기화**: `7a51f33` — `origin/main`의 PR #23(스쿼시) 되받기, 파일 diff 없는 no-op 머지.

## 검증

```
$ bash scripts/gate.sh
── typecheck
   ✅ typecheck   (core·mobile·tokens·web 4/4)
── test
   ✅ test        (mobile: 21 files, 159/159 · web: 45 files, 360/360)
── lint
   ✔ No ESLint warnings or errors
   ✅ lint
── secrets
   (시크릿 패턴 없음)
   ✅ secrets
GATE: PASS — push 허용
```
2회 연속 재실행으로 재확인(최종 트리 기준, 두 커밋 반영 후). 무관한 diff 오염·시크릿 노출·파괴적 SQL 없음 확인(두 이슈 모두 마이그레이션 없음, 시크릿·`.env` 미접촉).

## 리스크 & 후속

- **doc 모순**: `.claude/agents/architect.md:3,9,18,28`(FSD 언급)가 `.claude/rules/core/constitution.md:24`(FSD 디렉토리 실재하지 않음)와 모순 — 사람이 architect.md를 정정할 것을 권고. 오늘 밤은 상위 규칙(constitution.md)을 따랐으므로 실제 구현에는 영향 없음.
- **M-052(모바일 지도) 13일째 이상 blocked-human 유지**: 오늘도 동일 3안 모두 사람 승인 대기 확인. priority가 medium인 채로 남아 있으면 앞으로도 medium 순번마다 사전 제외 절차를 반복하게 된다 — 08-16·08-17·08-22~08-25 노트가 이미 제안한 3택1(경로 승인 / priority 하향 / `blocked-human` 상태 도입)이 아직 처리되지 않았음을 재차 확인한다.
- **09-03 리포트 공백**: 그날은 파이프라인이 아니라 사람이 직접 커밋했다(위 TL;DR 참조) — 파이프라인 결함은 아니지만, 향후 "사람이 직접 dev에 커밋한 밤"과 "파이프라인이 report-only로 헛돈 밤"을 리포트에서 구분할 방법이 없다는 점은 기록해둔다.
- **다음 밤 후속**: M-075/M-087은 사람 최종 리뷰 전까지 `doing` 유지(자동 `done` 승격 없음). 다음 밤 씨앗 후보는 남은 medium todo 중 최소 id인 M-081(`apps/web/src/app/report/page.tsx` 로컬 `formatDeadline` 재구현 — M-087과 유사하게 core `deadlineLabel` 미사용 케이스, 다만 웹 쪽이라 이번 클러스터와 무관).
